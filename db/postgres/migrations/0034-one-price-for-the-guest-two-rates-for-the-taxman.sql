-- Одна послуга — одна ціна для гостя, два податки для бухгалтерії.
--
-- Сніданок за 15 € — це 12 € Speisen (7%) і 3 € Getränke (19%), Lunchpaket
-- за 8 € — 70/30 (пропорцію приймає Finanzverwaltung, письмово від
-- бухгалтера пілота 22.08.2026). Моделювати це ДВОМА послугами означало
-- показувати гостю меню податкового кодексу — «їжа окремо, напої окремо».
-- Поділ — властивість проводки, не каталогу: JSON із компонентами
-- [{label, amount, vatCode}] розкладається в рядки фоліо в момент
-- нарахування, а всюди, де послугу видно (гостьова сторінка, бронь,
-- замовлення), вона лишається одним рядком з однією ціною.
ALTER TABLE "additional_services" ADD COLUMN IF NOT EXISTS "vat_split" TEXT;
