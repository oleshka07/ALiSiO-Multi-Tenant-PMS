Дампи сюди кладе `deploy/backup.sh <env>` на сервері; у git вони не потрапляють
(.gitignore). Тека потрібна порожньою, бо `rehearse-merge.sh` шукає найсвіжіший
дамп саме тут і без неї падає з «no dump found» ще до першого корисного кроку.
