/**
 * Вузькі двері модуля фінансів для ФОНОВИХ СКРИПТІВ.
 *
 *   import { repairAxes } from '@finance/axis';
 *
 * Окремий файл, а не `@finance` цілком, — з тієї самої причини, що
 * `@pricing/plans` і `@channels/outbox`: повний фасад тягне HTTP-обробники, а
 * вони імпортують `next/server`, якого в прод-образі НЕМАЄ (Next збирає
 * standalone і вирізає з `node_modules` усе, чого не просить рантайм).
 *
 * ── Чим це коштувало цього разу ─────────────────────────────────────────
 *
 * `seed-chart-of-accounts.mjs` брав `wrongAxisRows`, `unknownGroupRows` і
 * `repairAxes` з `@finance`, і зверху стояв коментар, який пояснював ЧОМУ:
 * «фасадом, не з `data/` напряму — цей скрипт код поза модулем, прямий
 * імпорт нутрощів пробиває межу». Довід правильний, висновок неповний:
 * третій варіант — вузькі двері — просто не розглянули, бо скрипт тоді
 * ніхто не запускав у образі.
 *
 * 11.09.2026 його почав кликати `deploy.sh`, і на живому деплої бети крок
 * упав рівно тим самим рядком, що й `apply-hotel` за пʼять днів до того:
 *
 *   Cannot find module '/app/node_modules/next/server'
 *     imported from /app/src/modules/finance/api/_guard.ts
 *
 * Застосунок при цьому піднявся, здоровʼя і всі чотири димові відповіді
 * зелені — падав КРОК ПІСЛЯ нього. Деплой лишився «успішним», а в лозі
 * стояло «план рахунків не засіяно».
 *
 * ── Межа цих дверей ─────────────────────────────────────────────────────
 *
 * Лише лагодження осей довідника статей. Це НЕ місце для «ще однієї
 * зручної функції з фінансів»: кожен доданий сюди експорт має бути з шару
 * даних і не тягнути обробників — інакше двері стають другим фасадом і
 * повторять те, від чого рятують.
 *
 * Тримає `scripts/check-entry-imports.mjs`, який після 11.09 бере входи не
 * лише з DEPLOY.md, а й з `docker exec … node scripts/…` у `deploy/*.sh` —
 * саме там жив цей скрипт, і саме тому гейт його не бачив.
 */
export { wrongAxisRows, unknownGroupRows, repairAxes } from '../data/axis-repair';
export type { AxisRow } from '../data/axis-repair';
