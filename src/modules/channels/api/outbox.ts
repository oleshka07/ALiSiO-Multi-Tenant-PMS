/**
 * Двері для писачів: «наявність/ціна цього обʼєкта змінилась» → черга.
 *
 *   import { noteAvailabilityChanged, noteRatesChanged } from '@channels/outbox';
 *
 * Це окремий файл фасаду, а не `@channels` цілком, навмисно: бронь, ціна й
 * блокування кличуть це з ГАРЯЧОГО шляху — з транзакції, що пише
 * `reservations` або `price_calendar`, — і тягнути туди всі обробники
 * модуля разом із `next/server` нема чого.
 *
 * ── Що пишеться, а що ні ────────────────────────────────────────────────
 *
 * Координата, не значення (0054): «наявність типу X на ночі A..B змінилась».
 * Число батчер прочитає в момент відправлення. Тому писачу байдуже, на
 * скільки змінилось і чи змінилось узагалі: зайва координата коштує один
 * виклик, пропущена — ніч, продану за числом, якого готель не називає.
 *
 * ── Лише те, що ІСНУЄ на тому боці ──────────────────────────────────────
 *
 * Тип без рядка в дзеркалі (ще не заведений, або без номерів) координати
 * не отримує: її нема кому адресувати, і через десять проходів вона стала
 * б «потребує уваги» ні за що. Перша відправка нового мапінгу — справа
 * самого дзеркала: `tableMirror().put` кладе діапазон до горизонту в ту
 * саму мить, коли пара зʼявляється (Ц16).
 *
 * ── Усім зʼєднанням обʼєкта, і вимкненим теж ────────────────────────────
 *
 * Вимкнене нічого не шле (батчер), але чергу отримує: після вмикання канал
 * має дістати ПОТОЧНИЙ стан того, що змінилось, а не старі ціни до чийогось
 * повного синку (`connectionsForProperty`).
 *
 * ── Діапазон, обрізаний з обох боків ────────────────────────────────────
 *
 * Не раніше сьогодні — минулих ночей вендор не приймає; не далі горизонту
 * (`OUTBOX_HORIZON_DAYS`) — далі ночі не існує ні для кого. Кінець без дати
 * (матриця без `valid_to`, тир без вікна, видалений тариф) означає «до
 * горизонту». Що обрізалось до порожнього — не пишеться взагалі.
 *
 * ── `t` — транзакція писача ─────────────────────────────────────────────
 *
 * Перший аргумент — той самий `t`, що пише бронь чи ціну. Черга, яка
 * поповнюється окремим кроком, розходиться зі станом при першому ж падінні
 * між ними; а зовнішній `getSql()` усередині `sql.tx` на Postgres — це інше
 * зʼєднання, тобто теж «окремий крок» (інваріант 11). Поза транзакцією
 * передають `getSql()` — свідомо, а не забувши.
 */

export {
  noteAvailabilityChanged, noteRatesChanged, clipToHorizon, lastNight, addDays,
} from '../data/outbox-notes';
export type { AvailabilityNote, RateNote } from '../data/outbox-notes';
