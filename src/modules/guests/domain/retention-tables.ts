/**
 * Що ретенція робить із кожною таблицею, яка тримає дані гостя (INC-305).
 *
 * ── Навіщо реєстр, а не просто запити в репозиторії ─────────────────────
 *
 * Знеособлення — єдина незворотна операція в системі. Його SQL перелічує
 * таблиці поіменно, тож НОВА таблиця з даними гостя просто лишається поза
 * ним: нічого не падає, нічого не видно, а персональні дані тихо переживають
 * термін зберігання. Це рівно клас «функція зникла» — і саме так сюди й
 * потрапили `guest_consents` та `consent_texts`: вони новіші за ретенцію.
 *
 * Тому список — ДАНІ, і повноту стереже `scripts/check-retention-tables.mjs`:
 * кожна таблиця з посиланням на гостя мусить бути названа в одному з трьох
 * кошиків. Сказати «лишаємо» дозволено — промовчати не можна.
 */

/** Знеособлюється: рядок лишається, персональні поля стираються. */
export const RETENTION_ANONYMISED: readonly string[] = ['guests', 'reservation_guests'];

/** Видаляється цілком. */
export const RETENTION_DELETED: readonly string[] = ['guest_registrations'];

/**
 * Лишається як є — і чому саме.
 *
 * `guest_consents` — головний рядок цього файла. Її НЕ чіпають не тому, що
 * забули, а тому, що вона за побудовою не містить персональних даних:
 * організація, посилання на рядок гостя, рід згоди, версія тексту, джерело і
 * дві дати. Ні імені, ні пошти, ні IP — на відміну від `guest_registrations`,
 * яку ретенція саме тому й видаляє (там `consent_ip`).
 *
 * Після знеособлення рядок каже: «запис такий-то погодився тоді-то на редакцію
 * таку-то» — і це рівно той доказ, який готель показує наглядачеві, причому він
 * не називає нікого. Видалити його означало б знищити доказ разом із даними,
 * яких він і не тримає.
 *
 * **Згоду при цьому НЕ відкликають.** Написати `revoked_at` означало б
 * стверджувати, що людина відкликала згоду, — а вона цього не робила. Це
 * підробка юридичного запису заради охайності. Розсилка знеособленому й так
 * неможлива: пошту стерто, слати нема куди.
 */
export const RETENTION_KEPT: Readonly<Record<string, string>> = {
  guest_consents:
    'не містить персональних даних (ні імені, ні пошти, ні IP) і є доказом для наглядача; '
    + 'знеособлений гість не має пошти, тож розсилка й так неможлива',
  reservations:
    'комерційний запис: бронь, суми й податки зберігаються за іншими термінами, '
    + 'а імена гостя в ній немає — вони в guests і reservation_guests',
  fin_folios:
    'книга проживання — тека @finance; персональні поля там не лежать, лише посилання',
};
