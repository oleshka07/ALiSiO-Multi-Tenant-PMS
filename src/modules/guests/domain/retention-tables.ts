/**
 * Що ретенція робить із кожною таблицею, яка тримає ПЕРСОНАЛЬНІ ДАНІ
 * (INC-305, розширено INC-306).
 *
 * ── Навіщо реєстр, а не просто запити в репозиторії ─────────────────────
 *
 * Знеособлення — єдина незворотна операція в системі. Його SQL перелічує
 * таблиці поіменно, тож НОВА таблиця з даними гостя просто лишається поза
 * ним: нічого не падає, нічого не видно, а персональні дані тихо переживають
 * термін зберігання. Це рівно клас «функція зникла» — і саме так сюди й
 * потрапили `guest_consents` та `consent_texts`: вони новіші за ретенцію.
 *
 * Тому список — ДАНІ, і повноту стереже `scripts/check-retention-tables.mjs`:
 * кожна така таблиця мусить бути названа в одному з трьох кошиків. Сказати
 * «лишаємо» дозволено — промовчати не можна.
 *
 * ── Властивість розширено: не «guest_id», а ПЕРСОНАЛЬНІ ДАНІ ────────────
 *
 * Перша редакція стерегла таблиці з `guest_id`. Виміряно: таких пʼять, а
 * таблиць із персональними КОЛОНКАМИ — двадцять. Тобто п'ятнадцять тримали
 * пошту, телефон, документ чи адресу живої людини і не були названі ніде.
 *
 * Що вважати персональним — `personal-data.ts`, визначенням у коді. Дивимось
 * на колонки, а не на назви таблиць: `waitlist` і `site_incoming_leads` не
 * мають у назві слова «гість», а тримають пошту й телефон людини.
 *
 * ── Три з цих таблиць — СПРАВЖНІ дірки, і вони названі, не залатані ─────
 *
 * `booking_drafts`, `site_incoming_leads`, `waitlist` тримають імʼя, пошту й
 * телефон людини, яка ще не стала гостем, і ретенція їх не чіпає НІКОЛИ:
 * термін зберігання для них не настає, бо вони не привʼязані до виїзду.
 * Полагодити це тут не можна — теки віджета й подій належать іншим сесіям, і
 * саме тому вони стоять у `RETENTION_GAPS`, а не в `RETENTION_KEPT`: гейт
 * зелений, але список дірок видно, і він не мовчить.
 */

/** Знеособлюється: рядок лишається, персональні поля стираються. */
export const RETENTION_ANONYMISED: readonly string[] = ['guests', 'reservation_guests'];

/** Видаляється цілком. */
export const RETENTION_DELETED: readonly string[] = ['guest_registrations'];

/**
 * Лишається як є — і чому саме.
 *
 * `guest_consents` — головний рядок цього файла. Її НЕ чіпають не тому, що
 * забули, а тому, що вона за побудовою не містить персональних даних:
 * організація, посилання на рядок гостя, рід згоди, версія тексту, джерело і
 * дві дати. Ні імені, ні пошти, ні IP — на відміну від `guest_registrations`,
 * яку ретенція саме тому й видаляє (там `consent_ip`).
 *
 * Після знеособлення рядок каже: «запис такий-то погодився тоді-то на редакцію
 * таку-то» — і це рівно той доказ, який готель показує наглядачеві, причому він
 * не називає нікого. Видалити його означало б знищити доказ разом із даними,
 * яких він і не тримає.
 *
 * **Згоду при цьому НЕ відкликають.** Написати `revoked_at` означало б
 * стверджувати, що людина відкликала згоду, — а вона цього не робила. Це
 * підробка юридичного запису заради охайності. Розсилка знеособленому й так
 * неможлива: пошту стерто, слати нема куди.
 */
export const RETENTION_KEPT: Readonly<Record<string, string>> = {
  // ── Не про гостя: власні дані готелю, персонал і платформа ────────────
  organizations: 'контакти самої компанії-клієнта, а не гостя',
  properties: 'контакти готелю — те, що друкують на рахунку',
  app_users: 'персонал готелю; його дані живуть за трудовим договором, не за терміном перебування',
  platform_users: 'працівники постачальника, не гості',
  platform_audit: 'журнал входу постачальника в рахунок; його пошта й IP — доказ доступу, і саме тому зберігаються',
  companies: 'юридична особа-платник: адреса й пошта фірми потрібні на фактурі роками після виїзду',
  property_guest_config: 'телефон рецепції, який показують гостю, а не телефон гостя',

  // ── Документи: стираються за податковим строком, не за нашим ──────────
  invoices: 'виданий документ: адреса й пошта покупця — його реквізити, зберігаються за податковим строком',
  fin_invoice_lines: 'рядок виданого документа; назва в ньому — частина того, що надруковано',
  fin_folio_items: 'рядок книги проживання; назва послуги, а не персональні дані гостя',
  gift_cards: 'ваучер — оборотний документ: контакт отримувача потрібен, поки він чинний',
  event_bookings: 'бронь залу: контакт замовника потрібен до й після події, теку подій веде інша сесія',

  guest_consents:
    'не містить персональних даних (ні імені, ні пошти, ні IP) і є доказом для наглядача; '
    + 'знеособлений гість не має пошти, тож розсилка й так неможлива',
  reservations:
    'комерційний запис: бронь, суми й податки зберігаються за іншими термінами, '
    + 'а імена гостя в ній немає — вони в guests і reservation_guests',
  fin_folios:
    'книга проживання — тека @finance; персональні поля там не лежать, лише посилання',
};

/**
 * Персональні дані, яких ретенція не чіпає НІКОЛИ, — і це дірка, не рішення.
 *
 * Відрізняється від `RETENTION_KEPT` тим, що там «лишаємо СВІДОМО і ось чому»,
 * а тут «мусило б стиратись, але не стирається». Гейт від цього не червоніє —
 * інакше довелося б або залатати чужі теки, або вимкнути гейт, — але список
 * існує, і мовчання про нього неможливе: він друкується щоразу.
 *
 * Спільна риса всіх трьох: людина ще не стала гостем, тож ВИЇЗДУ немає, а
 * ретенція рахує саме від виїзду. Термін для них не настає ніколи.
 */
export const RETENTION_GAPS: Readonly<Record<string, string>> = {
  booking_drafts:
    'імʼя, пошта й телефон незакінченої броні; виїзду немає, тож термін не настає — тека @bookings/@widget',
  site_incoming_leads:
    'заявка з сайту: імʼя, пошта, телефон людини, яка лише запитала — тека @widget, сесія 2',
  waitlist:
    'лист очікування: пошта й телефон; людина може так і не приїхати, і рядок лишається назавжди',
};
