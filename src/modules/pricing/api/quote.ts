/**
 * Вузькі двері до котирування — реекспорт ОДНІЄЇ функції, без обробників.
 *
 *   import { calculateQuote } from '@pricing/quote';
 *
 * Той самий взірець, що `@pricing/plans`, `@channels/outbox` і `@finance/axis`,
 * і з тієї ж причини: повний фасад `@pricing` тягне HTTP-обробники, а ті —
 * `next/server`, якого Next standalone у прод-образі не містить. Вхід, що
 * потрапив у такий ланцюжок, падає лише на сервері, руками, після деплою
 * (`check-entry-imports`).
 *
 * Прямий імпорт `../data/quote.repo` з чужого модуля пробив би межу
 * (`check-boundaries`) — а ціна це рівно те місце, де межу тримати треба:
 * друге джерело суми розходиться з першим у тижні, коли ніхто не дивиться
 * (інваріант 16).
 */
export { calculateQuote } from '../data/quote.repo';
