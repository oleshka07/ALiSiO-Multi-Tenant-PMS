/**
 * Аркуші дня — чотири друковані списки, які рецепція друкує щоранку.
 *
 * Zimmerliste (хто в домі), Frühstücksliste (скільки сніданків, з урахуванням
 * правила каналу й окремо замовлених), Schlüsselliste (заїзди й виїзди),
 * Tagesabschluss (виторг за ставками ПДВ із заморожених рядків фактур).
 *
 * ── Чому окремий модуль, а не частина bookings ──────────────────────────
 *
 * Він жив у `modules/bookings` як «ще один екран про броні», і за формою це
 * правда: кожен аркуш виводиться з бронювань, які вже є. Але за призначенням
 * це не бронювання, а РОБОЧИЙ ДЕНЬ готелю — і саме тому одному клієнтові він
 * потрібен щоранку, а іншому не потрібен зовсім: апартаменти без сніданку й
 * без рецепції не друкують нічого з цих чотирьох.
 *
 * Поки він сидів усередині bookings, вимкнути його означало вимкнути броні.
 * Тепер це власний каталог із власним ключем `day_sheets` у реєстрі фіч, і
 * готель прибирає його одним перемикачем, нічого більше не втрачаючи.
 *
 * ── Межі ────────────────────────────────────────────────────────────────
 *
 * Модуль читає лише `core`: `getSql`, `requireOrganizationId`, `todayFor`,
 * `withModule`. Жодного імпорту з `modules/bookings` — тому й переїзд обійшовся
 * без розплутування. Типи рядків експортуються звідси, а не з `@bookings`, де
 * вони стояли раніше.
 */
export { getDaySheet } from './day-sheets.handlers';
export type { StayRow, BreakfastRow, KeyRow, DayCloseRow } from '../data/day-sheets.repo';
