/**
 * Слово обʼєкта про оплату при заселенні — вузькі двері назовні.
 *
 * Двері, а не прямий імпорт `../domain/checkin-policy`, з тієї ж причини, що
 * `@bookings/overlap`: повний фасад модуля тягне `next/server`, якого немає в
 * прод-образі Next standalone, і вхід, що запускається `node scripts/…`,
 * падає лише на сервері, руками, після деплою (`check-entry-imports`).
 *
 * Що тут НЕ можна зробити — завести другий нормалізатор. `readCheckinPolicy`
 * один на весь продукт: рецепційний PATCH, кіоск і платіжний шлагбаум
 * гостьової сторінки мусять чути від обʼєкта ОДНЕ слово. Два прочитання
 * розійдуться так само, як розходяться два списки статусів, і розійдуться в
 * той бік, який видно лише гостю: рецепція пускає, сторінка не пускає.
 */
export {
  readCheckinPolicy,
  CHECKIN_PAYMENT_POLICIES,
  type CheckinPaymentPolicy,
} from '../domain/checkin-policy';
