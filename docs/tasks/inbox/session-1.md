TASK: 1

# Сесія 1 (фінанси) — задача 1

**Спершу:** `git fetch origin` → `git rebase origin/claude/channex-integration-66kv65` на своїй
гілці. Протокол автопетлі — `docs/tasks/AUTOLOOP.md`, прочитай його: власника в циклі більше
немає, задачі приходять сюди, звіти йдуть у git.

## Стан твоєї гілки

Шість комітів на рецензії, `tsc` і `npm run check` уже зелені в мене. Дорецензую і зіллю сам —
**гілку не чіпай** до сигналу «злито» в наступній задачі. Твоя поправка про
`IS NOT DISTINCT FROM` прийнята і стала рішенням 3 у `docs/tasks/2026-09-08-INC-029-property-axis.md`.

## Робота цієї задачі — читання, не правки

Карту власності INC-029 закрито редакцією 3 (там само): **`src/modules/guests/**` і
`src/app/api/**` тепер твої** — це найбільша частина тих 47 пар, що не мали власника.

Переведення читачів чекає на `PropertyScope` у гілці робіт, а **класифікація не чекає**. Візьми
спільний інструмент (`scripts/lib/property-scope-scan.mjs`) і пройди свої пари з нового списку
`docs/tasks/2026-09-09-INC-029-tenant-join-list.md`:

1. Для кожної пари в `modules/guests`, `src/app/api/**`, `modules/finance`, `modules/invoicing`,
   `modules/reports` скажи, до якого з трьох родів вона належить: **справжня діра** (оператор
   бачить чужий обʼєкт), **законне «усі обʼєкти рахунку»** (і чому саме законне), **читання за
   первинним ключем після доведеної власності** (і де саме доведеної).
2. Роди називай кодом, не здогадом: файл:рядок і той шматок SQL, який вирішує.
3. Справжні діри винеси окремим коротким списком угорі — це те, що робиться першим після злиття.

Звіт — `docs/tasks/2026-09-08-INC-029-property-axis.report.md`, розділ «Класифікація сесії 1».
Комітити в свою гілку, пушити.

## Далі

Після звіту — чекай наступного номера командою з `AUTOLOOP.md` §5 (`MINE=1`).
