TASK: 2

# Сесія 1 (фінанси) — задача 2

**Спершу:** `git fetch origin` → `git rebase origin/claude/channex-integration-66kv65`.
Протокол — `docs/tasks/AUTOLOOP.md`.

## Твою гілку ЗЛИТО

Рецензія — `docs/tasks/2026-09-09-review-round-19-finance-merged.md`. Прийнято **повністю**, без
пунктів «перед злиттям»; прогін після злиття теж зелений.

Дві речі назвав окремо, бо вони сильніші за те, що я просив. `categories.handlers` тепер
**відмовляє 409 з назвою статті й групи** замість того, щоб просто не мати дефолту. І причина
сліду прибирання виявилась не тією, яку я записав у раунді 17 — не порядок видалення, а читання
тенантної таблиці поза контекстом орендаря, третій випадок INC-014 у тому файлі. Обидва рази ти
виміряла, а не погодилась; так і треба.

`claude/block-4-hotel-day` заміщена твоєю гілкою і зливатись не буде.

## Робота цієї задачі — класифікація, переведення ще чекає

`PropertyScope` живе на `claude/inc-029-core` і в гілку робіт ще не влитий, тож **читачів осі не
переводь**. Класифікація не чекає.

Карта власності, редакція 3 (`docs/tasks/2026-09-08-INC-029-property-axis.md`): **`modules/guests`
і `src/app/api/**` тепер твої** — це найбільша частина тих 47 пар, що не мали власника.

Візьми спільний інструмент (`scripts/lib/property-scope-scan.mjs`; поки не влито — читай через
`git show origin/claude/inc-029-core:scripts/lib/property-scope-scan.mjs`) і пройди свої пари зі
списку `docs/tasks/2026-09-09-INC-029-tenant-join-list.md`:

1. Для кожної пари в `modules/guests`, `src/app/api/**`, `modules/finance`, `modules/invoicing`,
   `modules/reports` скажи, до якого з трьох родів вона належить: **справжня діра** (оператор
   бачить чужий обʼєкт), **законне «усі обʼєкти рахунку»** (і чому саме законне), **читання за
   первинним ключем після доведеної власності** (і де саме доведеної).
2. Роди називай кодом: файл:рядок і той шматок SQL, який вирішує.
3. Справжні діри — окремим коротким списком угорі. Це те, що робиться першим після злиття
   сесії 3.

Звіт — `docs/tasks/2026-09-08-INC-029-property-axis.report.md`, розділ «Класифікація сесії 1».
Комітити у свою гілку, пушити.

## Далі

Чекай наступного номера командою з `AUTOLOOP.md` §5 (`MINE=2`).
