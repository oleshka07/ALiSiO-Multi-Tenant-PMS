TASK: 3

# Сесія 1 (фінанси, гості, `src/app/api/**`) — задача 3

**Спершу:** `git fetch origin`, потім **злиття**, не перебазування:
`git merge origin/claude/channex-integration-66kv65`. Правило змінилось і
записане в `docs/tasks/AUTOLOOP.md`: сесії історію не переписують ніколи,
зводить контролер. Голова гілки робіт — `a8b04aae`, у ній уже є ядро осі
(`PropertyScope`, `requestPropertyScope`, `propertyScopeFilter`, фікстура на два
обʼєкти) — воно тобі знадобиться в кожному пункті нижче.

Класифікацію 47 пар прийнято повністю. Вимір спільним інструментом, три кошики,
пряма відмова від «жодної правки, поки означення не в гілці» — усе правильно.
Рецензія — `docs/tasks/2026-09-09-review-round-20.md`, розділ 3.

## Головне: чотири записи підтверджені по коду, і вони робляться першими

`docs/tasks/2026-09-09-INC-034-037-property-axis-writes.md` — я перевірив
кодом сам, не за звітом, і додав до двох із них обставину, якої в твоєму звіті
немає. Читай той документ повністю; тут — порядок і межі.

**П1. INC-034** — `src/app/api/booking-sites/[id]/listings/route.ts:76,:83`.
Сайт обʼєкта А кладе в `site_listings` номер обʼєкта Б і продає його. Варта
`withOwnedSite` уже повертає **ввесь рядок сайта** в `ctx.site`, а
`booking_sites.property_id` — `NOT NULL`. Тобто правка не потребує нового
запиту: `site.property_id` уже в області видимості, його бракує в умові.

**П2. INC-035** — `src/app/api/booking-sites/[id]/services/route.ts:58` (запис)
і `:18` (читання). Той самий рід, та сама правка тим самим `site.property_id`.

**П3. INC-036** — `src/modules/invoicing/data/stay-charges.repo.ts:431`.
Послуга чужого обʼєкта лягає в фоліо з **чужою ціною і чужим `vat_code`**, тоді
як мова документа береться від обʼєкта фоліо. Правка з двох частин, і другу не
можна пропустити: `fin_folios.property_id` **нульовий** (`schema.sql:816`), і
`createFolio` приймає його як `null`. Вузьке `AND s.property_id = ?` на фоліо
без обʼєкта відмовить у КОЖНІЙ послузі — це «функція зникла» з AGENTS §7. Тому:
є обʼєкт — звіряємо; немає — **названа відмова** окремою причиною
(`folio_without_property` і речення на екрані), не мовчання і не «отже, будь-яка».
Це рішення 3 контролера від 09.09 дослівно.

Підбирач цієї ж дірки вже полагоджено сесією 3 і вже в гілці робіт
(`additional-services.handlers.ts` ходить через `requestPropertyScope`), тож
після злиття екран чужих послуг не покаже — API покаже. Твій бік не чекає.

**П4. INC-037** — `src/modules/guests/data/registry.repo.ts:84,:155` і екран
`guest-registry/page.tsx:91-99`. Сума міського збору множить ночі кожного
обʼєкта на ЙОГО ставку і додає різні обʼєкти в одну цифру, яку подають державі.
Вісь у репозиторії вже є і мертва — екран її ніколи не ставить.

**І обставина, через яку «очевидна» правка була б порожньою:** маршрут
(`guests/api/registry.handlers.ts:28,96`) читає `searchParams.get('propertyId')`,
а провайдер і решта чотирнадцяти екранів шлють **`property_id`** (NAMING §8).
Переведений «як усі» екран відправив би `property_id`, маршрут прочитав би
`undefined`, і не змінилось би нічого — а правка виглядала б зробленою.
Маршрут переводиться на `requestPropertyScope`, а не на другий `searchParams.get`.

Кожен із чотирьох — **червона сцена до коду**, з числом у повідомленні
(«очікували збір обʼєкта А = 3400, отримали 7900»), і червоність доведена
виводом, а не словом.

## П5. Далі — переведення читачів твоїх тек

23 пари «законне усі обʼєкти» пишуться `ALL_PROPERTIES` із причиною
(міняється те, що це СКАЗАНО), 15 пар «за первинним ключем після доведеної
власності» — теж словом. Стелю храповика опускай тим самим комітом.

**Не бери** три пари `finance/api/reports.handlers.ts` і `paid-services.handlers.ts`,
про які ти сама поставила питання «фінанси — рівень рахунку чи рівень обʼєкта»:
питання законне, воно піде власнику разом із рештою, і доти вони лишаються як є.

## Межа

Тек сесій 2 і 3 не чіпай. `src/lib/db.ts` — спільний: свій блок, чужий не чіпати.

---

## Про твій коміт `b9ed7025` — і про мою помилку, яку він виправив

Ти маєш рацію двічі, і обидва рази проти мене.

**Задачу 2 я поставив зайвою.** Класифікація вже була зроблена комітом
`4440085d`, а мердж `e51a32c2` узяв `65448718` — шість комітів, — тож
класифікація в злиття не потрапила і я її не бачив. Це помилка контролера:
я звірив голови гілок, а не те, що саме увійшло в злиття. Замість повторення
роботи ти **переміряла її на злитому дереві** і знайшла зсув двох номерів у
`reports.handlers.ts` — це рівно те, чого варта була задача.

**Рішення 3 в інструменті відсутнє — і ти назвала другу половину, якої я не
побачив.** Я знайшов те саме про `NAMED_IN_FILTER` незалежно й тим самим
способом (два виміри збіглися, отже факт закритий). Але `bucket()` (рядок 226)
видає `unknown` **лише** за наявності підстановки, тож статично видима, але
невпізнана форма падає в `silent` і стає нерозрізненною від «осі немає
взагалі» — а рішення 3 казало саме протилежне. Це глибше за три регулярки, і
я цього не побачив.

Правка — у файлі сесії 3, і вона в її задачі 2 обома половинами, з твоїм
виміром і посиланням на твій звіт. Ти правильно зробила, що не чіпала чужу
теку, а назвала з виміром.

Практичний наслідок для тебе: доти твій фікс INC-033
(`partner-report.repo.ts:129`, `IS NOT DISTINCT FROM`) читається інструментом як
«мовчить» і сидить у списку 137 як «вісь орендаря». Не «лагодь» його вдруге і
не переписуй на `= ?` — на нульовій колонці це регресія.

Звіт — у той самий `docs/tasks/2026-09-08-INC-029-property-axis.report.md`.
Далі — чекай наступного номера (`MINE=3`).
