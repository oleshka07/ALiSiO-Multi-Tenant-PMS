TASK: 1

# Сесія 2 (канали) — задача 1

**Спершу:** `git fetch origin` → `git rebase origin/claude/channex-integration-66kv65`.
Протокол автопетлі — `docs/tasks/AUTOLOOP.md`: власника в циклі більше немає.

Р15.1 прийнято попередньо — рецензію допишу після злиття фінансової гілки. Обидві твої гілки
далі не чіпай історією; доливати коміти зверху можна і треба.

## П1. Рішення власника В1 — рід житла обирається ЯВНО

`docs/tasks/2026-09-09-owner-decisions.md`, рішення В1. Дослівно: «має бути чіткий вибір, один
раз обирається і закріплюється за готелем».

- Заведення відмовляє без роду житла: ні `provision-org.mjs`, ні `apply-hotel.mjs`, ні форма
  обʼєкта не ставлять `hotel` мовчки — названа відмова, як ти вже зробила для поясу.
- Вибір показує **групу тарифікації** (за обʼєкт / за юніт) у самому **онбордингу**, не лише в
  налаштуваннях обʼєкта: `LodgingBillingHint` уже є, доведи його туди.
- Поле в `hotels/*.json` стає обовʼязковим; три тестові файли лишаються `hotel`.
- Гейт: заведення без роду житла — названа відмова; сцена червона до правки, і червоність
  доведена виводом.

## П2. Р15.2 — мова названих відмов модуля

`catalog-sync.ts:102, :116, :119, :175` і `properties.repo.ts:146` кажуть оператору англійською,
а решта продукту відмовляє українською. Останній ще й вивалює всі 22 машинні значення переліку —
людині це не допомагає. Приведи до мови продукту і до людського формулювання.

## Чого НЕ робити

Читачів осі INC-029 не починай: означення «названо» звужене, базлайн сесії 3 ще на рецензії.
Частину B не переробляй — вона прийнята до рецензії.

Звіт — у той самий `docs/tasks/2026-09-09-channex-property-and-block-3.report.md`.
Далі — чекай наступного номера (`MINE=1`).
