TASK: 8

# Сесія 5 — задача 8: живі розриви частини Б, потім денна дельта і незатирання для кіоска

Рецензія Б — `docs/tasks/2026-09-10-review-winhotel-import-B.md` на `origin/claude/controller-2`.
Побудову прийнято; живий імпорт справжнього знімка в хмарі контролера показав чотири
розриви між мапою і живими даними. Спершу перечитай рецензію цілком.

## 1. Блокери живого проходу (у цьому порядку, кожен — гейт червоним першим)

1. `LEISTSTA.WG` — LNR групи; код групи — `service_groups.wgnr` (100…800). Джойн
   `wg → lnr → wgnr` для `lineKind` і `needsCatalogMatch`; фікстура, де `wg ≠ wgnr`.
   Очікування на живому: «Logis»/«Übernachtung» → `lodging`, wgnr 600 → `city_tax`,
   700/750/800 — поза каталогом і поза фоліо гостя (це каса/витрати; куди їх — назвати:
   staging `cash_article` або пропуск із лічильником).
2. Компанія = `ADR_WAHL = 1`. `DEBI_NR` — не ознака; зберегти в `companies.debtor_no`, якщо
   колонка вже є на гілці робіт (сесія 1, задача 11), інакше staging `debtor_no_pending` з
   лічильником. Фікстура: адреса з `DEBI_NR > 0` і `ADR_WAHL = 0` → гість.
3. Перевірка послуг — не відмова: групи 100/600/700/750/800 не звіряти; решту — нормалізовано
   (регістр, пробіли, `-`/`–`/`—`) і незнайдені → `reconcile.explained` з переліком.
4. Імпортна оплата (`source = 'import'`) іде повз фіскальну варту з позначкою походження
   `winhotel:<LNR>`; нова оплата — варта як була. Рішення контролера З34 у DECISIONS. Гейт:
   імпортна без позначки → відмова; рецепційна готівка на DE без `fiscal_de` → відмова як
   раніше (обидві сцени).

Після зеленого — звіт із числами очікуваними для живого проходу (контролер повторить його
сам: `lodging` за сумою, компаній ≈ 2 143, платежів у фоліо 436, `mismatch: false`).

## 2. Числами (рецензія Б, розділ «Перевірити»)

Майбутні 912/900; `overlap` 23 (дві броні на номері у Winhotel / `UMZUG_ZINR` / ніч виїзду);
`MARKSEG` не канал — знайти поле джерела в живому витягу; `merged_by_name` 1 495 — порахувати
різні дати народження/міста і передати число в звіт (правило — сесії 3); `ON CONFLICT` при
старті свіжої SQLite — чиє.

## 3. Для кіоска (рішення власника 10.09: «кожні 15 хв — ок»)

- **Режим агента `-Mode Delta`**: кожні 15 хв (окрема задача Планувальника) `isql` читає
  лише броні з заїздом або виїздом у вікні −1…+3 дні від сьогодні (`GASTKONT` + `BELEGUNG`
  + адреси цих броней + їхні `BUCHKONT`/`ZAHLUNGEN`), пише той самий формат витягу (jsonl +
  `aggregates.json` з `mode: "delta"`, вікно дат у ньому), шле тим самим маршрутом; сервер
  приймає дельту як знімок роду `delta` (0145: `winhotel_snapshots.mode` розширити, обмеження
  «один знімок на добу» — лише для `gbak`), імпорт дельти оновлює **тільки** броні з витягу
  і не чіпає решти (жодного `cancelled` за відсутністю — дельта не повна за означенням).
  Гейт: дельта без вікна → 400; повний імпорт після дельти не відкочує її; бронь поза вікном
  дельта не скасовує.
- **Незатирання**: імпорт (повний і дельта) ніколи не пише назад `status` (лише вперед:
  `confirmed → checked_in → checked_out`, `cancelled` — лише за TA_STATUS/відсутністю в
  повному), не чіпає `registration_status`, `reservation_guests`/`guest_registrations`,
  створені не імпортом (ознака — `winhotel_refs` для `reservation_guest` є лише в
  імпортних), не змінює й не видаляє `fin_folio_payments` без `source = 'import'`. Гейт
  червоним: кіоск заселив (status `checked_in`, `registration_status = 'registered'`,
  оплата рецепції) → повторний імпорт того ж знімка з `CI_STATUS 0` → усе лишилось.

Звіт — розділ «Задача 8». Потім цикл, `MINE=8`:

```
MINE=8; while :; do git fetch -q origin; N=$(git show origin/claude/controller-2:docs/tasks/inbox/session-5.md 2>/dev/null | head -1 | sed 's/TASK: //'); [ "${N:-0}" -gt "$MINE" ] && { git show origin/claude/controller-2:docs/tasks/inbox/session-5.md; break; }; sleep 300; done
```

## 0. Дописано 10.09, 14:05 (номер задачі не змінено) — номери міграцій

Смуга другої контрольної (`AUTOLOOP.md`) — `0400…0499`. Сесія 4 уже перейменувала свої
0140–0142 → 0400–0402 на `claude/block-apps` (`6f703f69`), бо `0140` зіткнувся з дебітором
сесії 1 у гілці робіт. **Першим комітом задачі 8:** злий `origin/claude/block-apps` (принесе
0400–0402 замість 0140–0142; перейменовані файли — ті самі міграції, дублікатів під двома
іменами лишитись не має), перейменуй свої `0143 → 0403`, `0144 → 0404` (ніде не накочені —
беты не досягли), нові — `0405…0409`; `db.ts`, ARCHITECTURE, звіти — за тими самими іменами.
Сесія 6 бере `0410…0419`.
