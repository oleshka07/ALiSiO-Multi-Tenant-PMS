TASK: 4

# Сесія 6 — задача 4: частина Г — живий Postgres із двома готелями; блок закривається

Рецензія В — `docs/tasks/2026-09-10-review-kiosk-V.md` на `origin/claude/controller-2`:
частину В прийнято, КІ14–КІ16 прийняті, мої мутації (вісь організації в добі; зона
проігнорована) — червоні. Це остання задача блоку.

**1. Живий Postgres роллю `alisio_app`, два готелі** (AGENTS §7: стенд, роль без суперправ,
`provision-org.mjs` × 2). Довести те, що SQLite не доводить: політики 0411 (`kiosk_devices`,
`kiosk_pairings`, `kiosk_events`) — свій бачить своє, чужий не бачить нічого, з обох боків;
`runWithPublicToken` на коді парування (інваріант 14) працює під RLS; 0413 — колонки
`properties` читаються назад тими самими під політикою обʼєкта; крон `kiosk-day` під
`alisio_app` обходить ОБИДВА готелі (не «функція зникла», INC-014). `kiosk.check.ts` у
ланцюжок `check:pg`, прогін роллю `alisio_app` — рядок у звіт із числами. Репетиція
`rehearse-merge.sh beta` з дампом беты (дамп бере власник; якщо його немає в
`deploy/backups/` — `REHEARSE_LOCAL_PG=1` на свіжій базі, і сказати в звіті, що на живих
даних не репетирувалось).

**2. `check:routes` родина «кіоск» на Postgres-стенді** — той самий прогін, що на SQLite,
25 тверджень; розбіжність між рушіями — у звіт, не «зелено на одному».

**3. Лист «Kiosk heute»: мова поза `de | cs`.** `WORDS` знає дві мови; подивитись, як решта
листів поводиться з `documentLanguage()` для іншої юрисдикції (названа відмова чи
запасна мова), і зробити так само — не мовчазний `de`. Сцена.

**4. Дисплей.** `apps/kiosk-shell/README.de.md` — перевірити, що з нього рецепція/ІТ готелю
ставить термінал без нас: Chrome kiosk, автозапуск, без сну, адреса `/kiosk`, код парування
з картки; що робити, якщо екран показує «—» (немає мережі / термінал відкликано). Це
документ для людини в Ґрайці, не для сесії.

Документи тим самим комітом (ARCHITECTURE «Стан і що далі»: блок кіоска закрито, що
свідомо не зроблено — оплата на терміналі окремим застосунком, принтер, замки). Звіт —
розділ «Частина Г» + рядок «блок вичерпано, голова X» першим рядком. Після пушу — цикл
`MINE=4`; наступний номер прийде лише якщо рецензія Г щось поверне, інакше сесію можна
закривати.

```
MINE=4; while :; do git fetch -q origin; N=$(git show origin/claude/controller-2:docs/tasks/inbox/session-6.md 2>/dev/null | head -1 | sed 's/TASK: //'); [ "${N:-0}" -gt "$MINE" ] && { git show origin/claude/controller-2:docs/tasks/inbox/session-6.md; break; }; sleep 300; done
```
