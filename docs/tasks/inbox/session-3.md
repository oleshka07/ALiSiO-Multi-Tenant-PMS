TASK: 3

# Сесія 3 (ядро) — задача 3

**Спершу:** `git fetch origin` → `git merge origin/claude/channex-integration-66kv65`
(голова `0346fb93`). Твоя гілка злита; далі працюй від гілки робіт.

## Задачу 2 прийнято повністю, і перевіряв я не звіт, а інструмент

Прогнав твій сканер на **своїх** зразках:

```
property_id IS ?                    → names
property_id IS NOT DISTINCT FROM ?  → names
property_id = ANY(?)                → unknown
осі немає взагалі                   → silent
```

Обидві половини рішення 3 виконані. І окремо про те, що ти знайшла **на власній
правці**: джойн орендаря пишуть обома боками, 70 і 40 місць, і перша редакція
давала їм різний кошик залежно від того, як його набрав автор. Перевірив після
правки — обидва напрямки `silent + tenantJoinOnly`, літерал `'prop_1'` →
`unknown` (зашитий рядок не є областю), `IN (SELECT …)` → `silent`,
`IS NOT NULL` → `silent`. Це §3.2.1, застосована до себе, і це найцінніше в
задачі.

Базлайн 456 → 454, осідання рівно там, де має бути. Те, що ти **сказала в шапці
сцени**, що друга половина не зрушила жодної пари, — саме те, що треба: інакше
наступний порахував би це покращенням.

## Два рішення контролера, які ти просила

**1. `gift_cards` — таблиця йде за своїми читачами, не навпаки.** Усі чотири
читачі в чужих теках: `src/app/api/gift-cards/**` — сесія 1 за картою
(`src/app/api/**` її), `modules/widget` — сесія 2. Таблиця з твого списку
**знімається цілком**; маршрути до тебе не переходять. Правило на майбутнє:
власність визначає **тека читача**, а не таблиця — таблиця не має теки.

**2. Пари поза картою.** Переміряв твоїм гейтом сам: поза всіма названими
теками лежить рівно **один** файл — `src/modules/auth/api/platform.handlers.ts`
(2 пари). Він **твій**. Решта, що виглядала «нероздана», насправді в теках, які
я досі не називав уголос; називаю тепер:

| Тека | Пар | Власник |
|---|---|---|
| `modules/bookings` | 78 | **сесія 3** |
| `src/app/api/**` | 68 | сесія 1 |
| `modules/widget` | 62 | **сесія 2** |
| `modules/guests` | 44 | сесія 1 |
| `modules/invoicing` | 40 | сесія 1 |
| `src/lib/db.ts` | 34 | спільний — див. нижче |
| `modules/properties` | 27 | **сесія 3** |
| `modules/channels` | 27 | сесія 2 |
| `modules/pricing` | 25 | **сесія 2** (тарифи їдуть у вендора) |
| `modules/finance`, `reports` | 25 | сесія 1 |
| `tasks`, `events`, `dashboard`, `day-sheets`, `core`, `auth` | 24 | **сесія 3** |

**`src/lib/db.ts` (34 пари) не чіпає ніхто.** Це міграції, де орендаря ще немає
за побудовою — стеля 34 лишається і не опускається. Якщо колись опуститься, це
буде означати, що хтось переписав міграцію, а не полагодив вісь.

## Робота цієї задачі

Твоє — **129 пар**, і найбільше в `modules/bookings` (78), з них 21 в одному
файлі `bookings/api/reservation.handlers.ts`. Порядок:

1. `bookings/api/reservation.handlers.ts` — найгустіше, і це картка броні,
   тобто екран, який оператор тримає відкритим цілий день;
2. решта `modules/bookings` (`service-orders.handlers.ts` 8,
   `data/booking-history.repo.ts` 6 і далі за списком `--list`);
3. `modules/properties` (27);
4. `tasks`, `events`, `dashboard`, `day-sheets`, `core`, `auth` — хвіст (24).

Як і доти: червона сцена до коду з **числом** у повідомленні, `ALL_PROPERTIES`
з причиною словом, стеля опускається тим самим комітом. Це багато, тож бери
скільки встигаєш за підхід і звітуй числом — не намагайся закрити все одразу.

## Межа

Тек сесій 1 і 2 не чіпай. `src/lib/db.ts` — не чіпай узагалі в цій задачі.

Звіт — у той самий `docs/tasks/2026-09-08-INC-029-property-axis.report.md`.
Далі — чекай наступного номера (`MINE=3`).
