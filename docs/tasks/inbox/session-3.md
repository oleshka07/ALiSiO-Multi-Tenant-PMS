TASK: 6

# Сесія 3 — задача 6: підхід 2 прийнято; продовжуй блок

Сайти бронювання по осі, пошук називає будинок, виправлене число — злито.

Задача — БЛОК, і він не скінчився. Далі за порядком: решта
`modules/bookings` (найгустіше `bookings/api/reservation.handlers.ts` — 21 пара
в одному файлі, картка броні), потім `modules/properties` (27) і хвіст
`tasks`/`events`/`dashboard`/`day-sheets`/`core`/`auth` (24).

Після кожного коміту — пуш, рядок у звіт, наступний шматок, без очікування
номера. І кажи **числом**, скільки з пар виявились не читачами: цей висновок
вартий не менше за правку, і в підході 1 він був найціннішим у звіті.

---

# Повний текст попереднього блоку (задача 5)

# Сесія 3 — задача 5: ПРОДОВЖУЙ задачу 4, вона не скінчилась

Підхід 1 прийнято і злито. Але блок — 129 пар, і ти зупинилась після першого
підходу, чекаючи номера. Чекати не треба було: правило змінене
(`docs/tasks/AUTOLOOP.md`, крок 5), задача це БЛОК, а не підхід.

Далі за порядком задачі 4: `bookings/api/reservation.handlers.ts` (21 пара в
одному файлі, картка броні), решта `modules/bookings` (78 разом),
`modules/properties` (27), хвіст `tasks`/`events`/`dashboard`/`day-sheets`/
`core`/`auth` (24).

Після кожного підходу — пуш, рядок у звіт, наступний шматок. І, як у підході 1,
кажи числом, скільки з пар виявились НЕ читачами: цей висновок вартий не менше
за правку.

---

# Повний текст блоку (задача 4)

# Сесія 3 (ядро) — задача 4

**Спершу:** `git fetch origin` → `git merge origin/claude/channex-integration-66kv65`.

## Підхід 1 прийнято

Замовлення послуг по осі обʼєкта злито. Найцінніше — не сама правка, а
висновок, який ти зробила з виміру: **129 пар — переважно не читачі**. Це те
саме, що я казав про 136 пар списку: число пар не є числом дірок, і сказати це
числом важливіше, ніж перевести ще десять рядків.

Твоє доповнення до `check-routes-live` (родина осі виросла до восьми маршрутів,
24 твердження з 96, і кожне поповнення знаходило свій дефект — `/api/fees` теж
віддавав 500 на чужий обʼєкт) я взяв у злиття окремо: у конфлікті таблиці
гейтів твоя редакція рядка перемогла нашу саме тому, що вона **доповнена**.

## Робота цієї задачі

Далі за списком твоїх тек, у тому самому порядку:

1. `modules/bookings` — 78 пар, і 21 з них в одному файлі
   `bookings/api/reservation.handlers.ts`. Це картка броні, тобто екран, який
   оператор тримає відкритим цілий день; починай з нього.
2. решта `modules/bookings` — `booking-history.repo.ts` (6) і далі за `--list`.
3. `modules/properties` — 27 пар.
4. хвіст: `tasks`, `events`, `dashboard`, `day-sheets`, `core`, `auth` — 24.

Як і доти: червона сцена до коду з **числом** у повідомленні,
`ALL_PROPERTIES` з причиною словом, стеля опускається тим самим комітом. І, як
у підході 1, **кажи числом**, скільки з них виявились не читачами — цей
висновок вартий не менше за правку.

## Межа

Теки сесій 1 і 2 не чіпай. `src/lib/db.ts` — не чіпай узагалі.

Звіт — у той самий `docs/tasks/2026-09-08-INC-029-property-axis.report.md`.
Далі — чекай наступного номера (`MINE=4`).
