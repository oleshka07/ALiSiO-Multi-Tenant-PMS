TASK: 1

# Сесія 3 (ядро) — задача 1

**Спершу:** `git fetch origin` → `git rebase origin/claude/channex-integration-66kv65`.
Протокол автопетлі — `docs/tasks/AUTOLOOP.md`: власника в циклі більше немає.

Звуження означення, базлайн **506 у 130 файлах**, список 136 пар і виправлення
`checklists/route.ts` прийнято попередньо — повна рецензія після злиття фінансової гілки.
Особливо цінне: ти назвала прогалину карти, а не обійшла її.

## Прогалину закрито — редакцією 3

`docs/tasks/2026-09-08-INC-029-property-axis.md`, «Карта власності, редакція 3». Твоє додалося:
**`src/modules/tasks/**` і `src/modules/dashboard/**`** (плюс `housekeeping`, який уже був).
`modules/guests` і `src/app/api/**` пішли сесії 1. `src/lib/db.ts` лишається спільним.

## Робота цієї задачі

Переводь читачів **своїх** тек на `PropertyScope`, у такому порядку:

1. `modules/events` і `gift_cards` — нуль названих читань, найгустіша тиша.
2. `modules/day-sheets` — аркуш дня, оператор бачить його щоранку.
3. `modules/tasks`, `modules/dashboard` — нове з редакції 3.
4. `modules/bookings` — `reservations` найбільша, лишай наостанок.

Кожен переведений читач — червона сцена до коду, з **числом** у повідомленні («очікували 5
номерів обʼєкта A, отримали 12»). Кожен виклик з `ALL_PROPERTIES` — з коментарем, чому саме всі.
Стелю храповика опускай тим самим комітом, що й правку.

## Межа

Тек сесій 1 і 2 не чіпай навіть коментарем. Потрібна зміна в чужій теці — рядок у звіт
(«файл, рядок, чому») і зупинка на цьому пункті, решта задачі робиться далі.

Звіт — `docs/tasks/2026-09-08-INC-029-property-axis.report.md`.
Далі — чекай наступного номера (`MINE=1`).
