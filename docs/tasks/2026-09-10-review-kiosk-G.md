# Рецензія Г — сесія 6 (кіоск), задача 4, голова `9d57738e`

Контролер 2, 10.09.2026, ~19:00 UTC.

## БЛОК «КІОСК» ВИЧЕРПАНО. Голова для злиття в гілку робіт — `9d57738e`.

`kiosk.check.ts` уже стоїть в обох ланцюжках `package.json` (`check` і `check:pg`), тож
злиття не потребує правки ланцюжків. Гілка містить гілку робіт до `3616cc36` (злиття
`origin/claude/channex-integration-66kv65`).

## Що перевірено — власним стендом, не звітом

**Справжній Postgres, роль `alisio_app` (`rolsuper = f`):** `initdb` на 55433, `schema.sql`
без помилок, усі 110 міграцій `ON_ERROR_STOP`, `rls-check.sql` — «all checks passed».
`kiosk.check.ts` під `DB_DRIVER=postgres` цією роллю — **23 сцени зелені**, включно з 1
(парування через `runWithPublicToken`), 8/19 (чужий рахунок — «немає»), 20 (доба трьома
осями). `tsc` 0.

**Моя мутація на інваріант 14:** у `pairingByCode` (`devices.repo.ts:112`) прибрано
`runWithPublicToken` — читання коду парування без перепустки на зʼєднанні. Під `alisio_app`
сцена 1 **червона**: «свіжий код: очікували 200, отримали 400». Політика 0411 справді
тримає: без токена на зʼєднанні рядка не видно.

**Знахідка сесії, підтверджена мною:** `SUBSTR(check_in, 1, 10)` у пошуку — на Postgres
`check_in` є `DATE`, `substr(date, int, int)` не існує, пошук гостя віддавав би 500 на кожен
запит; замінено півінтервалом. Це те, заради чого частина Г існувала: статика і SQLite цього
не бачать.

**Лист поза `de | cs`** → англійська, як `localeForLanguage()` у решті документів; сцена 21
доповнена. **`withoutAddress`** окремим числом у крона — «термінал є, адреси немає» більше не
ховається в `skipped`. **`check-routes-live`**: 125 тверджень, 27 родини «кіоск», однаково на
обох рушіях (за звітом; мій прогін — лише гейт, не живий сервер).

**README оболонки, §6 для рецепції** — прочитано очима: чотири стани екрана з діями,
«перезавантаження майже ніколи не допомагає», «чого термінал не показує ніколи» (повних
прізвищ, номерів документів, чужих броней — і якщо показує, це дефект, а не налаштування).
Так і треба писати для людини в холі.

## Знахідка поза блоком — контролеру 1

`db/postgres/schema.sql:1023` оголошує `fin_payment_methods.settles_to_debtor BIGINT DEFAULT
0`, а міграція `0141:63` — `BOOLEAN DEFAULT FALSE`. Перевірено grep-ом. На СВІЖІЙ базі
(новий клієнт, стенд §7, сам `check:pg`) писач довідника оплат впаде з «invalid input syntax
for type bigint». Таблиця в роботі сесії 1 (Д61); виправлення — рядок в `OVERRIDE`
генератора `pg-schema.mjs` + перегенерація. Сесія 6 правильно не чіпала чужу таблицю.

## Що свідомо не зроблено (ARCHITECTURE «Стан і що далі»)

Оплата на терміналі — окремий застосунок роду `payment` (Unzer, за рішенням власника К1);
принтер; керування замками. Репетиція `rehearse-merge` на живому дампі беты — за
контролером 1 при злитті, як для кожної гілки.

## Далі

TASK 5 сесії 6 — закрити сесію. Голова `9d57738e` — контролеру 1 через власника: злити в
гілку робіт → бета → власник дивиться екран на беті (ЧЕКПОІНТ 2 на стенді) → `main`.
