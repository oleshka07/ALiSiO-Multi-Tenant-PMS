# Рецензія Б2 — сесія 5, задача 8, голова `53550ce7`

Контролер 2, 10.09.2026, ~14:30 UTC.

## Вердикт

**Прийнято.** Усі чотири блокери рецензії Б закриті і доведені живим проходом; дельта й
незатирання зроблені з червоними гейтами (Б7–Б9, шість зломів названі). Лишилось троє
дрібних — у TASK 9, разом із перенумеруванням міграцій.

## Живий прохід №3 (той самий знімок 11.03.2025, `since 2025-01-01`, 187 с у SQLite)

Порівняно з проходом №2: **компаній 2 143** (було 17 265) — рівно адреси з `ADR_WAHL = 1`;
гостей 32 652 живих; `fin_folio_items` **3 519 на 202 710,49 € цент у цент**, з них
**`lodging` 1 268 рядків на 163 709,09 €**, `service` 2 251 на 39 001,40 € (було: усе
`service`); **платежів у фоліо 458 на 80 891,15 €, усі `source = 'import'`**, `fiscal_guard`
0 (було 295 у staging); послуги більше не відмовляють — 23 без пари в каталозі названі в
`explained` (Getränke, Frühstück - Speisen, Raummiete…). Перетини: 26, **усі
`winhotel_double`** — дві живі броні Winhotel на одному номері в ті самі дати, тобто його
власний овербукінг, staging правильний. `merged_by_name` 4 049, з них інша дата народження
584, інше місто 533 — число для сесії 3 (злиття гостей). Витяг і базу видалено.

## Що лишилось (TASK 9)

1. **Один рядок звірки досі червоний, тепер в інший бік:** «майбутні брони, живі, без staging»
   — Winhotel 889 (912 − 23), наші 901; `mismatch: true` через нього одного. Або наш бік
   рахує staged як імпортовані, або віднімання не з того числа. Порахувати обидва боки одним
   SQL і виправити рядок; гейт на фікстурі, де staged майбутніх ≠ 0.
2. **Джерело броні — не `EXT_SOURCE`:** живі значення — числа (`18778622`, `2140901178`…),
   це номери броней каналу, не назви; `source` = `direct` у 3 012 із 3 026. У `GASTKREF`
   (`booking_refs`) є ще колонки — подивитись у живому витягу, яка з них несе канал
   («Booking.com», «DIRS21», «Onlinebuchung»), і мапити її; номер каналу — в `external_ref`.
3. **Міграції — смуга 04xx (inbox §0):** злити `origin/claude/block-apps` (0140–0142 →
   0400–0402), перейменувати 0143 → 0403, 0144 → 0404, 0145 → 0405 (ніде не накочені);
   `db.ts`, ARCHITECTURE, звіти за новими іменами; жодного дубля міграції під двома іменами.
4. CI гілки на `53550ce7` — рядок у звіт (звіт каже «дивитись після пушу»).

Після цього застосунок готовий до першого живого запуску на беті зі свіжим бекапом із
сервера готелю (агент, AnyDesk — крок власника).
