# Чекпоінти: відкриті рішення власника

Один рядок — одне рішення, якого чекають. Заводить і закриває контролер. Закриття — датою і
відповіддю в тому ж рядку, рядок не видаляється. Власник уранці відкриває цей файл, а не звіти
трьох сесій.

**Що сюди потрапляє:** гроші, ринок, смак, незворотний крок. Усе інше контролер вирішує сам за
фактами з коду й інваріантами і сюди не пише.

## Відкрите

| # | З якого дня | Хто чекає | Рішення | Стан |
|---|---|---|---|---|
| Ч1 | 07.09 | сесія 1 | **Міграція матриці заселеності.** Звіт на дампі готовий — `docs/MATRIX-MIGRATION-REPORT-2026-09-05.md`; у гілку нічого не накочено. Питання: накочувати на живі готелі чи лишити стару матрицю до злиття в `main`. | відкрито |
| Ч2 | 07.09 | контролер | **Деплой бети і перегравання Channex.** Контролер каже: не з `8cdbddc` — спершу правка 1 рецензії раунду 2 (витік обмеження пари на тип із редактора дня). Власнику: підтвердити, що чекаємо новий коміт, а не деплоїмо зараз. | відкрито |
| Ч3 | 07.09 | сесія 2 | **Затвердити Д11–Д16.** Д11–Д13 прийнято контролером неявно 07.09; Д14 (перетягування = перенесення, ціна не перераховується), Д15 (закритий номер — конфлікт для перенесення), Д16 (реєстрація з картки пише обидві книги) — смак і продукт. | відкрито |
| Ч4 | 07.09 | контролер | **Фоліо групи: один рахунок на групу.** `fin_folios.reservation_id` вішає фоліо на одну бронь, `parent_id` у `modules/invoicing/` немає. Арифметика після К4 правильна, але «один рахунок на групу» не зроблено. Питання ринку: чи потрібне воно до першого живого готелю. | відкрито |
| Ч5 | 07.09 | контролер | **`site_rate_plans` ↔ `rate_plans`.** Правило цін і промо, звужені на тариф, не діють у формі бронювання (рецензія раунд 2, п. 4.1). Або звʼязати таблиці, або назвати обмеження на екрані. Перше — окремий блок. | відкрито |
| Ч6 | 07.09 | контролер | **Ліміт промокоду в котируванні оператора** (п. 4.5). `max_uses` стереже лише сайт; рецепція може віддати «останні 10» скільки завгодно разів. Свідоме рішення з підписом на екрані — чи закривати. | відкрито |

## Закрите

| # | День | Рішення | Відповідь |
|---|---|---|---|
| Ч0 | 07.09 | Обмеження на типі чи на тарифі (15а / Ц32) | **на тарифі**; тип — зручність запису на всі тарифи. Причина: тести 5/7/8 ставлять різні обмеження на тарифи одного типу |

## Рішення, які контролер ухвалив сам (власника не турбувати)

| День | Рішення | Підстава |
|---|---|---|
| 07.09 | Стелю `pricing` у `check-boundaries` не піднімати; пробою вже немає з `5563e8f` | факт: прогін на трьох комітах; `AGENTS.md:426` |
| 07.09 | Стелю `bookings` 3 → 2 сесії 3 прийняти; при появі дверей `@bookings` вона повертається до 3 і це не регрес | храповик валить збірку і з боку покращення |
| 07.09 | b3 розблоковується перебазовкою на `8cdbddc`, не зміною базлайну | `merge-base b3` = `92f4ce2` — вікно, де пробій жив |
| 07.09 | Правка `ari-adapter.check` cleanup (контекст орендаря) — сесії 1, окремим комітом після перегравання | не блокує CI: там суперкористувач |
