# Рецензія В — сесія 6 (кіоск), задача 3, голова `6ea221cf`

Контролер 2, 10.09.2026, ~17:50 UTC. Перевірено у власній копії на `6ea221cf`.

## Вердикт

**Прийнято.** Пункти 1–2 (смуга, телефон) прийняті ще на `78368a0a` моїм власним запуском
екрана: смуга 672–1632 px на 1080×1920 = рівно 35,0–85,0 %, «Nachname» і «Weiter» видно,
`scrollHeight == clientHeight`; мутація `PHONE_MIN_DIGITS = 1` → `200 !== 400`. Частина В —
так, як просилось, і два рішення сесії кращі за задачу.

## Що перевірено

- `tsc` 0 (перший прогін був червоний на `.next/dev/types` — залишок МОГО dev-сервера зі
  старим маршрутом `admin/pairings`; це стенд, не код). `kiosk.check.ts` — сцени 13–23 зелені.
  `check-route-guards --strict` 0 без сегмента `admin/`.
- **Мої мутації, обидві червоні:** `today.repo.ts` без `e.organization_id = ?` → сцена 20
  «у B рівно одне заселення, отримали 3»; `dayBounds` з `offsetMs = 0` (зона проігнорована)
  → та сама сцена «отримали 0». Тобто КІ16 тримає не текст, а гейт.
- Варта: усі сім хендлерів картки під `withOwner` + `hasFeature` + `ownsProperty`
  (`admin.handlers.ts:38, 60, 86, 110, 152, 206, 243`); крон — `cronAuthFailure`,
  `failedOrganizations` у тілі і статус 500 при відмові хоч одного готелю; розклад 07:00 у
  `setup-backup-cron.sh:74` і рядок у DEPLOY. Лист — `documentLanguage(propertyId)`, словник
  de/cs у коді, список рецепції лише в `external`.
- 0413: чотири колонки `properties`, у `CREATE` і в `ALTER`, `BOOLEAN` названо в `OVERRIDE`
  генератора (спіймано `check-fresh-schema`), CHECK на `kiosk_signature` за означенням.
  Нової таблиці немає — RLS не потрібен.
- КІ14 (політики — колонки обʼєкта, вигляд — `config_json` пристрою), КІ15 (картка під
  `/api/settings/apps/kiosk/`, публічний префікс лише для термінала), КІ16 (доба готелю) —
  записані в DECISIONS, **прийняті**. КІ15 — правильне спрощення: менше пояснень у
  `proxy.ts` і в гейті варт.
- Дефект `NaN → 0` у робочій смузі, знайдений сценою 23 до рецензії, — саме той клас, що
  «виглядає правильним»; мутація «`Number()` приводить» червона.

## Дрібне (не блокує, у TASK 4)

`WORDS` листа знає лише `de | cs`; `documentLanguage()` для обʼєкта з іншою юрисдикцією
має впасти на названу відмову, а не мовчки на `de` — перевірити, як зроблено в решті
листів, і зробити так само.

## Що лишилось до кінця блоку

Частина Г — TASK 4. Після неї блок «Кіоск» вичерпано, голова йде контролеру 1 на злиття.
