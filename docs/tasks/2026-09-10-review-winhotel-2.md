# Рецензія 2 — сесія 5 (схема Winhotel), задача 2, голова `3d88afa7`

Контролер 2 (Schlossberghotel / Застосунки), 10.09.2026, ~08:35 UTC.

## Вердикт

**Задачу 2 прийнято.** §2 задачі 1 закрито з витягу: 258 таблиць описано, 11 сутностей
мають таблиці або чесне «не знайдено», довідники переписані читабельно, контрольні числа
на 11.03.2025, `extract.sh` дістав крок агрегатів. Суперечність у задачі (дослівні імена
колонок проти PII-grep) сесія назвала першим абзацом і вирішила обережно — правильно за
протоколом; рішення контролера нижче.

## Перевірено фактами

- `git diff ccc6cf6b 3d88afa7 -- docs/research/winhotel/extract-out` порожній — сирий витяг
  не чіпався. Історія лінійна (`c391ad5f → ccc6cf6b → 3d88afa7`).
- `grep -riE "iban|@|strasse|str\."` поза `extract-out/` і `extract.sh` — порожній.
  `check-no-tenant-names` — «чисто — 1275 файлів», `check-docs-current` — чисто,
  `check-fatal-grep` — чисто (прогнано на голові в хмарі).
- Покриття `TABLES.md`: усі 258 імен з `table-counts.tsv` знайдені, крім одного —
  `PLZ_STRASSE`, свідомо скороченого до `PLZ_STR…` (див. рішення).
- Знахідка 1 (бронь = `GASTKONT`, не `BELEGUNG`) підтверджена DDL: `FK_BELEGUNG
  (LNR_GK) → GASTKONT(LNR)`, `FK_GASTKONT BUCHKONT(GK_LNR) → GASTKONT(LNR)`; у `GASTKONT`
  є `GASTNR_1/2/3`, `VERK_NR`, `BUCH_STATUS`, `LNR_KATE`, `LNR_ZINR`. Усього FK у базі 6 —
  збігається зі звітом.
- Знахідка 2: `< 1000` у DDL — 444 входження (звіт каже 442; різниця — пробіли/регістр,
  неістотно). Правило «`TA_STATUS < 1000` у кожному запиті імпорту» — правильне і вже в
  кроці агрегатів.
- `REFERENCE-SAMPLES.md`: 10 довідників, маски `FIRMA_A/B` збережені, `MANDANT_FISKAL` без
  ідентифікаторів, розділ «не виводились» названий. `COUNTS-2025-03.md`: 30 чисел із
  джерелами, решта — з готовим SQL. `bash -n extract.sh` ok.

## Рішення контролера щодо імен колонок

Дослівні імена таблиць і колонок вендора (`PLZ_STRASSE`, `ADRESSEN.STRASSE`, `IBAN`) —
**не персональні дані**, і в дослідницьких документах вони мають бути дослівними: інакше
`grep` по документах не знаходить таблицю. PII-grep приймання лишається, але виключає
відомі імена колонок словом, не текою — правило в задачі 3.

## Що лишається відкритим (не зауваження, а факти)

`Zimmer FIX zugesagt`, точні стани броні (`BUCH_STATUS`/`CI_STATUS`), остання
`Rechnung-Nr` проти 33 059 рядків `RECHNUNG`, відкриті дебіторські, сальдо ваучерів —
усе потребує прогону кроку агрегатів на базі. Контролер зробить це на наступному знімку
(`.fbk` власника ще на диску; свіжий бекап — питання до готелю). Сесії 5 бази не давати.
