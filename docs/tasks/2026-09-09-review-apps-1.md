# Рецензія 1 — сесія 4 (Блок «Застосунки»), голова `3070d15e`

Контролер 2 (Schlossberghotel / Застосунки), 09.09.2026, 22:30 UTC.
Рецензовано: `b167997b` (весь блок, 36 файлів, +2944/−356), `36b5c416` (злиття гілки
робіт `7e28abd8`), `14342813` (звіт), `3070d15e` (злиття `44c5a222` — задача з §3.8).

## Вердикт

**Прийнято по суті, злиття — після `beta → main` (З5), як і планувалось.** Гейт справжній,
стан звʼязку в одному місці, реєстр — одне джерело, чужі теки не порушено. Блок
**не скінчено** з однієї причини, що не залежала від сесії: пункт **§3.8 «Підключити
TSE»** приїхав у задачу комітом `44c5a222` після того, як сесія відгалузилась від
`b5067fa7`, і був злитий останнім комітом (`3070d15e`) уже після звіту. Він — задача 2.
Плюс три правки з цієї рецензії.

## Перевірено фактами (не зі звіту)

- **Прогін у хмарі контролера на `3070d15e`** після `npm ci`: `tsc --noEmit` — 0;
  `node src/core/apps.check.ts` — усі твердження зелені (SQLite);
  `check-boundaries --strict` — «40 по 16 модулях, у межах стелі»; `check:i18n` —
  3533/3533 de і cs.
- **Гейт уміє червоніти — доведено двома мутаціями на голові:**
  (1) `features.ts` `fiscal_de` → `kind: MODULE` ⇒
  `AssertionError: kind «fiscal_de» = module, а вирішено app`;
  (2) `app-connections.ts:116` `console.error` → `throw e` ⇒
  `Got unwanted rejection: reportError кинув, коли база відмовила` з
  `SqliteError: FOREIGN KEY constraint failed`. Обидві повернуто.
- **Межі тек** (`git diff --stat 44c5a222..b167997b`): поза списком «твої» —
  `fiskaly-sign-de.ts` (дозволений один виклик; зроблено обгорткою `reported()` — одне
  місце, приймаю), `mail/email.ts` (те саме + `probeMail` — прийнятно, бо йде тим самим
  `reported()`), `settings/page.tsx` (хаб; не названий ні своїм, ні чужим — рішення
  сесії правильне), `scripts/check-boundaries.mjs` (стеля 3 → 4 — **не приймаю**, нижче),
  `db/postgres/rls-check.sql` (+ рядки обох таблиць — потрібно), `docs/NAMING.md` (+6 —
  потрібно за §3.1 AGENTS), `navigation.ts` — два рядки замість одного (перейменування
  «Модулі та інтеграції» → «Модулі» + новий пункт): виправдано, бо старе слово брехало б.
- **Міграція 0140** прочитана цілком: обидві таблиці з `organization_id`, індексом,
  FK за означенням, RLS + FORCE + політика; унікальність по `COALESCE(property_id,'')`
  замість UNIQUE із NULL — правильно; CHECK статусу є; `app_calls` немає (як відкликано).
  `schema.sql` — лише дві нові таблиці, FK і одна перестановка колонок `broken_fields`
  (косметика, інваріант 10).
- **`app-connections.ts`**: `upsert` цілком у `try`, `lawAllows` по `scope` маніфесту,
  UPDATE→INSERT без `ON CONFLICT` по виразу (пояснено в коді), текст помилки обрізаний до 500.
- **`apps.ts`**: 9 маніфестів, «скоро» без `feature` і без `fields` (П5), шлюзи через
  `appCatalog(PAYMENT_PROVIDERS)` за посиланням — цикл імпортів розвʼязано перевіркою,
  як у `payments.check.ts`. `kind` у `features.ts`: core = `booking_engine`, `dashboard`,
  `invoicing`; app = `fiscal_de`, `online_payments`; решта module — як у §3.1 задачі.
- **`fiskaly-sign-de.ts`**: `BASE` досі `https://kassensichv.fiskaly.com/api/v2` — §3.8
  не робився (підтверджує, що пункт не бачили).
- Злиття `36b5c416`: після нього `check:i18n` 3533 — число зрушило на +1 проти
  звіту сесії (3532), тобто словники обʼєднано, а не обрано сторону.
- CI на гілці: перевірити не зміг — токен без права на Actions. Питання власнику.

## Що не приймаю або треба виправити (задача 2, крім §3.8)

1. **Стеля `invoicing` 3 → 4 у `check-boundaries.mjs` — повернути на 3.** Гейт був
   правий: `apps.check.ts` імпортує `modules/invoicing/data/` через межу. Це не
   «хибно-червоний гейт», який лагодиться в гейті, — це код під гейт. Правильні двері —
   фасад. **Дозволяю один рядок у чужій теці:** `src/modules/invoicing/api/index.ts` —
   `export { fiskalyDevice, fiskalyProbe } from '../data/fiskaly-sign-de';` (див. п. 3).
   Після цього `_handlers.ts` і гейт імпортують `@invoicing`, стеля назад 3, коментар
   про «3 → 4» прибрати (він описує стан, якого більше немає — AGENTS §2).
2. **`reported()` у fiskaly приписує вендору НАШУ відмову.** `signReceipt` кидає
   `'Receipt has no VAT split — sign the invoice, not a bare number'` до першого
   мережевого виклику, а `reported()` записує це як `status = 'error'` із цим текстом:
   готель побачить на картці «помилка TSE», хоча TSE ні до чого. Перевірку розбиття ПДВ
   винести перед `reported(...)` (або звітувати лише те, що впало на `call()` — на
   `fetch`/HTTP). Гейт: фікстура з чеком без розбиття → рядок `app_connections` для
   fiskaly НЕ змінюється (це третя вісь до інваріанта 26: «наша відмова / відмова
   вендора / успіх»).
3. **Проба fiskaly кнопкою** (п. 7.1 звіту) — робити, двері дозволені п. 1:
   `fiskalyProbe(config)` = auth + `GET /tss/{id}` через `reported()`, без транзакції
   (транзакція коштує підпису). `PROBEABLE` + картка.
4. **Посилання з `/app/platform` на `/app/platform/apps`** (п. 7.3) — дозволяю один
   `<Link>` у `src/app/app/platform/page.tsx`.
5. Пункт 7.2 (екран «Канали» без `feature: 'channels'`) — **не твій**, іде іншому
   контролеру через журнал; названий виняток у гейті лишається, як є.

## Зауваження без дії

- Імпорти в `fiskaly-sign-de.ts` відносними шляхами `../../../core/…`: 10 сусідніх
  файлів `data/` пишуть `@core/…`, цей — єдиний із відносними. Вирівняти при п. 2.
- `app_connections.property_id` FK на `properties(id)` без звірки з організацією —
  так само, як у решті тенантних таблиць із обʼєктом; закон тримає писач і RLS. Гаразд.
- DECISIONS З10–З16 — записані з обґрунтуванням; З13 (найгірший стан обʼєктів = стан
  картки) — правильно для каси.
