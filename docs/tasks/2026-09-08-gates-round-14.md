# Раунд 14, 08.09.2026 — гейти на голові гілки робіт прогнано

Контролер. Не рецензія коду, а **прогін**: те, чого не зробив попередній контролер, коли зливав
`block-4-hotel-day` і `block-live-routes-2`.

## Перше: голова була названа неправильно

`claude/JOURNAL.md` і передача стверджували, що голова гілки робіт — `b90ef5e`. Насправді
`origin/claude/channex-integration-66kv65` = **`20084df`** (сам документ раунду 13, 172 рядки).
Код на голові = `f1a0d99`. Помилка нульової ціни, але того самого роду, про який попереджала
передача — і стояла вона в самій передачі. **Правило: номер коміта в журналі — теж твердження
про код і пишеться після `git for-each-ref`, не з памʼяті.**

## Умови прогону

Хмарний контейнер (там є root): Postgres 16, `db/postgres/schema.sql` + 84 міграції, роль
`alisio_app` з тими самими грантами, що в `checks.yml`, `DATABASE_URL` під нею. Дерево — чистий
клон на `20084df`, не робоча копія власника. Прогнано **весь** склад `checks.yml`, три роботи.

| Крок CI | Результат |
|---|---|
| `npx tsc --noEmit` | зелено |
| `npm run check` | зелено |
| `npm run check:i18n` | зелено, cs і de — 3443 з 3443 |
| `npm run check:unwrapped` | зелено, 158 файлів |
| `npm run check:i18n-leak` | зелено |
| `npm run build` | зелено |
| `check-fresh-schema.mjs --settles` | 121 таблиця, 192 індекси, проходи 1 і 2 однакові |
| `check-schema-drift.mjs` | 121 таблиця, 1550 колонок, 394 індекси, 353 обмеження, дріфту 0 |
| `apply-hotel.mjs --all` двічі | ідемпотентно, жодного `+ ~ !` у другому прогоні |
| `npm run check:pg` роллю `alisio_app` | зелено |
| `provision-org.mjs` | `organization_id org_*`, базова мова `de` |
| `check-isolation.mjs` | зелено |
| `check-routes-live.mjs` | зелено, **72 твердження** |
| Playwright `playwright.ci.config.ts` | **32 з 32** |

Єдине відхилення від CI: Playwright запущено з підміненим `executablePath` (контейнер має
chromium 1194, проєкт пінить 1234). Це середовище, не код.

**Обидва структурні гейти 08.09 спрацювали видимо, а не мовчки пропустили себе:**
перший рядок прогону маршрутів — «ціль: 127.0.0.1 (локальна: так)», далі «збірка на
http://127.0.0.1:3000: очікуємо `9yK9C_QsUFGVI96zt-wT6`, сервер відповів 200».

## Межі цього доказу

Три блокери раунду 13 живуть на **незлитих** гілках. Перевірено машинно: ні `18ae8fe`, ні
`95afc44` не є предками `20084df`. Отже зелені гейти на голові нічого не кажуть про Р13.1,
Р13.2 і Р13.9. Так само на голові ще немає `check:onboarding` — він приходить разом із
`18ae8fe`.

## Побічно виявлено

- Робоча копія власника (`D:\Antigraviti\ALiSiO Multi-Tenant PMS`) чекнута на
  `cleanup/remove-antigravity-scaffold` (`e939efb`, 27.07). 665 файлів «змінені» — це чистий
  CRLF: з `--ignore-cr-at-eol` різниця лишається в одному `.gitignore`, +4 рядки. Локальні
  гілки застарілі: `claude/channex-integration-66kv65` = `cc13195`, `beta` = `ec0a995`.
- `origin/beta` = `0529dd5` — **18 комітів позаду** гілки робіт. Рівність «гілка робіт = beta»
  зараз не виконується.
- `claude/block-3-channels` — **87 комітів відставання** (у передачі стояло 71).
