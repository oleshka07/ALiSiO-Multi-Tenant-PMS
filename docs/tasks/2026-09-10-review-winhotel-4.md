# Рецензія 4 — сесія 5 (схема Winhotel), задача 4, голова `b1574ed0`

Контролер 2 (Schlossberghotel / Застосунки), 10.09.2026, ~09:55 UTC.

## Вердикт

**Задачу 4 прийнято.** `hotels/schlossberghotel.json` звірено з базою і приведено до неї;
розбіжності названі в `HOTEL-FILE-DIFF.md` з питаннями власнику. Блок сесії 5 (дослідження
+ файл готелю) вичерпано.

## Перевірено фактами (у хмарі на `b1574ed0`)

- Файл розгорнуто скриптом: **33 юніти = 31 справжній** (рівно перелік `ZIMMSTAM ZINR <
  9000`: 102–106, 109, 110, 201–206, 208–212, 214, 231–236, 238–242, 244 — без пропусків і
  зайвих) **+ AP 111/112**, лишені за README і названі питанням власнику. Поверхи 1/2/3 за
  `STOCK`; типи за `LNR_KATE` (110 B, 201/210 SD, 214 ES, 231/238 SK, 240/244 V) — збіг.
- Категорії: назви з `KATESTAM.BEMERK1`, місткість `ANZ_ERW/ANZ_K1` перенесена (DZD/DZK
  2+1, B 3/2/3), стелі Suite 4 і V 4 — з прайсу 2027, база не суперечить.
- `taxRates`: історія `STEUSTAM` додана (5 %/16 % 07–12.2020, 7 %/19 % з 2021) з
  `validTo`; `apply-hotel.mjs:402–405` справді пише `valid_to` — ключ `code + validFrom`
  тримається.
- Серія фактур: `code RE, prefix '', numberFormat '{seq}'` — без префікса й року, як
  22.591; стартовий лічильник не вигаданий (DIFF: у день X з `MAX(RECHNUNG.RECHNR)`).
- Послуги: 8 → 44 з `LEISTSTA` (`vatCode` за `STS`, категорія за `WARENGRU`); ціни з
  `BETRAG`/1000 або з прайсу 2027; нулі з бази лишені нулями і названі в DIFF. Haustier
  переведено на `reduced` за базою (STS 2, KONTONR 4301) — правильно триматись бази і
  спитати бухгалтера, DIFF це каже.
- `acceptance` 10 сценаріїв на 2027 — перерахував руками: DZK 2p NS 3 ночі 85→75×3 = 225,
  DZD 1p NS 3 ночі 64×3 = 192, DZD 2p HS 2 ночі 200, SD 3p HS (114+19)×2 = 266, SK 3p NS
  3 ночі (100+19−10)×3 = 327, V 4p HS 3 ночі 123×3 = 369, ES 2p NS 4 ночі 592, AP 80/135 —
  усі сходяться з прайсом 2027.
- Гейти: `check-hotels` — «чисто — 2 готелів»; `apply-hotel --dry-run` — «готелі
  відповідають файлам», exit 0; `check-no-tenant-names` — чисто (1275 файлів); PII-grep
  за README — 0; імен компаній у файлі — 0; `extract-out/**` не чіпався.

## Для власника (з DIFF, не для сесії)

Номер **201** у базі має `STOCK = 1`, у файлі — поверх 2 (за нумерацією й сусідами); AP
111/112 у Winhotel не велись — заводити як номери чи «на запит»; Haustier 7 % чи 19 % —
слово бухгалтера.
