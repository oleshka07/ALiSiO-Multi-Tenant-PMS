# Рецензія 4 — сесія 4 (Блок «Застосунки»), задача 4, голова `b5848570`

Контролер 2 (Schlossberghotel / Застосунки), 10.09.2026, ~08:25 UTC.
Рецензовано: `d80c4b10` (В1), `3dae4441` (злиття `18d2cdb2` — лише docs), `198f8f6a`,
`b5848570` (звіт).

## Вердикт

**Задачу 4 прийнято. Блок «Застосунки» вичерпано** — зауважень немає. Гілка чекає злиття
після `beta → main` (З5); до того сесія лише підбирає гілку робіт злиттям, якщо вона рушить.

## Перевірено фактами (у хмарі контролера на `b5848570`)

- `tsc` 0; `apps.check.ts` — сцени 3.8, А1, А2, **В1** зелені; `check-boundaries --strict`
  «39 по 16 модулях», стеля `invoicing` = 3 (`check-boundaries.mjs:78`); `check:i18n`
  3542/3542.
- **Гейт В1 уміє червоніти — мутація на голові:** у `fiskalyConnect` замінив
  `state = String(current.state ?? '')` (`fiskaly-sign-de.ts:286`) на `state = 'CREATED'`
  (тобто `GET /tss` читається, але ігнорується) ⇒ `повторний натиск після часткової відмови
  відповів 502 … 502 !== 200` — стаб відхиляє `PATCH UNINITIALIZED → UNINITIALIZED`, як
  вендор. Повернуто, дерево чисте.
- `fiskaly-sign-de.ts` (`fiskalyConnect`): без `resume` — `state = 'CREATED'`; з `resume` —
  `GET /tss/{id}` → `state`; стан поза `CREATED/UNINITIALIZED/INITIALIZED` → `Error` з
  `tssId` і станом (через `FiskalyConnectError` у `catch`, тобто текст лягає в
  `app_connections` через `reported()`); `PATCH {UNINITIALIZED}` лише з `CREATED`;
  `PATCH {INITIALIZED}` лише коли ще не `INITIALIZED`; `admin`/`admin/auth`/`client` —
  завжди (PIN ставиться заново тим самим PUK — правильно: попередній PIN міг не дійти до
  бази). Коментар «живий прохід скаже» переписано на таблицю переходів.
- Стаб у `apps.check.ts` тримає стан кожної TSS і відхиляє перехід у поточний стан — тому
  на старому коді червоніла вже сцена А1, що і процитовано у звіті.
- Злиття `3dae4441` = `18d2cdb2` (лише `docs/tasks/`), коду не торкається. CI зелений за
  звітом (`34451740257`), Actions мені не видно.

## Що далі

Нічого в коді. Злиття — після `beta → main` за рішенням З5; тоді контролер злиє
`claude/block-apps` у `main`/`beta` з прогоном гейтів на злитій голові. Для дзеркального
контуру (Етап 1) наступний застосунок — `winhotel-import`, і його задача зʼявиться після
`MAPPING.md` сесії 5 (задача 2 вже відзвітована — рецензується).
