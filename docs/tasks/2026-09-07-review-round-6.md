# Рецензія 07.09 (раунд 6): `2e6b79a` (сесія 2) і `6100c07` (сесія 3)

**Від:** контрольна сесія, 07.09.2026. Два агенти читали код і доводили кожне твердження прогоном або
мутацією; коди виходу зняті з `$?`. Попередні раунди — 3 (сесія 2) і 4 (сесія 3).

## Вердикти

- **b4 (`2e6b79a`) — злиття в головну НІ** до двох правок нижче. Правка 2.1 по суті зроблена
  правильно, але гілка відновлення лишає таблицю без індексів, і серед них — унікальний на гостьовий
  токен.
- **b5a (`6100c07`) — блок 5a не закритий** до правки 3.1: той самий секрет віддається іншими
  дверима. Решта раунду 4 лягла і доведена.

---

## Сесія 2 — `2e6b79a`

### Прийнято як є

`db.ts:7123` `BEGIN`, `:7140` `COMMIT`, прагма знімається перед `BEGIN` і повертається після
`COMMIT`, у `catch` (`:7145-7146`) є і `ROLLBACK`, і `PRAGMA foreign_keys = ON` — рядок у рядок як
`cm_mappings` (`:4496-4545`) і `accruals` (`:5200-5225`); індекси через `IF NOT EXISTS` (`:7130`);
звірка `after !== before` і `restored < indexSql.length` стоїть **до** `COMMIT` (`:7133-7139`).
Гейт червоний на `5141ed3` рівно на `db-boot.check.ts:176` («застосунок працює з ВИМКНЕНИМИ
зовнішніми ключами», `0 !== 1`) — твердження про прагму стоїть першим і його ніщо не маскує.
Мовчазний випадок названо вголос: перебудова без CHECK друкує
`[DB] 0094: CHECK на payment_status не знайдено — перебудову НЕ виконано`. Фікстура чесна —
`narrowBack` повертає всі 13 іменованих індексів, обрив відтворюється повним `CREATE` (67 колонок,
8 FK, живий CHECK). Обидві гілки прибирання доведені сценами і поводяться як описано.
Словники зведено насправді: жодного власного списку статусів поза `bookings/ui/payment-status.ts`,
`sites/[siteId]/…/BookingsTab.tsx` записаний у винятки гейта з причиною
(`payment-status.check.ts:174`). Усі три нові осі мають зуби (перевірено зламами). Д19 правдиве про
інструмент — на власній фікстурі спільний модуль дав каталог 0 унікальних, копія в місці рендеру — 3;
записане з умовою скасування. 2.3 зроблено, «Наст. 7 днів» живими словами в cs і de, правило в
`AGENTS.md` §4. Стеля `check-ui-tokens` затягнута по-справжньому (перерахунок власною копією
регексу: 74 і 7, рівно як у baseline). Прогони: `check` 0, `tsc` 0, build 0,
`check-boundaries --strict` 0, `check:i18n` 0, `check:pg` 0, drift 0 на 117 таблицях.

### Правка 2.5 — БЛОКЕР, дані: відновлення рятує рядки і мовчки викидає індекси

`db.ts:7094` перейменовує `reservations_new` назад і на цьому зупиняється; після перейменування CHECK
уже широкий, тож `narrow[0].includes("'partial'")` — істина, і гілка перебудови (`:7110-7142`, де
індекси й відтворюються) не виконується **ніколи**. Прогін: обрив рівно між `DROP TABLE reservations`
і `ALTER … RENAME`, старт застосунку — 20 броней на місці, `foreign_keys = 1`, `integrity_check ok`,
а `sqlite_master` дає **0** індексів із `sql`. Три наступні старти повертають лише 4 (їх
перестворюють інші міграції через `CREATE INDEX IF NOT EXISTS`). Втрачені назавжди 9, серед них
`CREATE UNIQUE INDEX idx_reservations_guest_token ON reservations(guest_page_token)`. Наслідок
відтворено: двом бронях поставлено один і той самий `guest_page_token` — жодної помилки, два рядки з
тим самим токеном. Це дослівно те, що описує коментар за десять кроків вище (`db.ts:5190-5195`):
«вже не швидкість, а два гості на одному посиланні».

Гейт цього не бачить за побудовою: сцена 5 має твердження про індекси (`db-boot.check.ts:181`), а
сцена 6 — та, у якій індекси й гинуть — перевіряє `done`, `rows`, `wide`, `leftover`, `fk`
(`:208-212`) і про індекси мовчить. Стан досяжний саме для баз, які вже пройшли старий 0094 і
померли між `DROP` і `RENAME` — тобто рівно для тих, заради яких гілка відновлення й написана.

**Правка:** після перейменування в `:7094` відновити індекси з явного списку (той самий, що в
`initSchema`), і додати в сцену 6 твердження `afterLost.idx >= fresh.idx` — воно червоне на нинішньому
коді.

### Правка 2.6 — продукт: сліпе перейменування переписало імена CSS-класів

`MobileTasks.tsx:833` тепер `m-dateChip` / `m-dateChip-active`, `:850` — `m-dateChips`, `:854` —
`m-statusChip` / `m-statusChip-active`. У стилях є тільки `.m-chips` (`mobile.css:371`), `.m-chip`
(`:382`), `.m-chip-active` (`:397`); жодного `m-dateChip*` чи `m-statusChip*` у дереві немає. Обидва
ряди чипів на екрані задач лишились без пігулкової форми, рамки, тла і підсвітки активного, а ряд
статусів ще й без горизонтального скрола. Слід у коментарі `:849` («Status filter dateChips»).
Повернути `m-chip`/`m-chips`/`m-chip-active`, імена параметрів лишити.

### Правка 2.7 — гроші на екрані: `Kč` полагоджено в одному місці з чотирьох

`MobileBookings.tsx:382` переведено на `useHotelCurrency()` — так. Але поруч живуть
`MobileGuests.tsx:340` (і `'0 Kč'` у гілці порожнечі), `MobileGuests.tsx:430`,
`MobileFinanceOverview.tsx:55` і `:101`. Той самий інваріант 20, той самий симптом — валюта першого
клієнта на телефоні кожного готелю, тепер у картці гостя й у фінансовому огляді. Гейта немає:
`check-reservation-currency.mjs` каже про `INSERT` без названої валюти, не про показ. **Вісь:** літерал
знака валюти в `src/components/**` і `src/app/app/**`.

### Дрібне

`types/database.ts:264` без `'partial'` — вливається в головну разом із гілкою (сесії 1).
У `BookingForm.tsx` лишилась колізія `.map(s => …)` на `:742` і `:793`, яку той самий коміт щойно
заборонив у `AGENTS.md` — сьогодні нешкідлива, гейта на правило немає. У `DECISIONS.md` Д19 —
друкарська «у виняткакх». У сцені 6 твердження про прагму стоїть останнім, у сцені 5 — першим;
вирівняти. Поза комітом: кожен старт друкує
`[DB] dead foreign key migration: table "accruals" already exists`.

### Злиття

`git merge-tree work b4` — рівно один конфлікт, `package.json:13`, скрипт `check`; адитивний з обох
боків (`work` +2 гейти, `b4` +12, жодна нічого не прибрала). Решта зливається автоматично.

---

## Сесія 3 — `6100c07`

### Прийнято як є

**2.1** доведено живим прогоном: production-складання проти Postgres під роллю `alisio_app`,
`housekeeper` з правами `['nav:dashboard']` — `GET /api/units` віддає список без `wifi_password` і
`lock_code`, власник отримує обидва (`units.repo.ts:36`, `units.handlers.ts:30`); повернення старої
варти валить `check-isolation` дослівно. **2.2**: `wifiFrom` бере рівень парою, гейт
`guest-page-levels.check.ts` червоніє на дореформеній редакції. **2.4** відтворено на живому
Postgres: зі `slice(-8)` другий готель падає `duplicate key … amenity_categories_pkey`, з
`crypto.randomUUID()` сіються обидва. **О7**: шість власних мутацій `currency.check` червоні, проза
зелена; балансування дужок робить те, заради чого написане. **2.12** ловить відтворений `55df209` на
другому і третьому кроці графа. **2.8**: дванадцять нових відмов `check-hotels`, кожна червона
своєю мутацією; `apply-hotel` двічі — 22 `+`/5 `=`, тоді 28 `=`. **2.3** підтверджено схемою: у
`finance_exchange_rates` рівно сім колонок, ознаки походження немає жодної — «чистити не можна» це
факт; `list-fx-rates.sh` лише читає і на живій базі назвав готель, який оголосив EUR як `manual`, а
бачить банківські 24.31 замість своїх 25.00. **2.10**: засів під `sql.tx`, `catch` на гостьовій
тепер пише в лог. **2.9**: чотири місця документації зійшлись. Прогони: `check` 0, `tsc` 0, build 0,
`check-boundaries --strict` 0, `check:i18n` 0, `check:pg` 0, drift 0 на 118 таблицях,
`check-isolation` проти живого застосунку 0.

### Правка 3.1 — БЛОКЕР приймання: той самий секрет іншими дверима

`GET /api/properties/[id]` віддає покоївці пароль мережі й код замка **кожного** номера. Доведено тим
самим живим прогоном: під роллю `housekeeper` запит повернув 200 і `wifi_password`, `lock_code`.
Маршрут під `withActor` (`properties/api/properties.handlers.ts:50`,
`app/api/properties/[id]/route.ts:3`), а репозиторій бере номери як `SELECT u.*`
(`properties/data/properties.repo.ts:63-64`). Задача §2.1 виконана для одного зі списків із двох.
Гейта немає: `check-isolation` питає лише `/api/units`, і О8 у DECISIONS звужене саме до нього — тому
не бреше, але й не покриває. Правка на рядок (та сама опція `secrets` або поіменний перелік колонок)
плюс твердження в `check-isolation`.

### Правка 3.2 — О2 має четвертий шлях

`guests/data/guest-portal.repo.ts:229`: `wifiFrom` викликається лише в гілці, де `propertyConfig`
існує; коли рядка `property_guest_config` немає взагалі, `merged = { ...unitTypeConfig }` і пара
розпадається. Живий резолвер: тип із самою назвою мережі віддає `wifi_network: "TYPE-NET-ONLY"`,
`wifi_password: null`. Фікстура гейта завжди кладе `property_guest_config` першим кроком, тому мовчить.

### Правка 3.3 — `check-bare-node` має дві дірки

Правило обходить граф файлів, до яких скрипт дотягується через `../src/…`, але **власні імпорти
скрипта в граф не входять**: `import { seedAmenityCatalog } from '@properties'` першим рядком
`scripts/provision-org.mjs` — гейт зелений, хоча голий node падає так само, як падав `55df209`. Друге:
`const late = await import('@properties')` на верхньому рівні `src/core/provisioning.ts` виконується
при завантаженні файла точнісінько як статичний імпорт — гейт зелений. Третє, дрібніше:
`fs.readdirSync('scripts')` не рекурсивний. Правка: перевіряти імпорти самого входу і рахувати
динамічні імпорти **верхнього рівня** нарівні зі статичними.

### Правка 3.4 — вісь у реєстрі не стереже того, що називає

`check-fixture-axes.mjs:346-349`, вісь «мова каталогу»: немає `scope`, і `distinct` з `min: 2` рахує
мови по всьому файлу. Обидва справжні готелі зведено на `'uk'` — гейт лишився зеленим, бо `min: 2`
добирає сцена запасної мови (`'pl'`). Сусідня вісь у тому самому коміті `scope` має. Решта пʼять
червоніють від виродження чесно.

### Правка 3.5 — `currency.check` ловить лише суфікс

`\b\w*rate\b\s+(NUMERIC|…)` не спрацьовує на `rate_manual NUMERIC(18,8)` — міграція з такою колонкою
проходить. Три названі написання покриті; сімʼя ширша.

### Правка 3.6 — цифри

Каталог — 12 категорій і 61 позиція, тобто 73 рядки на готель, а `amenities.repo.ts:51`, `:72` і
`ARCHITECTURE.md:2260` кажуть «72». `check-hotels` для діапазону з `prefix: "4"` і `from: 1` друкує
мітку `41…5`.

### Мови — відхилення від рішення власника, назвати його відхиленням

У коді чотири мови: `AmenityNames` має `uk`, `cs`, `de`, `en`, усі 73 рядки заповнені всіма чотирма,
неповних нуль. Польський, голландський і французький готель отримує англійську (перевірено:
`elevator` → `Elevator`), і гейт це стверджує окремою сценою. Обмеження записане чесно в О5
(`DECISIONS.md:159`), у шапці `amenity-catalog.ts` і в `ARCHITECTURE.md:1227` — **але як свідоме
обмеження сесії, а не як невиконана вимога власника.** Власник 07.09 відповів «перекласти всі сім
мов зараз». До семи лишається `pl`, `nl`, `fr` — 3 × 73 = 219 рядків в одному файлі
`properties/domain/amenity-catalog.ts`, без міграції, без зміни схеми і без зміни гейтів. Робота на
один підхід. Ціна зволікання: засів одноразовий, тож польський готель, заведений до перекладу,
лишиться з англійськими рядками, поки їх не перейменують руками.

### Кома §0 на b5a досі відсутня

`guests/data/guest-portal.repo.ts:78` — `unit_type_description` без коми; на цій гілці рівень
«номер» і `amenity_list` наскрізь перевірити неможливо. Злиття це лікує: `git merge-tree` бере кому з
головної і `wifiFrom` із b5a в одному файлі без конфлікту. Окремої правки не потрібно.

---

## Порядок

1. **Сесія 2:** правки 2.5 (індекси при відновленні — блокер), 2.6 (класи чипів), 2.7 (чотири `Kč`),
   дрібне. Після них — злиття b4 в головну (конфлікт лише в `package.json`, обʼєднати набори).
2. **Сесія 3:** правки 3.1 (секрет — перша), 3.2, 3.3, 3.4, 3.5, 3.6 **і сім мов каталогу** — одним
   підходом. Наступний блок після нього.
3. **Сесія 1:** після деплою і перегравання — правка 5.1 (фактура), 5.2, 5.3, далі 4.1–4.8 раунду 2.
