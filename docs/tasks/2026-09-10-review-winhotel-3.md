# Рецензія 3 — сесія 5 (схема Winhotel), задача 3, голова `dec1db0d`

Контролер 2 (Schlossberghotel / Застосунки), 10.09.2026, ~09:00 UTC.

## Вердикт

**Задачу 3 прийнято. Дослідницький блок вичерпано** — далі лише прикладна робота з того,
що знайдено (задача 4), і агрегати на свіжому знімку (робить контролер).

## Перевірено фактами

- `extract-out/**` не змінювався (`git diff 3d88afa7 dec1db0d -- …/extract-out` порожній).
- `TABLES.md` містить `PLZ_STRASSE` дослівно; PII-grep узято з `README.md:33` і прогнано
  `bash -c` — 0 рядків. Виключення словом, не текою: імена колонок/змінних вендора,
  `Musterstr.`, `${TS[@]}` скрипта, `'@` процедури, аліас `@…/`. Прийнятно: справжня
  адреса (`Hauptstrasse 5`) чи e-mail (`x@y.de`) під ці виключення не підпадають.
- `CORE-GAPS.md`: 15 посилань на `schema.sql:рядок`, вибірково звірено чотири
  (`:490 companies`, `:1222 guest_registrations`, `:1819 lodging_discount_reason`,
  `:2853 idx_reservations_unassigned`) і два на код (`companies/api/kernel.ts:55` —
  `payer_debtor_no: c.business_id`; `stay-charges.repo.ts:196` — `splitOtaAmount`) —
  усі вказують туди, куди сказано.
- `IMPORT-PLAN.md`: розділи 0–5 на місці; два способи доставки порівняно, рекомендація
  сесії — (а) щоденний/разовий gbak з відновленням у нашому контейнері, рішення лишено
  власнику. Це і моя рекомендація (шлях уже пройдено 10.09 на 407 МБ).
- `check-no-tenant-names`, `check-docs-current` — чисті (прогнано на голові).

## Що з цього йде далі

Пʼять найбільших дірок ядра (Sammelrechnung/дебітор; фірмовий тариф ↔ компанія; злиття
гостей; довідник способів оплати; згоди GDPR на гостя) — вхід для контролера 1 і сесій
1–3 після `beta → main`; записано в JOURNAL. Спосіб доставки — чекпоінт власника.
