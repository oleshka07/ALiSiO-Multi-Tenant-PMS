# Рецензія А — сесія 6, кіоск, частина А, голова `16d5de56`

Контролер 2, 10.09.2026, ~15:05 UTC.

## Вердикт

**Прийнято, ЧЕКПОІНТ 1 пройдено.** Три названі суперечності вирішую так: префікс `КІ1–КІ10` —
прийнято (моя помилка в задачі, К1–К8 справді зайняті каналами); PATCH кличе `decideCheckIn`,
а запис лишає собі в одній транзакції — прийнято, довід правильний (інваріант 11, одне вікно
запису); **`reservations.source = 'kiosk_walkin'` — дозволено: міграція `0412` розширює CHECK
одним словом (Postgres — CHECK за означенням, SQLite — перебудова зі зняттям і поверненням
індексів, лічильник звіряється), рядок у NAMING, `booking_sources` не чіпати** — це джерело
броні, а не канал.

## Перевірено фактами (у хмарі на `16d5de56`, справжні `node_modules`)

`tsc` 0 · `kiosk.check.ts` голим `node` — 12 сцен зелені · 0411: три таблиці з
`organization_id`, індексами, `ENABLE`/`FORCE ROW LEVEL SECURITY` і політикою **в тій самій
міграції** · `src/proxy.ts:45` префікс `/api/apps/kiosk/` із причиною і окремим сегментом
`admin/` під `withOwner` · парування: у базі sha256 коду, TTL 10 хв, обмін через
`runWithPublicToken` (інваріант 14) · вісь корпусу в токені й у кожному фасаді.

**Моя мутація, якої в звіті немає:** `devices.repo.ts:116` — умову строку коду знято
(`expires_at > ? OR 1=1`, кількість параметрів та сама, злам валідний) → гейт червоний:
`прострочений код: очікували 400, отримали 200`. Повернуто, дерево чисте. Перша спроба мутації
(прибрати `AND expires_at > ?` разом із параметром) дала `Too many parameter values` — це
невалідний злам, не червоне, і в рецензію не йде (AUTOLOOP, девʼятий підвид).

Дві вади, знайдені сесією по дорозі, — справжні і дорогі: `assignUnit` рахував тиск броні на
саму себе (у готелі з двома номерами типу остання вільна не призначилась би ніколи) і фасади
без осі будинку. Правка `check-public-routes` (варта питається у власному хендлері, право —
у модулі) — правильна, дві брехні гейта названі; `IDENTITY_READS` у `check-property-scope` —
той самий прийом, що `rlsIdentity`, і з обовʼязковою причиною. Прийнято.

## Що лишилось на потім (не блокує частину Б)

`check:pg` на справжньому Postgres роллю `alisio_app` з двома готелями — частина Г, як і
названо. Сцена 8 має заслони, яких поки нема чим порушити — теж названо чесно; частина Б дасть
код, і тоді сцену треба показати червоною ще раз.
