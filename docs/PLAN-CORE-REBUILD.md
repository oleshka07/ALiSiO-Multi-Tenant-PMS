# Порядок робіт: ядро цін та інвентарю + Channex

**31 серпня 2026.** Зводить два плани, які писалися паралельно й частково
про те саме:

- [docs/research/REBUILD-PLAN.md](research/REBUILD-PLAN.md) — перебудова ядра
  за дослідженням Hoteliera;
- [docs/CHANNEX-INTEGRATION.md](CHANNEX-INTEGRATION.md) v0.3 — інтеграція з
  менеджером каналів, виміряна на живому API.

Обидва лишаються чинними як **джерела**: перший — про продуктовий шар і
модель даних конкурента, другий — про вимоги Channex. Цей файл — про
**порядок**, і в конфлікті виграє він.

---

## 1. Чому їх довелося зводити

Плани перетинаються не по краях, а по центру. Фаза 1 перебудови містить
`ari_grid` замість `price_calendar`, `reservations.unit_type_id` і
`booking_revision` — це рівно ті таблиці, на яких стоїть Channex, і рівно ті
блокери, які виміряні в §2.3 інтеграційного ТЗ.

Робити їх двічі — найдорожчий зі шляхів. Тому: **ядро першим, за вимогами
Channex як специфікацією.**

---

## 2. Що вже зроблено — не планувати заново

| Зроблено | Де | Закриває в плані перебудови |
|---|---|---|
| `availabilityByDay()` — наявність стала об'єктом | `src/modules/properties/data/availability.ts:152`, гейт `check-availability-source` | передумова `ari_grid`, у плані не названа |
| `price_calendar.rate_plan_id` + унікальний індекс із `COALESCE` + міграція | `db/postgres/migrations/0048`, `priceNights()` зводить із заселеністю | Фаза 1, крок 4 — **вісь тарифу вже є** |
| `reservations.unit_type_id` (nullable) | `src/lib/db.ts`, та сама міграція | Фаза 1, крок 7 — **колонка є**; `unit_id` досі `NOT NULL` |
| `ChannelManagerPort`, клієнт Channex, лімітер, мок-сервер, гейт И1 | `src/modules/channels/` | Фаза 2, спринти 1–2 |
| Документація Channex у репозиторії — 111 сторінок + Postman | `docs/vendor/channex/` | — |
| `INVENTORY.md` — 20 розбіжностей документації з живим API | `docs/vendor/channex/INVENTORY.md` | специфікація для `ari_grid` |

Усе це на гілці `claude/channex-integration-66kv65`, поверх `main`.

---

## 3. Три поправки до плану перебудови

**Фаза 2 описана за застарілою чернеткою.** Вона посилається на
`channex-integration-spec.md` і містить «воркер окремим сервісом», «лімітер
20/хв» і «мапінг» окремим спринтом. Усе три зняті вимірюванням:

| У плані | Насправді |
|---|---|
| воркер окремим сервісом у compose | крон-маршрути `/api/cron/*`; другий процес — новий клас відмов без потреби (§3.4 ТЗ) |
| лімітер 20/хв | **10 запитів цін і 10 наявності на хвилину НА ОБ'ЄКТ**, і це два різні відра |
| спринт «мапінг» з екранами | Channel iFrame покриває підключення, мапінг і налаштування каналу (§3.6 ТЗ) |
| `channex-integration-spec.md` | `docs/CHANNEX-INTEGRATION.md` v0.3 |

**`ari_sync_queue` не існує.** План каже «замінити `ari_sync_queue` на
`ari_outbox`». Її видалила міграція 0032 разом із неробочою інтеграцією
Booking.com — тобто це створення нової таблиці, а не заміна.

**Перелік мертвих таблиць потребує ще одного проходу.** З вибірки: у
`invoices__rebuilt` **немає `CREATE TABLE`** — ім'я живе всередині ідіоми
перестворення таблиці (`_new` → копія → `DROP` → `RENAME`), тобто це робочий
код, а не мертва таблиця. Перш ніж щось дропати, список будується
`node scripts/audit-dead-data.mjs`, а не руками.

---

## 4. Де плани розходяться по суті — одне місце

**Чи має доплата за додаткового гостя вісь тарифу.**

- Рішення власника (§10.11 ТЗ): **ні**, надбавка спільна для тарифів одного
  типу номера. Ухвалене свідомо, з записаною ціною згортання назад.
- План перебудови: таблиця `extra_occupancy` за зразком Hoteliera, а там
  доплата задається на перетині `тариф × тип номера × харчування`.

Чинне рішення власника. `extra_occupancy` створюється **без**
`rate_plan_id`; колонка додається тоді, коли зʼявиться готель, у якого
доплата за другого гостя різна в базовому й безповоротному тарифі. Ознака
записана в §10.11.

В усьому іншому форми збігаються: ключ `ari_grid`
`(property, unit_type, rate_plan, date)` — це рівно три осі рішення власника,
а заселеність окремою таблицею — це і є «зведення в `priceNights()`».

---

## 5. Відкрите рішення: перейменовувати чи нарощувати

`price_calendar` уже має вісь тарифу; бракує `availability` і повного набору
обмежень. План пропонує замінити таблицю на `ari_grid`.

**Рекомендація: нарощувати на місці, не перейменовувати.** Перейменування —
це ще одна міграція живих цін, оновлення гейта `check-price-source` і
`LEGACY`-списків, і воно не додає жодної можливості. Ім'я справді стане
неточним (у «календарі цін» лежатиме наявність) — але це косметика, яку
дешевше закрити один раз, коли вміст таблиці усталиться.

> **Ухвалено власником 31.08.2026: нарощуємо, не перейменовуємо**
> (`docs/research/NORTHSTAR.md` §3). Питання закрите; крок 4 Фази 1 додає
> `availability` і обмеження до наявної `price_calendar`.

---

## 6. Порядок робіт

### Фаза 0 — гігієна (тиждень)
Як у плані перебудови, з поправкою про мертві таблиці з §3. Плюс головне:
**рішення про обсяг** — розділити ядро й модулі, вимикати модулі фіче-флагом
за тарифом. Реєстр фіч уже є (`src/core/features.ts`, `organization_features`),
тобто механізм не треба будувати — треба ним скористатися.

### Фаза 1 — ядро цін та інвентарю (6–8 тижнів)
Порядок плану перебудови, з викресленими вже зробленими кроками:

1. `taxes` — **не greenfield, це заміна наявної машинерії.** Виміряно на
   `origin/main` 31.08 перед початком робіт:

   | Уже існує | Де |
   |---|---|
   | `organizations.is_vat_payer` | у схемі; окремо заводити не треба |
   | `fin_tax_rates` — ставки з датами | `organization_id`, `code IN (standard, reduced, zero)`, `valid_from`/`valid_to`; читається в **5 файлах** `modules/invoicing` |
   | Турзбір | `properties.city_tax_per_night` (ставка), `reservations.city_tax_amount` (записана сума), `booking_sources.city_tax_included_default` (чи канал уключає); читається у **8 файлах** плюс звіт `/api/reports/city-tax` |
   | Німецькі коди | `lodging_tax_code`, `food_tax_code`, `drinks_tax_code` |

   Конструктор із восьми полів як **модель** правильний. Але поставити
   `taxes` поруч зі старим означає два джерела правди на ПДВ і на турзбір —
   рівно те, від чого існує інваріант 16 для цін. Отже це міграція:
   `fin_tax_rates` і три колонки турзбору переїжджають у `taxes`, читачі
   переводяться, старе прибирається одним комітом.

   **Інваріант 18 при цьому непорушний:** `reservations.city_tax_amount` —
   це вже нарахована сума на живих бронях. Міграція її НЕ перераховує;
   ставка обирається за датою послуги і лишається числом на нарахуванні.
   Фактура 2026 року не має змінитись від нової таблиці.

   ---

   #### Поправка 31.08.2026, після вимірювання коду

   Абзац вище пропустив **четверту** наявну машинерію, і саме вона виявилась
   правильним місцем для збору:

   | Що | Форма | Стан |
   |---|---|---|
   | `fin_tax_rates` | ставка ПДВ: РОЛЬ + число + дати, на організації | здоровий; це не збір, а ставка. Чіпати не треба |
   | `fees_taxes` | нарахування на обʼєкті: 5 множників, `is_included_in_price` | **це і є «збори»**; був порожній і напівживий |
   | турзбір на броні | 3 колонки + звіт | сума є, до документа не доходила |
   | `fin_folio_items.kind = 'city_tax'` | рядок рахунку зі своєю ставкою | існував у схемі, **не писав ніхто** |

   Отже нова таблиця `taxes` **не створюється**. Це той самий вибір, що в §5
   про `price_calendar`: нарощувати на місці. `fees_taxes` уже має обʼєктну
   область, RLS, п'ять множників, прапорець «уже в ціні», секцію у файлі
   готелю і читача в квоті — нова таблиця поруч була б п'ятою машинерією, а
   не консолідацією.

   **Зроблено (коміти 31.08):**

   1. `applies_to` + `collected_for` у `fees_taxes` (міграція 0050) —
      нейтральні до юрисдикції, інваріант 22.
   2. `is_included_in_price` нарешті щось означає: квота віддає `includedFees`
      окремо і не додає їх удруге. Контракт був написаний у
      `apply-hotel.mjs` і не виконувався.
   3. Турзбір доходить до рахунку рядком `kind='city_tax'` зі ставкою нуль;
      збір, позначений «уже в сумі», вирізається ПЕРЕД поділом — до цього
      він тихо оподатковувався ставкою проживання.

   **Лишається, і саме тут потрібне рішення власника** — чи входить турзбір у
   `total_price` броні. Сьогодні відповідь роздвоєна: `city_tax_amount` лежить
   ОКРЕМОЮ колонкою, а `city_tax_included` каже, чи він усередині. Перенести
   ставку `properties.city_tax_per_night` у рядок `fees_taxes` не можна, доки
   ця роздвоєність жива: квота додала б збір у `total`, `readQuote()`
   підставив би це число в поле «Вартість», і збір опинився б і в
   `total_price`, і в `city_tax_amount` — двічі.

   Це не технічна перешкода, а продуктове питання: **одне число з розкладом
   чи два числа поруч.** Опис і наслідки — у §7 нижче.

   Другий шлях документа — `reservation-invoice.repo.ts` — створює фактуру
   БЕЗ рядків, з `amount = total_price`. Виправлення №3 його не торкається:
   там немає позицій, отже й окремого рядка збору. Що з ним робити,
   вирішується разом із питанням вище.

2. `seasons`;
3. `rate_plans` — розширити (`based_rate_plan`, `adjustment`, `offer_type`);
4. `price_calendar` → додати `availability` і повний набір обмежень
   (§5 рішення власника);
5. `extra_occupancy` — **без** осі тарифу (§4);
6. `price_rules`;
7. ~~`reservations.unit_type_id`~~ **зроблено**; лишається зробити `unit_id`
   nullable і перевірити всі місця, що припускають наявність номера;
8. `cm_inbound_bookings` і `cm_outbox` (у ТЗ Channex вони так називаються);
9. прозорий розклад ціни в API.

### Паралельно з Фазою 1 — те, що цін не торкається
- **податки за моделлю Hoteliera** — крок 1 вище, автономний;
- **Channex: вбудова iFrame, вебхук на орендаря, приймання бронювань,
  листування** — вони стоять на `reservations` і `cm_*`, не на цінах;
- **продуктовий шар**: IA налаштувань, сайт як сутність N:M, booking engine.

### Фаза 2 — Channex ARI на готовому ядрі (3–4 тижні)
Фази 4–7 інтеграційного ТЗ: черга, батчер, повний синк, екран звірки,
сертифікація. Критерії — 14 сертифікаційних тестів.

---

## 7. Що лишається рішенням власника

| Питання | Блокує | Де описано |
|---|---|---|
| Ядро проти модулів: що вимикається фіче-флагом за тарифом | Фазу 0, а через неї все інше | REBUILD-PLAN §«Рішення про обсяг» |
| Акаунт Channex: продакшн-ключ і підписка | Фазу 2 | ТЗ §10.2 |

**Закриті:**

| Питання | Відповідь | Де |
|---|---|---|
| `price_calendar` нарощувати чи перейменовувати | нарощувати | §5, NORTHSTAR §3 |
| Чи входить турзбір у `total_price` | питання виявилось хибним — див. §7.1 | §7.1 |
| Ліміт ARI Channex «20/хв — на акаунт чи на property?» | **виміряно на живому API**: 10 запитів цін і 10 наявності на хвилину НА ОБʼЄКТ, і це два різні відра | §3, `vendor/channex/INVENTORY.md` |

### 7.1 Турзбір: питання було поставлене неправильно

Я питав «А чи Б: одне число чи два». Власник відповів фактами з ринку, і
вони показали, що обидві відповіді неповні:

| Реальний стан | Приклад |
|---|---|
| збір включений у ціну самим каналом | Booking.com, налаштований інклюзивно |
| канал взагалі не має окремого механізму | Airbnb — збір завжди в ціні |
| готель бере збір окремо й окремо його знімає | пряме бронювання |
| **на документі — окремим рядком і БЕЗ ПДВ** | скрізь, і це не опція |
| збору немає взагалі | більшість малих обʼєктів |

Отже це не вибір із двох, а **п'ять станів, які модель мусить уміти
одночасно**. І вона вміє — після робіт 31.08:

| Питання | Де відповідь | Чому саме там |
|---|---|---|
| скільки це коштує | рядок `fees_taxes` з `collected_for='authority'` | одне визначення: множник, звільнення, ставка |
| скільки вийшло на ЦІЙ броні | `reservations.city_tax_amount` | заморожене число (інваріант 18) |
| чи всередині суми броні | `reservations.city_tax_included`, дефолт з `booking_sources.city_tax_included_default` | це властивість **каналу**, а не продукту |
| як воно виглядає в документі | рядок `kind='city_tax'`, `vat_rate = 0` | завжди окремо й без ПДВ, у всіх п'яти станах |

**`total_price` — це те, що гість платить через цей канал.** Чи входить туди
збір, каже канал, і прапорець це записує. Документ однаковий незалежно від
відповіді: збір вирізається або додається, але на рахунку він завжди свій
рядок без ПДВ.

Подвоєння неможливе з двох боків: `apply-hotel.mjs` і `POST /api/fees`
відмовляють завести збір «для громади» обʼєкту, який уже має
`city_tax_per_night`; а `readQuote()` ставить «включено» істиною рівно тоді,
коли поклав збір у суму.

**Що лишилось і чому не зроблено зараз.** `properties.city_tax_per_night`
живий як друга (проста) машинерія для готелів, які вже на ній. Прибрати її
означає переписати `guests/data/registry.repo.ts` і `registration.repo.ts` —
а вони обидва **чеські за змістом**: «іноземець» там це
`nationality NOT IN ('CZ','CZE')` у чотирьох місцях, звільнення — вік `< 18`
із причиною чеським літералом, і жодного гейта на країну обʼєкта
(ARCHITECTURE §8). Міняти джерело ставки в коді, який однаково бреше
німецькому обʼєкту, — робити півкроку двічі. Це йде разом із модулем
юрисдикції, не окремою латкою.

---

## 7.2 Відповідь на питання NORTHSTAR §7

> «Чи є серед восьми чекпоінтів такий, до якого поточний шлях не веде взагалі?»

**Так, один: CP3.** Виміряно 31.08.2026, доказом, а не оцінкою.

| CP | Стан | Доказ |
|---|---|---|
| CP1 ціна має вісь тарифу | **шлях веде, майже там** | `price_calendar.rate_plan_id` (0048), `priceNights({ratePlanId})` зводить із заселеністю |
| CP2 наявність у тій самій сітці | шлях веде, не почато | колонки `availability` в `price_calendar` немає (`availability_blocks` — інша, старіша таблиця). Крок 4 Фази 1 |
| **CP3 бронь без фізичного номера** | **шлях НЕ веде** | `reservations.unit_id` — `TEXT NOT NULL` на обох двигунах (`schema.sql:1476`, `db.ts:266`). `unit_type_id` є і nullable — тобто половина зроблена, а сама вимога ні |
| CP4 ревізії ідемпотентні | не почато | таблиць `cm_*` у схемі немає жодної |
| CP5 зміни батчаться | не почато | порт і лімітер є; батчер — Фаза 2 |
| CP6 ціну рахує бекенд | **шлях веде, форма інша** | квота віддає `breakdown` / `accommodationTotal` / `feeBreakdown` / `includedFees` / `feesTotal` / `total`. Це розклад, але не той, що в NORTHSTAR (`price_without_rules → rules[] → price_without_taxes → taxes[] → total`). Різниця не косметична: у нас немає шару `rules[]`, бо немає `price_rules` (крок 6) |
| CP7 податки одним механізмом | **зроблено настільки, наскільки можливо без CP3** | 0050 + три коміти 31.08; `city_tax_amount` на живих бронях не перераховано жодного разу — міграція лише додає колонки |
| CP8 сертифікація Channex | не почато | Фаза 2 |

**Чому CP3 — саме той, про який варто було спитати.** Channex про `units` не
знає нічого: він адресує тип номера й тариф. Бронь із OTA приходить без
фізичного номера, і покласти її нікуди — `unit_id NOT NULL`. Тобто CP4
(ревізії) не може приземлитись, поки не знято CP3, а без CP4 немає CP8.
Один рядок схеми тримає три чекпоінти.

І це не «додати nullable»: `unit_id` читають десятки місць, які припускають,
що номер є — календар, аркуші дня, хаускіпінг, `unit_code` у рядках фоліо.
Тому крок 7 Фази 1 сформульований як «зробити nullable **і перевірити всі
місця, що припускають наявність номера**», і друга половина там дорожча.

**Наслідок для порядку робіт:** після добивання кроку 1 (податки) наступний
крок — не 2 (`seasons`), а **7 (`unit_id` nullable)**. Він блокує три
чекпоінти з восьми, а `seasons` не блокує жодного.

---

## 8. Правило, яке коштувало нам дня

Обидва плани писалися паралельно, і кожен мав під правильним висновком
щонайменше один хибний факт: гілка п'ятитижневої давнини в одному,
обрізаний клон і «різні дерева» в другому, теорія про White Label у
третьому. Жодного разу помилку не спіймала експертиза — щоразу її ловила
**перевірка**.

Звідси інваріант 21 в AGENTS.md: схема читається з `origin/main`, ніколи з
робочої копії; перед висновком з історії — `git rev-parse
--is-shallow-repository`. І хук на старті сесії, який каже про обидва стани.
