# Сертифікація Channex — карта покриття 14 сценаріїв

**Редакція 3, 05.09.2026 — після відмови.** Перша подача відхилена листом
05.09 (пройшов лише тест 11): тіло несло весь стан замість дельти, повний синк
— без обмежень, у тесті 3 — зайвий тариф. Розбір — `CHANNEX-REJECTION-2026-09-05.md`,
код — Блок 0.5 MASTER-PLAN, прогін власника — `CHANNEX-RETEST-PROTOCOL.md`.
Таблиця §3 переписана точними значеннями листа; колонка «task id» порожня до
нового прогону; новий §7 — обовʼязкова звірка кожного task id з тілом ДО
подання форми.

Редакція 2, 02.09.2026. Слово власника того ж дня: «перше завдання — не
прогін, а карта покриття»; після карти — девʼять уточнень (порядок, дефекти,
повний синк, розписки, формулювання для форми). Чим керуємось узагалі —
`docs/ORDER-OF-WORK.md`: мета — перший готель, який платить, і карта це
правило підтвердила: прогалини виявились не «під іспит», а ядром продукту.

## 0. Що це, і чого це не є

Джерело: `docs/vendor/channex/api-v.1-documentation/pms-certification-tests.md`.
Три речення звідти визначають форму цього документа:

> «This is **not a list of API calls to execute**. It is a checklist to verify
> that **your existing PMS product** correctly pushes data to Channex in
> response to **real user actions inside your PMS**.»
>
> «A standalone script, CLI, or Postman collection that posts the exact
> values from the tables below» — провал. «A "certification UI" built solely
> to trigger the test events» — провал.
>
> «Every test below will be reproduced on a live call by triggering the
> corresponding action in your PMS UI.»

Тому:

- **жоден task ID для форми не приходить зі скриптів.** `scripts/channex-*-live.mjs`
  лишаються внутрішньою перевіркою (інваріант 27) і зразками полів (інваріант
  28); у форму йдуть лише розписки викликів, які полетіли з робочого шляху
  після дії оператора на екрані ALiSiO;
- кожен рядок нижче називає **екран → дію → файл:рядок**, звідки після цієї дії
  летить виклик. Де екрана або дії немає — це робота до сертифікації (§4), або
  чесне «не підтримуємо» у формі (документ прямо дозволяє: «If your app doesn't
  support something please just mention you do not support it»);
- на четвертому етапі Channex просить змінити ціну в нашому UI і дивиться, чи
  полетів виклик. Дивитись вони будуть на екран «Ціни» і на екран
  «Канал-менеджер».

## 1. Один шлях від кліка до виклику

Усі сценарії 2–10 їдуть однією дорогою; відрізняється лише перший крок.

```
екран ALiSiO ── дія оператора
   └─ хендлер модуля (pricing / bookings / properties)
        └─ у ТІЙ САМІЙ транзакції: @channels/outbox
             noteRatesChanged()          modules/channels/data/outbox-notes.ts:75
             noteAvailabilityChanged()   modules/channels/data/outbox-notes.ts:53
               → рядок-координата в cm_outbox (діапазон ночей, Ц13/Ц15)
   ┈┈ крон щохвилини: deploy/setup-backup-cron.sh (channels-publish) ┈┈
   └─ app/api/cron/channels-publish/route.ts
        → modules/channels/api/publish-cron.handlers.ts:21   runChannelPublishCron
        → modules/channels/data/publish-all.ts:75            publishAllConnections (цикл по готелях, орендар усередині)
        → modules/channels/channex/ari-adapter.ts:91          ariFlush (ціна — priceNights(), наявність — availabilityByDay())
        → modules/channels/domain/ari-batch.ts:441            flushOutbox → :330 flushLane (одна пачка на смугу, до 8 МБ)
        → modules/channels/channex/client.ts:198              POST /availability   (смуга наявності)
        → modules/channels/channex/client.ts:203              POST /restrictions   (смуга цін і обмежень)
             через :579 postAri — обмежувач 10/хв на смугу на обʼєкт (channex/limiter.ts), 429 → пауза обʼєкта
```

Відповідь на питання чек-листа «where does `POST /availability` get called
from» — `src/modules/channels/channex/client.ts:198`, і викликач один:
крон розсилки. З обробника збереження виклику немає — між ними черга
`cm_outbox`.

**Вікно пачки — одна хвилина.** Крон бере все, що лежить у черзі зʼєднання, і
шле однією пачкою на смугу. Кілька правок одного тесту, зроблені в межах
хвилини, поїдуть одним викликом (тести 3, 4, 5, 7, 8 вимагають «1 API call»);
правки, рознесені більш як на хвилину, — двома. На демонстрації це просто
правило: зробити всі правки сценарію підряд і зачекати хвилину.

## 2. Тестовий обʼєкт — через наш майстер

Вимога вендора: «Test Property - (Provider Name)», USD, Twin Room і Double Room
по 2 місця, на кожному Best Available Rate 100 і Bed & Breakfast 120.
Створює його **наш майстер** (`/app/settings/channel-manager/connect`, крок
«каталог» → `syncConnectionCatalogFor`), не руки в панелі вендора.

| Що | Екран ALiSiO | Стан |
|---|---|---|
| Обʼєкт «Test Property - ALiSiO» | Налаштування → Обʼєкти (`settings/properties`) → «Додати» | ✓ є |
| Типи номерів Twin / Double, місткість 2 | Налаштування → Номери (`settings/units`) → тип номера з кодом і `max_occupancy` | ✓ є |
| Кількість номерів (тест 9 хоче Twin 8, Double 1) | там само → номери типу | ✓ є |
| Тарифи BAR і B&B на кожному типі, валюта USD | Налаштування → Тарифи (`settings/rate-plans`): назва, код, валюта, харчування, ціна дитини (`pricing/data/rate-plans.repo.ts`; валюта обʼєкта у вендора береться з першого тарифу, `data/catalog-sync.ts:121`, і замикається, щойно є ціна) | ✓ **П1 зроблено 02.09** |
| Ціни BAR 100 / B&B 120 на одному типі (засів 500 днів скриптом у ВЛАСНУ базу дозволений; тест 8 — з інтерфейсу) | Ціни → «Чия ціна»: базова ціна типу або тариф; редактор дня і масовий пишуть рядок тарифу (`price_calendar.rate_plan_id`, `data/price-calendar.repo.ts`); успадковані дні позначені ↑ | ✓ **П2 зроблено 02.09** |
| 500 днів РІЗНИХ цін (вендор відхиляє однорідні) | Ціни (`/app/pricing`) → «Масове редагування» (`pricing/page.tsx:548` → `/api/pricing/bulk`): діапазон дат, «усі / будні / вихідні», базова й вихідна ціна; плюс матриця заселеності з сезонами (`settings/pricing-matrix`) | ✓ є: кілька діапазонів + ціна вихідних + сезони дають варіацію по днях тижня і місяцях; варіація між тарифами — лише після П2 |

## 3. Карта 14 сценаріїв

Позначки: ✓ — є екран, дія і шлях; ✗ Пn — прогалина з §4; «форма» — чесно
позначити у формі.

| № | Сценарій — точні значення листа (2026, USD; 8 Twin / 1 Double; Twin BAR, Twin B&B, Double BAR, Double B&B) | Екран → дія оператора | Поля в тілі — і НІЧОГО більше | Викликів | task id (ред. 3 — порожньо до прогону) |
|---|---|---|---|---|---|
| 1 | Повний синк, 500 ночей, кожна ніч із базовим рядком (мінімум 1) і варіативною ціною | Налаштування → Канал-менеджер → крок 5 → «Повний синк (500 ночей)» (`connect.handlers.ts fullSyncChannelConnection` → `data/full-sync.ts` → батчер → `client.ts` по одному виклику на смугу) | `rates`, `stop_sell`, `min_stay_arrival`, `max_stay`, `closed_to_arrival`, `closed_to_departure` — усі шість (Ц34 (в), дефолти явні) | 2 | |
| 2 | Twin BAR, **22.11 = 333** | Ціни → «Чия ціна» = **BAR** → день → ціна → зберегти (`pricing.handlers.ts updatePricing` → `price-calendar.repo.ts upsertPrices`, маска з різниці) | `rates` | 1 | |
| 3 | у межах хвилини: Twin BAR 21.11 = 333; Double BAR 25.11 = 444; Double B&B 29.11 = 456.23 | той самий шлях, тричі підряд («Чия ціна» — щоразу тариф, не базова ціна типу) | `rates`; три пари в одному тілі | 1 | |
| 4 | масово ×3: Twin BAR 01–10.11 = 241; Double BAR 10–16.11 = 312.66; Double B&B 01–20.11 = 111 | Ціни → «Масове редагування» × 3 (`bulk.handlers.ts` → `bulkUpdatePrices`) | `rates`; один `date_range` на тариф | 1 | |
| 5 | мінімум ночей: Twin BAR 23.11 = 3; Double BAR 25.11 = 2; Double B&B 15.11 = 5 — ціну не чіпати | Ціни → день → «Мін. ночей» → зберегти; обмеження на тип (П7, Ц32) | `min_stay_arrival` | 1 | |
| 6 | «Закрито»: Twin BAR 14.11; Double BAR 16.11; Double B&B 20.11 | Ціни → день → прапорець «Закрито» → зберегти | `stop_sell` | 1 | |
| 7 | масово: Twin BAR 01–10.11 CTA on, CTD off, max 4, min 1; Twin B&B 12–16.11 CTA off, CTD on, min 6; Double BAR 10–16.11 CTA on, min 2; Double B&B 01–20.11 min 10 | Ціни → «Масове редагування» лише полями обмежень (ціна — «Не змінювати») | лише обмеження (`min_stay_arrival`, `max_stay`, `closed_to_arrival`, `closed_to_departure` — ті, що змінились), без `rates` | 1 | |
| 8 | Twin BAR 01.12.2026–01.05.2027 = 432, CTA off, CTD off, min 2; Double BAR той самий діапазон = 342, min 3; Double B&B не чіпати | Ціни → «Масове редагування» × 2 — з інтерфейсу | `rates` + `min_stay_arrival` (+ `closed_to_*` для Twin) | 1 | |
| 9 | бронь Twin заїзд 21.11 виїзд 22.11 → Twin 21.11 = 7; бронь Double 25→26.11 → Double 25.11 = 0 | Планер → нове бронювання (`reservations.handlers.ts` → `stay-notes.ts noteAvailabilityChanged`) | `availability` | 1–2 | |
| 10 | Twin ночі 10–16.11 = 3 → заблокувати 5 номерів **з 10.11 до 17.11 (виїзд не входить)**; Double ночі 17–24.11 = 4 недосяжно з 1 номером — сказати у формі | Календар → блокування номерів (`availability-blocks.handlers.ts`, `lastNight(date_to)`) | `availability`, `date_from 2026-11-10`, `date_to 2026-11-16` | 1–2 | |
| 11 | Бронь від каналу: створити, змінити, скасувати, з ack | Booking CRS у панелі вендора → у нас бронь у «Бронювання» | — | — | **пройдено 03–05.09** (`42cf26e5-…`, `34baf9e5-…`); повторювати не треба |
| 12 | Ліміти | — | `channex/limiter.ts`: 10/хв на смугу на обʼєкт, 429 → пауза хвилину | — | «так» |
| 13 | Лише дельти, не повний синк за таймером | — | черга координат з маскою полів; повний синк лише дією (`check-no-timer-fullsync.mjs`) | — | «так» |
| 14 | Анкета | — | §5 | — | |

Поля в тілі кожного виклику — на екрані «Останні відправлення» (колонка «Поля в
тілі», кнопка «показати тіло», `cm_sends`). Редактор дня шле всю форму; маска
рахується з різниці з тим, що лежало (`upsertPrices`), тож у тесті змінюється
лише поле, яке він перевіряє. Ціна в тестах 2–8 ставиться **тарифу** («Чия
ціна» → BAR або B&B): базова ціна типу розсилається на всі пари типу — саме
так у першій подачі в тіло тесту 3 потрапив Twin B&B (`505e5d24…`).

**Де оператор бачить task ID.** Налаштування → Канал-менеджер → картка
зʼєднання → «Останні відправлення» (02.09, П6): коли, смуга, тип номера,
тариф, ночі, task id. Розписка з відповіді вендора лягає на відправлену
координату (`cm_outbox.receipt`, 0061; `domain/ari-batch.ts` групує рядки за
розпискою, `data/outbox.repo.ts markSent`); рядок-діапазон, що поїхав кількома
пачками, несе всі свої через кому. У форму копіюється з цієї таблиці. Поруч —
кнопка «Звірити з каналом» (П6 частина 2): читає календар вендора назад для
останніх відправлень і показує збіг / розбіжність / не звірено з обома числами;
розбіжне повертається в чергу з причиною, і наступний прохід шле поточне.

**Приймальний прогін після області обʼєкта (Задача 2, Блок 1) — 05.09.2026,
бета `c720030`.** Оператор з інтерфейсу, обʼєкт обрано в шапці: тест 3 —
ціна DBL на 09–10.09 для двох тарифів одразу (BAR і BB, task
`27e8ec72-a915-40b2-906d-263c267cd936`, 12:51) і TWN на 10.09 (task
`ad954475-8f88-4347-8f6c-9062937e433c`, 12:47); тест 7 — «Мін. ночей» 2 на
10.09 (той самий рядок ціни, видно бейджем `min 2` у календарі); тест 9 —
«Закрито» поставлено і знято на тих самих датах (обмеження їдуть у рядку
ціни, Д7/INC-016). Звірка: порівняно 714 · збігається 714 · розбіжностей 0 ·
не звірено closed 32 (2026-09-09 – 2026-12-04). Перенесення області обʼєкта
не змінило шляху до вендора — критерій PROPERTY-SCOPE-PLAN §5 виконано.

Спостереження з того ж прогону: на 09–10.09 DBL лишилась **ціна 0 при
відкритому дні** — система прийняла нуль і відправила його в канал (та сама
розписка `27e8ec72`). На тестовому обʼєкті це нічого не коштує; на живому
нуль у каналі — або відмова OTA, або продаж за нуль. Заведено в Блок 2 як
2.0 (див. BUILD-PLAN, Блок 2): ціна 0 — не ціна.

## 4. Дефекти і прогалини — робота до сертифікації

Порядок — слово власника: **П1 → П2 → Д1+Д2 разом → П6 → П5.** П6 перед П5,
бо коли робитимемо повний синк, task id уже треба бачити. Кожен пункт —
окремий коміт; строгість — за ціною помилки (AGENTS, інваріант 29): П2, Д1,
Д2, П5, П6 возять гроші й мовчать, коли ламаються, — гейт червоним першим;
форма тарифу (П1) — звичайна акуратність.

### Живі дефекти — не «бракує для тесту»

| № | Що | Чому це дефект уже сьогодні | Що зробити |
|---|---|---|---|
| ~~Д1~~ | Мінімум і максимум ночей, CTA, CTD пишуться в `price_calendar` і **не читались ніким** (зроблено 02.09, INC-012 «Виправлено») | прямий віджет продає одну ніч там, де готель вимагав двох: він знає лише `min_stay` тарифу САЙТУ (одне число на тариф, `widget/api/widget-availability.handlers.ts:134`), календарний — ні; котирування (`nightly-price.ts:180`) бере лише ціну. Обіцянка гостю, якої готель не давав | **два споживачі, не один**: котирування (`priceNights` віддає обмеження ночі, і реєстрація/віджет відмовляють) і батчер (`RateChange.minStay/maxStay/noArrival/noDeparture` з базового рядка, `ari-batch.ts:514`). Гейт — на обох, фікстура з ДВОМА значеннями min stay (інваріант 26) |
| ~~Д2~~ | «Закрито» писалось і **не читалось ніким** (зроблено 02.09, INC-012 «Виправлено») | віджет продає ніч, яку готель закрив; у вендора `stop_sell: false` | ті самі два споживачі: закрита ніч → `missing` у котируванні (не продається, інваріант 17) і `RateChange.closed = true` для всіх тарифів типу в батчері (И14 — разом із ціною, sticky). Наявність не чіпаємо: «закрито» — це не «номерів нуль» |
| П3а | Масовий редактор не має CTA/CTD/max | тест 7 «одним рухом» | три поля в `BulkUpdateInput` і формі — звичайна акуратність |
| ~~Д3~~ | «Ціни за заселеністю» (`settings/pricing-matrix`) не передавав `property_id` — ні на читанні, ні на записі (знайдено 02.09 на беті, коли в організації власника зʼявився другий обʼєкт; **виправлено того ж дня**) | `requirePropertyId` правильно відмовляє «more than one property — property_id is required», але екран не мав, де його взяти: матриця порожня, комірка не пишеться. Кожен готель із другим обʼєктом упирається в це на першому ж числі | селектор «Обʼєкт» на екрані (лише коли обʼєктів більше одного), `property_id` у GET матриці, POST ціни, POST знижки й котируванні; типи номерів — лише обраного обʼєкта |
| ~~Д4~~ | «Тарифи» (`settings/rate-plans`): селектор обʼєкта без підпису з дефолтом «перший за датою створення», і тариф нема чим видалити (знайдено 02.09 там само; **виправлено того ж дня**) | BAR і B&B тестового обʼєкта лягли на старий обʼєкт, і цього не було видно; прибрати їх було нічим — `route` мав лише PATCH | підпис «Обʼєкт» на селекторі; `DELETE /api/pricing/rate-plans/[id]` → `deleteRatePlan` видаляє лише ЧИСТИЙ тариф і називає відмову: `has_prices`, `mapped` (є в `cm_mappings` — дзеркало без оригіналу), `in_use` (є бронювання); координати черги тарифу йдуть разом із ним; чужий — 404. Сцена в `rate-plans.repo.check.ts` була червоною до варт |
| ~~Д5~~ | Крони `channels-publish` і `channels-pull` на Postgres пропускали ВСІХ орендарів (знайдено 02.09 на беті на тесті 8: «Чекає відправлення: 4», `run-cron.sh` → `organizations: 0, sent: 0`; **виправлено того ж дня**, INC-014) | `hasFeature(org, 'channels')` питалось ДО `runWithOrganization`; політика `organization_features_tenant` ховала рядок, дефолт OFF — черга росла, крон зелений. Той самий шлях у вебхука й публічного віджета | `hasFeature()` без орендаря читає в контексті названої організації; обробники кронів питають прапорець у контексті названої організації; гейт `features.tenant.check.ts` у `check` і `check:pg` (червоний на Postgres до фікса) |
| ~~Д6~~ | Мінімум ночей не доходив до каналу (знайдено 02.09 на беті на тесті 8: ціни доїхали, звірка показала `minStay` у нас 2 / у каналі 1 на всіх опціях і повертала координати в чергу по колу; **виправлено того ж дня**, INC-015) | `ari-payload` слав віртуальне `min_stay`; вендор застосовує його лише при `min_stay_type ≠ both`, а наш майстер заводить обʼєкт без settings → `both` → поле мовчки ігнорується. Звірка читала `min_stay_through` першим — теж не те поле | шлемо `min_stay_arrival` явно, звірка читає `min_stay_arrival`; `channex.check` очікує явне поле, `verify-adapter.check` тримає обидві осі (`arrival ≠ through`). **03.09: корінь був інший** — мінімуму в тілі не було взагалі, див. Д7; явне поле лишається правильним за ari.md |
| ~~Д7~~ | Обмеження дня не виїжджали з пачки без наявності (знайдено 03.09 на беті після деплою Д6: крон дослав 4 координати, звірка знову 2/1; **виправлено того ж дня**, INC-016) | `ariFlush` запамʼятовував проміжок дат лише при захопленні наявності, а `restrictionsAt` читав обмеження в ньому → пачка з самих цін (повернення звірки, правка мінімуму чи «закрито» в календарі) їхала без `min_stay_arrival`, CTA/CTD, «закрито» | проміжок на кожну смугу свій (`spans[kind]`); `ari-adapter.check` сцена 7: пачка лише з цін везе мінімум 3 і 2 з базових рядків, наявність на інший день не краде проміжок — був червоним до фікса. **Живе 03.09 07:45Z:** після деплою крон дослав 4 координати (task `e3125614-…`), звірка «1662 · збігається 1662 · розбіжностей 0» — `min_stay_arrival` прочитано назад як 2. Підтверджено лише в горизонті звірки (90 ночей: 2026-12-01/02); решта діапазону тесту 8 надсилається заново |
| ~~Д8~~ | Ревізія-зміна з каналу не перераховувала `nights` і не оновлювала гостя (знайдено 03.09 на тесті 11: у каналі «1 комната x 3 ночи» і нове імʼя, у нас «2 н.» і старе імʼя; **виправлено того ж дня**) | `nights` — збережена колонка, яку `UPDATE` у `applyRevision` не чіпав; гість (окремий рядок на канальну бронь) не оновлювався з ревізії | після `UPDATE` ночі перераховуються з рядка; картка гостя цієї броні оновлюється `COALESCE`-полями ревізії; `inbound-bookings.check` сцена «зміна» — ночі 4 і гість Anna Nova, був червоним до фікса |
| ~~Д9~~ | Зміна ТИПУ номера з каналу лишала бронь у кімнаті старого типу (знайдено 03.09 на тесті 11: Booking CRS перевів бронь Twin → Double, у нас вона лишилась у T2 типу Twin з `unit_type_id = Double`; **виправлено того ж дня**) | наявність рахує зайнятою КІМНАТУ (Twin), а `noteStay` клав у чергу тип броні (Double): Double у каналі не зменшувався, Twin не оновлювався — клас Ц16, тихий овербукінг | при зміні типу `unit_id = NULL` (бронь у смузі «Без номера» нового типу, рецепція ставить заново); `noteStay` кладе в чергу і тип кімнати, і тип броні; `inbound-bookings.check` сцена Д9 — кімната знята, у черзі обидва типи, був червоним до фікса. **Доведено живим 03.09 після деплою `3e2ae5e`:** тип назад на Twin → наявність DBL і TWN 07–10.12 одним викликом, task `13dbae13-de00-4c8f-8455-d169a4b7e9bf` |

Записано як INC-012 у `docs/SECURITY-FINDINGS.md`: знайдено картою, не
інцидентом; живих готелів ще немає.

### Прогалини

| № | Що | Чому блокує | Що зробити |
|---|---|---|---|
| ~~П1~~ | Тарифи (`rate_plans`) без екрана | тестовий обʼєкт має два тарифи на тип; валюта вендора — з тарифу | **зроблено 02.09**: `settings/rate-plans` + `/api/pricing/rate-plans` (GET/POST, PATCH); писач `pricing/data/rate-plans.repo.ts` через двері `@channels/outbox`; гейт `rate-plans.repo.check.ts` (орендар, код, замкнена валюта). Тариф «на типі» зʼявляється з першою ціною на дату — це П2. Зняття з продажу — Блок 2.1, 05.09: `PATCH { is_active: false }` → ціни не існує, координати «закрито» до горизонту на кожну пару з дзеркала; гейти `rate-plans.repo.check.ts` сцена 7 і `ari-adapter.check.ts` сцена 8, обидві червоним до зміни |
| ~~П2~~ | Ціна тарифу на дату не редагується | BAR 100 ≠ B&B 120 на одному типі; тести 3, 4, 8. **Це гроші** — повна строгість | **зроблено 02.09**: селект «Чия ціна» на екрані «Ціни»; `upsertPrices`/`bulkUpdatePrices` з `ratePlanId` пишуть рядок тарифу; `getPriceMonth` з тарифом показує власне число або успадковане (↑); тариф звіряється з обʼєктом типу й орендарем; координата в черзі — лише на пару тарифу. Гейт `price-calendar.repo.check.ts`: два тарифи, одна дата, дві ціни (інваріант 26) |
| ~~П6~~ (зроблено 02.09: частина 1 — розписка на координаті й на екрані; частина 2 — «Звірити з каналом» читає календар назад через опції, розбіжне назад у чергу з причиною; живий прохід замкнув коло) | Розписки (task id) не зберігаються — **асинхронний провал невидимий** | Channex приймає ціну як ЗАДАЧУ: `200` і task id не означають, що зміну застосовано (живий прогін 01.09 читав назад зарано й бачив старе число). Без розписки ми не знаємо, чи доїхала ціна — «мовчить і коштує грошей», той самий клас, що черга. І форма стадії 3 | розписка лягає на відправлену координату (`cm_outbox.receipt`, міграція; `markSent(ids, receipt)`), екран «Канал-менеджер» показує останні відправлення з task id і координатами; **стан задачі перевіряється читанням назад** — у вендора немає ендпоінта стану задачі (шукано в `api-v.1-documentation/`: `type: "task"` лише у відповідях), тож «доїхало» доводить `GET` календаря вендора для координати після паузи, як у `channex-ari-live.mjs`; розбіжність — координата назад у чергу з назвою |
| ~~П5~~ (зроблено 02.09) | Повний синк | тест 1; вихід на прод; відновлення після збою | `data/full-sync.ts`: один діапазон на змаплений тип і на змаплену пару до горизонту (500 ночей) — не 500 × N координат, — один прохід батчера, рівно два виклики, розписки, `last_full_sync_at` лише коли все поїхало; кнопка в майстрі й при ввімкненні розсилки; ніколи за таймером (`check-no-timer-fullsync.mjs`); живий прохід `channex-full-sync-live.mjs` |
| **П7** | Обмеження — на тип номера, не на тариф | тести 5 і 7 ставлять різне на тарифах одного типу | не будувати; у форму **не** «не підтримуємо обмеження» (це неправда, і тести 5 та 7 стали б пропущеними), а правду: «restrictions are modelled per room type and apply to every rate plan of that room type» — підтримка з грубішою деталізацією, не її відсутність |

Після П1 → П2 → Д1+Д2 (зроблено 02.09) → П6 → П5 кожен рядок §3 має ✓, і тест іде з екрана. П3а, П5 і П6 зроблено 02.09 — кожен рядок §3 має ✓, тест іде з екрана.

### Перед стадією 2 — де це все має жити

Код П1–П5 лежить у гілці `claude/channex-integration-66kv65`; бета крутить
код до нього, тож Test Property заводити поки НЕМАЄ де. Крок, якого не було в
жодному плані (рецензія 02.09): злити гілку в `beta` (на `7f5a7a7` — 34
коміти вперед, 0 позаду, fast-forward без конфліктів) і задеплоїти бету з
`APP_URL` у `deploy/env.beta` — без нього вебхук не реєструється. Далі
власником на беті: Функції → «Канали продажу (OTA)», майстер підключення,
засів 500 днів (масовий редактор або скрипт власника), тест 8 з інтерфейсу.
Повний порядок — `docs/HANDOVER-2026-09-01.md`, крок 8.

## 5. Відповіді анкети (тест 14) — чернетка з фактів коду

| Питання | Відповідь |
|---|---|
| Min Stay Through чи Arrival? | Arrival: шлемо `min_stay_arrival` явно (`ari-payload.ts:203`) — мінімум для заїзду в цю дату, так само його читає котирування. Віртуальне `min_stay` не шлемо: обʼєкт із `min_stay_type=both` (дефолт нашого майстра) його ігнорує за ari.md (Д6, INC-015); живий доказ явного поля — звірка після Д7 (INC-016). |
| Які обмеження не підтримуємо? | Підтримуємо всі: stop sell, CTA, CTD, min/max stay (після Д1/Д2). Формулювання для форми — дослівно: «Restrictions are modelled per room type and apply to every rate plan of that room type» — грубіша деталізація, не відсутність (П7). |
| Кілька типів номерів і тарифів на тип? | Так (дзеркало пар тип × тариф, Ц10). |
| Потрібні дані карток? | Ні. |
| PCI / токенізатор? | Ні (§10.9 ТЗ). |
| Тест 12, ліміти | Так: обмежувач 10/хв на смугу на обʼєкт, 429 → пауза обʼєкта на хвилину, без негайного повтору. |
| Тест 13, дельти | Так: черга координат у транзакціях доменних змін; повний синк — лише дією оператора або нічним завданням не частіше раз на добу. Повний синк існує лише як дія — кнопка або ввімкнення розсилки, рівно два виклики; за таймером ніколи (`check-no-timer-fullsync.mjs`) |
## 6. Що НЕ робити

Зараз не робимо, навіть якщо здається логічним (`ORDER-OF-WORK.md` §5):
самостійна реєстрація, майстер онбордингу, оплата підписки, звіти, покоївки,
листування, конструктор сайту. Перші 5–10 готелів заводяться руками —
навмисно.

- Не писати скрипт, який шле значення з таблиць тесту. Він проходить на
  папері і провалює четвертий етап на 100% (їхні слова).
- Не робити «екран для сертифікації». Кожна дія з §3 — це звичайний екран
  готелю, яким оператор користується без Channex.
- Не тримати UUID або числа з документа в коді.
- Не вмикати Booking.com на цьому етапі: сертифікація йде на тестовому обʼєкті
  staging, реальні OTA — після пʼятого етапу.

## 7. Звірка до подання — обовʼязковий крок (ред. 3)

Форма не подається, доки контролер не звірив КОЖЕН task id з тілом:

1. Власник проходить протокол (`CHANNEX-RETEST-PROTOCOL.md`) по одному тесту,
   з хвилиною між тестами, і після кожного переписує з екрана «Останні
   відправлення» task id і список полів у тілі.
2. Контролер бере кожен task id → рядок `cm_sends` (тіло за кнопкою «показати
   тіло») → перевіряє проти рядка таблиці §3: смуга, тарифи (пари), дати,
   суми, **набір полів — рівно той, що в колонці «Поля в тілі», нічого
   більше**, кількість викликів. Один зайвий ключ у будь-якому тілі — тест
   переграється, не подається.
3. Лише після «збігається все» — форма: 12 «так» (ліміти, дельти, min stay
   arrival, обмеження per room type), «ні» на картки/PCI, ідентифікатори
   тесту 11 з попередньої подачі.

Що для цього є в коді: `cm_sends` (0066) з тілом дослівно й `summary.fields`;
екран Канал-менеджер → «Останні відправлення»; маска полів координати
(`cm_outbox.field_mask`), яка й робить тіло дельтою (Ц34); гейт
`ari-adapter.check.ts` сцени 9–11 — форма тіла на справжньому клієнті для
тестів 2, 5, 6, 7 і один `date_range` для тесту 4.

**Живий прохід 05.09.2026 (кодова сесія, staging, Test Property, вікно
2027-08-10…11 — далеко від дат тестів, щоб не зачепити підготовку власника):**
повний стан — 2 виклики, читання назад через `GET /restrictions`: ціни 16/16,
`stop_sell` 16/16, наявність 16/16, і на кожній опції `min_stay_arrival 1`,
`max_stay 0`, `closed_to_arrival false`, `closed_to_departure false`
(дефолти Б2 доїхали); потім зміна ціни тарифу BAR справжнім писачем
(`--reprice`) — тіло лише з `rates`, читання назад — нова ціна. Журнал
`cm_sends` обох проходів — у звіті сесії.
