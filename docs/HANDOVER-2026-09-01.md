# Передача сесії — 1 вересня 2026

Гілка `claude/channex-integration-66kv65`, останній коміт — див.
`git log --oneline -1`. Клон **повний** (`is-shallow-repository = false`).

Цей файл існує, щоб наступна сесія не починала з археології. Він каже, що
зроблено, що зроблено **свідомо не до кінця**, і що треба переміряти, перш
ніж на цьому будувати.

> **Редакція 8 (01.09, майстер підключення — Ц18, Ц19).** Ключ у кожного
> готелю свій, з облікових даних організації, перевіряється одним GET при
> вставці; майстер відновлюваний (стан із бази, повторний вхід не подвоює);
> вікно мапінгу вендора вбудоване разовим токеном, скутим на сервері,
> `frame-src` названо явно; фініш — звірка Ц8 «N з N не змаплені». Живий
> прогін пройшов усі три речі. Далі — вебхук (И7) або листування (фаза 6),
> за словом власника.
>
> **Редакція 7 (01.09, крок «крон + екран застряглих + бюджет обʼєкта»).**
> Зроблено: `/api/cron/channels-publish` щохвилини через
> `publishAllConnections()`; екран «Канал-менеджер» із чергою кожного
> зʼєднання і кнопкою «Повернути в чергу»; обмежувач один на процес,
> ключ зʼєднання — одинадцятий виклик за хвилину відмовляється незалежно від
> проходу; транспортна невдача (429, 5xx, пауза) не рахує спроби рядка;
> застрягле крон доповідає поіменно, але не червоніє (Ц17). `check:pg` у CI.
> Наступний крок — майстер підключення (§4).
>
> **Редакція 6 (01.09, після рецензії №4 — крок «запис у `cm_outbox` з
> доменних транзакцій»).** Зроблено: діапазон ночей у черзі (Ц15, 0059),
> двері `@channels/outbox` з ручкою транзакції (Ц16), 21 писач у 12 файлах,
> гейт `check-outbox-writers`, перша відправка з нового мапінгу. Стенд
> Postgres знайшов два розходження двигунів, невидимі на SQLite (INC-011):
> `applyRevision` під `deps.tx` писав повз транзакцію, `claimBatch` клав
> текстову мітку в TIMESTAMPTZ. Виправлено; `npm run check:pg`. Діапазон
> доведено на дроті: 5 рядків → 2 виклики → 21 клітинка. Наступний крок —
> крон з екраном застряглих (§4).
>
> **Редакція 5 (01.09, рецензія №4 — «батчер ніколи не розмовляв із
> Channex»).** Три речі за рецензією: межа спроб і стан «потребує уваги»
> (Ц14); сайтова половина модифікатора записана як ВІДОМЕ розходження форми
> (Ц7); і живий прогін батчера — `scripts/channex-ari-live.mjs`, один
> обʼєкт, один прохід, читання назад. Дві відповіді з трьох — «так, на
> дроті» (ціна зі зсувом +10%, `stop_sell` знявся і знову ліг, коли ціна
> зникла); третя — «staging не дає 429», 27 викликів за 8 с без відмови.
> І одна знахідка ЧИТАННЯМ перед прогоном: координата ціни не несла типу
> номера (Ц10 у смузі цін, И15). Розділи 3–8 і 10 переписані.
>
> **Редакція 4 (01.09, друга рецензія власника).** Рецензія розділила моє
> «одне широке питання» на дві різні речі, і перша виявилась не рішенням:
> `unit_types` уже має `max_adults`/`max_children`, `reservations` — `adults`/
> `children`/`infants`; розділ є всюди, сплющила його **лише** цінова вісь.
> Тобто 422 був помилкою в одному слові (`max_occupancy` замість
> `max_adults`), а не вибором між падінням і звуженням асортименту, як я
> записав. **Виправлено**, доведено червоним гейтом і живим прогоном.
> Друга половина — скільки коштує дитина — ухвалена власником як Ц12.
> Заразом розгаданий `PUT` з `options`.
>
> **Редакція 3 (01.09, після рецензії власника).** Рецензія зняла одне моє
> припущення («`sell_mode` задається лише при створенні» — неправда, поле
> оновлюване), звела дві мої знахідки в одну (голий `rate` це наслідок
> розходження режиму й опцій) і додала третю перевірку, якої я не зробив би
> сам: **звірити сенс `price_occupancy` з їхньою віссю дорослих ДО того, як
> писати `per_person`**. Саме вона знайшла найгірше — 422, що валить
> заведення каталогу для будь-якого готелю з сімейними номерами. Порядок
> «вимір → звірка сенсу → рішення» виявився не формальністю: у зворотному
> порядку ми записали б рішення, яке ламає сімейні номери.
>
> Редакція 2 того ж дня: крок «прогнати каталог проти живого staging»
> виконано. Розділи 4–8 переписані під новий стан; що змінилось — у §10.

---

## 1. Що в останніх комітах

| Коміт | Що в ньому |
|---|---|
| `9a56f81` | **майстер підключення** (Ц18, Ц19): `data/connect.ts`, `channex/connect-adapter.ts`, один запис адаптера на провайдера, сім маршрутів, сторінка `/connect`, `channex-connect-live.mjs` |
| (цей) | **вебхук вендора** (Ц20): двері `api/webhook.handlers.ts` за токеном і секретом, `channex/webhook-adapter.ts` (реєстрація з читанням назад, ротація, проба, прибирання), засувка `data/wake.ts`, журнал `data/events.repo.ts`, міграція 0060, шість маршрутів, кнопки в майстрі й на екрані «Канал-менеджер», `channex-webhook-live.mjs` |
| `f5453aa` | **крон розсилки + екран застряглих + бюджет обʼєкта** (Ц17): `publish-all`, `/api/cron/channels-publish`, `/api/channels/connections`, `sharedChannexLimiter`, транзієнтні невдачі без спроби; `check:pg` у CI |
| `cdd0d4b` | **запис у чергу з доменних транзакцій**: Ц15 діапазон (0059), Ц16 двері з ручкою `t`, 21 писач, `check-outbox-writers`; INC-011 — два розходження двигунів зі стенда Postgres, `check:pg` |
| `d45f0fa` | інваріанти 26 і 27, `check-fixture-axes`, `ari-adapter.check.ts` (429 через справжній клієнт) |
| `ef2d198` | **батчер розмовляв із вендором**: `ari-adapter.ts`, двері `flushConnectionOutboxFor`, `channex-ari-live.mjs`; межа спроб (Ц14); вісь пари в смузі цін (И15); Ц7 на дроті на +10% |
| `eb016ff`, `d8efd26` | **батчер ARI**: ядро з трьома правилами, «закрито» як три відповіді (Ц13), Ц7 половина каналу (0058), `rates[]` (И12) |
| `0cfaf0b` | **Ц12** зроблено: вісь дорослих, дитина надбавкою (0057) |
| `7f74576` | **живий прогін каталогу**: приймання §10.10 доведене на двох організаціях; закрито шов до фази 5; двері модуля замість пробою межі |
| `ca41ad6` | **вимір закритого тарифу**: `stop_sell` липкий, ціна його не знімає (И14); виправлені формулювання Ц11 та И2; `deploy/measure-price-modifier.sh` |
| `f03323e` | доповнення до документації: сказано вголос, що `syncConnectionCatalog()` не має викликача |
| `780403a` | **`syncCatalog()`** — каталог заводиться: три шари, вісь пари в дзеркалі (0056), тариф створюється закритим |
| `e99531f` | **Ц9** — `rate_plans.fixed_price` видалено (0055) |
| `11f49b6` | **Ц7** — рішення про ціну записане |

---

## 2. Таблиці `cm_*` — звідки кожна

| Таблиця | Міграція | Що означає одним рядком |
|---|---|---|
| `cm_connections` | 0052 | зʼєднання готелю з менеджером каналів. Ключ API тут **не** лежить — він у `channel_credentials` під нейтральним іменем `channel_manager` (Р9). `remote_property_id` тепер **заповнюється** при заведенні каталогу — до 01.09 його не писав ніхто, див. §5 |
| `cm_inbound_bookings` | 0052 | журнал вхідних ревізій; `UNIQUE(connection_id, remote_revision_id)` (CP4) |
| `cm_mappings` | 0053, **розширена 0056** | дзеркало «наше ↔ їхнє». Ключ — `(connection_id, entity_type, local_id, unit_type_id, occupancy)` |
| `cm_outbox` | 0054, **0059** | черга вихідних змін. Тримає **координату**, не значення; координата може бути **діапазоном ночей** (`stay_date_to`, Ц15) |
| `cm_events` | 0054 | сирі вхідні події вебхука |

Чого **немає навмисно** (§4.3 плану): `cm_channels`, `cm_channel_listings`,
`inventory_buffer`. Цим володіє Channex, доступ дає iFrame.

---

## 3. Що вже має гейт

Усі перелічені в `npm run check`, тобто падають на збірці.

| Перевірка | Що стереже |
|---|---|
| `channex.check.ts` | клієнт: конверт, `200 OK` з `meta.warnings` = помилка (И4), пауза по обʼєкту |
| `revision-map.check.ts` | переклад ревізії без вигадування |
| `inbound-bookings.check.ts` | CP4: та сама ревізія двічі — одна бронь |
| `pull-bookings.check.ts` | `ack` **після** коміту |
| `pull-all.check.ts` | пропуск, помилка і порожньо — три різні речі |
| `mappings.repo.check.ts` | дзеркало належить орендарю, ключується парою |
| `outbox.repo.check.ts` | черга: злиття тримає індекс; **застрягле не роздається і видиме; знята координата виходить із названою причиною; координата без адресата не лягає** |
| **`ari-batch.check.ts`** | **батчер: помилка звільняє захоплення; координата → «ціни + відкрито» або «закрито»; пачка за розміром тіла; межа спроб; вісь пари; Ц7 на ненульовому** |
| **`ari-adapter.check.ts`** | **справжній клієнт із підставленим транспортом: автентичний `429` → один виклик, координата назад, обʼєкт на паузі; `200`+`warnings`; `500`; чистий `200`** |
| **`check-fixture-axes --strict`** | **інваріант 26: фікстура не вироджена по осі твердження (реєстр осей у гейті)** |
| **`outbox.check.ts` (api)** | **двері писачів: змаплені пари, всі зʼєднання, обрізання, відкат транзакції; вісь пулу — `npm run check:pg`** |
| **`check-outbox-writers --strict`** | **И16: писач наявності або ціни без дверей** |
| `catalog.check.ts` | приймання §10.10 на памʼятних заглушках |
| **`catalog-sync.check.ts`** | **новий**: після заведення каталогу зʼєднання ЗНАЄ свій обʼєкт на тому боці |
| `catalog-target.check.ts` | поля Channex: тариф закритий, місткість не вигадана |
| `check-vendor-isolation --strict` | імена вендора не виходять за `channex/`; шов композиції один |
| `check-price-source --strict` | друге джерело ціни; голий `fixed_price` (Ц9) |

**Чого гейта ще немає:** живий прогін батчера (він свідомо не гейт —
відповідь залежить від живого API; це скрипт, §7), звірка «створене
змаплене» (та сама причина).

---

## 4. Точний наступний крок

**Фаза 4: батчер доведений на живому. Далі — шов зі світом, і в такому
порядку.**

1. ~~Запис у `cm_outbox` з доменних транзакцій~~ — **зроблено** (Ц15, Ц16,
   `check-outbox-writers`). Писачі кличуть `noteAvailabilityChanged` /
   `noteRatesChanged` з `@channels/outbox` ручкою `t`; координата — діапазон
   ночей, обрізаний горизонтом. Чого НЕ зроблено: видалення тарифу не має
   обробника взагалі (карта 01.09) — коли зʼявиться, координата ДО видалення.
2. ~~Крон `/api/cron/channels-publish` + екран застряглих~~ — **зроблено**
   (Ц17): `publishAllConnections()` тієї ж форми, що стрічка; екран
   «Канал-менеджер» показує чергу і повертає застрягле; обмежувач один на
   процес — одинадцятий виклик за хвилину відмовляється незалежно від
   проходу (гейт, не сюрприз); транспортна невдача не рахує спроби. Розклад
   у `setup-backup-cron.sh` (щохвилини). Не зроблено: обмежувач у памʼяті
   ОДНОГО процесу — два контейнери одного середовища ділили б бюджет
   наосліп; коли таке зʼявиться, стан переїде в базу.
3. ~~Майстер підключення~~ — **зроблено** (Ц18, Ц19): `/app/settings/
   channel-manager/connect`, сім маршрутів під `manage_properties` і фічею
   `channels`, стан із бази, вікно вендора разовим токеном, звірка Ц8.
4. ~~Вебхук вендора~~ — **зроблено** (Ц20, 02.09): двері
   `/api/webhooks/channel-manager/<token>` (сигнал, не дані: `send_data:
   false`, невідомий токен 404, невірний секрет 401, робота — після
   відповіді через `after()` і засувку; `after()` не гарантія — сигнал, що
   загубився зі смертю процесу, ловить крон), реєстрація при ввімкненні розсилки
   з читанням назад і мʼяким станом (без `APP_URL` — вмикається, показує
   «не зареєстровано»), проба `/webhooks/test`, заміна секрету, відʼєднання;
   події «увага» на екрані «Канал-менеджер». Міграція 0060 (політика
   `cm_connections` за `app.public_token`) доведена на Postgres роллю без
   суперправ: 0 рядків до, 1 після, чужий токен 0, запис відхилено. Не
   зроблено: засувка й обмежувач — у памʼяті процесу (Ц17); приймання з
   тестовим акаунтом Booking.com (§10.3 ТЗ) чекає на акаунт.
5. ~~Три речі перед сертифікацією~~ — **зроблено** (02.09, слово власника):
   інваріант 28 (поле чужої відповіді має бути побачене живим: зразки
   `docs/vendor/channex/live/` з живих скриптів, реєстр `channex/
   live-fields.json`, гейт `check-live-fields --strict`; мок віддає форму
   зразка); `send_data: true` при читанні назад — відмова з назвою, двері
   зберігають лише конверт сигналу; формула «вебхук пришвидшує, стрічка
   гарантує; `after()` — не гарантія» у коді й документах.
6. ~~Карта покриття сертифікації~~ — **зроблено** (02.09, друге слово
   власника: жоден task ID для форми не приходить зі скриптів):
   `docs/CHANNEX-CERTIFICATION.md` — для кожного з 14 сценаріїв екран, дія
   оператора, файл:рядок виклику. Один шлях: екран → хендлер → `@channels/
   outbox` у тій самій транзакції → крон щохвилини → `client.ts:198/203`.
7. **Далі — за `docs/ORDER-OF-WORK.md` і девʼятьма уточненнями власника
   (02.09):** ~~П1 екран тарифів~~ (зроблено: `settings/rate-plans`,
   `pricing/data/rate-plans.repo.ts`, гейт) → ~~П2 ціна тарифу на дату~~ (зроблено:
   селект «Чия ціна» на екрані «Ціни», рядок тарифу в `price_calendar`, гейт
   `price-calendar.repo.check.ts`) → ~~Д1+Д2 разом~~ (зроблено: `stayRefusal()`,
   `NightlyPrices.restrictions/closed`, віджет відмовляє з назвою, батчер шле
   min/max stay, CTA, CTD і stop_sell при ціні; INC-012 «Виправлено»); П3а
   (масовий редактор: макс. ночей, CTA, CTD на діапазон); П6 частина 1
   (`cm_outbox.receipt`, 0061: розписка вендора на відправленій координаті,
   «Останні відправлення» на екрані, `recentChannelSends`); ~~П6 частина 2~~
   (зроблено: `domain/verify.ts`, `channex/verify-adapter.ts`, кнопка
   «Звірити з каналом», двері `verifyConnectionSendsFor`; ендпоінта задачі у
   вендора немає — стан читанням календаря назад через опції, розбіжне назад
   у чергу з причиною; живе: нуль наявності там тримає `stop_sell` — везе
   наявність, не прапорець) →
   ~~П5~~ (зроблено: `data/full-sync.ts` — один діапазон на тип і на пару
   до горизонту, один прохід, рівно два виклики; кнопка в майстрі й при
   ввімкненні розсилки; `check-no-timer-fullsync.mjs`;
   `channex-full-sync-live.mjs`). П7 — у форму як грубіша деталізація,
   не відсутність. Не робимо зараз: реєстрацію, онбординг, оплату, звіти,
   покоївок, листування, конструктор сайту.

8. **Перед стадією 2 — злиття й деплой бети (крок, якого не було в жодному
   плані; названо рецензією 02.09).** Усе з П1 по П5 живе в
   `claude/channex-integration-66kv65`; бета крутить код до цього. Станом на
   `7f5a7a7` гілка на 34 коміти попереду `beta` і на 0 позаду — злиття
   fast-forward, `git merge-tree` без конфліктів. Порядок:
   1. злити гілку в `beta` (PR або fast-forward — рішення власника; у main
      не пушити, потік main ← beta);
   2. у `deploy/env.beta` мають стояти `APP_URL=https://<хост бети>` (без
      нього реєстрація вебхука відмовляє: `app url not configured`),
      `APP_SECRET_KEY`, `CRON_SECRET` — зразок `deploy/env.beta.example`;
   3. `deploy/deploy.sh beta` — міграції (0061 `cm_outbox.receipt`) накотить
      `migrate.sh` між збіркою і рестартом; крони `channels-publish`
      (щохвилини) і `channels-pull` (кожні 5 хв) уже в
      `setup-backup-cron.sh`; `deploy/smoke.sh beta` — дим;
   4. на беті власником: Налаштування → Функції → увімкнути «Канали продажу
      (OTA)» для організації (`/api/settings/features`, лише власник);
   5. майстер: Налаштування → Канал-менеджер → Підключення — ключ staging,
      зʼєднання (тестове середовище), каталог (створює Test Property на тому
      боці), вікно мапінгу, «Увімкнути розсилку» (реєструє вебхук і робить
      повний синк);
   6. засів 500 днів — масовий редактор «Ціни» або скрипт власника (окремого
      сіду цін у репозиторії сьогодні немає); тест 8 — лише з інтерфейсу;
   7. task id для форми — «Останні відправлення» на екрані «Канал-менеджер»
      і відповідь кнопки «Повний синк (500 ночей)».

   Злиття 02.09 (fast-forward, `d84a732`) впало в CI на задачі «hotel
   onboarding»: гілка була червоною з `3a0547f` двадцять пушів поспіль і
   ніхто не дивився (INC-013, два JS-викликачі зі старим ключем `persons`).
   Виправлено тим самим днем; далі — знову fast-forward у `beta` і запуск
   Actions `beta ← beta`. Деплой `034c6f7` пройшов (міграції 0054–0061,
   дим 4/4, готелі з файлів звірились).

   Прохід власника по кроках 5–6 того ж дня знайшов два дефекти екранів
   в організації з ДВОМА обʼєктами (Д3, Д4 у CHANNEX-CERTIFICATION §4):
   матриця заселеності без `property_id` не читала й не писала нічого, а
   тарифи лягли на перший обʼєкт за датою створення й не видалялись.
   Обидва виправлено; після деплою — спершу «Тарифи» → обʼєкт зі старими
   BAR/BB → «Видалити», потім знову крок 5 на Test Property.

   Далі того ж вечора дійшли до тесту 8: каталог заведено, розсилку
   ввімкнено, повний синк двома викликами з task id, вебхук і пробна
   доставка 200. Три масові правки за 01.12.2026–01.05.2027 лягли в чергу
   і НЕ поїхали: крон `channels-publish` на Postgres пропускав усіх
   орендарів (INC-014 — `hasFeature` без контексту орендаря під RLS).
   Звірка з каналом при цьому показала розбіжності саме на 01.12 і
   повернула їх у чергу — читання назад працює. Після деплою фікса крон
   дошле чергу сам; далі — «Звірити з каналом» (розбіжностей 0) і task id
   з «Останніх відправлень».

   Після деплою INC-014 крон дослав чергу за хвилину ОДНИМ викликом
   (task `d4489412-0955…` — тест 8 як «1 API call»), ціни в каналі
   збіглися, а мінімум ночей — ні: віртуальне `min_stay` обʼєкт із
   `min_stay_type=both` ігнорує (INC-015, Д6). Виправлено явним
   `min_stay_arrival`; після деплою крон дошле дві повернуті координати,
   і звірка має дати 0.

   Не дала: 03.09 після деплою INC-015 крон дослав чотири координати
   (task `89d53b01-…`, `a851a102-…`), звірка знову 2/1. Справжній корінь —
   INC-016 (Д7): `ariFlush` знав проміжок дат лише з захоплення НАЯВНОСТІ,
   і пачка з самих цін читала обмеження з порожнього проміжку — мінімум
   ночей, CTA/CTD, «закрито» з календаря в тіло не потрапляли ніколи, і
   на старому коді теж. Діагноз INC-015 при цьому був висновком зі звірки,
   не побаченим тілом (тіла ARI не логуються — робота: дайджест тіла поруч
   із розпискою). Виправлено проміжком на кожну смугу; гейт —
   `ari-adapter.check` сцена 7, червоний до фікса.

   **Доведено живим 03.09 07:45Z** (деплой `0fa98d5`): звірка повернула
   чотири координати, крон дослав їх одним викликом (task
   `e3125614-d217-4d69-845a-599f53f84241`), повторна звірка — «порівняно
   1662 · збігається 1662 · розбіжностей 0». INC-015 і INC-016 закриті.

   **Того ж ранку власник пройшов тести 2–11 з інтерфейсу** — кожен одним
   викликом, task id у CHANNEX-CERTIFICATION §3 (2 `fd298345`, 3 `1ca2dd63`,
   4 `0dc60845`, 5 `938e2074`, 6 `222e192b`, 7 `91e3d68d`, 8 `c31e96f5`,
   9 `77976577`, 10 `4fe6a624`); фінальна звірка 567/567, розбіжностей 0,
   «не звірено: closed 32» — ночі з нулем наявності, де stop-прапорець
   каналу нічого не значить. Тест 11 через Booking CRS: створення → вебхук →
   стрічка → ack за секунду, зміна й скасування доїхали; знайдено Д8 —
   після зміни з каналу `nights` і гість не оновлювались (виправлено, гейт
   `inbound-bookings.check` червоний до фікса; доведено живим після деплою
   `864ae40`: ревізія 77f89621 → картка 07–11.12, «4 н.», нове імʼя). Той
   самий живий прогін показав Д9: канал перевів бронь Twin → Double, а вона
   лишилась у кімнаті Twin — наявність рахувала Twin, канал отримував
   Double (тихий овербукінг). Виправлено: зміна типу знімає кімнату, у чергу
   йдуть обидва типи; сцена Д9 у тому ж гейті; доведено живим після деплою
   `3e2ae5e` (task `13dbae13-…`: DBL і TWN одним викликом). Прогін
   сертифікації завершено; далі — анкета (контролер власника пише відповіді
   з переліку task id у §3 CHANNEX-CERTIFICATION).

   **Задача 1 з docs/NEXT-TASKS.md зроблено 03.09 ввечері:** код броні на
   боці каналу (`external_uid`, BDC-…) на картці, у списку й в обох пошуках;
   на картці одна вкладка «Історія змін» замість двох (обидві читали
   `booking_activity_log`, перша — без автора); ревізії з каналу пишуть
   запис в історію через двері `@bookings/history` (хто — OTA, що — різниця
   знімків, код і ревізія); історію бачать власник, директор, менеджер.
   Гейт: `inbound-bookings.check` — три ревізії, три записи (червоний до
   фікса); `booking-history.check` — різниця знімків. Далі — задача 2
   (область готелю), приймання: тести 3, 7, 9 на беті і звірка 0. **CI-запуск 475 на цьому коміті впав на збірці** (не на гейтах): картка
   броні імпортувала `@bookings/history`, а той тягне репозиторій →
   `@core/db/async` → `next/headers` у чанк браузера; `tsc` і `npm run check`
   мовчали. Виправлено окремими клієнтськими дверима
   `modules/bookings/ui/booking-history.ts` (лише чистий домен) у коміті
   Задачі 2; урок — перед пушем клієнтських змін ганяти `next build`, а не
   лише `tsc`.

   **Задача 2 з docs/NEXT-TASKS.md зроблено 03.09 вночі (область обʼєкта):**
   один провайдер `src/ui/PropertyScopeContext.tsx`, перемикач у шапці
   (десктоп і телефон, лише коли обʼєктів 2+), `?property=<id|all>` в адресі
   вкладки, кука `property_scope` через `POST /api/auth/property`, `/api/auth/me`
   віддає `properties` і `property`. Вісім екранів більше не тримають власного
   `propertyId`; екрани налаштувань за «Усі обʼєкти» просять обрати
   (`PropertyRequired`), а не беруть перший. Гейт `check-property-scope.mjs`
   у `npm run check` — був червоним на 11 місцях у 8 файлах до перенесення.
   План, межі й свідомо не зроблене — docs/PROPERTY-SCOPE-PLAN.md; рішення
   П10. **Приймальний критерій ще НЕ закрито:** після деплою на бету треба
   повторити тести 3, 7, 9 і одну звірку (порядок нижче, §0.1).

   **Блок 1 з docs/BUILD-PLAN.md зроблено 04.09 вночі:** назва обʼєкта в
   шапці видна завжди (з одним обʼєктом — підпис, з двома — перемикач);
   списки звужуються за областю і показують колонку «Обʼєкт» за «Усі»
   (броні, дашборд, звіти; API приймають `property_id`); один набір станів
   екрана `components/ui/State.tsx` (порожньо з кнопкою, завантаження,
   помилка, відмова) стоїть на тарифах, цінах, номерах, обʼєктах, бронях,
   дашборді; токени й патерни — docs/UI-LANGUAGE.md (П11); інвентар «що
   було» — PROPERTY-SCOPE-PLAN §0; гейт доведено червоним на навмисному
   стані (§4 там само). **ЧЕКПОІНТ рецензента** — скріншоти перемикача й
   екранів у новій області надіслано власнику; Блок 2 не починався до
   відповіді. Приймальний критерій (тести 3, 7, 9 і звірка на беті) — той
   самий, що для Задачі 2, порядок у §0.1 нижче.

   **Приймання Задачі 2 і Блоку 1 на беті — 05.09, закрито.** Оператор
   пройшов тести 3, 7, 9 з області обʼєкта: task id `27e8ec72…` (DBL, BAR і
   BB, 09–10.09) і `ad954475…` (TWN, 10.09), звірка 714/714/0
   (CHANNEX-CERTIFICATION §3). Відкритим лишається лише чекпоінт рецензента
   щодо мови інтерфейсу. Спостереження прогону — ціна 0 на 09–10.09 DBL
   пішла в канал — заведено в Блок 2 як 2.0.

### 0.1 Що зробити оператору після деплою Задачі 2 на бету

Усе — з інтерфейсу бети, як у §3 CHANNEX-CERTIFICATION; нічого руками на
сервері. Перед початком: у шапці бети зʼявиться перемикач обʼєкта, якщо в
організації їх два й більше. Обрати **той обʼєкт, що підключений до
Channex** (майстер каналів: Налаштування → Канал-менеджер → Підключення
покаже його назву в картці «Обʼєкт»). За «Усі обʼєкти» екрани цін і
тарифів покажуть картку «Оберіть обʼєкт» — це очікувано, не поломка.

| Крок | Де | Що зробити | Що має статись |
|---|---|---|---|
| 1 | шапка | обрати обʼєкт у перемикачі; відкрити «Ціни» у другій вкладці | у другій вкладці той самий обʼєкт; в адресі обох `?property=<id>` |
| 2 | тест 3 | Ціни → тип DBL → тариф BAR → одна дата, змінити базову ціну; те саме для другого тарифу на ту саму дату | «Канали»: два рядки «ціна … BAR/…» з task id, звірка 0 розбіжностей |
| 3 | тест 7 | Ціни → та сама пара → поставити «Мін. ночей» 2 на одну дату | рядок «обмеження …» з task id; у Channex Restrictions на дату — min stay 2 |
| 4 | тест 9 | Ціни → «Закрито» на одну дату для DBL/BAR, потім зняти | два рядки з task id; у Channex stop sell зʼявляється і зникає |
| 5 | «Канали» | натиснути «Звірити» | `розбіжностей 0`; «ще застосовуються» доходить до 0 за хвилину |
| 6 | адреса | у першій вкладці перемкнути на інший обʼєкт (або «Усі») | друга вкладка НЕ змінилась (її адреса тримає свій обʼєкт); нова вкладка без параметра відкривається на останньому обраному |

Крок 6 — саме те, заради чого область в адресі, а не лише в сесії
(NEXT-TASKS, «дві вкладки поруч»). Якщо на кроках 2–4 task id не
зʼявляються — обʼєкт у шапці не той, що підключений: перевірити крок 0.
Результат (task id і «розбіжностей 0») — у кодову сесію одним
повідомленням, і Задача 2 закривається записом у CHANNEX-CERTIFICATION.

Що НЕ робити перед цим: не переносити сайтову половину модифікатора
(Ц7 — відоме розходження форми, окрема робота); не заводити другу варту на
опції в каталозі (шапка `catalog-target.ts`).

Живий прогін повторюється однією командою, і його варто ганяти після кожної
зміни в батчері або адаптері:

```
APP_SECRET_KEY=… CHANNEX_API_KEY=… \
node scripts/channex-ari-live.mjs --org <orgId> <connId>            # звіт
node scripts/channex-ari-live.mjs --org <orgId> <connId> --confirm  # прохід + читання назад
```

---

## 5. Що зроблено СВІДОМО не до кінця

Це не борг і не недогляд. Не «лагодити», не побачивши причини.

**Маршрут заведення каталогу не додано.** Функція тепер має двері фасаду
(`syncConnectionCatalogFor` у `@channels`) і викликача —
`scripts/channex-catalog-live.mjs`. Маршрута немає навмисно: маршрут без
варти й без екрана це кнопка, яка створює сутності в чужому акаунті. Майстер
підключення викличе саме цю функцію, і саме тому вона написана як двері, а
не як внутрішність.

**Половина модифікатора на САЙТІ лишена в старій формі — свідомо.**
Канальна половина зроблена (0058, знакове число, батчер зсуває). Сайтова
тримає пару «відсоток + слово напрямку» і має трьох живих читачів у ціновому
шляху віджета; переїзд міняє те, що бачить гість на прямому сайті. Записано
в Ц7 як ВІДОМЕ розходження форми з наміром звести — не як забуте.

**Крон розсилки ARI не заведений.** Батчер доведений на живому (§7), але
крон без екрана застряглих координат — черга, про яку ніхто не дізнається
(Ц14). Обидва йдуть разом, §4.

**Перевірки бази на SQLite не доводять Postgres** (INC-011). Стенд §7
знайшов дві діри за один прохід; `npm run check:pg` ганяє DB-перевірки
каналів на пулі, але в CI його ще немає — робота `live` має Postgres і
могла б їх крутити. Не зроблено тут: це зміна workflow, окремий коміт.

**`429` на живому не побачено.** Не тому, що не пробували: 27 викликів за
8 с на один обʼєкт у двох смугах — staging мовчить. Шлях «помилка звільняє
захоплення» доведений на моку й у домені; далі бити по спільному акаунту —
інваріант 25.

**Тариф створюється закритим, а не з ціною.** Переміряно (§6.2): це працює
і чому саме — тепер відомо точно.

**Каталог не оновлює те, що вже є.** Свідомо: повторне надсилання тарифу
перезаписало б засів календаря.

**Бронь на кілька кімнат не заводиться** (`multi_room_not_supported`) і
**не підтверджується** навмисно: лишається в стрічці.

**Обрізання заселеності ВИПРАВЛЕНО** (редакція 4): шов ріже по `max_adults`,
а не `max_occupancy`. Це було не рішення, а помилка в одному слові — колонка
стоїть поруч від першого дня. Твердження, яке це тримає, живе в
`property-rate-plans.check.ts` і до виправлення було червоним.

**Ц12 зроблено, ціни в канал віддаються** (редакція 5): вісь дорослих,
дитина надбавкою тарифу. Другої варти на опції в адаптері немає навмисно —
пояснення в шапці `catalog-target.ts`.

**И12 виконано:** `rateValues()` кладе `rates[]`, і на живому вони лягли на
всі опції (§7).

**Ніч, у якої ціна є не на всі заселеності пари, закривається цілком.**
`stop_sell` — на тариф, не на опцію; послати частину означало б лишити решту
зі старою ціною. Строго й навмисно — шапка `ari-adapter.ts`.

---

## 6. Два числа, які треба переміряти

### 6.1 Скільки рядків у `site_rate_plans` НА ПРОДІ — ✅ ЗМІРЯНО (Р12)

**`rows_total = 0`**, виміряно оператором 01.09 роллю, що обходить RLS, і
скрипт це тепер друкує й перевіряє сам. Перенесення за Ц7 безкоштовне.
Нижче — як воно було до виміру, лишено як історія методу.

Попереднє «нуль рядків» виміряне на **локальній базі розробки**.
Обґрунтування Ц7 «перенесення колонки безкоштовне» стоїть саме на цьому
числі.

Скрипт написано і перевірено засівом: `deploy/measure-price-modifier.sh`.
Він міряє **чотири** колонки, не одну — `pricing_modifier_percent` без
`pricing_modifier_type` не означає нічого (10 — це дешевше чи дорожче?), а
`pricing_mode`/`derived_from_plan_id` це сусідня машинерія того ж рішення.

**Знахідка на місці:** відсоток і напрямок — два поля, які можуть
СУПЕРЕЧИТИ одне одному (відʼємне число з напрямком «дешевше» — подвійне
заперечення). Перенесення за Ц7 має звести їх до одного знакового числа,
інакше асиметрію замінить двозначність.

### 6.2 Чим створюється закритий тариф — ✅ ЗМІРЯНО

Контрольований дослід на живому staging: два тарифи, однакові в усьому,
крім шапки, та сама дата, та сама ціна.

| Шапка при створенні | До ціни | Після `POST /restrictions` |
|---|---|---|
| без `stop_sell` | `rate 0.00`, `stop_sell true` | `rate 95.00`, **`stop_sell false`** |
| `stop_sell: true` | `rate 0.00`, `stop_sell true` | `rate 95.00`, **`stop_sell true`** |

Три висновки:

1. `stop_sell: true` скаляром API приймає і розгортає в `[true×7]` по дню
   тижня; закриття стоїть на КОЖНІЙ даті календаря. Код правильний.
2. **Календар засівається однаково.** Тариф, створений БЕЗ ключа `rate`
   взагалі, лягає як `rate: "0.00"` на кожну дату до горизонту, на всіх
   опціях. Обґрунтування Ц11 «ключа немає, тож календар не засівається»
   було неправильне; сама заборона лишається, але стереже вона інше.
3. **Прапорець ЛИПКИЙ** (И14): ціна його не знімає. Батчер фази 4, який шле
   саму лише ціну, лишить весь заведений каталог закритим назавжди — без
   помилки з чийогось боку.

Нульову дату закриває і сам Channex (рядок 1, стовпчик «до») — тобто
«канал продасть ніч за нуль» не відтворюється. Але `unavailable_reasons`
порожній: причини API не називає, тож це недокументована чужа поведінка, і
наш `stop_sell` лишається як друга варта саме тому.

---

## 7. Що знайшов живий прогін (нове, 01.09)

### Приймання §10.10 — ДОВЕДЕНО

`node scripts/channex-catalog-live.mjs --org <orgId> <connId>… --confirm`

```
  Alpine Test Lodge:    2 типи / 2 тарифи / 3 пари / 7 заселеностей / EUR
  Riverside Test Rooms: 4 типи / 1 тариф  / 3 пари / 7 заселеностей / CZK
  форми справді різні: так     обидва зійшлися: так     ✓ ПРИЙНЯТО
```

Обидві зійшлися з вендором по обох ендпоінтах, вісь пари ціла, другий
прохід створює нуль. Пропуски названі: `no_price`, `no_rooms`,
`unit_type_not_mapped`.

### Діра, яку читання не показувало

`cm_connections.remote_property_id` **ніхто не писав**. Колонка лише
читалась, і `pullConnection()` починається з `if (!conn.remotePropertyId)
throw`. Тобто після бездоганного заведення каталогу броні з каналів не
приїжджали б НІКОЛИ, а виглядало б це як несправність стрічки. Кожна фаза
окремо правильна; порожній був шов між ними. Закрито, тримає
`catalog-sync.check.ts` (перевірка була червоною до виправлення).

### Пастка в назві `/rate_plans/options`

Це **НЕ** список заселеностей. Дослівно: «list of all RATE PLANS … without
additional details». По рядку на ТАРИФ. Заселеності — в `options[]`
всередині `GET /rate_plans`.

Вказівка попередньої редакції цього файла («звірити з
`GET /rate_plans/options`»), виконана буквально, оголошує РОЗБІЖНІСТЬ там,
де обидві сторони праві: наші 7 опцій проти їхніх 3 тарифів. Тепер скрипт
читає обидва: `options` — по тарифах і по осі пари (лише там є
`room_type_id`), список — по заселеностях (И13).

### И12 порушено в коді

`rateValues()` кладе голий `rate`. Виміряно на живому тарифі з трьома
опціями: `rate: 13000` → `occ1*=130.00, occ2=0.00, occ3=0.00`, відповідь
`200 OK` **без warnings**.

Пастка **ширша**, ніж було записано: і И12, і INVENTORY казали «на тарифі
`sell_mode: per_person`». Виміряно на `per_room` — те саме. Формулювання з
умовою читалося як дозвіл. Виправлено в обох файлах.

### Режим тарифу суперечить його опціям — і це вже валить каталог

Три виміри, зроблені за рецензією власника в порядку «спершу оновлюваність,
потім сенс осі, і лише тоді рішення».

**1. `sell_mode` ОНОВЛЮВАНИЙ.** Попереднє «задається лише при створенні»
було припущенням і воно неправильне: поле стоїть у тілі
`PUT /rate_plans/:id` прямо в прикладі доків. Вимір: `PUT` із самим лише
`sell_mode` дає `200` в обидва боки, читання назад підтверджує. Мовчазного
ігнорування немає — це не И4. **Але `PUT` із `options` у тілі падає 422** на
власну ж назву тарифу (двічі, механізм не зʼясований): режим виправний
дешево, набір опцій — схоже, ні.

**2. Дефект — один, не два.** Документація дослівно
(`rate-plans-collection.md:668`, 512): `per_room` бере ОДНУ опцію на
максимальну місткість і означає «ціна однакова на будь-яку кількість
гостей»; `per_person` — по опції на кожну кількість ДОРОСЛИХ. Ми не шлемо
`sell_mode` взагалі, тобто створюємо `per_room`, і кладемо в нього опцію на
кожну заселеність. Голий `rate`, що рухає лише основну опцію, — **наслідок**
цього, а не окрема пастка: на `per_room` опція за контрактом одна.

**3. Наша вісь рахує не те — і половина цього виявилась помилкою, а не
вибором.** Рецензія власника: `unit_types` уже має `max_adults`,
`max_children`, `max_occupancy`, `base_occupancy` (`origin/main:src/lib/db.ts`
182–185), `reservations` — `adults`, `children`, `infants` (272–274). Розділ
є всюди; сплющила його ЛИШЕ `price_occupancy.persons`.

Тому 422 виправлено одразу: шов різав по `max_occupancy`, а мав по
`max_adults`. Сімʼя 2+2 нічого не втрачає — опція 2 і є правильна. Доведено
живим прогоном: сімейний тип, який валив прохід, тепер заводиться.

Лишається справжнє питання — **скільки коштує дитина**; ухвалено як Ц12.

`price_occupancy.persons` годується
`adults + children` у трьох незалежних місцях (`day-sheets.repo.ts:164`,
`widget-reserve.handlers.ts:307`, `quote.repo.ts:19`); окремої ціни дитини в
моделі немає. У них `per_person` — по дорослих, діти через `children_fee`.

Це вже ламає, а не тільки загрожує. Шов ріже заселеності по `max_occupancy`,
Channex валідує по `occ_adults`:

```
тип «2 дорослих + 2 дітей», опції 1..4  →  422
  "occupancy 3, 4 exceeds the room type's max adults occupancy of 2"
опції 1..2                              →  201
```

422 при створенні тарифу валить **увесь** прохід каталогу. Тобто буденний
сімейний номер із цінами на 1–4 особи сьогодні не заводиться. Прогін §7
цього не спіймав випадково: у тестового `FAM` було чотири дорослих.

### Звірка «створене змаплене» (Ц8) — з надії стала попередженням

`GET /channels` віддає мапінг-айтеми. Скрипт каже вголос:
`каналів у обʼєкта: 0 — жоден тариф нікуди не продається`, з переліком
непокритих. Читається й `is_active`: тариф, змаплений лише на **вимкнене**
зʼєднання, продається так само нікуди.

Це навмисно **не** гейт збірки: відповідь залежить від живого API, і
червона збірка від чужого простою — це збірка, яку вчаться ігнорувати.

### Писачі й діапазон (редакція 6)

Карта писачів (агент, 51 виклик) дала три речі, яких читання коду не дало б
швидко: жодного виклику черги в `src/`; `enqueueChange` не вміє приєднатись
до чужої транзакції; пошуку зʼєднань за обʼєктом немає. Усі три закрито.
Стенд Postgres — ще дві, невидимі на SQLite (INC-011).

Діапазон на дроті: `channex-ari-live.mjs --confirm --from 2026-10-05 --days 3`
кладе 2 + 3 рядки-діапазони замість 15 подобових — той самий результат,
21 з 21 по всіх трьох питаннях.

### Батчер розмовляв із вендором (редакція 5)

`scripts/channex-ari-live.mjs --org <orgId> <connId> --confirm` — 15
координат (6 наявності, 9 цін на 3 пари × 3 дати), один прохід:

```
ПРОХІД (678 мс): відправлено 15, повернуто 0, знято 0, потребує уваги 0, викликів 2
  1. ціна лягла:        21 з 21     (1500 → 1650.00: модифікатор +10% на дроті)
  2. stop_sell знявся:  21 з 21
  3. наявність лягла:   21 з 21     (4 і 2 вільних)
```

І назад: матриця перестала бути чинною → той самий прохід приїхав як
`stop_sell: true` на 7 з 7 клітинок, вендор лишив останню ціну поруч
(`1650.00⛔`). «Закрито» як стан працює в обидва боки.

**`429` — не отримано.** 27 викликів за 8 с (12 наявності + 15 цін, кожен
окремим клієнтом, тобто повз наш обмежувач) на один обʼєкт staging —
жодної відмови. Документований ліміт там не діє. Питання 3 рецензії стоїть
на моку (`channex.check.ts`, `rateLimited`) і домені (сцена 1).

**Знайдено читанням до прогону:** координата ціни не несла типу номера.
Один наш тариф на чотирьох типах — чотири їхні (Ц10), і `pricesAt(ratePlanId,
date)` не мав чим цінувати. Шістнадцять тверджень домену стояли на одному
типі й були зеленими в обох світах. Тепер И15, три гейти червоними до зміни.

---

## 8. Відкрите до власника

| Питання | Що блокує | Чому це не факт і не «вже вирішено» |
|---|---|---|
| ~~Ненульовий модифікатор на проді~~ | — | **закрито Р12:** таблиця порожня, живих готелів немає (Р11) |
| ~~Що означає вісь `persons`~~ | — | **закрито Ц12:** дорослі; дитина — надбавка тарифу |
| Ц8 проти поради вендора «не робити тариф на OTA» | нічого сьогодні | вибір між розмноженням тарифів і модифікатором на канал. **Нове:** у Channex є рідний `derived_option` на рівні зʼєднання І на рівні мапінгу — тобто модифікатор на канал у них уже існує, і Ц7 варіант В лягає на нього без нової осі |
| Ядро проти модулів: що вимикається фіче-флагом | фазу 0 | ціна й пакування |

---

## 9. Стеля, у яку впирається множення пар

Наш один тариф на N типах = N тарифів у Channex (Ц6, Ц10). З
`property-size-limits.md`: готель — 20 типів / 200 тарифів на обʼєкт;
оренда житла — 50 типів / **10 тарифів на КОЖЕН тип**. Понад стелю — не
відмова, а гроші.

🔴 **Живий обʼєкт віддає інші числа** (INVENTORY §14): `max_count_of_room_types`
50, `max_count_of_rate_plans` 400, `max_count_of_occupancies` **10** —
останнє вдвічі НИЖЧЕ за документовані 18. Числа лежать у самому обʼєкті,
тож питати треба обʼєкт, а не документацію.

**Повний синк — це 2 виклики, а не години.** Один `POST /restrictions` несе
необмежену кількість значень до 10 МБ; ліміт 10+10 на хвилину рахує
ВИКЛИКИ. Стеля тут — розмір тіла, і росте вона там, де множить пара.
Батчер має різати пачку **за розміром**, не за кількістю рядків.

---

## 10. Що лежить на staging-акаунті вендора

Прогін §7 залишив по собі два обʼєкти, і це навмисно: фаза 4 штовхатиме
ціни саме в них, а перестворювати каталог щоразу — марна робота.

| Обʼєкт | id на тому боці | Що в ньому |
|---|---|---|
| ALiSiO Integration Sandbox | `cbffa533-…` | від сесії 31.08, три тарифи |
| Alpine Test Lodge | `65dea8cf-…` | 2 типи, 3 тарифи, 7 опцій |
| Riverside Test Rooms | `50c16603-…` | 3 типи, 3 тарифи, 7 опцій |

Прибрати — `DELETE /properties/{id}`; скрипт цього навмисно не вміє
(видалення в чужому акаунті не має бути побічним ефектом звірки).

**Четвертий обʼєкт на акаунті — НЕ наш**: це справжній клієнт вендора.
Звідси інваріант 25 (AGENTS §3): досліди тільки на своїх обʼєктах, і це
стосується й читання, яке змінює стан — `ack` прибирає ревізію зі стрічки
назавжди, `activate` міняє чуже зʼєднання. Звичайний `GET /properties`
дозволений: інакше не дізнатися, що там наше.

Усі проби цієї сесії прибрані (перевірено читанням: пісочниця повернулась до
двох типів і трьох тарифів).

**Вебхуки (02.09).** Живий прохід `channex-webhook-live.mjs` зареєстрував
вебхук на Alpine (`callback_url` — двері бети з токеном зʼєднання
`cmc_12320beb28fc0fc2`), прочитав назад, дав вендору постукати і **прибрав**:
GET назад — 404. У спільному акаунті нашого вебхука немає. Лишити його
зареєстрованим уміє лише прапорець `--keep` — для оператора на беті, коли
адреса справжня і код із дверима вже розгорнуто.

**Після живого прогону батчера (редакція 5)** у календарі Alpine на тому
боці лежать СПРАВЖНІ числа нашого тестового готелю, і прибирати їх нема
чого: ціни зі зсувом +10% на 01–03.10 (1650/2200/2750 на DBL, 1650 на SGL),
01.11 закрита з останньою ціною поруч, наявність 4/2; проба 429 поклала
наявність DBL на 03–14.11 і ціну BAR×DBL на 15–29.11 (по одній координаті
на виклик, 27 викликів). Локальна черга `cm_outbox` порожня — усе
відправлене; модифікатор зʼєднання в локальній базі повернуто на 0, матриця
цін — на «завжди чинна».

Локально під це заведено дві організації (`alpine-test`, `riverside-test`)
з тарифами й зʼєднаннями. Це **тестова база розробки**, не прод.

---

## 11. Правила, які тут коштували дорого

**Двоє нових за рецензією №4 (записані в AGENTS як інваріанти 26 і 27):**

- **Фікстура не вироджена по осі твердження.** Пʼять разів за добу один
  звір: `DBL`/`TRI` з рівною місткістю, одна ніч, нульовий модифікатор, один
  тип номера, засів на двох із трьома дорослими. Тепер реєстр осей у
  `check-fixture-axes.mjs`; нова перевірка з віссю додає туди рядок.
- **Живий прохід закриває фазу лише якщо читає назад.** `200 OK` не означає
  нічого (И4). Між заглушкою й живим API — сходинка: справжній клієнт із
  підставленим транспортом і автентичним тілом вендора.

Повний список — `AGENTS.md`. Ці зʼявилися за останні дві доби:

- **Схема читається з `origin/main`**, ніколи з робочої копії; перед
  висновком з історії — `git rev-parse --is-shallow-repository`.
- **Суттєва зміна починається з ЧЕРВОНОГО гейта.** За 01.09 цей крок був не
  формальністю, а способом знайти діру §7: перевірка написана першою і
  показала `actual: null`.
- **Живий API — вища інстанція за наш документ, і за нашу памʼять теж.**
  За один день три записані твердження виявились неточними (Ц11, И2, И12) —
  усі три були написані з документації або з одного досліду, і жодне не
  було брехнею; вони просто описували вужчий випадок, ніж читалися.
- **Гейт, який тебе спіймав, найчастіше правий.** `check-price-source`
  і `check-boundaries` обидва зупинили цю сесію, і обидва вказали на
  краще рішення, ніж було задумано.
