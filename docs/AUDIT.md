# Аудит застосунку: шари, інструменти, знахідки

Стан: **2026-08-24**. Цей документ відповідає на питання «як перевірити весь
застосунок цілком» — не одним натиском, а п'ятьма шарами, кожен з яких ловить
свій клас поломок. Половина шарів у репозиторії вже існувала (гейти в
`npm run check`, CI, ізоляційні перевірки); цей документ додає відсутні й
каже, що чим ловиться, щоб те саме не шукали руками двічі.

Розділ 2 — знахідки прогону 2026-08-24. Вони лишаються тут, поки не
виправлені: чат ніхто не перечитує, а документ — читають.

---

## 1. П'ять шарів

| Шар | Що ловить | Чим | Коли |
|---|---|---|---|
| 1. Статика | мертві файли/експорти/залежності, цикли, типи | `npx tsc --noEmit` · `npm run audit:dead-code` (knip) · madge | перед комітом / раз на тиждень |
| 2. Гейти | доменні інваріанти — усе з таблиці в AGENTS.md §4 | `npm run check` | кожен коміт + CI |
| 3. Обхід UI | мертві кнопки, консольні помилки, 4xx/5xx, крахи сторінок | `npm run audit:ui` | після помітних змін UI |
| 4. AI-рев'ю | race conditions, недороблені гілки, логічні дірки | `/code-review`, `/security-review` по модулю | на PR / по модулю |
| 5. Runtime | помилки з реальними даними реальних клієнтів | Sentry/GlitchTip — **ще не підключено** | постійно, після інтеграції |

### Шар 1 — статика

`tsc` уже тримає CI. Нове — **knip** (`npm run audit:dead-code`): файли, які
ніхто не імпортує, експорти, які ніхто не кличе, залежності, які ніхто не
використовує. Конфіг у `knip.jsonc` пояснює, чому скрипти й `public/` не
рахуються мертвими. Це звіт, не гейт: список треба читати очима, бо «ніхто не
імпортує» інколи означає «викликається ззовні способом, якого knip не бачить».

Цикли залежностей: `npx madge --circular --ts-config tsconfig.json
--extensions ts,tsx src` — разовий інструмент, ставити в залежності не треба.

### Шар 2 — гейти

Були до цього аудиту; список і причини — AGENTS.md §4. Аудит додав один:
`check-price-source.mjs` (інваріант 16 — ціну ночі рахує лише
`modules/pricing`; історію див. розділ 2.1). Біль лінтера тут свідомо не
чіпається: `npm run lint` має ~5000 успадкованих зауважень і не гейтиться,
поки їх не розгребено (пояснення — у коментарі `.github/workflows/checks.yml`).

### Шар 3 — обхід UI (краулер)

`scripts/audit-ui.mjs` логіниться оператором, обходить усі внутрішні
посилання BFS-ом, тисне кожну видиму кнопку і записує: кнопки без реакції
(`DEAD_BUTTON`), кнопки без імені для скрінрідера (`BUTTON_NO_NAME`),
консольні помилки, запити з 4xx/5xx, крахи сторінок, `href="#"`.

```bash
npm run build:win && npm start &                # прод-збірка: dev на 183 маршрутах вмирає
node scripts/provision-org.mjs --name "Audit" --slug audit \
  --email audit@test --password '…' --language uk
AUDIT_EMAIL=audit@test AUDIT_PASSWORD='…' npm run audit:ui
```

Скільки сторінок відкриється — залежить від наповнення тестової організації:
реєстр фіч ховає пункти меню, порожній готель — цілі екрани (гола організація
дає ~16 сторінок, з увімкненими фічами й даними — кратно більше). Найкорисніший
прогін — по організації, наповненій як реальна, але без прод-даних.

**Тільки на одноразовій базі** — правило те саме, що в `smoke-writes.mjs`:
краулер у safe-режимі не тисне submit і деструктивні кнопки й відповідає
Cancel на кожен `confirm()`, але це все одно залогінений користувач, який
клікає все підряд. `DEAD_BUTTON` — робочий список для перегляду, не вирок:
кнопка, чия реакція повільніша за 800мс, може потрапити хибно. `HTTP_5XX` і
`PAGE_CRASH` — справжні завжди.

### Шар 4 — AI-рев'ю

По модулю за раз (`/code-review src/modules/finance`), не по всьому проєкту —
на великому обсязі рев'ю змиває деталі. Для безпеки — `/security-review`;
знахідки, що підтвердились, ідуть у SECURITY-FINDINGS.md за звичним правилом.

### Шар 5 — runtime

Єдиний шар, якого немає зовсім. Половина багів — це не логіка, а реальні
дані реальних клієнтів; їх ловить тільки збирач помилок на проді. Кандидати:
Sentry SaaS (швидко) або self-hosted GlitchTip поруч на VPS (дані не
покидають сервер — для гостьових PII це аргумент). Інтеграція: DSN в env,
`Sentry.captureException` у глобальний error handler і в `console.error`
гілки хендлерів. Це окрема задача; поки її немає, цей рядок — визнана діра.

### Прогін по специфікації (Spec Inventory)

Окремого SPEC.md **немає навмисно**: [PRODUCT.md](PRODUCT.md) вже написаний
за правилом «нічого з майбутнього часу» і є переліком тверджень про те, що
система вміє. Друга копія розійшлася б із першою (див. CLAUDE.md — чому один
файл, а не два). Прогін робиться так:

> Візьми docs/PRODUCT.md, розділ N. Для кожного твердження знайди код, який
> його реалізує, і клас: **реалізовано** (з file:line) / **частково** (що
> саме бракує) / **не знайдено**. Нічого не виправляй, тільки звітуй.

Розбіжність «написано є — у коді нема» — це або баг документа (виправити
PRODUCT.md у тому ж коміті), або знахідка сюди в розділ 2.

---

## 2. Знахідки 2026-08-24

### 2.1 Ціна: одна відповідь у `priceNights()`, і три обходи повз неї

Інваріант 16 (AGENTS.md) звів чотири цикли по днях в один резолвер
`modules/pricing/data/nightly-price.ts`. Але запити до цінових таблиць повз
модуль лишилися — і саме вони дають «на різних екранах різні числа». Тепер
їх тримає гейт `check-price-source.mjs --strict` (у `npm run check`): нові
обходи блокуються, а ці три — визнаний борг у його LEGACY-списку:

| Де | Що розходиться | Виправлення |
|---|---|---|
| `src/modules/channels/domain/booking-com/ari.ts:150` `buildARIFromPriceCalendar` | у Booking.com їдуть ціни **тільки** з `price_calendar`, зі своєю копією weekend-правила; матриця `price_occupancy` і LOS-знижки ігноруються. Готель з заповненою матрицею продає напряму за одними цінами, а через OTA — за іншими | будувати ARI від `priceNights()`/`cheapestByDay()`; рішення, як мапити ціни за кількістю гостей на rate plans каналу, ухвалюється до коду |
| `src/app/api/booking-sites/[id]/listings/route.ts:31` | «ціна від» в адмінці сайтів — `MIN(base_price)` по календарю; публічний місячний календар відповідає з матриці через `cheapestByDay()`. Два екрани показують різне «від» | спитати `cheapestByDay()` і тут |
| `src/modules/widget/api/widget-calendar-public.handlers.ts:227` | злиття «матриця + свій MIN() по календарю» з **третьою** копією weekend-правила (`getDay()` у локальній зоні — у `dayPrice()` це UTC) | навчити `cheapestByDay()` падати на день-календар і видалити локальний запит |

Той самий клас, але не про таблиці, тому гейтом не ловиться:

| Де | Що | Виправлення |
|---|---|---|
| `src/components/booking/BookingViewModal.tsx:489-500` | зміна дат бронювання рахує нову ціну лінійно: `total/oldNights × newNights` — без переквоти. Продовження на вікенд чи інший сезон отримує ціну, якої немає в жодному прайсі | на зміні дат питати `/api/pricing/quote` і показувати різницю; ручне редагування ціни лишається окремим полем |
| `src/components/booking/BookingViewModal.tsx:1346-1352` | туристичний збір у вкладці показаний як `дор. × ночі × 25 CZK` — **захардкоджені** і ставка, і валюта. `BookingForm` поруч бере ставку з налаштувань. Німецький готель бачить чужу ставку в чужій валюті | брати ставку/валюту звідти ж, звідки їх бере `recalcCityTax` у BookingForm |

Контекст, щоб не шукати заново: операторська форма (`BookingForm`) квотує
через `/api/pricing/quote` лише коли поле ціни порожнє — введена руками ціна
свідомо перемагає (це фіча, не баг). Віджет (`widget-reserve`) дисциплінований:
квота через `priceNights()`, відмова 409 на непокриті ночі, `occupancyPriced`
чесно вимикає доплату за гостя.

### 2.2 Мертвий UI

| Де | Що |
|---|---|
| `src/modules/widget/ui/BookingIframePage.tsx:1093` | кнопка «застосувати промокод» — `onClick={() => { }}`. Гість вводить код, тисне, нічого не відбувається — у грошовому шляху віджета |

Це єдиний **статично** видимий випадок; решту шукає шар 3 (краулер) у
живому застосунку.

### 2.3 Мертвий код (knip, після чесного конфігу)

20 файлів, які ніхто не імпортує; найпомітніші:

- `sites/[siteId]/_components/OfferWorkflowTab.tsx`, `SiteGiftCardsTab.tsx` —
  вкладки, які існують, але не підключені до сторінки сайту;
- `components/ai/ChatWidget.tsx`, `components/mobile/MobileCardList.tsx`;
- `modules/channels/domain/vrbo-parser.ts`;
- `modules/finance/data/cashback-calculator.ts`, `pdf-loader.ts`,
  `performance-score.ts`; `modules/guests/data/chat.repo.ts`;
- `modules/widget/ui/BookingWidgetEmbed.tsx`; `core/security/secrets.ts`;
- `modules/*/events/published.ts` — **9 з 10 модулів**: шина подій оголошена,
  але публікує лише bookings. Або шину використовують, або це передчасна
  абстракція, яку чесніше вирізати за процедурою AGENTS.md §2.

Залежності, які ніхто не імпортує: `date-fns`, `imapflow`, `mailparser`,
`xml2js` (+ їхні `@types`, `@types/bcryptjs`, `@types/pdf-parse`), dev:
`esbuild`. `imapflow`/`mailparser` — слід вирізаного імпорту пошти
(finance-wrapper); перед видаленням глянути `archive/finance-wrapper`.

Повний список: `npm run audit:dead-code`. Видалення мертвого модуля — за
процедурою AGENTS.md §2 (тег + гілка archive/).

### 2.4 Цикли залежностей (madge, 15)

14 із 15 — пари `*Tab.tsx ↔ *Modal.tsx` у `finance/settings/_components`
(вкладка імпортує модалку, модалка — типи з вкладки). Ламаються винесенням
спільних типів у файл поруч. П'ятнадцятий — `guests/domain/ai/ocr-document.ts
↔ mrz-parser.ts`. Не блокує збірку, але це готові кандидати на «чому цей
компонент не оновлюється».

### 2.5 Перевірити наступним (підозри, не вироки)

- `src/app/api/booking-sites/**` — сесія перевіряється в тілі хендлера, без
  guard-а і без контексту орендаря на з'єднанні; `booking_sites` читається за
  голим `id`. На Postgres це або «тихо порожньо», або міжтенантне читання —
  саме клас із SECURITY-FINDINGS §12. Прогнати `audit-routes.mjs` і
  `check-isolation.mjs` по цих маршрутах.
- `ari.ts:207-213` — обхід днів через `new Date`/`toISOString` у локальній
  зоні сервера; на машині не в UTC зсуне зайнятість на добу.

---

## 3. Порядок тижня

1. **Сьогодні**: розібрати 2.1 — вирішити, що робити з ARI-пушем (це
   найдорожча розбіжність: її бачить гість з Booking.com).
2. **Сьогодні**: полагодити кнопку промокоду (2.2) або прибрати її з екрана.
3. **Завтра**: краулер по свіжій організації (шар 3), розібрати список.
4. **Цього тижня**: спробувати прогін по специфікації на одному розділі
   PRODUCT.md (§ бронювання) — це і перевірка коду, і перевірка документа.
5. **Далі**: шар 5 (Sentry/GlitchTip) — окремою задачею з деплоєм.
