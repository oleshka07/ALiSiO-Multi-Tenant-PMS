# Журнал рішень

**Навіщо цей файл.** 31.08.2026 сесія запитала власника, нарощувати
`price_calendar` чи перейменовувати його в `ari_grid`. Це рішення на той
момент було ухвалене **тричі**: сесія сама його рекомендувала, власник
сказав «нарощувати», дослідницька сесія підтвердила з обґрунтуванням.
Питання повернулося вчетверте.

Причина не в конкретній сесії. **Рішення ухвалювались у чаті й не
потрапляли туди, куди агент дивиться перед роботою.** Чат закінчується,
контекст обнуляється, питання відроджується. Власник відповідає на нього
знову — і платить за це часом і роздратуванням.

Тому: одне рішення — один рядок. З датою, автором і місцем, де записане
обґрунтування. Рішення, якого тут немає, вважається неухваленим; рішення,
яке тут є, **не перепитується**.

---

## Як цим користуватись

**Перед тим як спитати власника** — інваріант 23 в AGENTS.md — питання
класифікується:

| Тип | Приклад | Що робити |
|---|---|---|
| **Уже вирішено** | «нарощувати чи перейменовувати» | подивитись у цю таблицю |
| **Факт** — відповідь є в коді або в дослідженнях | «чи входить збір у `total_price`» | знайти: `git show origin/main:…`, `docs/research/`, `docs/vendor/channex/` |
| **Гроші, ринок, смак** | «скільки коштує тариф», «чи віддаємо сайт безкоштовно» | **спитати власника** |

Питати можна лише про третє. Для перших двох у репозиторії є 71 ендпоінт
конкурента зі схемами, 63 екрани з полями, 111 сторінок документації
Channex і сам код на `origin/main`.

**Записувати рішення сюди — частина того ж коміту**, що й код, який його
втілює. Так само, як документацію (AGENTS.md §2).

**Скасувати рішення можна**, і це нормально: рядок не видаляється, у нього
дописується «скасовано ДД.ММ.РРРР, бо…». Історія рішень цінніша за
охайність таблиці.

---

## Продукт і обсяг

| № | Рішення | Ким, коли | Обґрунтування |
|---|---|---|---|
| П1 | Не переписувати систему; перебудувати ядро цін та інвентарю | обидві сесії, 08.2026 | [research/REBUILD-PLAN.md](research/REBUILD-PLAN.md) |
| П2 | Channex одразу, за власним портом (`ChannelManagerPort`), не прямі інтеграції з OTA | власник, 29.08.2026 | [CHANNEX-INTEGRATION.md](CHANNEX-INTEGRATION.md) §3.2 |
| П3 | Канали в порядку: Booking → Airbnb → VRBO | власник, 29.08.2026 | там само §1 |
| П4 | `booking_engine` ON за замовчуванням, `site_builder` OFF (конструктор — платний) | власник, 31.08.2026 | міграція 0049, [research/BOOKING-ENGINE-FREE-TIER.md](research/BOOKING-ENGINE-FREE-TIER.md) |
| П5 | Ключа `site_builder` в реєстрі поки НЕМАЄ: конструктора в коді не існує, прапорець стеріг би порожнє | кодова сесія, 31.08.2026 | коміт `219547d`, `src/core/features.ts` |
| П6 | `events` (зали й заходи) OFF за замовчуванням — цільовий сегмент 1–15 номерів | власник, 31.08.2026 | міграція 0049 |
| П7 | Бронь створюється ДО оплати і без неї; оплата змінює стан броні, а не створює її | власник, 31.08.2026 | [research/BOOKING-ENGINE-FREE-TIER.md](research/BOOKING-ENGINE-FREE-TIER.md) §4 |
| П8 | Продукт для Європи **І** України одночасно; специфічне для юрисдикції — окремим модулем | власник, 31.08.2026 | AGENTS.md інваріант 22, [ARCHITECTURE.md](ARCHITECTURE.md) §6.6 |
| П9 | **Непризначені броні мусять бути видимі в планері.** OTA-бронь лягає на тип номера без кімнати; якщо її нікуди не показати, вимога «бронь без номера» технічно виконана, а готель броні не бачить. Окрема смуга планера — частина кроку CP3, не «потім» | рецензія, 31.08.2026 | зразок: окрема смуга в Hoteliera (`harvest/`, не в git); [PLAN-CORE-REBUILD.md](PLAN-CORE-REBUILD.md) §7.2 |
| П10 | **Область обʼєкта — одна на оболонку, в адресі й у куці; екран не тримає власного `propertyId`.** Значення: `?property=<id\|all>` в адресі вкладки (головне — дві вкладки на два готелі не бʼються), кука `property_scope` памʼятає останній вибір для нової вкладки, інакше «Усі обʼєкти» — ніколи не перший. Екрани налаштувань вимагають рівно один обʼєкт (`PropertyRequired`), списки приймають «Усі». `/app/p/<id>/…` не заводиться: `?property=` не чіпає 77 маршрутів і жодного `href`. Списки за областю поки не звужуються — свідомо | власник, 03.09.2026 | [PROPERTY-SCOPE-PLAN.md](PROPERTY-SCOPE-PLAN.md), NEXT-TASKS.md Задача 2, гейт `check-property-scope.mjs` |
| П11 | **Мова інтерфейсу: патерни Channex, не їхні пікселі; календар — за Hoteliera, бо в Channex його немає; три покращення проти них — історія різницею, видно «хто», порожні стани з дією.** Один набір токенів (`globals.css` `:root`) і чотири стани екрана (`components/ui/State.tsx`); екран не вигадує власних кольорів і власних порожніх станів. Роботи йдуть блоками за BUILD-PLAN; зупинка лише на ЧЕКПОІНТ | власник, 03.09.2026 | [BUILD-PLAN.md](BUILD-PLAN.md) «Мова інтерфейсу», [UI-LANGUAGE.md](UI-LANGUAGE.md) |

## Ціни та інвентар

| № | Рішення | Ким, коли | Обґрунтування |
|---|---|---|---|
| Ц1 | Три осі щоденної ціни: тип номера + тариф + доба. Заселеність — не четверта вісь, вона зводиться в `priceNights()` | власник, 30.08.2026 | [CHANNEX-INTEGRATION.md](CHANNEX-INTEGRATION.md) §10.11 |
| Ц2 | Доплата за додаткового гостя **спільна для тарифів** одного типу номера. Свідоме спрощення з ознакою перегляду: готель, у якого доплата різна в гнучкому й безповоротному тарифі | власник, 30.08.2026 | там само; тримає `nightly-price.check.ts` |
| Ц3 | **`price_calendar` нарощуємо, не перейменовуємо в `ari_grid`.** Перейменування — міграція живих цін, правка гейтів і `LEGACY`-списків, і жодної нової можливості. Ім'я стане неточним — це косметика, яку дешевше закрити один раз, коли вміст усталиться | власник, 31.08.2026 (**ухвалено тричі**) | [PLAN-CORE-REBUILD.md](PLAN-CORE-REBUILD.md) §5, [research/NORTHSTAR.md](research/NORTHSTAR.md) §3 |
| Ц4 | `extra_occupancy` створюється **без** `rate_plan_id` — наслідок Ц2 | власник, 31.08.2026 | [PLAN-CORE-REBUILD.md](PLAN-CORE-REBUILD.md) §4 |
| Ц5 | Ніч, яку не покриває жодне джерело, **не продається** (`quote.missing`), а не оцінюється в нуль | інваріант 17 | AGENTS.md §3 |

## Податки і збори

| № | Рішення | Ким, коли | Обґрунтування |
|---|---|---|---|
| Т1 | `fees_taxes` і `fin_tax_rates` **не зводяться в одну таблицю**: нарахування ≠ ставка. Перше — скільки додати до ціни, друге — яка ставка ПДВ на дату послуги | кодова сесія, 31.08.2026 | [PLAN-CORE-REBUILD.md](PLAN-CORE-REBUILD.md) Фаза 1 крок 1 |
| Т2 | Нової таблиці `taxes` **не буде**: `fees_taxes` нарощується на місці. Той самий вибір, що Ц3 | кодова сесія, 31.08.2026 | там само |
| Т3 | Класифікатори збору нейтральні до юрисдикції: `applies_to` (`all`/`adults`), `collected_for` (`property`/`authority`). **Не** `tax_type = 'tourist_tax'` — це закодувало б одну країну в спільній колонці | кодова сесія, 31.08.2026 | міграція 0050, AGENTS.md інваріант 22 |
| Т4 | **`total_price` = те, що гість платить через цей канал.** `city_tax_included = 1` → збір усередині суми; `0` → сума без нього, збір поруч. Чи входить — властивість КАНАЛУ (`booking_sources.city_tax_included_default`), не продукту | власник + код, 31.08.2026 | коментар біля колонок у `src/lib/db.ts`, [PLAN-CORE-REBUILD.md](PLAN-CORE-REBUILD.md) §7.1 |
| Т5 | На документі збір **завжди** окремий рядок без ПДВ, незалежно від Т4. Керує `fin_folio_items.kind='city_tax'` + `vat_rate = 0`, а не прапорець броні | кодова сесія, 31.08.2026 | [ARCHITECTURE.md](ARCHITECTURE.md) §6.1, `stay-charges.city-tax.check.ts` |
| Т6 | Двох турзборів не буває: або колонка обʼєкта, або рядок `fees_taxes` з `authority`. Обидва разом відхиляються — і в файлі готелю, і на екрані | кодова сесія, 31.08.2026 | `apply-hotel.mjs`, `POST /api/fees` (409) |
| Т7 | Віку в моделі звільнення НЕМАЄ: квота знає лише `adults`/`children`. Персональне звільнення живе на реєстрації (`reservation_guests.fee_exempt`) | кодова сесія, 31.08.2026 | шапка `pricing/domain/fees.ts` |
| Т8 | `properties.city_tax_per_night` **поки лишається** другою (простою) машинерією. Прибрати = переписати чеський за змістом реєстр гостей; іде разом із модулем юрисдикції | кодова сесія, 31.08.2026 | [ARCHITECTURE.md](ARCHITECTURE.md) §8, [PLAN-CORE-REBUILD.md](PLAN-CORE-REBUILD.md) §7.1 |
| Т9 | **Контракт наперед: коли зʼявиться ПРРО, рядком 11 фіскального чека керує `fin_folio_items.kind = 'city_tax'`, поза базою ПДВ.** Сьогодні ПРРО не існує — єдиний фіскальний модуль це німецький TSE (`invoicing/domain/fiscal/fiscal-device.ts`, `fiscal_de` OFF). Тобто `kind` доходить до фоліо й інвойса, але фіскального чека для України немає, а це РІЗНІ документи з різними правилами | рецензія + код, 31.08.2026 | перевірено грепом: жодного Checkbox, ПРРО чи ключа `fiscal_ua` в коді немає |

## Процес та інструменти

| № | Рішення | Ким, коли | Обґрунтування |
|---|---|---|---|
| Р1 | Канонічне дерево — `main`. Схема читається `git show origin/main:`, ніколи з робочої копії. Перед висновком з історії — `git rev-parse --is-shallow-repository` | інваріант 21, 31.08.2026 | AGENTS.md §3; коштувало двох хибних звітів за один день |
| Р2 | Збірка образу — у GitHub Actions, сервер лише тягне `APP_IMAGE`. Компіляція на сервері заборонена | власник, 26.08.2026 | [DEPLOY.md](DEPLOY.md); інцидент: диск 100 %, PANIC у Postgres, 502 у чужих проєктів |
| Р3 | Оператору не диктуються разові команди на сервер. Немає скрипта — спершу створюється скрипт | власник | AGENTS.md §5 |
| Р4 | Документація Channex завантажена в репозиторій як фікстура (111 сторінок + Postman), а не читається з мережі щоразу | кодова сесія, 30.08.2026 | `docs/vendor/channex/` |
| Р5 | Ліміт ARI Channex — **не** «20/хв на акаунт». Виміряно на живому API: 10 запитів цін і 10 наявності на хвилину **на обʼєкт**, два різні відра | вимірювання, 30.08.2026 | `docs/vendor/channex/INVENTORY.md` |
| Р6 | Цей журнал. Перед питанням до власника — перевірити його, код і дослідження | власник, 31.08.2026 | AGENTS.md інваріант 23 |
| Р7 | **Суттєва зміна починається з ЧЕРВОНОГО гейта**: спершу перевірка, потім побачити її червоною, і лише тоді зміна. Перевірка, яка ніколи не була червоною, доводить лише те, що вона запускається | власник, 31.08.2026 | AGENTS.md інваріант 24; за одну сесію крок «побачити червоною» знайшов діру у власному гейті, беззмістовне твердження в check-isolation і втрату індексів при перебудові |
| Р8 | **`channels` — платний модуль, дефолт OFF.** Прямі броні даємо (`booking_engine` ON: вони не коштують нам нічого і виграють клієнта); за доступ до ЧУЖОГО трафіку беремо. Ключ зʼявився РАЗОМ зі своєю вартою, а не наперед: `cron/channels-pull` не опитує стрічку готелю без нього, і екран облікових даних без нього не показує поля менеджера каналів | власник (записано в коментарі до `booking_engine`), 31.08.2026 | `src/core/features.ts`; прапорець без варти — перемикач-обманка, через який зʼявився `payments.check.ts` |
| Р9 | **Ключ менеджера каналів зберігається під нейтральним іменем `channel_manager`, не під іменем вендора.** Який саме менеджер обслуговує готель, каже `cm_connections.provider` — рядок у базі, а не назва поля в ядрі. Один готель — один менеджер каналів: двоє одночасно розсилали б наявність один поверх одного | інваріант И1, 31.08.2026 | `src/core/integration-credentials.ts`; гейт `check-vendor-isolation` |
| Р10 | **Шов композиції — один файл, і гейт знає про нього вужчим правилом, а не винятком.** Рівно один раз ім'я модуля вендора мусить бути написане, інакше жоден маршрут до адаптера не дістанеться. У `modules/channels/providers.ts` ім'я вендора дозволене ЛИШЕ в рядку `import`; будь-яке інше входження валить гейт так само, як усюди. Список винятків `check-vendor-isolation` лишається порожнім | кодова сесія, 31.08.2026 | перевірено зламом: чуже поле у шві — гейт червоний |
| Р11 | **На проді немає жодного живого готелю — усі дані там тестові (станом на 01.09.2026).** Це не дрібниця, а множник ціни: рішення, які після першого клієнта коштують міграції живих грошей (інваріант 18 забороняє переписувати нараховане), зараз коштують рівно стільки, скільки коштує написати код. Саме тому Ц12 робиться зараз, а не «колись»: перетлумачення `price_occupancy.persons` з «усіх гостей» на «дорослих» сьогодні не чіпає жодної живої броні. **Рядок має термін придатності:** щойно зʼявиться перший клієнт, він скасовується, і кожне таке рішення знову дорожчає | власник, 01.09.2026 | сказано в рецензії; підтвердження числом — `deploy/measure-price-modifier.sh prod` |
| Р12 | **Число з проду виміряне 01.09.2026: `site_rate_plans` ПОРОЖНЯ** (`rows_total = 0`). Перенесення `pricing_modifier_percent` на точку збуту за Ц7 — чистий рух схеми, нічиїх грошей не торкається; робиться одним комітом із батчером фази 4. Передумова знята. Вимір зроблено роллю-власником під `row_security = off`, і тепер скрипт це ДРУКУЄ і відмовляє, якщо роль не обходить RLS: нуль, отриманий роллю під політикою, читається так само, як чесний нуль, і це найгірший можливий неправдивий результат | оператор + `deploy/measure-price-modifier.sh prod`, 01.09.2026 | вивід у передачі §6.1 |
| Ц6 | **Мапінг «наша пара тип × тариф = один тариф каналу» вмонтував спрощення §10.11 у КАНАЛ, не лише в розрахунок ціни.** Це не нове рішення й не привід міняти старе — це запис про те, що ознака перегляду §10.11 («готель, якому треба інакше, вимагатиме четвертої осі; це міграція, а не переписування») тепер неточна: четверта вісь означатиме ще й перебудову мапінгу в менеджері каналів по всіх готелях, бо `cm_mappings.local_id` тримає наші id тарифів. Перечитати ознаку ДО фази 4, поки її не написано | рецензія, 01.09.2026 | `property-rate-plans.ts`; §10.11 «свідоме обмеження» |
| Ц7 | **ЦІНА ОДНА: база з `price_calendar × rate_plans`, названа `priceNights()`.** Кожна точка збуту (сайт, зʼєднання з каналом) має власний `pricing_modifier_percent`, який ЗСУВАЄ базу і нічого не називає. Модифікатор однієї точки збуту ніколи не потрапляє в іншу. Структурні відмінності (харчування, умови скасування, EB/Last Minute) — окремий тариф, за потреби похідний від батьківського, як у Hoteliera і Channex. `fixed_price` у сайтовому дереві видалено разом із мертвою гілкою. Ефективна ціна по кожній точці збуту має бути видима в UI. **Варіант В із трьох:** А (модифікатор лише на сайті) лишав асиметрію, яка й породила розвилку; Б (власна ціна на каналі) вимагав четвертої осі, винятку в інваріанті 16 і перебудови мапінгів (Ц6) | власник, 01.09.2026, [research/PRICE-FORK-DECISION.md](research/PRICE-FORK-DECISION.md) ред. 2 | Hoteliera перевірена по знятих екранах: одна база + стос правил (`price_without_rules`/`price_with_rules`), тариф походить від тарифу, серед осей правил КАНАЛУ немає, а різниця по точці збуту — видимість (`Hide on IBE Website`). Форма збігається з `derived_option` Channex, тож зсув можна рахувати в нас, а колись делегувати їм, не міняючи модель |  **ЗРОБЛЕНО 01.09.2026 — половина КАНАЛУ.** `cm_connections.pricing_modifier_percent` (0058), знаковим числом замість пари «відсоток + напрямок»: два поля можуть суперечити одне одному. Батчер зсуває базу через `percentOf(x, pct, 0)`. Асиметрії, яка породила розвилку, більше немає: зсувати вміють обидві точки збуту. **Половина САЙТУ не нормалізована**: `site_rate_plans.pricing_modifier_percent` має трьох живих читачів у ціновому шляху віджета, тож її переїзд на `booking_sites` міняє те, що бачить гість на прямому сайті — окрема робота, а не хвіст цієї. Твердження стоять на НЕНУЛЬОВОМУ модифікаторі навмисно: при нулі застосований зсув і забутий зсув дають однакове число, і гейт був би зеленим в обох світах. **Відоме розходження ФОРМИ (записано 01.09 за рецензією №4):** сайтова половина тримає пару `pricing_modifier_percent` + `pricing_modifier_type` (відсоток і слово напрямку, дефолт `less`), канальна — одне знакове число. Це одна модель у двох записах, не дві моделі; намір — звести до знакового числа тоді, коли сайтова половина переїде на `booking_sites`. Доти обидва поля читають різні читачі, і жодне не потрапляє в чуже дерево (тримає `ari-batch.check.ts`, твердження про модифікатор сайту). Зводити «заразом» не можна: пару читають три місця цінового шляху віджета, і зміна форми там міняє те, що бачить гість на прямому сайті.
| Ц8 | **«Канал» у Ц7 сьогодні означає ЗʼЄДНАННЯ з менеджером каналів, а не окремий OTA.** `cm_channels` не існує (план §4.3 відклав її свідомо), тож за одним `cm_connections` сидять усі OTA разом і модифікатор у них спільний. Різниця МІЖ OTA робиться не другим модифікатором, а окремим тарифом, видимим лише в потрібному OTA. **Але цю видимість закриває НЕ наш код.** `cm_mappings` каже лише «наша пара тип × тариф = ось цей тариф Channex»; який OTA той тариф побачить, вирішують `rate_plans` у `POST /api/v1/channels` — мапінг-айтеми ВСЕРЕДИНІ Channex, яких ми не викликаємо (§4.3 віддав екран каналів в iFrame). Тобто **сьогодні Ц8 закривається інструкцією готельєру: «зайдіть у Channex і не мапте цей тариф на Booking»** — і поки Channel API не викликається з нашого боку, ніякий гейт цього не втримає. Коли зʼявиться рівень каналу, він стане ще однією точкою збуту тієї самої форми; модель від цього не міняється | вимірювання + рецензія, 01.09.2026 | `channel-api.md`: «For each Channex rate plan **to be sold on the channel**, build a mapping item… Send … the mapping items as `rate_plans`». Схема: таблиці рівня каналу немає |
| Ц9 | **`rate_plans.fixed_price` видалено, і колонки з власною ціною тарифу більше не буде.** Наслідок Ц7, а не прибирання сміття: колонка, у якій тариф МОЖЕ написати власне число, — заряджена рушниця, і наступний читач зʼявився б без жодної помилки. За всю історію `src/` вона не мала ані читача, ані писача (`git log -S`), а три гілки у віджеті питали її в обʼєкта із `site_rate_plans`, де такої колонки немає взагалі. Postgres чистить міграція 0055 і **відмовляє**, якщо в колонці є хоч одне непорожнє значення: «писача не було» — твердження про код, не про базу (інваріант 13). Тримає `check-price-source.mjs`: голий `fixed_price` — відмова, у лапках (значення `discount_type` у знижках) — дозволено | кодова сесія, 01.09.2026 | `audit-dead-data.mjs` цього не бачить за означенням — слово живе в коді як значення знижки, тож колонка виглядає використаною |
| Ц10 | **Дзеркало мапінгу ключується ПАРОЮ «тип номера × тариф», а не тарифом** (`cm_mappings.unit_type_id`, міграція 0056). Це не нове рішення, а Ц6, доведене до схеми: у них тариф належить типу номера (`room_type_id` обовʼязковий), тож наш тариф із цінами на трьох типах — це три їхні тарифи. Старий ключ мовчки лишав би в дзеркалі один із трьох. Знайшлося не читанням: контракт `catalog.check.ts` було написано ДО коду, і саме він показав, що схема цього не вміє | кодова сесія, 01.09.2026 | `mappings.repo.check.ts` — перевірка була червоною до 0056 **Проведено крізь смугу цін 01.09:** `RateChange` несе `unitTypeId`, ціна питається парою (`pricesAt(unitTypeId, ratePlanId, date)`), тіло адресує тариф за парою (`PairMap` в `ari-payload.ts`), координата ціни без типу в чергу не лягає (`enqueueChange` відмовляє). Знайдено ЧИТАННЯМ перед першим живим прогоном батчера: до того `RateChange` ніс сам `ratePlanId`, тобто один наш тариф на чотирьох типах не мав чим ні цінуватись, ні адресуватись, і жоден із шістнадцяти тверджень домену цього не бачив — усі стояли на одному типі. Три гейти були червоними до зміни. |
| Ц11 | **Каталог створює лише те, чого немає, і створює тариф закритим — саме `stop_sell: true`, а не `rate: 0`.** Різниця не термінологічна: нуль — це не «закрито», це «безкоштовно». У наших опціях ключа `rate` немає взагалі, і гейт стверджує обидві половини окремо. **Переміряно на живому staging 01.09.2026 — рішення підтверджене, обґрунтування виправлене.** Підтверджено: `stop_sell: true` скаляром API приймає і розгортає в `[true×7]` (по дню тижня), і закриття стоїть на КОЖНІЙ даті календаря, не лише в шапці тарифу. Виправлено: «ключа `rate` немає, тож календар не засівається» — неправда. Тариф, створений БЕЗ `rate` взагалі, лягає в календар як `rate: "0.00"` на кожну дату до горизонту, на всіх опціях. Нуль там уже є, і не продається ніч ВИКЛЮЧНО через `stop_sell`. Гейт на відсутність `rate` лишається — але стереже він не «незасіяний календар», а «не публікуємо числа, якого ніхто не називав». Виправлено і друге: «канал продасть ніч за нуль» контрольованим дослідом НЕ відтворюється — нульову дату Channex закриває сам, навіть у тарифа без нашого прапорця. Наш `stop_sell` — друга варта, і тримати її треба саме тому, що перша чужа, недокументована (`unavailable_reasons` порожній) і може змінитися мовчки. Відкриває тариф фаза 4 — і мусить зняти `stop_sell` ЯВНО: ціна його не знімає (И14). Зміна назв і полів наявного каталогу — окрема робота з окремим рішенням | кодова сесія, 01.09.2026 | `catalog-target.check.ts`; інваріант 17 і И2 |
| Ц12 | **Вісь заселеності розділяється: `price_occupancy.persons` → ДОРОСЛІ, дитина отримує власну ціну надбавкою.** Не тому, що так у Channex, а тому, що так уже влаштована решта схеми: `unit_types` має `max_adults`/`max_children`/`max_occupancy`, `reservations` — `adults`/`children`/`infants`. Розділ є всюди, сплющила його ЛИШЕ цінова вісь. Форма — з Hoteliera, екран «Extra occupancy»: групи «тариф × типи номерів», два роди гостя (Adult / Child з віковою вилкою), колонка на план харчування, у клітинці окремо LODGING і MEAL (% або сума) і прапорець додаткового ліжка. На Channex лягає без залишку: дорослі → опції заселеності, діти → `children_fee`/`infant_fee`, харчування → `meal_type`. **Мінімальна версія дозволена:** одна надбавка за дитину на тариф, без вікових вилок і без розділення ліжко/їжа — вилки й харчування додаються потім ПОЛЯМИ, саме тому що вісь уже буде правильна. **ЗРОБЛЕНО 01.09.2026 у мінімальній версії:** `rate_plans.child_extra_gross` (0057), вісь домену перейменована `persons` → `adults` плюс `children`/`childExtraGross`, чотири цінові викликачі переведені (`quote.repo`, `widget-reserve`, `widget-availability`, `widget-config-public`). `day-sheets.repo` у переліку НЕ був ціновим — там `persons` це лічильник голів для аркуша дня, і сума дорослих із дітьми там правильна. **Рецензія власника того ж дня знайшла три недоробки, усі виправлені:** (1) твердження про надбавку стояло на ОДНІЙ ночі, де «20 за ніч» і «20 за перебування» дають те саме число — переписане на три ночі (417 проти 377) і доведене навмисним зламом; (2) підпис осі в інтерфейсі лишався «Гостей», тобто готельєр вписав би сімейну ціну в стовпчик дорослих — екран цінової матриці перепідписано, і заразом виявилось, що його «приміряти котирування» я зламав, лишивши старий параметр `persons`; (3) `max_children` після розділу осі не стеріг НІХТО — раніше це ловилось випадково, бо `persons` упирався в `max_occupancy`; тепер `priceNights()` відмовляє з окремим прапорцем `overCapacity`. Дорого стане після першого клієнта: інваріант 18 забороняє переписувати вже нараховане | власник, 01.09.2026 | `harvest/org-settings-extra-occupancy/text.txt` (диск власника, не в git — форму переказано зі слів рецензії); `rate-plans-collection.md:668` |
| Ц13 | **Координата черги лишається ПОДАТОВОЮ; діапазон — безпечне розширення, і вводиться він на боці ЗАПИСУ, а не читання.** Питання постало так: видалення тарифу це N координат чи одна з діапазоном? Відповідь із двох частин. (а) Пʼять випадків «джерела вже немає» — це РОЗВʼЯЗАННЯ координати, а не її форма: функція питає «яке значення в цієї координати зараз», і їй байдуже, прийшла дата поодинці чи з діапазону. Тобто вузька форма їх не сковує, і писати їх можна було не чекаючи. (б) Діапазон при цьому безпечний саме тому, що черга тримає КООРДИНАТУ, а не значення (0054): два рядки, що перекриваються, розвʼязуються з одного джерела в один момент, тож дають однакове число — надлишкове перевідправлення, ніколи не розходження. Це знімає єдине заперечення проти діапазонів. **Тому:** одне зняття мапінгу має класти ОДИН рядок із діапазоном, а не тисячу подобових; форма додається тоді, коли зʼявиться запис-на-видалення, і не раніше — сьогодні її нікому заповнювати | кодова сесія, 01.09.2026 | `domain/ari-batch.ts`; шапка 0054 |
| Ц14 | **Межа спроб координати черги — 10; далі стан «потребує уваги», повернення лише рукою оператора.** Без межі гучна відмова через тиждень така ж тиха, як мовчання (рецензія №4). Десять — бо ліміт вендора 10/хв на обʼєкт і пауза після помилки хвилина: кілька проходів поспіль чесно впираються в `429` на тому самому рядку, і три спроби оголошували б «увагу» там, де був жвавий вечір; сто — це тиждень щохвилинних падінь, тобто та сама тиша. Число одне на домен і чергу (`DEFAULT_MAX_ATTEMPTS`), шов передає його обом. Застряглі читає `stuckChanges()`, повертає `retryStuck()` з обнуленням лічильника; нова зміна тієї самої координати рядка не додає і лічильника не скидає — координата вже названа, а після повернення поїде її ПОТОЧНЕ значення. Знята координата (минула дата) виходить із черги тим самим `sent_at`, за яким черга рахує, з `last_error = 'retired: …'`: окремої колонки немає навмисно, стан рідкісний і читає його людина; доказ «ми це слали» — `sent_at` БЕЗ такого префікса | кодова сесія за рецензією власника, 01.09.2026 | `domain/ari-batch.ts`, `data/outbox.repo.ts`, `api/ari.handlers.ts` (двері `stuckChannelChanges`/`retryStuckChannelChanges`); обидва гейти були червоними до зміни |
| Ц15 | **Координата черги може бути ДІАПАЗОНОМ ночей — `cm_outbox.stay_date_to`, включно, NULL = одна ніч (0059).** Форма з Ц13, яка чекала на писача: запис матриці без дат це кожна майбутня ніч, подобово — тисячі рядків на зʼєднання за один клік і секунди в транзакції ціни, діапазоном — один. Кінець входить у ключ злиття (той самий діапазон двічі — один рядок; перекриття безпечне, Ц13). Писач обрізає горизонтом `OUTBOX_HORIZON_DAYS = 500` (стільки бере повний синк), батчер — сьогоднішнім днем і розкладає по ночах сам; рядок, частина якого не поїхала, повертається ЦІЛИМ, а не позначається відправленим за першою пачкою. Доведено на дроті: 5 рядків-діапазонів → 2 виклики → 21 клітинка | кодова сесія, 01.09.2026 | `outbox.repo.check.ts`, `ari-batch.check.ts` сцена 12, `channex-ari-live.mjs --confirm` |
| Ц16 | **Писачі кажуть каналам через двері `@channels/outbox` у ТІЙ САМІЙ транзакції; координати отримують лише змаплені типи й пари; усі зʼєднання обʼєкта, вимкнені теж; новий мапінг кладе першу відправку сам.** Чотири рішення в одному, бо вони тримають одне одного: (а) двері беруть ручку `t` писача — черга, що поповнюється окремим кроком, розходиться зі станом при першому ж падінні, а зовнішній `getSql()` усередині `sql.tx` на Postgres це інше зʼєднання (інваріант 11; доведено зламом на стенді — червоне на Postgres, зелене на SQLite); (б) незмаплений тип координати не отримує — нема кому адресувати, і через десять проходів він став би «потребує уваги» ні за що; (в) вимкнене зʼєднання чергу отримує — після вмикання канал має дістати ПОТОЧНИЙ стан, а не старі ціни до чийогось повного синку; (г) `tableMirror().put` кладе діапазон до горизонту в мить появи пари — тариф на тому боці створено закритим, і без цієї координати він лишився б закритим назавжди без жодної помилки (та сама тиша, що И14, поверхом вище). Тримає `check-outbox-writers.mjs`: писач таблиць наявності або ціни без дверей — червона збірка | кодова сесія, 01.09.2026 | `data/outbox-notes.ts`, `api/outbox.check.ts`, `catalog-sync.check.ts`; 21 писач у 12 файлах |
| Ц17 | **Крон розсилки — щохвилини; бюджетом обʼєкта володіє обʼєкт; транспортна невдача не рахує спроби; застрягле крон доповідає, але не червоніє.** Чотири речі одного проходу. (а) `sharedChannexLimiter()` — один на процес, ключ зʼєднання (И10): ручний «повернути в чергу» і черговий прохід крона в одну хвилину ділять один бюджет, одинадцятий виклик відмовляється сам; межа — один процес, два контейнери одного середовища ділили б наосліп, тоді стан переїде в базу. (б) 429, 5xx, власна пауза, мережа — причина ПРОХОДУ: рядок повертається без спроби, інакше крон раз на хвилину зʼїв би десять спроб за десять хвилин простою вендора і поставив би всю чергу в «потребує уваги» — той самий шум, від якого межа Ц14 мала рятувати; претензія вендора до ЗНАЧЕННЯ (422, warnings, незмаплене) — спроба рядка. (в) `needsAttention` у доповіді крона поіменно, але `success` від нього не залежить: червоний крон щохвилини на тиждень усі вчаться ігнорувати, і разом із ним справжній провал (ключ, провайдер, падіння); застрягле — справа екрана «Канал-менеджер». (г) Щохвилини, бо вендор просить пачки по 30–60 с і паузу після помилки хвилину — крон раз на хвилину і є та пауза | кодова сесія за рецензією власника, 01.09.2026 | `publish-all.check.ts`, `ari-adapter.check.ts` сцена 6, `ari-batch.check.ts` сцена 13; усі були червоними |
| Ц18 | **Ключ менеджера каналів — у кожного готелю свій, з облікових даних організації (Р9); майстер веде до поля «ключ» і не знає, звідки він.** Радіус вибуху — один готель; відкликання ключа і рахунок від вендора — справа готельєра; один спільний акаунт означав би, що помилка мапінгу відправить ціни готелю А на обʼєкт готелю Б, і жоден RLS цього не спіймає — на тому боці RLS немає (інваріант 25 на проді). Тертя онбордингу відкладається без витрат: «готель приніс свій» і «ми завели акаунт і вписали за нього» — один шлях коду, рішення про перепродаж — комерційне і ухвалюється пізніше. **Ключ перевіряється одним GET одразу після вставки**: помилковий падає в полі вводу, а не тихо через добу на першому проході крона; простій вендора — не «ключ неправильний» (503, не 422). **Майстер відновлюваний**: стан не зберігається, а виводиться з записаного (ключ → зʼєднання → обʼєкт на тому боці → увімкнення); повторний вхід продовжує і не створює других сутностей у чужому акаунті. Майстер і є відсутній викликач `syncConnectionCatalog()` | власник, 01.09.2026 | `data/connect.ts`, `connect.check.ts` (червоний до коду), `channex-connect-live.mjs` |
| Ц19 | **Вікно мапінгу вендора вбудовується разовим токеном, скутим на СЕРВЕРІ; ключ API у браузер не потрапляє.** Пʼять умов: (1) токен просить сервер, у браузер іде лише адреса вікна — складати її на клієнті з ключа означало б тихий витік; (2) токен обмежений обʼєктом, живе 15 хвилин, спалюється при першому вході, у базі не зберігається, у журнал не пишеться (`Cache-Control: no-store`); (3) право — `manage_properties`, те саме, що в екрана черги; (4) чужий origin названо явно — `frame-src` лише для двох хостів вендора на одній сторінці, наші cookie туди не йдуть, `referrerPolicy=no-referrer`, `sandbox`; (5) фініш майстра — не «готово», а звірка Ц8 «N з N тарифів не змаплені на жоден канал» — інакше готельєр закриє вікно в переконанні, що продає. Мова вікна — лише перетин із мовами продукту (`en`, `de`), решта бачать `en`, і `ru` ніколи не підставляється (§3.6) | власник, 01.09.2026 | `channex/connect-adapter.ts`, `channex.check.ts` сцена 24, `domain/reconcile.check.ts`, `next.config.ts` |
| Ц20 | **Вебхук вендора — сигнал, не дані; двері публічні за токеном, секрет у заголовку; вебхук пришвидшує, стрічка гарантує.** Підпису у вендора немає («do not include a built-in HMAC signature»), тому `send_data: false`: у тілі лише `event`, `user_id`, `property_id`, `timestamp`, жодної броні; двері (`POST /api/webhooks/channel-manager/<webhook_token>`, шлях нейтральний — И1, імʼя вендора не в URL) пишуть сирий рядок у `cm_events`, відповідають одразу і ПІСЛЯ відповіді (`after()`) будять той самий прохід стрічки, що й крон, через засувку `data/wake.ts` (один прохід на зʼєднання, пачка сигналів — один додатковий). **`after()` — не гарантія:** смерть процесу між відповіддю і проходом (деплой, масштабування, OOM) губить сигнал безслідно; ловить крон стрічки. Читати це треба лише так: вебхук пришвидшує, стрічка гарантує — не «after() доробить». Невідомий токен → 404, невірний секрет → 401 без жодної роботи (порівняння постійного часу; секрет на зʼєднання, 64 hex, різний для staging і проду, змінюваний — спершу вендор, потім база). Реєстрація ідемпотентна через `remote_webhook_id`, з читанням назад (`is_active` і `send_data` за замовчуванням `false` — створений без явного `is_active: true` мовчить; лагодиться PUT-ом, невиліковне — відмова); маска без `ari` (И9) і без `*`; заголовок `X-Webhook-Secret`. Реєструється при ввімкненні розсилки МʼЯКО: без `APP_URL` або при простої вендора зʼєднання вмикається, стан вебхука повертається назвою і видний на екрані з кнопкою «зареєструвати». Локальна розробка вебхуків не отримує — крон `channels-pull` не вимикається ніколи. Тіло вебхука не є джерелом даних і на нашому боці теж: двері зберігають лише конверт сигналу (`event`, `property_id`, `user_id`, `timestamp`), решту відкидають і називають; адаптер при читанні назад ВІДМОВЛЯЄ на `send_data: true` з назвою причини, не лагодить мовчки — увімкнене кимось `send_data` означало б ПІБ гостей у `cm_events` повз ретенцію GDPR. Відʼєднання прибирає вебхук у вендора (інакше в акаунті готельєра лишаються мертві виклики) і вимикає розсилку; рядок `cm_connections` лишається — під ним прийняті броні (доказ). Причина грошима: інтервал крона — це вікно овербукінгу; бронь із OTA існує, а номер тим часом продається на прямому сайті. | 02.09.2026, власник; факти з `docs/vendor/channex/api-v.1-documentation/webhook-collection.md`; живий прохід `scripts/channex-webhook-live.mjs`; гейти `api/webhook.check.ts`, `channex/webhook-adapter.check.ts`, `data/wake.check.ts`, `data/events.repo.check.ts`; міграція 0060 |
| Ц21 | **Порядок робіт: мета — перший готель, який платить; першим іде те, що найдовше чекає на чужу відповідь; строгість пропорційна ціні помилки.** Правило, за яким складається план, — `docs/ORDER-OF-WORK.md`. Звідси: Channex першим (чужий календар — форма, дзвінок, доробки), а «доробити для Channex» і «доробити ядро» — одна робота (карта покриття це підтвердила: прогалини виявились ядром, не «під іспит»). Свідомо НЕ робимо зараз: самостійну реєстрацію, майстер онбордингу, оплату підписки, звіти, покоївок, листування, конструктор сайту — перші 5–10 готелів заводяться руками навмисно. Гейти залізні там, де помилка мовчить (ARI, черга, броні, ціна); на екранах — звичайна акуратність. Скасовує правило поява конкретного готелю, готового стати першим. | 02.09.2026, власник (з рецензентом); `docs/ORDER-OF-WORK.md` |
| Ц22 | **Стан задачі вендора — читанням календаря назад, не опитуванням задачі; розбіжне — назад у чергу з причиною; звірка — рукою оператора і живим проходом, ніколи таймером.** 02.09.2026, П6. Ендпоінта стану задачі у вендора немає (`type: "task"` лише у відповідях `POST`), тож «доїхало» доводить `GET /restrictions` через опції заселеності (И13). Три відповіді — збіг, розбіжність, не звірено — і третя не змішується з першими двома: поле, якого вендор не віддає, не рахується ні збігом, ні приводом переслати. Молодше за хвилину не звіряється (вендор кладе задачі асинхронно). Ніч ціни звіряється разом із наявністю свого типу, бо при нульовій наявності `stop_sell` у відповіді вендора — ефект, не збережений прапорець: збережений `false` при нулі не губиться, а зростання наявності збереженого `true` не знімає (вимір `channex-stop-sell-live.mjs`, 02.09). Обґрунтування — CHANNEX-INTEGRATION §4.4 «Звірка». |
| Ц23 | **Повний синк — це дія, не розклад: рукою оператора або при ввімкненні розсилки, ніколи за таймером; один діапазон на змаплений тип і на змаплену пару до горизонту, той самий батчер, рівно два виклики.** 02.09.2026, П5. Скасовує «повний синк лише з нічного завдання» (старе И6): тест 13 сертифікації відкидає повний синк за таймером як логіку, а стан між діями тримають дельти з транзакцій (Ц16) і звірка (Ц22). Не 500 × N координат у `cm_outbox` — черга тримає діапазони (Ц15). `last_full_sync_at` ставиться лише проходом, який нічого не повернув. Нічний повний синк, якщо колись знадобиться, — свідоме рішення власника, не дефолт. Тримає `check-no-timer-fullsync.mjs`; обґрунтування — CHANNEX-INTEGRATION §4.4 «Повний синк», §3.4. |
| Ц24 | **Ціни немає — це NULL, не нуль.** `price_calendar.base_price` nullable (0062); рядок лише з обмеженнями лишає ціну порожньою, ніч не продається і в канал їде `stop_sell`; нуль і відʼємне писачі відмовляють — «не продавати» це «Закрито». Без базової ціни номер не відкривається ні для гостя, ні для каналу. **Доповнено 05.09 за рецензією коміту:** те саме правило для `weekend_price` — поля немає в запиті → не чіпати, явний `null` → прибрати, нуль читається як «немає» і прибирається з бази (0064); до того збереження обмеження стирало ціну вихідних, а нуль у ній продавав пʼятницю за нуль | власник, 05.09.2026 | INC-017 у [SECURITY-FINDINGS.md](SECURITY-FINDINGS.md), [BLOCK-2-CHANNEX.md](BLOCK-2-CHANNEX.md) 2.0 |
| Ц25 | **Заведений у вендора тариф не видаляється — він знімається з продажу.** `PATCH /api/pricing/rate-plans/[id] { is_active: false }` з екрана «Тарифи»: ціни тарифу не існує ні для гостя, ні для каналу (`priceNights` → усі ночі `missing`, `ratePlanRetired`), у каталог він не йде, а в чергу лягає координата на кожну його пару з дзеркала до горизонту — канал закриває ночі (`stop_sell`, И14). Дзеркало й ціни в календарі лишаються; «Повернути в продаж» відкриває їх знову тією самою дорогою. `DELETE` для заведеного тарифу — і далі відмова `mapped` | власник (BUILD-PLAN 2.1), зроблено 05.09.2026 | [BLOCK-2-CHANNEX.md](BLOCK-2-CHANNEX.md) 2.1; гейти `rate-plans.repo.check.ts` сцена 7, `ari-adapter.check.ts` сцена 8; INC-019 у [SECURITY-FINDINGS.md](SECURITY-FINDINGS.md) |
| Ц26 | **Режим ціни тарифу обирає готель: «за номер» або «за особу».** `rate_plans.sell_mode` (0063): `per_room` — одна ціна на номер на будь-яку кількість гостей (ціна тарифу на дату; матриця й надбавка рахуються на базову заселеність, доплати за гостя не буває), у вендора одна опція на дорослу місткість; `per_person` — своя ціна на кожну кількість дорослих (надбавка з матриці поверх ціни тарифу), опція на кожну до `max_adults` — включно з тими, яких матриця не знає (тоді ціна дорівнює ціні тарифу) — **(б) скасовано частково 05.09.2026 (розділ A п.3 рецензії): опція без рядка матриці — ніч без ціни на цю кількість дорослих (інваріант 17), а не ціна тарифу: у канал вона ціни не отримує й випадає з тіла, пара при цьому НЕ закривається (Блок 0.6 B1, 05.09; до того одна така опція закривала всю пару `stop_sell`); каталог `per_person` заводить лише заселеності з джерелом ціни (базова + ті, що знає матриця); до того четверо без рядка коштували дешевше за трьох**. Режим їде у вендора явно; без вибору — `per_person` (так заводились усі тарифи); замкнений, щойно тариф заведено (набір опцій там не переробити). Наявні тестові тарифи на staging лишаються `per_room` з опцією на кожну заселеність — суперечність, знята лише для нових тарифів; правити на тому боці — `PUT sell_mode` окремим кроком, коли буде потреба | власник, 05.09.2026 («на вибір готелю; у Channex — як там прийнято»); межі й число за кодовою сесією | [BLOCK-2-CHANNEX.md](BLOCK-2-CHANNEX.md) 2.2; гейти `rate-plans.repo.check.ts` сцена 8, `nightly-price.check.ts`, `property-rate-plans.check.ts`, `catalog-target.check.ts`; INC-020 |
| П12 | **Структура продукту — за Hoteliera: верхнє меню (Dashboard · Availability & Rates · Planner · Reservations · Guests · Reports · Settings), Settings у пʼяти групах, платформний екран як `change-organisation`, Setup progress 8 кроків.** Дизайн свій; П11 («патерни Channex») звужується до картки броні й порожніх станів. Бічної панелі немає. **Зроблено 05.09.2026 (Блок 1):** `TopNav` з каталогу `nav-items.ts`, Налаштування чотирма групами (Обʼєкт · Ціни · Продаж · Модулі; Commerce і API — відкладені, П20) з «Легендою», Звіти одним входом (`/app/reports`, аналітика → `/app/reports/sales`), платформна таблиця з фільтрами, Setup progress 8 кроків (домен + `GET /api/properties/[id]/setup-progress` + картка на дашборді) | власник, 05.09.2026 | MASTER-PLAN §1; `harvest/org-settings/text.txt`, `walk-01-onboarding` |
| П13 | **Визначення готовності циклу:** власник заводить готель руками (файл + `apply`, ~15 хв), і готель живе повний день з екранів. Самореєстрація, білінг, майстер онбордингу в продукті — після 5–10 ручних заведень | власник, 05.09.2026 | MASTER-PLAN §0 |
| П14 | **Порядок блоків:** гігієна і злиття → оболонка → ядро цін → канали → день готелю → онбординг руками → ПРРО. Скасовує порядок BUILD-PLAN | власник, 05.09.2026 | MASTER-PLAN §4 |
| П15 | **Модулі за ключем (дефолт OFF = платно):** `tasks`, `reports`, `day_sheets` переходять в OFF; нові `guest_page`, `sites`, `fiscal_ua`; `dashboard`, `booking_engine`, `invoicing` ON. Наявні готелі отримують явний `enabled=true` міграцією. Тарифів підписки і білінгу немає — лише прапорці. **Зроблено 05.09.2026 (Блок 0.2, міграція 0065)** для пʼяти ключів; `fiscal_ua` навмисно НЕ заведено — ключ приходить разом зі своїм модулем у Блоці 6 (П5: прапорець без варти — обманка). Варти: `guest_page` — `withGuestReservation`/OCR (404 гостеві), `withModule` на налаштуваннях, крон нагадувань; `sites` — `withOwnedSite`, створення, аналітика; список сайтів лишається під `booking_engine` (його читає екран коду віджета). Гейт `features.check.ts`: ключ модуля без названого файла варти — червона збірка | власник, 05.09.2026 | MASTER-PLAN §3; зразок 0045/0049 |
| П16 | **Організація може мати кілька обʼєктів; обʼєкт — одиниця цін і каналу** (не переходимо на «організація = готель» Hoteliera). Перемикач обʼєктів і «Усі обʼєкти» — П10 | власник, 05.09.2026 | STATE-AND-QUESTIONS п.6 |
| П17 | **Сауно-чано-флоу віджета вирізається**; погодинні послуги — звичайні `services`. **Зроблено 05.09.2026 (Блок 0.3):** віджет, гостьова сторінка, налаштування віджета, `service-embed.js`, серверні `book-slots`/`book-breakfast`, переклади, CSS; тег `sauna-flow-before-removal`, гілка `archive/sauna-flow`; таблиці лишено (історичні дані) | власник, 05.09.2026 | ARCHITECTURE §8 «Лишається» |
| П18 | **Тема: світла за замовчуванням, темна перемикачем**; один набір токенів, два набори значень. **Зроблено 05.09.2026 (Блок 1):** `:root` світла, `html[data-theme="dark"]` темна; перемикач у меню акаунта; вибір у `localStorage` + кука `theme`; атрибут ставить інлайновий скрипт у `<head>` до першого малювання, а не `cookies()` на сервері (те зробило б динамічною кожну сторінку разом із маркетинговою — рішення кодової сесії); гейт `check-ui-tokens` тримає стелю літералів кольору | власник, 05.09.2026 | UI-LANGUAGE §2 |
| П19 | **`en` операторського інтерфейсу — відкладено** (є uk, cs, de); юрисдикції циклу — UA, DE, CZ; валюти — кілька з фіксованим курсом на сайті, документ у валюті обʼєкта | власник, 05.09.2026 | MASTER-PLAN Блок 5 |
| П20 | **Відкладено свідомо** (не показується в меню): маркетинговий сайт, Public API, Webhooks, Messages гість↔готель, Commerce/products, онлайн-оплата, DSFinV-K, Telegram, банк-імпорт, конструктор сайту, спільні номери (mutual rooms), самореєстрація, білінг. Список — `docs/LATER.md` | власник, 05.09.2026 | STATE-AND-QUESTIONS п.17, 37 |
| П21 | **Новий готель — це нова ОРГАНІЗАЦІЯ, а не другий обʼєкт наявної. Два будинки під однією юрособою лишаються можливими, але це рідкісний випадок, а не спосіб завести другий готель.** Власник, 10.09.2026, на вимір: у живій беті **жодного** рахунку з двома справжніми будинками немає — `aurora` має один обʼєкт, `schlossberghotel` два, і другий («Test Property - ALiSiO») завели ми самі під перевірки осі. Тобто вісь обʼєкта пів дня тримали блокером заради випадку, якого в базі не існує. Наслідки, і їх три. **Перший: ізоляція готелів береться з осі ОРЕНДАРЯ, яку тримає база** — RLS, `FORCE ROW LEVEL SECURITY`, 59 живих тверджень; окремі фізичні бази НЕ робимо, бо ту саму ізоляцію вони дають ціною N міграцій, з яких невдала проявляється як «функція зникла», тобто рівно тим тихим провалом, від якого ми й закриваємось (плюс `purge-tenant.sh` уже зносить одного орендаря цілком і доводить нуль рядків). **Другий: `property_id` лишається в схемі** — вендор рахує гроші за обʼєкт ($7 проти $0.50 за юніт, К16), і два будинки однієї юрособи законні; але робота по осі обʼєкта переходить із блокера в гігієну і перестає займати три сесії. **Третій: перемикання між готелями для готельєра** — це не нова машинерія, а зняте обмеження з наявної: платформна сесія вже входить рівно в один готель (`platform_sessions.acting_organization_id`), поза ним кожен маршрут відмовляє як чужому, і вхід/вихід пишеться в аудит рахунку. Бракує лише переліку «в які рахунки ця людина може входити» — сьогодні `enterOrganization` пускає в будь-який, і без переліку готельєр дістав би доступ до всіх. Ціна вибору названа: історія гостя і лояльність (Д52) між двома готелями одного власника **не переносяться** — власник сказав, що гість, який відпочиває то в одному, то в іншому, практично не трапляється | власник, 10.09.2026 | вимір на копії беты `alisio-beta-daily-2026-09-10`; `src/core/auth/platform.ts:112`; задача 9 сесії 1 |
| Ц27 | **Сезони — сутність (`seasons`, `season_prices`), ціна сезон × тип × тариф рендериться в `price_calendar`** тим самим писачем, що масовий редактор; точкове перевизначення дати живе в календарі (`source='manual'`) і не затирається. `price_occupancy` перестає бути джерелом базової ціни. Ц3 і інваріант 16 цілі: канал і віджет читають те саме. **Крок 1 зроблено 05.09** (0068, `seasons.repo.ts`, екран Налаштування → Сезони); міграція періодів матриці — після звіту на дампі (чекпоінт власника) | власник, 05.09.2026 | MASTER-PLAN §2.1; Hoteliera seasons/overridden_from |
| Ц28 | **Похідні тарифи** (`pricing_type='derived'`, база ± % або сума) рахуються в нас і рендеряться в календар; `derived_option` Channex не використовуємо (Ц7). **Зроблено 05.09 (0069)**; уточнення кодової сесії: похідний без свого рядка на дату — ніч без ціни, базову ціну типу не успадковує; базою — лише звичайний тариф того ж обʼєкта; валюта — валюта бази; перерендер залежних — після кожного запису календаря | власник, 05.09.2026 | §2.2; `rate-plans.repo.check` 10 |
| Ц29 | **Тариф: харчування — атрибут** (`meal_plan`), не вісь; політика скасування — перелік із шести; умови оплати; `sell_mode` per_room/per_person — вибір готелю, платформа підтримує обидва; тип пропозиції Standard/EB/LM/Special. **Уточнено 07.09 (рецензія п.8):** перелік політики, умови оплати й тип пропозиції заводяться разом із першим споживачем — `cancellation_policy` сьогодні не читає ніхто, текст гостю йде з `site_rate_plans`; до злиття сайтового дерева з `rate_plans` (Ц7) — у LATER.md | власник, 05.09.2026 | §2.2, §2.3; LATER.md |
| Ц30 | **Надбавки за заселеність — повна модель:** вікові вилки на організації, `base_occupancy` типу, правила тариф × тип × (дорослий/дитина за вилкою) × (проживання/харчування) × (%/сума) × extra bed; `child_extra_gross` мігрує в правило і зникає; діти йдуть у Channex `children_fee`. Скасовує Ц2 («доплата спільна для тарифів») і мінімальну версію Ц12 — вісь дорослих лишається. **Зроблено 05.09 (0070)** — усе, крім `children_fee` (інваріант 28: поле не бачене в живій відповіді). Уточнення кодової сесії: дорослий понад базу БЕЗ правила й далі бере різницю матриці — перехід, доки періоди матриці не мігровано в сезони (звіт на дампі); дитина — лише правило; правила по вилках без відомого віку дітей — `childAgesRequired`, не вгадана вилка (вік у бронях поки не збирається); дорослих менше бази — без знижки; «за номер» — дорослих не доплачують, дітей — так | власник, 05.09.2026 | §2.4; `harvest/org-settings-extra-occupancy`; `extra-occupancy.check`, `nightly-price.check` |
| Ц31 | **Правила цін і промо** — шар над надбавками, до зборів; `rules[]` у розкладі; `weekend_price`, `price_los_tiers`, `coupons` мігрують у правила з доведеною рівністю чисел. **Зроблено 06.09 (0071)** — таблиця, домен, писач, екран, `priceNights` з розкладом, промо з кодом і лічильником. Уточнення кодової сесії: одна таблиця `price_rules` з `kind = promo` замість окремої `promotions` (умови й дія ті самі); правила компонуються за пріоритетом на результат попереднього; у канал їдуть лише правила, вирішувані без дати бронювання й коду (EB/LM і промо — ні); один код у віджеті — одне джерело знижки (код, відомий купонам, до правил не доходить). **Три міграції відкладено з питаннями власнику** (§2.5): купони — ширша семантика (сайти, послуги); `weekend_price` — щойно заведена в клітинку сезону; LOS-тіри — «обирається один» проти «компонуються» | власник, 05.09.2026 | §2.5; `price-rules.check`, `price-rules.repo.check`, `nightly-price.check` |
| Ц32 | ~~**Обмеження лишаються на типі номера**~~ — **скасовано 07.09.2026 власником: обмеження на ТАРИФІ, тип — зручність запису на всі тарифи.** Причина: тести 5/7/8 сертифікації ставлять різне на тарифи одного типу («Unexpected rate plan in update» при обмеженнях на типі), Hoteliera тримає min/max stay на тарифі, Channex — на тарифі за означенням. Модель (0072): рядок пари несе обмеження nullable — ефективне обмеження пари = значення пари, якщо задане, інакше базовий рядок типу; «закрито» — тип АБО пара; тип закритий → усі пари. Писачі з тарифом пишуть у пару (координата лише на неї); «на всі тарифи типу» (прапорець, дефолт увімкнений) — у базовий рядок, координата на всі пари, власні значення пар не затираються; `null` на парі — «як у типу». Читачі (`dayRestrictions`/`restrictionsAt`, `priceNights`, батчер, звірка) — ефективне пари. Історія: 05.09 (Блок 0.6 A1) обмеження з будь-якого екрана йшли в базовий рядок типу (INC-022) — знято | власник, 07.09.2026 | docs/tasks/2026-09-07-restrictions-per-rate-plan.md; `price-calendar.repo.check` 11, `ari-adapter.check` 12/14, `verify-adapter.check` 10, `nightly-price.check` |
| Ц36 | **Писач НЕ відмовляє в записі на тип значення, що дорівнює власному значенню пари.** «Оператор не чіпав поле» і «оператор свідомо ставить типу те саме число» на боці писача не розрізняються й не мають розрізнятися: правило «мовчки не спрацювати, бо значення збіглося» зробило б «на всі тарифи типу» ненадійним — тип не можна було б виставити на число, яке вже є в однієї з пар. Різницю тримає ЕКРАН: `changedDayFields` (`pricing/ui/day-edit.ts`) не називає полів, яких оператор не змінював, тож у писача просто немає що записувати. Гейти: `day-edit.check` — з 07.09 (правка 5.2 раунду 5) твердження ПОВЕДІНКОВЕ: тіло будує `buildDayPayload` у `pricing/ui/day-edit.ts`, і сцена стверджує, що воно віддає, а не форму виразу; про екран лишився один рядок «модалка кличе `buildDayPayload`». До того це було текстове твердження про `page.tsx` і пропускало три саботажі того самого дефекту (сирий стан поверх обчисленого, проміжна змінна, порожня база порівняння) — усі чотири тепер червоніють. Плюс сцена 13 `price-calendar.repo.check`. Уточнює Ц32 | кодова сесія за рецензією контролера, 07.09.2026 | рецензія раунд 2 правка 1, раунд 3 правки 1.2 і 1.4 |
| Ц37 | **Мінімум ночей — ціле від одиниці; нуль і відʼємне відмовляють з назвою `min_stay_invalid`.** Той самий клас, що нуль у ціні (05.09, бета): очищене поле форми дає `Number('') === 0`, `min={1}` стереже лише стрілки, і нуль поїхав би в канал як `min_stay_arrival: 0` — число, якого готель не називав. «Не продавати» — це «Закрито», а не нуль ночей. Варта в обох писачах (`upsertPrices`, `bulkUpdatePrices`), названа відмова в обох хендлерах, затиск у модалці дня; `null` проходить — для пари це «як у типу» | кодова сесія за рецензією контролера, 07.09.2026 | рецензія раунд 3 правка 1.1; `price-calendar.repo.check` сцена 14 |
| Ц38 | **Промокод списується на ЗАМОВЛЕННЯ, а не на номер, і до першої броні.** Замовлення з віджета — це `bookingQuantity` окремих броней (до 20 з публічного тіла), і кожна з них продана за акційною ціною, тож ціле замовлення забирає `bookingQuantity` використань одним `UPDATE` з умовою `current_uses + N <= max_uses`. Часткового списання не буває: вміщається все або не списується нічого, і відмова (`promo_exhausted`, 409) приходить, коли жодної броні ще не існує. До 07.09 виклик стояв УСЕРЕДИНІ циклу по номерах: із залишком 1 і замовленням на 3 перша бронь уже лежала в базі за акційною ціною, а друге коло віддавало 409 — половина замовлення створена, гроші пораховані по-різному, повтор робить ще одну. Альтернатива «одне використання на все замовлення» відкинута: тоді ліміт «10 акційних номерів» означав би 10 замовлень будь-якого розміру, тобто число на екрані не про те, за що готель платить | кодова сесія за рецензією контролера, 07.09.2026 | рецензія раунд 2 правка 4.3; `price-rules.repo.check` сцени 6a/6b |
| Ц39 | **У канал їдуть лише правила, вирішувані на ОДНІЙ ночі.** Батчер цінує кожну дату як окрему поїздку на одну ніч — інакше не можна, координата менеджера каналів саме така. Тому умови про поїздку цілком відсікаються явно: `min_los`, `max_los`, `period_of_checkin`, `period_of_checkout` (дата бронювання й промокод відсікались і раніше). Доти «1–2 ночі +30 %» проходило на КОЖНІЙ даті — гість OTA з семи ночей платив коротку надбавку сім разів, а напряму не платив зовсім; «заїзд у ці дні» діяло поночі; «виїзд» — зі зсувом на добу; `min_los` мовчки не їхало ніколи. Правило на період ПРОЖИВАННЯ (дати, дні тижня) їде: воно про саму ніч. Альтернатива «лишити як свідоме наближення» відкинута — наближення тут коштує грошей гостя і рахунку готелю. **Відсікається ПРАВИЛО ЦІЛКОМ, а не сама умова** (уточнено 07.09 за рецензією раунду 7, П1): `undecidableOnOneNight` дивиться на наявність `min_los`/`max_los` чи вікна заїзду-виїзду і викидає весь рядок. Наслідок, який готель побачить: «зимова знижка −25 % на період проживання, max_los 30» — тобто широка межа, поставлена про всяк випадок, — у канал не поїде взагалі, і OTA купуватиме ті ночі ДОРОЖЧЕ, ніж прямий гість (200,00 замість 150,00). Це свідомо: обрізати умову й лишити дію означало б продавати в каналі за правилом, якого готель не називав, а помилка в бік недоплати гірша за помилку в бік повної ціни. Хто хоче знижку в каналі — не ставить на неї межі тривалості | кодова сесія за рецензією контролера, 07.09.2026 | рецензія раунд 2 правка 4.2; `ari-adapter.check` сцена 15 |
| Ц40 | **Правило, звужене на тарифи, у формі бронювання власного сайту не діє — і це написано на екрані.** Віджет котирує без `ratePlanId`: тариф сайту живе в `site_rate_plans`, звʼязку з `rate_plans` немає, а поїздка без тарифу під список тарифів не підходить за означенням. Готель заводив «−10 % на BAR», бачив знижку в квоті адміністратора й у каналі, а на власному сайті її не було, і ніхто про це не казав. Звʼязок `site_rate_plans` ↔ `rate_plans` — окрема зміна схеми, у LATER; до неї межа тримається текстом на екрані правил і числом у гейті | кодова сесія за рецензією контролера, 07.09.2026 | рецензія раунд 2 правка 4.1; `nightly-price.check` |
| Ц41 | **Ліміт використань промокоду рахується для ОНЛАЙН-бронювань.** Квота адміністратора приймає промокод і дає знижку, але `redeemPromoCode` кличе лише віджет, тож `max_uses` стереже сайт. Це не помилка розрахунку, а межа обліку: бронь із PMS бере суму з тіла, і лічити її використанням означало б лічити те, чого адміністратор не бачить у формі. Названо на екрані правил; закривається разом із «промокод у формі броні PMS» (Блок 4) | кодова сесія за рецензією контролера, 07.09.2026 | рецензія раунд 2 правка 4.5 |
| Ц42 | **Порядок правил однакового пріоритету не залежить від локалі середовища: порівняння самими рядками, не `localeCompare`.** Правила компонуються одне на результат іншого, тож переставлений порядок — це інші гроші, і на прямому шляху теж: пара «Б −10 %» і «а +20 сумою» на ночі 200,00 давала 198,00 (кирилична «Б» після латинської «а» за правилами української локалі), тепер дає 200,00 (кодова точка «Б» менша). Число змінилось у КОЖНОМУ котируванні, де є два правила однакового пріоритету, — не лише в каналі; на день правки таких правил у клієнтів немає (`price_rules` заведена 06.09, міграція 0071). Альтернатива «зафіксувати локаль» відкинута: вона лишає порядок залежним від таблиці сортування конкретної версії ICU, тобто від того, на чому зібрано образ. Правильний порядок — той, який однаковий скрізь; «правильніший за алфавітом» тут не критерій, бо пріоритет задає оператор числом | кодова сесія за рецензією контролера, 07.09.2026 | рецензія раунд 2 правка 4.7, раунд 7 П2; `price-rules.ts:203` |
| Ц43 | **Відмова, яку можна показати людині, кидається `refuse()`; `catch` розбирає рід через `handleError()`.** Голий `Error` у шарі даних не відрізняється від помилки драйвера, і `catch`, який віддає `e.message`, віддає обидва: так клієнт діставав текст CHECK зі списком дозволених значень і назвою колонки, зі статусом 400 — тобто поломка не виглядала поломкою і в лог не йшло нічого. `refuse(text, status)` кидає `Refusal` (типово 400; 409 там, де запит правильний, а стан не дозволяє відповісти), `handleError(scope, err)` віддає названу відмову своїм статусом і текстом, а будь-що інше — в лог і 500 загальним реченням. Примітив живе в `core/http/refusal.ts` БЕЗ жодного імпорту: кидають його шари даних, які виконуються і поза Next, у прод-образі без `next/*` — перша спроба покласти його поруч із `handleError` одразу почервонила `check-entry-imports`. Тримають `check-error-leak --strict` (друга вісь: `catch` із `e.message` і 4xx над `try`, у якому є `await`) і `core/http/errors.check.ts` | кодова сесія за рецензією контролера, 07.09.2026 | рецензія раунд 3 «Чуже — кому віддати», раунд 7 П3 і П6 |
| Ц44 | **Число на екрані цін називає своє джерело, а простий режим не дає невидимому полю перебити видиме.** Привід не гіпотетичний: 07.09.2026 власник — автор системи — не зміг поставити ціну 333 на 22 листопада. Код відпрацював як написано: на дні лежала ціна вихідних 115, вона перебиває базову у Пт/Сб/Нд, і ніде не було сказано, звідки взялося число. Рішення з трьох частин. (1) Клітинка й редактор дня показують ПОХОДЖЕННЯ ціни («115 · ціна вихідних», «120 · матриця заселеності»), і ключ дає той самий резолвер, що цінує ніч гостю (`priceNights` → `monthGuestPrices`), і тим самим обʼєктом приходить САМЕ ЧИСЛО: вивести підпис із полів рядка не можна, бо матриця перекриває календар, — а взяти число з рядка, а підпис із резолвера означало б підписати чуже число, що рецензія раунду 9 і знайшла на живій базі («100 · матриця заселеності» при матриці 120, Р9.2). (2) «Розширені ціни» — перемикач ОРГАНІЗАЦІЇ (колонка `organizations.pricing_advanced`, міграція 0112; для готелю на 1–15 номерів організація і є готель — рішення контролера Р9.6, перенесення на `properties` у LATER), за замовчуванням вимкнений: у простому режимі видно «Ціну», «Мін. ночей» і «Закрито», а ціна вихідних, CTA/CTD, максимум ночей і матриця — за перемикачем. Не `localStorage`: другий адміністратор інакше бачив би інший набір полів. (3) У простому режимі зміна ціни ПРИБИРАЄ ціну вихідних явним `null` — поле, якого на екрані немає, не має права перебити щойно введене число; якщо ціну не міняли, ціна вихідних лишається недоторканою. Це діє на ОБОХ екранах: перша редакція закрила лише денну модалку, і на масовому редакторі — тобто на тому шляху, яким ціну ставлять на місяць, — привід блоку відтворювався далі (Р9.1). «Закрито» лишається і в простому режимі: для готелю на 1–15 номерів це головна щоденна дія, і в переліку розширених полів його немає (рішення кодової сесії) | власник (привід і межі), кодова сесія (три частини), 07.09.2026 | Блок 6; `day-price.check`, `day-edit.check` сцени 5a/5b, `bulk-edit.check`, `price-origin.check`, `price-calendar.repo.check` сцени 15–18; рецензія раунд 9 Р9.1–Р9.3 |
| Ц50 | **PGlite не є доказом: він доводить діалект, не порядок рядків, не пул і не політики.** Двічі за 07.09.2026 зелений PGlite розійшовся зі справжнім Postgres, і обидва рази вада була в проді (INC-014 — політики; INC-027 — порядок рядків при рівних ключах). Тому `DB_DRIVER=pglite npm run check:pg` — прогін «побачити свої помилки раніше», а доказом лишається стенд AGENTS §7 (роль `alisio_app`, справжній пул) або крок CI. Наслідок для тверджень: **про рядок, якого може бути кілька, стверджують КІЛЬКІСТЬ, а не значення першого знайденого** — `.find()` зелений на тому рушії, чий порядок випадково зручний, і саме так сцена 18 була зеленою на SQLite при живій ваді на Postgres | кодова сесія за дорученням контролера, 07.09.2026 | INC-014, INC-027; AGENTS §7. Номер Ц49 закріплено за сесією 3 (рецензія раунд 10 §4, правило про прибирання) |
| Ц48 | **`priceNights` читає базу рівно одним завантажувачем — `loadPricingContext`.** Виклик без готового контексту будує його сам, на свій діапазон; сітка місяця будує один на місяць і передає кожному з тридцяти запитань. Другого, «швидкого» читання цін не заводиться навіть коли воно очевидно простіше: це було б друге джерело правди про ціну (інваріант 16), а на копіях цей проєкт уже горів чотири рази — чотири копії правила вихідних, Блок 6. Контекст, який не накриває поїздку або не має вікових вилок для котирування з дітьми, — ГОЛОСНА відмова, не тиха добудова: тихо неправильна ціна гірша за 500. Тримає `month-origins.check` двома половинами: лічильник запитів (181 → 6) і звірка «ті самі числа», яка червоніє, щойно контекстний шлях розійдеться з безконтекстним | кодова сесія за рецензією контролера, 07.09.2026 | рецензія раунд 10 Р10.1; взірець — `cheapestByDay` |
| Ц47 | **Сітка цін питає, скільки коштує ОДНА ніч — те саме питання, що ставить канал.** `monthGuestPrices` цінує кожну дату як перебування на одну ніч (`nights: 1`, `checkIn: date`), як `channex/ari-adapter.ts`. Спершу був один виклик на весь місяць (`nights: lastDay`), і це давало неправильні числа мовчки: знижка за довжину перебування і правила «від N ночей» лягали на кожну клітинку, бо тридцять ночей — одна довга поїздка. Ціна: тридцять викликів замість одного на відкриття екрана; це екран одного оператора, не гарячий шлях. Наслідок, важливіший за швидкість: число на екрані оператора і число, яке поїде в OTA, збігаються за побудовою Кадрування ТЕ САМЕ, що в каналі, лише за формою запитання (одна ніч) — не за аргументами: екран питає `channel: 'operator'` і з сьогоднішнім `bookedAt`, канал — `'channel'` і `null`, тож числа можуть різнитись правилами, які в канал не їдуть. Перша редакція стверджувала рівність — це було неправдою (Р10.5) | кодова сесія за рецензією контролера, 07.09.2026 | рецензія раунд 9 Р9.2, Р9.8; раунд 10 Р10.1, Р10.5; `price-calendar.repo.check` сцена 16 (LOS-тір −20 робить вісь кадрування невиродженою), `month-origins.check` |
| Ц46 | **Функція, яка вибирає статус ЗАПЕРЕЧЕННЯМ («не наш клас — 400»), не лишається виправленою — вона прибирається.** `propertyErrorStatus(e)` жила поруч із `requirePropertyId`, який двічі ходить у базу, і правило «PropertyNotFound → 404, будь-що інше → 400» накривало помилку драйвера: п'ятнадцять обробників віддавали клієнтові текст бази зі статусом 400 і без жодного рядка в лозі (INC-026). Виправляти її на місці означало б лишити те, що запрошує помилку знову, тому функції немає: `PropertyNotFound` — це `Refusal` зі статусом 404, дві інші відмови `requirePropertyId` — `refuse(…, 400)`, а всі п'ятнадцять місць ходять через `handleError('<де>', e)`. Те саме стосується роду, вгаданого за ТЕКСТОМ: `/property/i.test(message)` у матриці заселеності проходило «relation "properties" does not exist». Уточнює Ц43 | кодова сесія за рецензією контролера, 07.09.2026 | рецензія раунд 8 Р8.1; INC-026; `core/http/errors.check.ts` сцена 5 |
| Ц49 | **Прибирання, яке щось видаляє, або рахує рядки, або не має права мовчати — а імена, які воно називає, звіряються з базою НАПЕРЕД.** Два роди тихої брехні, обидва вимірені. Перший: `try { … } catch {}` над `DELETE` ховав два запити, мертвих від першого коміту (`invoice_items` не існує ніде; в `accruals` немає `reservation_id`), і знайшлись вони читанням логу postgres-контейнера, а не гейтом. Другий: прибирання поза контекстом орендаря під `alisio_app` не падає — воно чіпає НУЛЬ рядків; виміряно на стенді: без контексту після прибирання лишалось `{conns:1, props:1}`, у контексті — нулі, а зелене тримав каскад від `DELETE FROM organizations`. Тому: тенантне прибирання йде в `runWithOrganization`, а живі гейти (`check-routes-live`, `check-isolation`) питають каталог про кожну таблицю й колонку ДО першого `DELETE` — ловити виняток недосить, бо прибирання по броні на чистій базі не виконується взагалі (контрольний прогін старої обгортки з мертвим іменем виходить з нулем). Рід відмови розрізняється за SQLSTATE (`42P01`, `42703`) плюс дві форми SQLite, а не за текстом `does not exist`: під нього підпадають «role/database/prepared statement … does not exist», і на застарілій базі гейт назвав би вигадану причину. **Загального гейта «оператор повернув успіх і нічого не зробив» не заводимо** — рішення контролера: помилка була видима з боку клієнта (Postgres віддає `ERROR`, драйвер кидає), лог контейнера знадобився лише тому, що виняток гасив порожній `catch`. Це не асиметрія видимості, а код, який відмовився дивитися | кодова сесія за рецензією контролера, 07.09.2026 | рецензія раунд 10 §4 (Р10.11–Р10.13, метазнахідка); `scripts/lib/db-names.mjs` + `db-names.check.mjs` |
| Ц45 | **Прапорець «записати на тип номера» знятий за замовчуванням.** Був увімкнений, і через нього тест 7 сертифікації дав 35 координат замість 4 і торішнє «unexpected rate plan»: оператор ставив обмеження «на цей тариф», а воно лягало на ТИП і їхало на кожну його пару. Дія, яка розширює наслідок за межі того, що оператор назвав, не може бути замовчуванням. Підпис прямий — «Записати на тип номера — діє на всі тарифи, крім тих, що мають власне значення», — а не перелік наслідків; попередження про власне значення тарифу лишається і стоїть ДО кнопки збереження. Заразом «Застосувати до» скидається на «Всі дні» на кожне відкриття масової форми (`key` на місці виклику): вибір «Тільки вихідні», зроблений раз, не має мовчки застосуватись до наступної правки. Уточнює Ц32 | кодова сесія за завданням власника, 07.09.2026 | Блок 6 п.3–4; `day-edit.check` сцена 5b |
| Ц34 | **Тіло до каналу несе лише те, що змінилось (маска полів координати), а `stop_sell` їде у трьох випадках:** (а) змінився стан «закрито» базового рядка, (б) ніч втратила чи здобула джерело ціни (NULL ↔ число, знятий/повернутий тариф), (в) повний синк. Зміна лише ціни на відкритій ночі `stop_sell` не несе — вендор тримає свій прапорець, і «ціна не знімає закриття» (И14) далі істинне. Повний синк несе чотири обмеження явними дефолтами (мінімум 1, максимум 0 = без межі, заїзд/виїзд дозволені); у дельтах дефолти не вигадуються. Уточнює И14 і Ц13 (маска звужує тіло, не джерело) | контролер за дорученням власника, 05.09.2026 | лист Channex 05.09 (Б1, Б2), `CHANNEX-REJECTION-2026-09-05.md`, міграція 0066 |
| Ц35 | **Пара «тип × тариф» існує в каталозі лише з ціною `> 0`; зміна типу не розсилається на пари знятих тарифів.** Рядок обмежень без ціни пари не робить; знятий тариф (Ц25) лишається в дзеркалі, але координати від типу не отримує — лише названий явно. `505e5d24…` у листі — законна пара B&B × Twin, не залишок: у тілі тесту 3 вона з правки базової ціни типу; у тестах ціна ставиться тарифу | кодова сесія за розбором контролера, 05.09.2026 | `CHANNEX-REJECTION-2026-09-05.md` §2–3.3 |
| Ц33 | **Повнота цін — гейт продажу:** комбінація сезон × тип × тариф без ціни або дірка в сезонах блокує ввімкнення розсилки в канал і тримає Setup progress | власник, 05.09.2026 | §2.6 |
| К1 | **Мультикімнатна бронь з каналу = батьківська + дочірні**, ack після коміту групи | власник, 05.09.2026 | Блок 3 |
| К2 | **Рівень OTA — власний екран** (`cm_channels` як дзеркало Channel API): спершу читання й показ мапінгу тарифів по OTA; запис — після живого виміру по одному OTA (ЧЕКПОІНТ). Уточнює Ц8 | власник, 05.09.2026 | Блок 3 |
| К3 | **Продакшн-акаунта Channex ще немає**; `environment='production'` вмикається після сертифікації | власник, 05.09.2026 | Блок 3 |
| К9 | **Обʼєкт у вендора ОНОВЛЮЄТЬСЯ, і лише коли розійшлося.** `CatalogTarget` умів тільки створювати, а створення кличеться лише при порожньому дзеркалі — тож рід житла й пояс, змінені готелем, не доходили до вендора ніколи (Р13.9). Додано `PUT /properties/:id`, а перед ним `GET /properties/:id` і порівняння пополе: без читання «оновити, якщо розійшлося» стало б «писати PUT на кожен синк» — зайвий запит і зайвий шанс перетерти те, чого ми не чіпали. Оновлюються лише `PROPERTY_FIELDS_WE_OWN` (назва, валюта, країна, місто, адреса, індекс, пошта, телефон, пояс, рід житла); фото, опис і `facilities` лишаються за вендором. Дзеркало на неіснуючий обʼєкт — відмова, не перестворення (інваріант 13) | кодова сесія, 09.09.2026 | `channex/client.ts`, `catalog-target.ts`, `domain/catalog.ts`; `catalog-target.check` |
| К10 | **Перелік родів житла — у ядрі, вендорські значення — у модулі каналів.** Писач обʼєкта в базовому модулі відмовляв за списком вендора КАНАЛІВ навіть готелю, який каналів не купував (Р13.15), а `channels` — модуль платний і OFF за замовчуванням. Перелік переїхав у `@core/lodging-kinds`; `channex/property-type.ts` мапить його у `property_type` вендора мапою тотальною й перелічною — `as string` сховав би рід, якого вендор не знає, і той повернувся б 422 посеред створення каталогу. Мапа за межі `channex/` не виходить (`check-vendor-isolation`). Збіг наших значень із вендорськими сьогодні повний, і це стан, а не домовленість: перейменує вендор — зміниться мапа, не ядро | кодова сесія, 09.09.2026 | `core/lodging-kinds.ts`; `channex/property-type.ts`; `check-boundaries --strict`, друга вісь |
| К11 | **Мовчазної Праги немає на жодному живому шляху до поясу.** Чотири місця, не три: `organizationTimezone` (читач «дня готелю»), `parseICal` (дефолт аргументу), `saveGeneralSettings` (ПИСАЧ — налаштування без поля пояса мовчки записували чеський пояс) і сам `provisionOrganization`, закритий раніше. Пояс вирішує, де проходить межа доби, а канал торгує датами заїзду: празький пояс в українського готелю — це зсунуті на добу броні без жодної помилки в логах. `DEFAULT 'Europe/Prague'` на самій колонці лишився свідомо — на ньому тримаються 56 файлів фікстур у чужих модулях; рядок у `docs/LATER.md` з умовою і командою | кодова сесія, 09.09.2026 | `core/hotel-day.ts`, `channels/domain/ical.ts`, `properties/api/general-settings.handlers.ts`; LATER |
| К12 | **Рід житла — вісь РАХУНКУ вендора, і група лежить поруч зі значенням.** Лист вендора 09.09.2026 (`docs/vendor/channex/billing-2026-09-09.md`, збережений дослівно): готельна група тарифікується $7 за обʼєкт, оренда — $0.50 за юніт, 14 + 8 = 22, і це рівно наша множина. Тобто поле перестало бути «коректністю каталогу» і стало грошима готелю: неправильний рід дає неправильний рахунок, доки хтось не звірить виписку (інваріант 29). Розбиття лежить ОДНІЄЮ таблицею в `channels/ui/billing-group.ts` доменними словами (`per_property`/`per_unit`) — там, звідки його читає екран у браузері; адаптер перекладає в слова вендора (`channex/property-type.ts`). Двох таблиць немає свідомо: два списки одного факту розходяться мовчки. Сум у коді немає — $7 і $0.50 змінюються без нас, код називає ОСНОВУ | лист вендора, 09.09.2026 | `channels/ui/billing-group.ts`; `channex/property-type.ts`; `property-type.check` |
| К13 | **Пояс ставиться завжди явно, іменем IANA; вендорське `[optional]` нас не звільняє.** Лист: «Always set timezone explicitly to an IANA name. Do not omit it — arrival dates follow the property timezone day boundary.» У вендорському доці те саме поле позначене `[optional]` (`hotels-collection.md:430`), і саме ця розбіжність — причина, чому варта потрібна В НАС: вендор дозволяє пропустити, а наслідок (зсунуті дати заїзду) лягає на готель. Дві окремі сцени відмови, бо це дві різні поломки: порожній рядок `NOT NULL` дозволяє і він мовчки випадав з тіла через умовний спред; вигадана зона проходить перевірку на непорожність і вмирає 422-ю вже посеред створення каталогу. Умовний спред для обох полів прибрано — тіло будує один `propertyAttributes` | лист вендора, 09.09.2026; Р13.12 | `catalog-sync.ts`; `catalog-sync.check` |
| К14 | **Обʼєкт без активного каналу у вендора коштує НУЛЬ.** Лист: «Live = at least one active channel on the property. Creating a channel alone does not bill it (new connections start disabled). We invoice on the 1st … for properties that are live then; properties with no active channel stay free.» Рішення не технічне, а продуктове, і на нього можна спиратись: заводити готелі в Channex наперед безкоштовно — масове заведення, демо-обʼєкти, підготовка до сезону. Наш код це вже поважає: нові зʼєднання створюються вимкненими | лист вендора, 09.09.2026 | `docs/vendor/channex/billing-2026-09-09.md` |
| К15 | **Зміна обʼєкта видима оператору як розходження каталогу, і має дію поруч.** Рід житла — вісь рахунку вендора, а до каналу він доїжджає лише синком каталогу; до Р15.1 оператор міняв поле, бачив успішне збереження і не дізнавався, що у вендора лишився старий рід. Ознака рахується ЛОКАЛЬНО з мітки `cm_connections.catalog_synced_at` (0130), без походу до вендора: екран не має права витрачати чужий бюджет запитів на підказку, а гейт не має залежати від мережі. Питає ЕКРАН окремим маршрутом `GET /api/channels/catalog-state`, а не писач обʼєкта: дати `properties` знання про канали означало б четверту залежність базового модуля від продаваного, рівно ту, яку прибрав К10. Справжня звірка з вендором (`propertyDrift`) лишається всередині синку | рецензія раунду 18, Р15.1, 09.09.2026 | `0130`; `connections.repo.ts`; `connect.handlers.ts`; `CatalogStaleNotice.tsx`; `catalog-stale.check` |
| К16 | **Рід житла — ЯВНИЙ вибір готелю на кожному шляху заведення, а не дефолт.** Рішення власника В1 (09.09.2026): «має бути чіткий вибір, один раз обирається і закріплюється за готелем». Причина — К12: поле стало віссю рахунку вендора, тож підставлений `hotel` кемпінгу означає чужий тариф, виставлений мовчки, і побачить це не код, а виписка першого числа. Тому названа відмова, як із валютою і поясом, СКРІЗЬ, де народжується обʼєкт: `provisionOrganization` (і через нього `provision-org.mjs`, `apply-hotel.mjs`, живий прохід онбордингу), писач `createProperty` і форма обʼєкта. Файл готелю мусить називати `property.propertyType` — і шаблон теж, бо шаблон копіюють, а поля, якого в зразку немає, не буде і в копії. При ПРАВЦІ порожнє поле лишається «форма про це мовчить»: інакше екран, який поля не надіслав, стирав би вибір. Онбординг питає рід у кроці «Обʼєкт» і показує там наслідок для рахунку (`LodgingBillingHint`) — той, хто заводить готель, бачить його до екрана каналів, а не після виписки | власник В1, 09.09.2026 | `core/provisioning.ts`; `properties.repo.ts`; `setup-progress.ts`; `provisioning-lodging-kind.check`; `check-hotels.mjs` |
| К17 | **Названі відмови модуля каналів — мовою продукту, і вони називають предмет, а не колонку.** Р15.2: чотири відмови `catalog-sync` і одна `properties.repo` говорили англійською («catalog: property_type is not set …»), а остання ще й вивалювала всі 22 машинні значення переліку. Названа відмова існує рівно для того, щоб її ПРОЧИТАЛА людина: написана так, вона виглядає як витік винятку, і перше, що робить оператор, — шукає, кому це переслати. Формула: що не так, чим це обертається, що зробити. Перелік значень оператор бачить списком у формі обʼєкта, і саме туди його відсилає текст. Гейт стереже ВЛАСТИВІСТЬ, не формулювання (§3.2.1): `namedRefusal()` в `catalog-sync.check` вимагає трьох речей нараз — це відмова (400, не голий виняток), вона мовою продукту, і вона називає предмет; доведено трьома окремими зломами | рецензія раунду 18, Р15.2, 09.09.2026 | `catalog-sync.ts`; `properties.repo.ts`; `catalog-sync.check` |
| К18 | **Канал володіє тим, що ЗАБРОНЮВАЛИ; готель — тим, що нарахували і що вписала людина.** Рішення власника 09.09.2026 на питання, поставлене рецензією раунду 21. Ревізія OTA, яка прибирає кімнату з групового бронювання, доти робила `DELETE FROM reservation_sub_bookings`, а `reservation_line_items.sub_booking_id` має `ON DELETE CASCADE` — тобто розбивка кімнати, яку рецепція вписала руками при поділі групи між платниками, зникала мовчки з волі вендора. **Уточнення виміру, і воно змінює ціну питання:** справжні гроші фоліо (`fin_folio_items` — послуги, мінібар, турзбір, ночі) тримаються за `folio_id`/`reservation_id`, а не за рядок групи, і каскадом НЕ чіпаються; фактура тим більше ціла, бо вона копія (0013). Стирається саме набране людиною, і `reservation_line_items` не читає жоден читач фактури — лише картка броні й екран поділу між платниками. Правило: рядок групи прибирається, лише якщо на ньому НІЧОГО не висить — ні гостя, ні позицій; інакше він лишається, дочірня бронь стає скасованою (історію тримає вона), і випадок іде рецепції РІШЕННЯМ, а не тихим станом. Тримає не уважність, а база: каскад на `reservation_line_items.sub_booking_id` перевертається на заборону, тож майбутній `DELETE` відмовляє, а не нищить. Причина не технічна: група з трьох кімнат, де компанія платить за дві, а третя за себе, після тихого стирання міняє розкладку між платниками сама і без сліду — а пізнє скасування кімнати майже завжди означає штраф, підставу для якого ті самі рядки й тримають | власник, 09.09.2026; рецензія раунду 21 | `0131`; `@bookings/group-rooms` `releaseGroupRoom`; `inbound-bookings.repo.ts`; сцена 2e в `inbound-bookings.check` |
| К19 | **Вісь обʼєкта у вхідному шляху каналу — `oneProperty(conn.propertyId)`, а не `ALL_PROPERTIES`.** Контролер, переміривши 12 пар INC-029 у `modules/channels`, написав: «жодне не діра — це читання за первинним ключем усередині ланцюжка, чия власність доведена `connectionInTenant`; там треба не правка, а `ALL_PROPERTIES` із причиною словом». Перша половина істинна, друга — ні, і різниця не термінологічна. `ALL_PROPERTIES` означає «цей читач НАВМИСНО дивиться на всі будинки рахунку» (зведений звіт, крон, консолідована шахматка); ревізія ж адресована ОДНОМУ будинку, і його називає `cm_connections.property_id` — колонка, яку жоден `UPDATE cm_connections` не чіпає. Написати там «усі» означало б лишити наступному читачеві рішення, якого ніхто не ухвалював, — рівно те мовчазне «усі», з якого почався INC-029. Тому 11 пар звужені до будинку зʼєднання, і лише 12-та — `channelsSyncedAt` — справді `ALL_PROPERTIES`: там читається САМЕ зʼєднання за своїм ключем, тобто носій осі, і звузити його можна було б лише взявши будинок із цього ж рядка. Звуження не косметичне: воно перетворило два тихі шляхи на названу відмову (тип номера сусіднього будинку в ревізії; журнал, що показує на бронь сусіднього будинку) | сесія 2, задача 6, 09.09.2026; незгода з вказівкою контролера, доведена виміром | `inbound-bookings.repo.ts`; `channels.repo.ts`; `inbound-bookings.property-axis.check`; і те саме в iCal — `ical-sync`, `ical-export`, `ical-channels`, `connections.repo`, три крони, `ical-scope.check` (INC-041) |
| К20 | **Ключ ідемпотентності публічного бронювання — це ІДЕНТИФІКАТОР броні, виведений із подання; окремої колонки немає.** INC-046: у віджеті не було ключа взагалі, і повтор давав гостю 409 на власну бронь, 403 на витрачене рукостискання або дві броні. Ключ рахується з ЗМІСТУ броні (сайт, номер, дати, кількість, гість) плюс стрічка `Idempotency-Key`, якщо клієнт її прислав, і з нього виводиться `reservations.id`. Тобто роль ключа бере первинний ключ таблиці — найсуворіше обмеження, яке вже є: атомарність без нової колонки, індексу й міграції (схему цього разу не чіпаємо — там сесія 3). Зміст броні входить у хеш ЗАВЖДИ, і це не надмірність: стрічку обирає клієнт, два гості одного сайту можуть прислати однакову, і без змісту другий отримав би ЧУЖУ бронь разом із її токеном гостьової сторінки — доведено мутацією. Стрічка не обовʼязкова, бо в `public/widget/` лежить зібраний бандл у кешах браузерів, і жорстка вимога відмовляла б справжнім бронюванням; виведений ключ — не мовчазний дефолт (інваріант 8), правило назване в коді й повертається полем `origin`. **Дві межі, обидві потребують колонки:** ключ не зберігається дослівно (у підтримці його не показати) і «той самий ключ з іншим тілом» не відрізняється від нового бронювання (Stripe відповідає 422, ми — звичайним шляхом). Перевірка повтору стоїть ДО споживання одноразового рукостискання — поставлена після, вона не спрацювала б ніколи | сесія 2, задача 9, 10.09.2026 | `widget/domain/reserve-idempotency.ts`; `widget-reserve.handlers.ts`; `widget-reserve.idempotency.check` |
| К21 | **Повтор бронювання віддається лише на КЛІЄНТСЬКИЙ ключ; виведений ключ обороняє базу, але не відповідь.** (10.09.2026, рецензія контролера, INC-047.) Довід К20 «ідентифікатор виводиться з подання, тож хто не робив цієї броні, не назве її змісту» істинний лише наполовину: у відповіді їде `guestPageToken` — перепустка до гостьового порталу, оплати й документів (інваріант 14), — а зміст подання відгадуваний (номер публічний, дати перебирає календар, імʼя й телефон знає знайомий). Повтор при цьому стоїть ДО рукостискання, тобто це оракул без обмеження швидкості: 201 означає «вгадав». Тому повтор віддається на `origin === 'client'`; виведений ключ лишається і тримає ідентифікатор броні детермінованим, тож дві однакові вставки неможливі. Для живого віджета не змінюється нічого — обидва входи вже шлють `Idempotency-Key`. **Свідомо НЕ зроблено**: повніша правка (позначати рядок рукостискання спожитим разом із відбитком подання, замість видаляти) — це колонка в `widget_handshakes`, а смугу схеми тримає сесія 3; напрям записаний в `SECURITY-FINDINGS` INC-047 |
| К22 | **Звʼязок «фірма → тариф» — ТАБЛИЦЯ `company_rate_plans`, не колонка на `rate_plans`.** (10.09.2026, INC-205, CORE-GAPS п.1.) Виміряно на базі Ґрайца, не вгадано: `PREISCODE 3 Firmenpreise` — ОДИН прайс-код на ВСІХ корпоративних гостей, а коди 8 і 10 — по одному на конкретну фірму. Тобто один тариф обслуговує багато фірм, і колонка цього не виражає: «Firmenpreise» довелося б завести по разу на кожну з 12 фірм готелю — дванадцять однакових тарифів зі своїми календарями цін, які треба тримати синхронними руками (клас інваріанта 20). Колонку `companies.rate_plan_id` відкинуто з двох причин: `companies` цього раунду тримає сесія 1, і — важливіше — «кому видно цей тариф» це властивість ТАРИФУ, її читає котирування, а фірма з двома тарифами (конференційний і загальний) звичайна річ, яку колонка забороняє. **Ознака фірмового тарифу — ЗВʼЯЗОК, а не `is_hidden`**: галочка ставиться руками і відповідає за канал; трималась би варта на ній, тариф без галочки продавався б кожному. Уточнює `ARCHITECTURE §6.3.1` (рядок «на броні, не окремим тарифом») — той рядок правлено, не заведено новий |
| К23 | **Фірмовий тариф і поступка на броні СКЛАДАЮТЬСЯ, і застосовуються на різних кроках.** (10.09.2026, INC-205.) Тариф каже, ЯКА ціна в цієї фірми; `lodging_discount_percent` (§6.3.1) — чому саме цьому гостю менше, з причини, якої немає в жодній таблиці. У Winhotel вони теж складаються: `ADRESSEN.PR_CODE` і `ADRESSEN.RABATT` стоять на одній адресі, і другий діє додатково. Числами: 80,10 (тариф) − 10 % = 72,09, і це не 75,60 (поступка від стандарту), не 80,10 (поступка загубилась) і не 84,00 (тариф загубився). Подвійного застосування немає ЗА ПОБУДОВОЮ — тариф дає ціну в котируванні, поступку застосовує `splitOtaAmount()` уже на рахунку, — і саме тому це стверджується числом у `company-rate-quote.check.ts`: «за побудовою» тримається рівно доти, доки хтось не додасть відсоток ще й у котирування, а тоді гість заплатить 64,88. Тихого дефолту тут немає (інваріант 8): правило назване, і рахунок показує, ВІД ЧОГО поступка |
| К4 | **Група з каналу — рідна форма продукту: майстер САМ Є кімнатою №1, кімнати 2..n — дочірні, і на кожну кімнату рядок `reservation_sub_bookings`.** Скасовано першу редакцію («конверт»: батьківська без типу з сумарними гостями і сумою бронювання) — рецензія 07.09 показала, що вона ламала кожного читача мовчки: Zimmerliste і сніданок рахували гостей двічі (`day-sheets.repo.ts`), турзбір двічі (`city-tax.handlers.ts`), календар малював три плашки на дві кімнати (`lanes.ts`), список показував бронь без типу, а PATCH дозволяв поставити конверт у номер. Тепер майстер несе тип, дати, гостей і суму СВОЄЇ кімнати; ключ кімнати лежить в `external_uid` кожного рядка (`${код}#${ключ}`), майстра теж — інакше нема за чим упізнати, яку кімнату він тримає. Зникла кімната майстра → майстер переймає першу живу (rekey), а рядок, що її тримав, забирає стару кімнату і скасовується: рядки міняються місцями, бо скасований майстер із живою кімнатою — це бронь, яку картка показує скасованою, поки гість заселяється. Сума бронювання (555 при кімнатах 300 + 210) і його заселеність — у журналі ревізій та в історії броні, не в рядку. Один раз група — завжди група: прибрана кімната СКАСОВУЄТЬСЯ, а не стирається | кодова сесія 3, 06.09.2026; переписано 07.09.2026 за рецензією контролера | `inbound-bookings.check.ts` сцени «дві кімнати різних типів» (створити → змінити → порядок → прибрати кімнату 2 → прибрати кімнату №1 → скасувати, з читанням `availabilityByDay` після кожного кроку), ARCHITECTURE §8 «Блок 3» |
| К5 | **Кімната ревізії впізнається ключем «або всі з OTA, або всі позиційні»; `booking_room_id` ключем не буває.** Стабільного ідентифікатора вендор не дає: `ota_unique_id` лише в Booking.com, у Booking CRS `null`. Мішанина `[u:49, i:1]` заборонена — додана посередині кімната зсунула б позиційні ключі й дві кімнати обмінялися б рядками. Недокументоване `booking_room_id` виглядає стабільним і НЕ є ним: чотири редакції одного бронювання дали чотири різні набори UUID (вимір 06.09.2026), тобто ключ із нього створював би нову дочірню бронь на кожну редакцію. Ціна позиційного ключа названа: при видаленні першої з двох кімнат виживе рядок першої з даними другої — стан групи правильний, спадковість рядка ні; кращого з тим, що дає вендор, немає | кодова сесія 3, 06.09.2026 | `domain/feed.ts` `groupRoomKeys()`, `live-fields.json` → `GET /booking_revisions/feed` |
| К7 | **`cm_channels` повертається в схему як ДЗЕРКАЛО, і форму їй задає той самий довід, яким її колись прибрали.** §4.3 ТЗ викинув її з доводом «наша копія протухне мовчки» — довід чинний, тому в рядку немає нічого НАШОГО (жодного прапорця, який можна змінити тільки в нас), прохід перезаписує дзеркало ЦІЛКОМ (канал, якого немає у відповіді, зникає), мітка свіжості лежить на зʼєднанні (`channels_synced_at`: «нуль каналів» ≠ «ще не питали»), вік видно на екрані, а вендора питаємо не частіше разу на годину. Найгірший стан — «дані годину як застарілі», а не «розходяться назавжди». Що змінилось: ціна відсутності рівня OTA виявилась вищою — без нього Ц8 закривався інструкцією готельєру і жоден гейт цього не тримав. Щойно в рядку зʼявиться перше НАШЕ поле, перезапис почне його стирати, і рішення про протухання доведеться ухвалювати заново | кодова сесія 3, 06.09.2026 | міграція 0100, `channels.repo.check.ts`, CHANNEX-INTEGRATION §4.3 |
| К8 | **Каталог доступних OTA не зберігається взагалі.** Перелік адаптерів належить вендору і росте (57 на 06.09.2026); копія протухла б так само, як протухла б копія налаштувань каналу. Читається на «Оновити» і живе рівно стільки, скільки та відповідь. З тієї ж причини `cm_channels.ota_code` не має `CHECK`: закритий словник означав би, що готель, який підключив новий канал, бачить порожньо | кодова сесія 3, 06.09.2026 | `GET /channels/list`, екран «Канали (OTA)» |
| К6 | **Дочірні броні групи заводить `applyGroup` у `channels/data/inbound-bookings.repo.ts`, а не фасад `@bookings`.** Єдині двері фасаду — `createSubBooking`, і це HTTP-хендлер під `withPermission`: він вимагає сесію оператора, бере `unitId` із тіла запиту (а бронь із каналу приходить БЕЗ номера, П9), копіює тип і дати майстра (а кімната має власні) і пише через `getSql()` повз транзакцію викликача (інваріант 11). Тобто дверей для цього випадку немає; функція мінімальна і живе в модулі каналів, як уже живе `applyRevision`. Потрібні двері назвати сесії 2, коли вона переробляє картку броні. Після К4 сюди ж пише `reservation_sub_bookings` (таблиця вже спільна: `bookings` і `widget`), тим самим прийомом і в тій самій транзакції | кодова сесія 3, 06.09.2026 | `bookings/api/sub-bookings.handlers.ts:72`; задача Блоку 3 §1 |
| Р13 | **Р11 підтверджено 05.09.2026: на проді лише тестові дані (Schlossberghotel — тестова конфігурація, можна міняти).** Злиття `beta → main` — після репетиції міграцій на дампі (`restore-test.sh`) і звіту; деплой робить власник | власник, 05.09.2026 **Скасовано 10.09.2026 власником у частині «тестові дані»:** готель у Ґрайці (Schlossberghotel) — **справжній перший клієнт**, а не тестова конфігурація. На беті він досі був тестовим, і те, що там лежить, можна стирати без жалю; але далі він переїжджає на прод, і саме з ним доводиться інтеграція. Наслідок для черги: хвиля Winhotel (`docs/tasks/2026-09-10-winhotel-wave-after-main.md`) йде ПЕРШОЮ після `beta → main`, бо чотири її пункти — умова імпорту, а імпорт стоїть перед запуском. Частина «злиття `beta → main` після репетиції на дампі» лишається чинною і виконана: репетиція пройшла 10.09.| Блок 0 |
| Р14 | **Інваріант 21 читає схему з `beta`, поки `beta` не злита в `main`;** після злиття — знову `main` | власник, 05.09.2026 | AGENTS.md §3 |
| Р15 | **Контрольна сесія (Cowork) ставить блоки й перевіряє по коду; кодова сесія виконує блок цілком; мікрозадач немає; журнал роботи — `claude/JOURNAL.md` у проєкті, читається першим** | власник, 05.09.2026 | MASTER-PLAN §0 |
| Ц51 | **Модель тенантності не міняємо: спільна схема + RLS лишається.** Питання власника 08.09 «чи немає капітальнішого рішення, як у великих компаніях». Галузеве меню — спільна схема з RLS, схема на орендаря, база на орендаря, комірки; ALiSiO стоїть на найсильнішому варіанті спільної родини, і для готелів на 1–15 номерів база на орендаря коштує дорожче за весь продукт. Головне: **жодна з цих моделей не спіймала б жодного з наших випадків** — засів іде під власником схеми під час міграцій (INC-025, INC-028), змішування обʼєктів живе ВСЕРЕДИНІ одного орендаря (INC-029), а зашита CZK не є тенантністю взагалі (інваріант 22). Зміна моделі — місяці роботи, яка не закриває жодної знайденої вади. Замість неї: перенести на вісь обʼєкта той самий прийом, що спрацював на осі орендаря — зробити її структурною, а не такою, що тримається на памʼяті | контролер за фактами, 08.09.2026 | `docs/tasks/2026-09-08-INC-029-property-axis.md`; `units.handlers.ts:28`, `units.repo.ts:46` |

## День готелю (Блок 4, паралельна сесія 2)

| № | Рішення | Ким, коли | Обґрунтування |
|---|---|---|---|
| Д1 | **Вкладення до броні — окрема таблиця `reservation_files` у тому ж сховищі `data/uploads/<org>/…`, що й решта файлів готелю; ретенція разом із бронню (CASCADE), GDPR-цикл знеособлення не чіпає; PDF дозволено.** Не колонка `attachments_json` на броні: файл має власника, час і того, хто поклав, а рядок із цим переживає перейменування файла. Не окреме сховище: одне місце, яке вже звіряє організацію в шляху (`GET /api/uploads`) | кодова сесія, 06.09.2026 | міграція 0090; `bookings/api/reservation-files.handlers.ts` |
| Д2 | **Виселення з боргом — політика ОБʼЄКТА `none \| warning \| blocking`, дефолт `warning`; борг — з фоліо, без фоліо — зі статусу оплати броні; невідоме слово читається як `blocking`.** `warning` дефолтом, бо попередження не зупиняє сімʼю з валізами о сьомій ранку і не мовчить; `blocking` — вибір готелю, у якого борг на виїзді це помилка процесу. Фоліо має перевагу над статусом, бо статус — слово, а фоліо — числа; виставлений документ боргу не знімає. Дві осі політики × боргу — гейтами до коду **Свідомо:** `PATCH {status: checked_out, payment_status: paid}` одним тілом обходить `blocking` — варта читає статус оплати, що стане чинним разом із запитом, тобто рецепція, яка каже «оплачено» і «виселено» одним рухом, і є той, хто ухвалює виняток (рецензія 07.09 п.7) | кодова сесія (форма — Hoteliera General settings, дефолт — MASTER-PLAN §1.2), 06.09.2026 | міграція 0091; `checkout-balance.check.ts`, `checkout.repo.check.ts` |
| Д3 | **«Перерахувати ціну за новою заселеністю» — явна кнопка, не перемикач при збереженні; квота через `POST /api/pricing/quote` (той самий шлях, що форма броні), порівняння в `bookings/domain/requote.ts`; підтвердження пише суму й рядок історії `guests_change`.** Ціни в коді картки немає; турзбір оновлюється лише коли він усередині суми (як при зміні дат); квота, чий total не складається з її ночей, відкидається | кодова сесія, 06.09.2026 (Hoteliera: «Recalculate price based on the new occupancy») | `requote.check.ts` — три ночі різних цін |
| Д4 | **Оплата на фоліо, яка закриває рахунок (залишок ≤ 0 при нарахованому > 0), ставить `reservations.payment_status = paid` — з картки, після відповіді сервера, не тригером у базі.** Готівка іде як підтверджена (`payment_method: 'cash'`), решта — як позначка до звірки (`'folio'`). Саме це слово читає варта заселення; без звʼязку рецепція платила б двічі — у фоліо і в «статусі оплати». Тригер відкладено: два писачі одного поля (позначка з картки і фоліо) розійшлися б мовчки, а клієнтський крок видно в історії як `payment_status_change` | кодова сесія, 06.09.2026 | `card/FolioPanel.tsx` (`pay`) |
| Д5 | **Послуга з каталогу лягає на фоліо через `service_id` у `POST /api/finance/folios/[id]/charges`: назву, ціну і ставку ПДВ за датою бере сервер (`postCatalogService`, той самий шлях, що замовлення з гостьової сторінки), у тілі запиту ціни немає.** Ручний рядок (`kind: manual` з явною ставкою) лишається — це наявні двері для того, чого в каталозі нема | кодова сесія, 06.09.2026 | `invoicing/data/stay-charges.repo.ts` |
| Д6 | **Зміна статусу броні, її ночі в каналі (`noteStay`) і дзеркалення в дочірні броні — ОДНА транзакція `sql.tx`.** До того каскад був окремим «non-fatal» викликом: падіння між ними лишало головну бронь виселеною, а дочірні — ні. Усе, що має статись разом зі статусом (номер стає брудним після виселення — 2.2), додається під ту саму ручку `t` | кодова сесія, 06.09.2026 | `reservation.handlers.ts` `updateReservation` |
| Д7 | **Вкладок на картці чотири — Проживання · Фінанси · Файли · Історія змін; «Messages» не малюється (П20).** Старі вкладки «Оплата/Реєстрація/Групи/Збір/Примітки» розкладені по перших двох, а не лишені поруч: шість вкладок і чотири — це два різні екрани, і Hoteliera має рацію | кодова сесія за задачею контролера, 06.09.2026 | `BookingViewModal.tsx`, `MobileBookingDetail.tsx` |
| Д8 | **Стан прибирання і його журнал належать ОБʼЄКТУ (`@properties`), housekeeping — екран над фасадом.** `units` пише лише properties, тож і `unit_cleaning_log` пише він же (`setCleaningStatus(t, …)` — стан і рядок журналу однією ручкою); housekeeping не має куди писати SQL (`check-boundaries`). Модуль housekeeping — ядро без ключа (MASTER-PLAN §3: готель без прибирання не існує) | кодова сесія, 06.09.2026 | `properties/data/cleaning.repo.ts`, `@properties/kernel` |
| Д9 | **Виселення бруднить номер броні і номери дочірніх у ТІЙ САМІЙ транзакції, що статус; падіння журналу відкочує статус.** Виселена бронь із чистим номером, про який ніхто не знає, гірша за відмову. Заселення в неприбраний номер — попередження `unit_dirty` у відповіді, не заборона: рецепція бачить і вирішує | кодова сесія за задачею §2.2, 06.09.2026 | `bookings/data/reservation-write.repo.check.ts` — червоним до коду |
| Д10 | **Нове право `manage_housekeeping`** — бачити борд і міняти стан; його мають покоївка, рецепція, менеджер, власник. Не `manage_properties`: покоївка не редагує номерний фонд, але саме вона каже «чисто». Чекліст зміни на телефоні досі ходить старими дверима під `manage_properties` — переведення окремим кроком | кодова сесія, 06.09.2026 | `core/auth/permissions.ts` |
| Д11 | **Компанії — новий довідник `companies` у ядрі, НЕ звʼязаний з обліковими контрагентами.** Задача називала `fin_counterparties` як таблицю, яку не чіпати і з якою не звʼязувати; такої таблиці в репозиторії немає взагалі (жодного `CREATE`, жодної міграції — лише згадки в MASTER-PLAN і в самій задачі). Тобто рішення «не звʼязувати» виконано буквально: звʼязку нема, бо нема з чим. Коли облік заведе контрагентів, компанія-платник стане кандидатом на джерело для них, а не навпаки | кодова сесія за задачею §2.3, 06.09.2026 | `companies/`, 0093 |
| Д12 | **Вибір компанії на броні переписує ЗНІМОК `reservations.invoice_company_*` з довідника; документ і фоліо читають знімок, а не живий рядок.** Сім полів `invoice_company_*` існували до довідника і їх читає весь фактурний контур (`invoices.handlers`, `fin_folios.payer_*`); замість другого шляху компанія лягає в них при виборі — і фактура 2026 року не переписується, коли фірма переїде 2027-го (те саме правило, що для платника фоліо, 0024). `company_id = NULL` чистить знімок. Ручне редагування семи полів на картці лишилось: воно править знімок, не довідник | кодова сесія, 06.09.2026 | `bookings/api/reservation.handlers.ts`, `@companies/kernel` |
| Д13 | **Права на компанії — як на гостей: читає кожен, хто увійшов; пише `manage_guests`. Окремого права немає.** Компанія — той самий довідник контрагентів, що гість, лише юрособа; хто заводить гостя, заводить і його фірму. Екран — під «Гості» (`/app/guests/companies`, `nav:guests`), як у Hoteliera «Guests / Companies». Компанію з бронями не видалити (409) — лише в архів: вона стоїть на документах | кодова сесія, 06.09.2026 | `companies/api/companies.handlers.ts` |
| Д14 | **Перетягування в планері — це ПЕРЕНЕСЕННЯ, не розтягування: кількість ночей не міняється, ціна не перераховується.** Смуга лягає туди, куди її кинули, з урахуванням дня, за який узяли; дати й довжина міняються через форму або кнопку перерахунку на картці (Д3). Інший тип номера — підтвердження (гість платив за інший тип), той самий тип — тихо, безномерна бронь — призначення. Клієнт лише не пропонує програшний хід (`drag.ts`); суддя — сервер: 409 з кодом `unit_occupied` або `unit_blocked` | кодова сесія за задачею §2.4, 06.09.2026 | `calendar/drag.ts`, `bookings/data/conflicts.repo.ts` |
| Д15 | **Закритий номер (`availability_blocks`) — конфлікт для перенесення броні, як і чужа бронь.** До цього PATCH дивився лише на броні: гість переїжджав у номер на ремонті, планер малював його поверх «🔒 Закрито». Одне місце (`findStayConflict`) відповідає формі перенесення, перетягуванню й розселенню однаково. Басейн (`is_pool`) конфлікту не має за означенням | кодова сесія, 06.09.2026 | `conflicts.repo.check.ts` — червоним до коду |
| Д16 | **Реєстрація гостя з картки пише ОБИДВІ таблиці, як портал; цільова модель — одна таблиця, окремим кроком після Блоку 4.** Сьогодні дві книги гостей: `guest_registrations` (рахує `registration_status`, стереже заселення) і `reservation_guests` (портал, Meldeschein, Evidenční kniha). Рецепція писала лише першу — книга гостей не бачила рецепційних реєстрацій (рецензія 07.09 п.3, notes п.9). Мінімум у цьому блоці: один писач `addReceptionRegistration` у `@guests` пише обидві й видаляє обидві; картка читає `guest_registrations`. Ціль (рішення контролера): `guest_registrations` як звʼязка + реєстраційний знімок (або FK `reservation_guests.registration_id`), один писач через фасад `@guests` | контролер, 07.09.2026; кодова сесія — мінімум | `guests/data/registration.repo.ts` |
| Д17 | **«Хто скасував бронь» читається з ЖУРНАЛУ, а не з нової колонки.** Фільтр «скасовані клієнтом» (§2.5) потребує автора скасування; журнал броні від 03.09.2026 його вже називає — скасування з каналу лягає дією `channel_cancelled`, скасування рецепцією — `status_change` від людини з сесією. Колонка `cancelled_by` дублювала б це і розійшлася б із журналом при першому писачі, який про неї забуде; писача в каналах я торкатися не можу (§2.6). Межа чесності записана в коді: скасування з каналу ДО 03.09.2026 сліду не лишили, і фільтр їх не показує — це «ми не знаємо», а не «скасував готель» | кодова сесія за задачею §2.5, 06.09.2026 | `bookings/data/list-filters.ts` |
| Д18 | **`payment_status = 'partial'` — повноцінне значення колонки, і ставлять його ГРОШІ, не оператор.** Фільтр «частково» з джерела форми (Hoteliera `reservations-filter-payment-status`) вимагав значення, якого CHECK не приймав, — при тому що два писачі його вже ставили: `recalcReservationPaymentStatus` (внесків більше нуля й менше за суму) і `POST /api/payments` з `type: deposit|partial`. Обидва падали, депозит лягав у фінанси, бронь лишалась «не оплачено». Розширено CHECK (0094), а не заведено окремий обчислюваний фільтр: писач уже існує й має рацію, зламана була схема. На формі броні пункт видно, але він `disabled` — другий писач одного поля розійшовся б із першим, та сама причина, що в Д17. Словник статусів — один на всі екрани (`bookings/ui/payment-status.ts`); чотири копії брехали кожна по-своєму | кодова сесія за задачею §2.5, 07.09.2026 | міграція 0094; `bookings/data/payment-status.check.ts` |
| Д19 | **Список статусів оплати у формі броні лишається СВІДОМОЮ копією словника — як носій каталогу перекладів, і гейт тримає їх слово в слово.** `scripts/extract-strings.mjs` дістає літерали лише через МІСЦЕ РЕНДЕРУ в тому самому файлі, тож зі спільного модуля (`bookings/ui/payment-status.ts`) слова в `catalogue.json` не потрапляють узагалі — а це «100 % покриття» при українському слові на німецькому екрані. Прибрати копію не можна, поки інструмент такий; лишити її без гейта означало б тихе розходження назв. Тому копія одна, названа у винятках гейта з причиною, і звіряється зі словником. Скасовується, щойно екстрактор навчиться брати ключем «константа + вузол-власник» (передано сесії 1) | кодова сесія за рецензією 07.09 раунд 3 п. 2.2–2.3, 07.09.2026 | `bookings/data/payment-status.check.ts`, твердження 7–8 |
| Д20 (Ч7) | **Фоліо — ЄДИНА книга проживання: статус оплати броні й борг на виселенні виводяться з нього, а гроші, привʼязані до броні, потрапляють У фоліо, а не живуть поруч.** Дві книги вже дали видиму розбіжність: 3000 наперед із 5000 через фоліо — борг 2000 на виселенні, ті самі 3000 через касу — 5000, і бронь, оплачена половиною, у фільтр «частково» не потрапляла. Наслідок для `checkout-balance`: `balanceFromReservation` для `partial` повертає `null` («числа немає»), а не весь `total_price` — вигадане число на екрані гірше за відсутнє, бо за ним рецепція вимагає в гостя гроші, яких він не винен; політика вирішує сама, і `blocking` при цьому не пускає. Власник назвав це рішення «Ч7 / Д19»; Д19 у цьому файлі вже зайняте носієм каталогу перекладів, тож номер тут Д20 — назва Ч7 збережена | власник, `docs/tasks/2026-09-07-owner-answers.md` В3, 07.09.2026 | міграція 0095; `bookings/data/folio-book.check.ts` — дві сцени, що дають однаковий результат |
| Д21 | **Ручна операція у Фінансах із `reservation_id` статусу броні НЕ рухає — ні створення, ні правка, ні видалення, ні обʼєднання.** Це прямий наслідок Д20 і зміна поведінки: раніше дохід, заведений через Фінанси → Операції, давав `partial`/`paid`. Тепер слово виводиться з фоліо, а Фінанси в книгу гостя не пишуть, тож усі сім місць `operations.handlers.ts` питають книгу, куди самі нічого не клали. Гроші, які має бачити рахунок гостя, вносяться «Оплатою» на картці броні або касою (`payment-bridge`) — обидві пишуть у фоліо; ручна операція у Фінансах лишається проводкою обліку. Двері з Фінансів у фоліо свідомо не робились: це рішення про документи, і воно власникове | сесія 2, рецензія раунд 8 Р8.9, 07.09.2026 | `finance/api/operations.handlers.ts` — `createOperation` і `recalcReservationPaymentStatus` |
| Д22 | **Гроші, зняті з фінансової книги, знімаються і з книги гостя — одними дверима, і ЛИШЕ ті, які ця операція туди й поклала.** Видалення платежу чистило лише `fin_operations`, а перерахунок читає фоліо: рядок зникав, бронь лишалась `paid`, виселення відчинене, грошей уже ніде немає — і жодної помилки. Двері одні: `reverseOperationInFolio` (`finance/api/folio-reversal.ts`, листковий файл — інакше цикл імпортів). Зустрічний рядок, а не видалення платежу: `fin_folio_payments` не має видалення за задумом. **Уточнено 07.09 (Р10.6):** реверс знімав будь-який дохід із `reservation_id`, а `source` не розрізняє — місток теж пише `'manual'`; видалення ручної проводки бухгалтера забирало з рахунку гостя ЧУЖІ гроші. Тепер операція несе `folio_payment_id` (0096), знімається саме той рядок, у тому фоліо, де він лежить, і зі способом та документом того ж платежу — без останнього німецька каса з TSE зустрічного рядка не приймає взагалі. **Свідомо не зроблено:** видалення ПОВЕРНЕННЯ (`op_type = 'expense'`) грошей у фоліо назад не додає — зустрічний рядок уміє лише зменшувати сплачене, а додати позитивний платіж «бо колись було повернення» означало б вигадати гроші; слово при цьому перераховується, тож розбіжність видно | сесія 2, рецензія раунди 8 (Р8.7) і 10 (Р10.6, Р10.8), 07.09.2026 | `scripts/check-payment-delete.mjs --strict`; сцена Ж у `folio-book.check` |
| Д23 (Ч8) | **Маркер очікуваної оплати НЕ пише рядок у книзі гостя і не рухає слово оплати.** «Очікується переказ» лишається очікуванням на броні, видимим рецепції в журналі. Причини, у порядку ваги: книга гостя містить ФАКТИ, не наміри (Д20/Ч7) — маршрут маркера існує саме тому, що гроші ще в дорозі; `fin_folio_payments` не має видалення за задумом, тож помилковий клік виправлявся б зустрічним рядком у рахунку живого гостя; борг на виселенні рахується з фоліо, тож маркер відчинив би двері боржникові (клас Р8.5). Третій стан платежу («заявлений/підтверджений») заводиться тоді, коли його попросить готель, а не як побічний ефект маркера | власник делегував контролеру, `docs/tasks/CHECKPOINTS.md` Ч8, 07.09.2026 | `app/api/payments/route.ts` — маркерна гілка |
| Д24 | **`unpaid` виводиться з РУХУ грошей у книзі, а не з їх відсутності.** Є рядки оплат, а сума їх нуль — платіж зняли зустрічним рядком, слово правдиве; рядків немає — книга про оплату не каже нічого, і слово броні лишається. Інакше перерахунок стирав би те, що поставила людина: бронь із виставленим рахунком чекає переказу під `payment_requested`, а німецький обʼєкт без TSE не тримає у фоліо готівки взагалі — там це стан ПОСТІЙНИЙ, і `unpaid` переписувався б на кожному дотику до броні | сесія 2, рецензія раунд 10 Р10.9, 07.09.2026 | `bookings/domain/folio-payment.ts`; чисті твердження в `folio-payment.check` |
| Д25 | **План рахунків і бізнес-юніти сіються НА КОЖНУ організацію, зі сталим `code` і випадковим ідентифікатором.** Довідник сіявся один раз на всю базу (`SELECT id FROM organizations LIMIT 1`, проти інваріанта 1) з літеральними первинними ключами, тож другий комплект був неможливий, а `provisionOrganization` не сіяв нічого. `code` унікальний у межах організації (інваріант 3); код у Фінансах резолвить статтю через `categoryIdByCode`, літералів `'ec_*'` не лишилось. Список один на всіх — `core/chart-of-accounts.ts`, без імпортів, бо провіженінг виконується голим node. **Свідомо не зроблено:** складений зовнішній ключ `(organization_id, category_id)` — написаний і доведений червоністю, але відкочений: `pg-schema.mjs` складених ключів не вміє й розкладає їх на два одноколонкові, з яких один означає не те. Робиться разом із правкою генератора | сесія 2, рецензія раунд 10 §3 (INC-025), 07.09.2026 | міграція 0097; `core/chart-of-accounts.check` — два готелі, у кожного своє |
| Д26 | **Свіжозаведений готель має все, щоб узяти перші гроші: план рахунків, ОДНУ касу у своїй валюті і власника, якому ця каса стоїть за замовчуванням.** Каса одна, не набір: банк, термінал і клірингові рахунки заводить сам готель під свої реквізити. Валюта каси — організації, бо місток шукає запасний рахунок ЗА ВАЛЮТОЮ операції: каса в кронах для німецького готелю не знайшлася б | сесія 2, INC-028 ланка 2, 08.09.2026 | `core/provisioning.ts`; `check-onboarding-live` |
| Д27 | **«Валюта компанії» — валюта ОРГАНІЗАЦІЇ, не крона.** `computeAmountCompany` приймає її аргументом і не конвертує взагалі, коли валюта операції та сама; `fx_rate` для власної валюти лишається `null`, а не `1` — «конверсії не було» і «курс один до одного» це різні твердження. Доти в ядрі обліку стояло `to_currency = 'CZK'` літералом, і готель, чия базова валюта не крона, не міг провести готівкову оплату ВЗАГАЛІ. Продукт будується для Європи і України (інваріант 22) | сесія 2, INC-028 ланка 4, 08.09.2026 | `check-currency-literals`, друга вісь; доведено прогоном на двох готелях — EUR падав, CZK проходив |
| Д28 | **Засів довідників живе ТІЛЬКИ в `provisionOrganization`. Міграції даних не сіють.** Міграція котиться до того, як орендар існує: `SELECT id FROM organizations LIMIT 1` у ній не знаходив нічого, і саме тому на чистій базі план рахунків не діставався НІКОМУ (INC-028 ланка 1). «Полагодити умову» означало б лишити рід помилки на місці — сівач, що вдає міграцію. Міграція 0097 робить лише СХЕМУ (колонка `code`, індекси, підпис історичних рядків). Організація, заведена раніше, лишається без довідника і чує про це НАЗВАНОЮ відмовою 409; разова дія адміністратора — `scripts/seed-chart-of-accounts.mjs` (AGENTS §5: немає скрипта — спершу створюється скрипт). Наявних баз міграція не чіпає | контролер, INC-028, 08.09.2026 | `core/provisioning.ts`; `scripts/seed-chart-of-accounts.mjs`; `core/chart-of-accounts.check` |
| Д29 | **`SELECT … FROM organizations LIMIT 1` заборонений там, де від вибору залежить, ЧИЇ дані зʼявляться; дозволений там, де результат однаковий для будь-якої організації.** Чотири копії цього рядка сіяли дані конкретному орендареві — і були вадою (інваріант 1). Пʼята (`db.ts:541`) — проба СХЕМИ: чи приймає CHECK на `app_users.role` нинішній перелік ролей; організація потрібна лише як дійсний зовнішній ключ, рядок одразу видаляється. Це сказано в коді прямо, разом з ознакою — інакше наступний прийме пробу за зразок | контролер, INC-028 п.4, 08.09.2026 | `src/lib/db.ts:541` |
| Д30 | **`check-onboarding-live` — гейт, а не ручний прохід**: `npm run check:onboarding` і крок роботи `live` у CI, роллю ЗАСТОСУНКУ (`alisio_app`). Доти Д26 називав його гейтом, а бігав він руками — і саме тому Р12.1 ніхто не побачив. Роль не суперкористувацька навмисно: заведення теж мусить проходити під політиками, і перший же прогін під нею знайшов, що прохід умикав модулі готелю `INSERT`-ом поза контекстом орендаря з `.catch(() => {})` | сесія 2, Р12.2, 08.09.2026 | `.github/workflows/checks.yml`, робота `live` |
| Д31 | **Стаття плану рахунків отримує ОСІ (`op_type`, `classifier`) від засіву, разом із назвою.** Не від міграції, не «потім»: стаття без осей існує, показується і приймає операції, а P&L кладе її в «Інше» нижче EBITDA, форма витрати не показує зовсім, автопідбір не знаходить. Значення явні в `core/chart-of-accounts.ts`, бо два рядки навмисно відхиляються від свого гурту — `variable` (COGS, а рядок P&L власний) і `investors` (Financing, але НАДХОДЖЕННЯ). Уже заведеним готелям осі підписує `scripts/seed-chart-of-accounts.mjs` — за `code` і лише там, де порожньо. **Номер змінено 09.09.2026:** цей рядок був заведений як «Д28» того самого дня, що й Д28 вище, і два різні рішення півдоби стояли під одним номером — коміти, які на «Д28» посилаються, означають те з них, що старше за годиною (рішення контролера, 15:01). Номер розведено, не переухвалено; сам рядок не змінився | сесія 2, Р12.1, 08.09.2026 | INC-029; `check-onboarding-live` (оренда в OPEX, податок у «Податках», «Інше» нуль) |
| Д32 | **Кожне поле операції, яке вказує в довідник, доводить належність цьому готелю — у коді, не ключем.** `category_id`, `account_from_id`, `account_to_id`, `project_id`, `counterparty_id` перевіряються через `ownedFinanceRow`, чужий id → 404 (інваріант 5). Одноколонковий FK доводить лише існування рядка, а RI-тригери Postgres ідуть повз RLS — тобто база тут не сторож. Складений ключ лишається в LATER зі своєю умовою (правка `pg-schema.mjs`). **Номер змінено 09.09.2026:** цей рядок був заведений як «Д29» того самого дня, що й Д29 вище, і два різні рішення півдоби стояли під одним номером — коміти, які на «Д29» посилаються, означають те з них, що старше за годиною (рішення контролера, 15:01). Номер розведено, не переухвалено; сам рядок не змінився | сесія 2, Р12.3, 08.09.2026 | INC-030; `operation-scope.check`, і в `check:pg` |
| Д33 | **Номер у журналі рішень означає ОДНЕ рішення, і це тримає машина.** 08.09.2026 дві сесії дописали по два рядки за одну добу — «Д28» і «Д29» стояли двічі кожен, півдоби реєстр мав два різні рішення під одним іменем (Р13.3). Розведено так: ПІЗНІШИЙ за годиною рядок дістає новий номер (Д31, Д32), старший лишається на місці, нічого не видаляється — на номер посилаються коміти. Уважність цього не тримає: обидві сесії взяли «наступний вільний» у різних копіях гілки, зіткнення зʼявилось при злитті, де реєстр уже ніхто не читав | сесія фінансів, Р13.3, 09.09.2026 | `scripts/check-decisions-registry.mjs --strict` у `npm run check` |
| Д34 | **Варта належності — ОДИН список полів і таблиць на всі шляхи запису, не копія в кожному хендлері.** Р12.3 закрив `createOperationInTx` і `updateOperation` власним циклом у `operations.handlers.ts`; за добу знайшлося ще три двері в те саме поле — авто-правило (`set_category_id` і рідня), шаблон регулярного платежу, бюджетний рядок, — і жодні про ту варту не знали (Р13.1, Р13.7). Список переїхав у `data/owned.repo.ts` (`CATALOGUE_REFERENCES`, `RULE_ACTION_REFERENCES`, `requireOwnedReferences`); нове поле додається туди один раз. Дві реалізації однієї перевірки розходяться, шість — розходяться шість разів | сесія фінансів, Р13.1, 09.09.2026 | `operation-scope.check`, сцени авто-правил; `owned.repo.ts` |
| Д35 | **Авто-правило доводить належність на ЗБЕРЕЖЕННІ, не на спрацюванні.** Правило зберігається один раз, а спрацьовує на кожній наступній операції готелю: варта на вході — це одна перевірка замість тисяч, і оператор чує відмову тоді, коли ще памʼятає, що робив. RLS тут не сторож і це сказано в коді: політика `fin_operations` дивиться на `organization_id` ОПЕРАЦІЇ, а не на власника статті, тож операція своя + стаття чужа проходить і на Postgres. Чужий id → 404. Названа відмова їде через `handleError`, не через глухий `catch … 400` (інваріант 6) | сесія фінансів, Р13.1, 09.09.2026 | `auto-rules.handlers.ts` `validateActions`; `operation-scope.check` |
| Д36 | **Мітки операції — шосте поле того самого тіла запиту, і в них ОДНІ двері.** `tag_ids` їхало повз варту пʼяти полів: обидва входи писали клієнтські id прямо у звʼязку, а три читачі джойнили `finance_tags` без орендаря — на SQLite чуже імʼя мітки поверталось у відповідь API (рід INC-014). Причина не в неуважності: `fin_operation_tags` не має `organization_id`, орендар у ній читається ЛИШЕ через `finance_tags`, тож про джойн можна забути в кожному з шести місць окремо. Тепер `data/operation-tags.repo.ts` памʼятає за всіх, і `add_tag_ids` авто-правила йде тією самою вартою — це одне поле і два входи, не два поля. Чужа звʼязка, яка ВЖЕ лежить у базі, не прибирається мовчки: вона просто невидима цьому готелю | сесія фінансів, Р13.2, 09.09.2026 | `operation-tags.check`, вісім сцен; і в `check:pg` |
| Д37 | **Правило «вісь із групи обліку» одне на весь продукт, і джерело правди — `core/chart-of-accounts.ts`, бо саме воно сіє.** Правило жило в трьох місцях і в двох розходилося із засівом: форма нової статті казала `Financing → expense`, засів — `investors: Financing, income`, а легасі-бекфіл не мав `Financing` у жодному `WHEN` і давав `other/other`. Ціна в грошах: готель, який заводив власну статтю фінансування, діставав статтю, якої немає у формі надходження; а стаття з `other/other` клала внесок власника в «Інше» нижче EBITDA — гроші, ПРИНЕСЕНІ в готель, зменшували його чистий результат. Тепер `AXIS_BY_STD_GROUP`, `Financing → income` за засівом. ЧЕКПОІНТ лишено відкритим: чи має фінансування бути видиме і в надходженні, і у витраті — питання про форму обліку, варіанти в `.notes.md` | сесія фінансів, Р13.5, 09.09.2026 | `core/chart-of-accounts.ts`; `pnl-classifier.check` |
| Д38 | **Вісь, якої читач звіту не знає, НАЗИВАЄТЬСЯ — «не знаю, куди це» і «це інше» різні твердження.** `COALESCE(classifier, 'other')` у `reports.handlers.ts` жив поруч із правильним читачем у `money-metrics.ts`: ліки написані й не застосовані, і жоден гейт цього не бачив, бо після Р12.1 усі фікстури сіються з осями. Тепер один вираз `CLS_SQL` на обидва місця, а стаття, чиєї групи немає в жодному `WHEN`, дає названу відмову з ІМЕНЕМ статті замість тихого рядка «Інше» (інваріант 13). Стаття без групи взагалі — окремий рядок «не класифіковано», гроші з нього не зникають | сесія фінансів, Р13.4, 09.09.2026 | `money-metrics.ts` `CLS_SQL`; `pnl-classifier.check`, вісім сцен числами |
| Д39 | **Вісь статті лікується за РОЗБІЖНІСТЮ з правилом, а не за порожнечею — і спершу за `code`, лише потім за `std_group`.** Р12.1 підписував осі там, де порожньо, і саме тому не чіпав бази, яким гірше за всіх: легасі-бекфіл `db.ts` ставив осі НЕПОРОЖНІМИ й неправильними (`Financing` не мав жодного `WHEN` і падав в `other/other`; `Revenue` діставав `classifier='other'`), після чого бекфіл порожнього не спрацьовував уже ніколи, а `--list` показував «без осей: 0». Порядок «код, потім група» не переставляється: `variable` (COGS, але «Змінні») і `investors` (Financing, але надходження) навмисно відхиляються від групи, і лікування самою групою зламало б обидва. Групу, якої немає в переліку, НЕ вгадуємо — таку статтю називає читач П&L (інваріант 13). Ціна, обрана свідомо: вісь, яку готель змінив через `PATCH /api/finance/categories`, теж повернеться до правила — неправильна вісь це гроші не в тому рядку звіту щомісяця, а свідома зміна рідкісна й повторюється за пів хвилини; `--list` показує список ДО лікування, поіменно | сесія фінансів, Р13.6, 09.09.2026 | `data/axis-repair.ts`; блок 0120 у `db.ts`; міграція `0120`; `axis-repair.check` |
| Д40 | **Спрацювання зламаного правила операцію НЕ змінює, і мовчазного пропуску немає в жодному разі.** Варта стоїть на збереженні (Д35), але правило могло лягти в базу раніше за неї. Тоді спрацювання пропускає його ЦІЛКОМ — не «крім поганого поля»: половина правила це не правило, операція дістала б комбінацію, якої готель ніколи не описував. Поля з чужим посиланням пишуться в `fin_auto_rules.broken_fields` (міграція 0121) і їдуть у список правил, де оператор бачить позначку «зламане» з назвою поля. Не `console.error`: лог контейнера не читає ніхто, і саме цей клас тиші коштував проєкту найдорожче. Успішне збереження очищає позначку | сесія фінансів, Р13.1, 09.09.2026 | `auto-rules-engine.ts`; `operation-scope.check`, сцена 2d |
| Д41 | **Шаблон регулярного платежу не гасне з ПЕРШОЇ відмови, а відмова видима готелю.** Рушій ловив виняток і ставив `is_active = FALSE` одразу: помилка конфігурації тихо вимикала регулярний платіж — оренда переставала нараховуватись, причина лишалась у логу контейнера, а погашений шаблон сам не вмикається. Три відмови ПОСПІЛЬ (успішний прогін скидає лічильник), бо найчастіша відмова тут тимчасова: курсу валюти на сьогодні ще немає. `failed_runs`, `last_error`, `last_error_at` (0121) показуються в списку шаблонів. Свідома ціна: `next_run_at` зсувається і після відмови — пропущене нарахування замість рушія, який зупинився на одному зламаному шаблоні; видно її в лічильнику | сесія фінансів, Р13.7, 09.09.2026 | `recurring-engine.ts`; `recurring-scope.check` |
| Д42 | **Колонка з JSON читається і як рядок, і як розібраний обʼєкт — бо `JSONB` драйвер віддає обʼєктом.** `parseRule` робив `JSON.parse(actions_json)` під глухим `catch`, а `conditions_json`/`actions_json` на Postgres це `JSONB`: драйвер повертає обʼєкт, `JSON.parse` розбирає рядок «[object Object]», кидає, рукав ковтає — і правило дістає порожні умови. `matchesAllConditions` на порожньому списку дає `false`, тобто на Postgres НЕ СПРАЦЬОВУВАЛО ЖОДНЕ авто-правило: ні категоризація, ні автопідбір контрагента. На SQLite та сама колонка TEXT, і там усе працювало — зелено там, де розробка, мертво там, де клієнт (рід INC-014). Зіпсований JSON тепер не «порожній», а зламаний: правило позначається і не застосовується (Д40) | сесія фінансів, знайдено `check:pg` роллю `alisio_app`, 09.09.2026 | `auto-rules-engine.ts` `parseJsonColumn`; `operation-scope.check`, сцена «справне правило СПРАЦЮВАЛО» |
| Д43 | **Рушій регулярних платежів обходить ГОТЕЛІВ, кожного у своєму контексті.** Тут стояв один запит без орендаря. На SQLite він бачив усі шаблони; на Postgres політика звіряє `organization_id` з налаштуванням, якого ніхто не робив, і запит не знаходив нічого — тобто регулярні платежі не нараховувались узагалі, а «створено 0» виглядає рівно так само, як спокійний день. Прод на Postgres. Той самий клас і та сама форма ліків, що вже описані в `cron/guest-reminders`. Відмова одного готелю не спиняє решти | сесія фінансів, знайдено `check:pg`, 09.09.2026 | `recurring-engine.ts` `runRecurringTick`; `recurring-scope.check` |
| Д44 | **Правило осі має ОДИН дім, і це тримає властивість, а не вираз.** Д38 звів вираз до `CLS_SQL` — і цього виявилось мало: «один читач на обидва місця» було виконано в ОДНОМУ місці з чотирьох. Живий екран «PNL-2», кешфлоу, розклад по бізнес-юнітах і ПИСАЧ дочірньої статті лишились кожен зі своїм мовчазним дефолтом, а гейт був зелений, бо стеріг вираз. Тепер гейт стверджує ВЛАСТИВІСТЬ: у `src/modules/finance/**` немає жодного дефолту осі поза `money-metrics.ts` — ні `COALESCE(classifier, …)` у SQL, ні `classifier || '…'` у виразі. Разом із виразом переїхав і обовʼязок називати невідоме: `refuseUnknownAxis` — теж одні двері. Розбір AST, не грепом: стрипер коментарів це окремий рід вади (Р14.2), а в AST його немає за побудовою | сесія фінансів, Р14.1, 09.09.2026 | `pnl-classifier.check` — статичне твердження плюс число в рядку «PNL-2» |
| Д45 | **Звіт партнера ідентифікується парою «період × обʼєкт», а не періодом; заміна публікації не переносить рядок під інший обʼєкт і не передає чужий токен.** Пошук «уже опублікованого» не називав обʼєкт, а `UPDATE` переписував `property_id` — той самий рядок, той самий токен, нова належність. Тепер пошук звіряє належність через `IS NOT DISTINCT FROM` (звіт по всьому рахунку має `property_id IS NULL`, і `= ?` не збігся б із ним ніколи — тобто такий звіт при кожній публікації плодив би новий токен), а `UPDATE` `property_id` не чіпає взагалі: рядок, знайдений за належністю, її вже має; рядок з іншою — це ІНШИЙ звіт, і він публікується новим рядком. Сусіди шва не повторюють: `revokeReport` і `rotateToken` ходять за `id` + орендарем, тобто належність доводить сам рядок; `listReports` — читання по всьому рахунку і належить до INC-029 | сесія фінансів, INC-033, 09.09.2026 | `partner-report.repo.check`, десять сцен; і в `check:pg` |
| Д46 | **Сайт-вітрина належить ОБʼЄКТУ, і продає лише його номери, типи й послуги.** `booking_sites.property_id` — `NOT NULL`, а варта `withOwnedSite` звіряла тільки рахунок: сайт обʼєкта А приймав у `site_listings` номер обʼєкта Б і продавав його, а екран послуг пропонував увімкнути послуги сусіднього обʼєкта. Нового запиту для правки не треба — варта вже повертає ввесь рядок сайта, тож обʼєкт відомий. Коментар над старою вартою («Owning the SITE does not make a room yours») називав полагоджену половину: про сусідній рахунок правда, про сусідній обʼєкт ні | сесія 1, INC-034/INC-035, 09.09.2026 | `site-property-scope.check` — свій номер приймається, чужий 404, і числом у `site_listings` |
| Д47 | **Послуга з каталогу лягає у фоліо ТІЛЬКИ свого обʼєкта; фоліо без обʼєкта відмовляє поіменно.** У нарахування їдуть `price` і `vat_code`, тобто ставка юрисдикції — звірка з рахунком це не звірка з обʼєктом. Друга половина рішення важливіша за першу: `fin_folios.property_id` нульовий і фоліо без броні досяжне (подія, рахунок компанії), тож вузьке `AND s.property_id = ?` відмовило б у КОЖНІЙ послузі під виглядом «послуги не знайдено». Стан названо: `folio_without_property` + речення на екрані (інваріант 13, AGENTS §7) | сесія 1, INC-036, 09.09.2026 | `catalog-service-scope.check` — своя 70, чужа відмовлена, фоліо без обʼєкта відмовляє своїм словом |
| Д48 | **Книга гостей і сума збору — по обʼєкту, бо подання робиться по закладу; «усі обʼєкти» лишається СКАЗАНИМ вибором.** `totalFees` множить ночі кожного обʼєкта на ЙОГО ставку і додає різні обʼєкти в одну цифру — вона неправильна за побудовою, а не за обставинами. Репозиторій приймає `PropertyScope` (не `propertyId?: string`), маршрут — `requestPropertyScope`, а не другий `searchParams.get`: старий маршрут читав `propertyId`, тоді як провайдер і решта екранів шлють `property_id` (NAMING §8), тож переведений «як усі» екран не змінив би НІЧОГО, а правка виглядала б зробленою | сесія 1, INC-037, 09.09.2026 | `registry-scope.check` — 40 в А, 105 у Б, 145 «усі» |
| Д49 | **`property_id=all` — це СКАЗАНЕ «усі обʼєкти», і жоден читач не має права прийняти його за ідентифікатор.** Провайдер області пише саме це слово в адресу вкладки, тож посилання з ним ходять по пошті. `getReport` мав власну вісь і підставляв `all` у `property_id = ?`: звіт віддавав нулі, і виглядало це як «за цей період нічого не було». Другий стан того ж роду — чужий обʼєкт: приватна вісь давала порожній звіт замість 404, тобто «немає даних» і «немає такого обʼєкта» були нерозрізненні (інваріанти 5 і 13). Обидва закриває один виклик `requestPropertyScope`; приватних осей у читачів більше немає | сесія 1, задача 4, 09.09.2026 | `reports-scope.check` — 2000/9900/11900 і 404 на чужий обʼєкт |
| Д50 | **Двері осі впізнаються за ЗМІННОЮ, ініціалізованою прямо з `propertyScopeFilter`, тож обгортка над ними коштує доведеності.** Читач, який будує фільтр функцією (`scopeSql(alias)`) або дописує його `query +=` окремим оператором, для гейта лишається «невизначеним»: він виглядає проскоупленим, а рахується як недоведений — і стеля не опускається, хоч вада полагоджена. Виміряно двічі: `reports.handlers` через обгортку дав 6 при стелі 5 (нове порушення на ПРАВИЛЬНОМУ коді), `registry.repo` через `query +=` лишався 5 із 5. Обидва пишуться однаково просто без обгортки, тож правило дешеве; альтернатива — вчити сканер зшивати оператор через кілька присвоєнь, і це його задача, а не читачів | сесія 1, задача 4, 09.09.2026 | `check-property-scope --strict`; reports 5 → 0, registry 5 → 2 |
| Д51 | **Рядок із нульовим `property_id` видно з КОЖНОГО обʼєкта, а не з жодного.** У `fin_folios` і `fin_folio_payments` колонка нульова, і рахунок без будинку — досяжний стан: подія, рахунок компанії. Звичайний `propertyScopeFilter` на такому рядку тихо ховає його з обох списків: рядок є, його не видно, у логах нічого — той самий звір, що рядок без орендаря (інваріант 12). Тому ці два читачі беруть `propertyOrSharedFilter`. Ціна вибору названа: рядок зайвий в обох списках; альтернатива — гроші, яких не бачить ніхто, і вона дорожча. Для `fin_cash_closings` питання не стоїть — там `property_id NOT NULL` | сесія 1, задача 5, 09.09.2026 | `invoicing-scope.check` — рахунок без обʼєкта порахований у 3 і 4, і стверджений на імʼя |
| Д52 | **Гість — по РАХУНКУ, гроші — по ОБʼЄКТУ. Одне питання, дві відповіді, і вони різні навмисно.** Рішення власника 09.09.2026 (В11) на питання, яке сесія 1 підняла двічі незалежно — в історії гостя і у фінансах. Підстава не в зручності, а в тому, ЧИЙ це факт. **Лояльність — властивість компанії:** `guests` має власний `organization_id` і не має `property_id`, гість фізично той самий у двох будинках, і постійний гість, який у другому готелі виглядає новим, — це втрачена знижка і образа. Тому `total_stays`, `total_revenue`, `last_booking_status`, фільтри «має майбутню бронь» і «має компанію» лишаються по рахунку, а 15 пар `guests.repo`/`gdpr.handlers`/`guests-export` пишуться `ALL_PROPERTIES` **із причиною словом** — поведінка не змінюється, змінюється те, що це СКАЗАНО. **Гроші — властивість будинку:** каса стоїть у ньому, ПДВ і звітність у кожного свої, а `getExpectedPayments` і `getPaidServices` віддають ПОРЯДКОВІ рядки з `guest_name` і `unit_name` — тобто оператор, обравши обʼєкт А, читав імена гостей і номерів обʼєкта Б. Три пари `finance/api/reports.handlers.ts` і `paid-services.handlers.ts` переводяться на `PropertyScope`; «усі обʼєкти» лишається окремим СКАЗАНИМ вибором у шапці. **Ціна вибору названа і мітигована:** портьє обʼєкта А бачить у картці гостя «5 перебувань, 12 000», з яких у його будинку могло не бути жодного, — тому число на екрані підписується «по всіх обʼєктах», інакше воно читається як своє і не перевіряється. GDPR від рішення не залежить і лишається по рахунку ЗА ЗАКОНОМ: стаття 15 — вивантаження однієї людини, стаття 17 — знеособлення по всіх бронях рахунку, і звуження зробило б «Erasure complete» неправдою | власник В11, 09.09.2026 | `guests.repo.ts`; `gdpr.handlers.ts`; `finance/api/reports.handlers.ts`; `paid-services.handlers.ts` |
| Д53 | **Ваучер гаситься лише в тому будинку, який його продав; список ваучерів на вкладці сайта — по обʼєкту САЙТА.** `gift_cards.property_id` — `NOT NULL`, тобто ваучер продав будинок і той будинок винен послугу. `activate` брав `reservation_id` із тіла й звіряв бронь лише з рахунком, тож ваучер обʼєкта А гасився проти броні обʼєкта Б: зобовʼязання одного будинку закривало виручку іншого, і переказу між ними немає в жодних книгах. Це наслідок Д52 «гроші — по обʼєкту», а не окреме рішення: ваучер — та сама каса, тільки видана наперед. Ваучер, дійсний у будь-якому будинку мережі, — інший продукт, і його немає: колонка `NOT NULL`. Для СПИСКУ область називає сайт (він належить одному будинку і сказаний точніше за перемикач у шапці), а без `site_id` — `requestPropertyScope`; чужий сайт і чужий обʼєкт — 404, а не порожній список. Унікальність КОДУ лишається по рахунку і сказана `ALL_PROPERTIES`: два будинки з однаковим кодом дали б рецепції два ваучери з різними номіналами на один папірець | сесія 1, INC-042, 09.09.2026 | `gift-card-scope.check` — 3/4/7, погашення чужої броні 404 і ваучер лишається непогашеним |
| Д54 | **Конфігурація фінансів отримує вісь обʼєкта у формі НУЛЬОВОЇ колонки: `NULL` = «на весь рахунок», старшинство — будинок перед рахунком.** Суть вирішив власник 09.09.2026 (В11, продовження Д52): «не можуть бути одні фінанси на 2 обʼєкти, бо в них різна бухгалтерія і різні правила по ПДВ і всьому можуть бути; тому і налаштовуватися це все має окремо». Форму вирішив контролер, і це технічне, не власникове: обовʼязкова колонка вимагала б вигадати будинок кожному наявному рядку шести таблиць і зламала б готель з одним обʼєктом, який про обʼєкти не думає, — а нульова лишає всі наявні рядки законними «спільними» і дає другому будинку сказати своє. Це той самий взірець, що Д51, `channel_rate_rules` і `invoices`. **Дві названі межі, без яких форма шкідлива.** Перша: для `fin_tax_rates` «спільне за замовчуванням» МОВЧАТИ не має права — ставка ПДВ, узята від рахунку для будинку в іншій країні, це інваріант 8 і інваріант 22 разом, і сума їде в документ держави; спільний набір застосовується, тільки якщо країна будинку збігається з країною рахунку, інакше названа відмова з назвою будинку і країни. Друга: `invoice_series` — `UNIQUE (organization_id, code)` не пускає двом будинкам мати свою серію з тим самим кодом, а дві окремі бухгалтерії з однією наскрізною нумерацією — діра в книгах; індекс перекладається на `COALESCE`, бо постґресівський `UNIQUE` не дедуплікує `NULL` (той самий прийом, що `idx_channel_rate_rules_row`)  **Уточнено 10.09.2026 виконанням (сесія 1), двома пунктами, і обидва — вимушені кодом, а не смаком.** (1) «Країна рахунку» дослівно нездійсненна: колонки країни в `organizations` НЕМАЄ — там `legal_address`, `vat_no`, `is_vat_payer`. Юрисдикцію в цьому коді визначає `properties.country`, і саме її читають `documentLanguage()` і `getOrgIdentity` (`FROM properties … ORDER BY created_at LIMIT 1`). Тому країна рахунку = країна НАЙСТАРШОГО будинку — наявний у сусідньому файлі взірець, а не нове правило; напрямок помилки безпечний: правило вміє лише ВІДМОВИТИ, ніколи не підставити чужу ставку. (2) Таблиць не шість, а вісім: `invoice_counters` і `invoice_periods` теж дістали вісь. Це не розширення обсягу, а умова другої межі: знявши `UNIQUE (organization_id, code)`, ми зробили б дві серії з одним кодом співмешканцями ОДНОГО лічильника і ОДНОГО замка місяця — другий будинок продовжував би чужі номери, а закриття місяця в одному заморожувало б документи іншого, і жодне з двох не сказало б ані слова. Лічильник і період належать РЯДКОВІ серії, не її коду| контролер за суттю власника, 10.09.2026; уточнення — сесія 1 | `docs/tasks/2026-09-09-INC-038-finance-config-property-axis.md`; задача 8 сесії 1; `docs/tasks/2026-09-10-INC-038-finance-config.report.md` |
| Д55 | **Готельєр із кількома рахунками — це ПЛАТФОРМНИЙ користувач роду `hotelier` з переліком членства, а не другий шлях у `app_users`.** Розвилку назвала задача 9; обидва боки законні, і вибір такий із трьох причин. **Перша — П21 називає її прямо**: «перемикання між готелями для готельєра — це не нова машинерія, а зняте обмеження з наявної». `platform_sessions.acting_organization_id` уже тримає рівно один рахунок, поза ним кожен маршрут відмовляє як чужому, вхід і вихід уже пишуться в аудит рахунку — усе це побудоване й перевірене, і другий шлях означав би другу копію того самого. **Друга — один пароль**: облікові дані лежать у `platform_users`, а `app_users.password_hash` того рядка знімається при заведенні членства (`--keep-local-password` лишає обидва СВІДОМО і каже про це вголос). Альтернатива — зробити `app_users.organization_id` нульовим і завести членство там — зачепила б кожну політику схеми, яка порівнюється саме з цією колонкою. **Третя — особа лишається рахунковою**: членство називає `app_user_id` (NOT NULL), тож усередині готелю людина діє СВОЇМ рядком — своє імʼя в журналі змін, своя роль, свої права. Ціна названа: у готельєра інший екран входу (`/app/platform/login`), і як він на нього потрапляє з головного екрана — окрема розвилка, названа у звіті й свідомо не вирішена мовчки | сесія 1, 10.09.2026 | `docs/tasks/2026-09-10-P21-hotel-switcher.report.md`; `src/core/auth/platform.ts`; міграція 0133 |
| Д56 | **Номер дебітора видає ГОТЕЛЬ зі свого діапазону — це не реєстраційний номер фірми, і не глобальне число платформи.** До 0140 `fin_folios.payer_debtor_no` заповнювався з `companies.business_id`, тобто два різні поняття жили під одним іменем: реєстраційний номер видає держава, і фірма без реєстрації — приватна особа, закордонний партнер — лишалась у картці дебітора порожньою, хоча борг у неї є. Тому `companies.debtor_no` з лічильником `organizations.next_debtor_no`, старт якого готель ставить із своєї старої системи. **Унікальність — на організацію, не глобальна**: діапазон 10000–12599 у першого клієнта це діапазон DATEV, а не випадкові числа, і глобальна унікальність відібрала б у другого готелю його звичну нумерацію (інваріант 3 і 20 одночасно). На Postgres це частковий унікальний індекс, тож вісь доводиться лише в `check:pg` з ДВОМА орендарями. Видача — одним інкрементом усередині транзакції, форма та сама, що в `allocateInvoiceNumber`, і коментар посилається на неї, щоб її не винайшли втретє. `MAX(debtor_no)+1` відкинуто свідомо: видалена фірма звільнила б свій номер, і наступна сіла б у чужу картку | кодова сесія 1 (фінанси), 10.09.2026 | 0140; `companies.repo.ts` `allocateDebtorNo`; `debtor-no.check.ts`; `docs/research/winhotel/CORE-GAPS.md` §2 |
| Д57 | **Фоліо може належати ПЛАТНИКУ, а не броні — і незмінним лишається борг кожної броні.** Готель, який продає фірмам, виставляє ОДНУ фактуру за пʼять перебувань (`RECHNUNG.VERK_NR` у Winhotel); у нас це було неможливо: `moveCharges` відмовляла, щойно рядок і цільове фоліо належали різним броням. Відмова стояла НАВМИСНО — «splitting who PAYS must not change what is OWED», — тож знята вона не мовчки: на її місце стало твердження про суму, і воно в ОДНІЙ сцені з переносом (друге без першого — стара поведінка, перше без другого — тихо втрачені гроші). Нова умова названа прямо: рядок їде на фоліо ПЛАТНИКА, якщо платник той самий, і стара причина лишилась у коментарі поруч. **Вимір при цьому виправив саму передумову**: стара відмова спрацьовувала лише коли бронь названа з ОБОХ боків, тож на будь-яке фоліо без броні рядки їхали й до правки — без жодної перевірки платника. Тобто правка не знімає сторожа, а ставить його там, де його не було | кодова сесія 1 (фінанси), 10.09.2026 | 0140 (`fin_folios.company_id`); `folio.repo.ts` `moveCharges`; `payer-folio.check.ts` |
| Д58 | **`due_date` ВИВОДИТЬСЯ з умов фірми, а «сплачено» = фактура мінус платежі по ній.** Строк, введений руками, розходиться з `companies.payment_terms_days` і розходиться мовчки — до першого прострочення про це не дізнається ніхто; тому він рахується при виписці з дати ПОСЛУГИ документа й фірми, а гість без фірми строку не має взагалі (рахунок на виїзді оплачують на місці). «Сплачено» — не прапорець: прапорець розходиться з платежами при першому ж частковому внеску. Арифметика та сама, що в `openGross`, і лишається однією. **Нагадування (Mahnung, рівень і дата) свідомо НЕ роблено**: це модуль «дебіторка», не ядро (інваріант 22) — записано напрямом у ARCHITECTURE §8 | кодова сесія 1 (фінанси), 10.09.2026 | `folio.repo.ts` `dueDateFor`, `openInvoicesOfCompany`; `companies/api/kernel.ts` `companyPaymentTerms` |
| Д59 | **Умов оплати немає — і відповідей ДВІ, межа по РОДУ фоліо.** Інваріант 8 забороняє мовчазний дефолт, але два випадки різні за ціною помилки (інваріант 29). **Фоліо ПЛАТНИКА** існує рівно тому, що хтось назвав фірму платником, тобто заводить її боржником; боржник без строку оплати — недороблене налаштування, а виписування останній момент, коли на нього хтось дивиться, тож тут **названа відмова** («set them before invoicing»), і додатковий довід у тому, що нагадування (Mahnung) інакше рахувало б прострочення від `NULL`. **Звичайне фоліо стою**, чия бронь має фірму без умов, — це «оплата на місці», і воно НЕ блокується: портьє на виїзді не має впертись у налаштування довідника; строку немає, і це відсутність, а не дефолт. Підставити «14 днів» не можна в жодному з двох — вигаданий строк це вимога грошей на дату, якої готель із фірмою не домовляв, а виправити виписаний документ можна лише сторно (той самий довід, що в інваріанті 17). Обидва напрямки осі доведені зломом: `payerFolio = true` червонить звичайне фоліо, `= false` — фоліо платника | кодова сесія 1 (фінанси), 10.09.2026 | `folio.repo.ts` `dueDateFor`; `payer-folio.check.ts` §6; задача 12 §А |
| Д60 | **Старим фоліо `company_id` НЕ заповнюється — ні міграцією, ні за схожістю.** 0140 додала колонку без зворотного заповнення, тож рахунок, який оголосив себе фірмовим ДО неї (`payer_kind = 'company'`, `company_id IS NULL`), не потрапляє у «відкриті фактури фірми» — читач фільтрує саме по `company_id`. Спокуса зіставити його з довідником по `payer_debtor_no` **відхилена**: цією ж хвилею зміст поля змінився (був реєстраційний номер держави, став номер, який видає готель), і зіставлення за полем зі зміненим змістом привʼязало б фактури не до тієї фірми — а виявилось би це на ВИДАНОМУ документі, який виправляють лише сторно. `payer_name` не годиться з тієї ж причини, з якої 0140 і завела колонку: текстовий знімок не порівняти. **Ціна вибору названа: старі фірмові рахунки лишаються поза списком фактур фірми, доки фірму на них не назве людина.** Вона прийнятна, бо ВИМІРЯНО: такий рядок не «невидимий з обох боків» — він є в загальному списку рахунків готелю (`listFolios(undefined, scope)` через `propertyOrSharedFilter`, Д51); гроші видно, не видно тільки належності. Привʼязати не до тієї фірми гірше, ніж не привʼязати зовсім. Інструмент для рішення — `deploy/count-payer-folios.sh`, який друкує самі рядки з датою: створене до 0140 це «колонки тоді не існувало», після — справжній дефект | кодова сесія 1 (фінанси), 10.09.2026, за виміром контролера на дампі бети | `deploy/count-payer-folios.sh`; `folio.repo.ts` `openInvoicesOfCompany`; DEPLOY.md §«Перед першим справжнім клієнтом» |
| Д61 | **Спосіб оплати — РЯДОК довідника готелю, і клас стоїть НАД ним, а не замість нього.** Чотири класи (`cash | card_terminal | transfer | voucher`) лишаються в CHECK, бо ними живе КОД: готівка й термінал ідуть у касову книгу і під §146a AO, переказ ні. Але вони відповідають на «як із цим поводитись», а не на «чим саме заплатили»: у живого готелю другий список має 24 рядки, і «картка» там — кілька способів із РІЗНИМИ рахунками обліку, які бухгалтер розрізняє в книгах. Скласти їх у чотири кошики означає втратити `KONTONR`. Тому `fin_payment_methods` несе `ledger_account`, `settles_to_debtor` (оплата «на дебітора» грошима не є — вона переносить борг на фірму, прямий місток до Д56) і `is_active`; платіжка має і `method` (клас), і `method_id` (рядок). **Два джерела одного факту неможливі за побудовою**: `recordPayment` бере клас ІЗ РЯДКА, а названий поруч клас, що йому суперечить, — відмова; інакше рядок «Visa» з класом `card_terminal` і `method='transfer'` поруч дав би платіж, якого каса не бачить, а бухгалтерія числить карткою. **Засів чотирьох стандартних рядків робить МІГРАЦІЯ**, не код при першому відкритті екрана: інакше готель, який туди не заходив, дістав би платіж, що вказує в нікуди; зворотне заповнення тут можна (на відміну від Д60), бо зміст `method` не мінявся. **Назв засів не пише** (`name IS NULL` = «стандартна назва класу»): міграція не може вгадати мову готелю (О5), а чеське слово німецькому портьє гірше за нічого. **Видалити використаний спосіб не можна** — названа відмова з ЧИСЛОМ платежів; невикористаний видаляється (рядок із помилкою в назві має зникати, інакше довідник обростає сміттям, яке ніхто не чіпає); `is_active` прибирає з пропозицій, не чіпаючи наявних платежів. `ON DELETE RESTRICT` тримає це і в базі, але відмова названа: база сказала б «порушення зовнішнього ключа», а портьє має прочитати, що робити | кодова сесія 1 (фінанси), 10.09.2026 | 0141; `payment-methods.repo.ts`; `payment-methods.check.ts`; CORE-GAPS п. 7 |
| Д62 | **Знижка на послугу — окремий рядок із відʼємною сумою, який УСПАДКОВУЄ ставку свого рядка.** Зменшити суму рядка «на місці» простіше і гірше двома боками одразу: у книгах лишається одне зменшене число без сліду знижки (бухгалтер не бачить, ЩО коштувало повну ціну), а в документі держави сума за ставкою перестає дорівнювати сумі рядків цієї ставки. Друга половина — саме те, що тут легко втратити: «взяти якусь ставку» виглядає нешкідливо, бо ПІДСУМОК при цьому правильний, і помилка вилазить лише в декларації. Для проживання цієї вади немає за побудовою (`lodging_discount_percent` застосовується після відділення сніданку і лишається в тому ж рядку); для послуг не було чим її уникнути. Джерело робить так само: знижка — окрема послуга з власним `STS` (`RABATT` під 7 %, `RABATT_19` під 19 %, різні рахунки обліку). «Від чого знижка» — КОЛОНКА (`discount_of_item_id`, `ON DELETE CASCADE`), а не текст опису: сторно батьківського рядка інакше лишає знижку, яка вказує в нікуди (клас Р10.14 всередині фоліо). Відмови: знижка більша за рядок (це вже не знижка, а виплата), знижка зі знижки (число, якого ніхто не називав), «відсоток і сума разом». Пакетний поділ (`PREISSPLITTING`) НЕ роблено — пакетів у нас немає, і механізм під неіснуючу сутність вгадував би її форму | кодова сесія 1 (фінанси), 10.09.2026 | 0142; `folio.repo.ts` `discountCharge`; `service-discount.check.ts`; CORE-GAPS п. 5 |
| Д63 | **Образ несе `scripts/` і `src/` ЦІЛКОМ — інструменти оператора живуть у ньому, а не поруч.** Обрано з трьох шляхів, і два відкинуто виміром. **«Запускати з чекауту на сервері»** — ні: база справді досяжна з хоста (`docker-compose.yml` публікує її на `127.0.0.1:${PG_PORT}`, тож коментар у `Dockerfile` про «loopback усередині мережі» застарів і виправлений), але на сервері **немає `node_modules`** — усе збирається в Docker (`to-postgres.sh:201`), тож node-скрипт із хоста не запуститься. **«Курований мінімум `src/`»** — ні, попри те що мінімум ВИМІРЯНО (обхід графа імпортів до нерухомої точки: 43 модулі `src/` на три інструменти, 44 на сам `apply-hotel`): перелік — це друге джерело істини, і перший же новий `import` робить його неправдивим, а дізнаємось ми про це наступним ЧЕРВОНИМ ДЕПЛОЄМ, як 10.09. 18 МБ вихідників поруч із `better-sqlite3` на 13 МБ — ціна, яку видно і яка не росте несподівано. Разом із `src/` їде `tsconfig.json` (з нього `module-aliases.mjs` бере `paths`) і **рід модуля для вихідників**: у корені образу лежить `package.json` від Next standalone, а не наш (наш `type` не заданий узагалі), тож `.ts` читалися як CommonJS і кожен імпорт падав — знято прогоном у відтвореній файловій системі образу, не міркуванням. Дірка була ширша за один скрипт: у образі запускають **сім** інструментів, і жодного там не було; мовчало це тому, що `apply_hotels` пропускав файли на `_`, а провал `seed-demo-stays` деплой навмисно не валить — єдиний крок, що «працював», був тим, чию відмову ковтали. Тримає `check-image-tools --strict` | кодова сесія 1 (фінанси), 10.09.2026 | `Dockerfile`; `scripts/check-image-tools.mjs`; задача 15 |
| Д64 | **Невдале накладання файла готелю НЕ відкочує середовище — і має лишатися червоним.** Питання поставив контролер, і вимір відповів на нього інакше, ніж припускала обидві сторони: `deploy.sh` **уже** нічого не відкочує. Контейнер піднято до цього кроку, `apply_hotels` виконується після здоровʼя і димових перевірок, а його код повертається в `HOTELS_OK` і лише в `exit` наприкінці. Тобто 10.09 бета лишалась живою і відповідала — заблоковано було рівно **прод**, вартою «prod ships only what beta has RUN». **І це правильно, тому нічого не міняємо.** Готель, який не відповідає своєму файлу, — це справжня розбіжність: 31 номер, 44 послуги, ставки ПДВ і серія фактур першого клієнта. Зробити крок «мʼяким» (`\|\| echo`, як у `seed-demo-stays`) означало б, що прод поїде поверх ненакладеного файла — а саме мовчазний провал `seed-demo-stays` і ховав цю дірку місяцями. Ціна вибору названа: червоний деплой беты блокує прод до полагодження, і це дешевше за готель, налаштований наполовину | кодова сесія 1 (фінанси), 10.09.2026, за виміром на прохання контролера | `deploy/deploy.sh` `apply_hotels`; `.github/workflows/deploy.yml` «prod ships only what beta has RUN» |

---

## Обʼєкт і налаштування

Блок 5a: номери, зручності, валюти. Префікс `О` — сесія 3.

| № | Рішення | Ким | Де |
|---|---|---|---|
| О1 | **Вид із вікна — вільний текст на НОМЕРІ (`units.view`), а не довідник.** Вид конкретного номера («у двір», «на липу») готель пише словами: довідник змусив би вигадувати код для одиничного випадку і застаріє на першому ж клієнті з нетиповим фондом. Стандартизований вид, який їде в OTA і на сайт, — це ЗРУЧНІСТЬ типу з категорії VIEW (0111), тобто інша сутність з іншим життям: вона спільна для всіх номерів типу, перекладається і мапиться на канал. Дві різні речі не змішуються в одну колонку | кодова сесія 3, 06.09.2026 | 0110; `units.repo.ts`; ARCHITECTURE §8 «Блок 5a» |
| О6 | **Ціна, бронь, фоліо й документ — у валюті ОБЛІКУ; чужа валюта буває лише показом із підписом «≈ за курсом».** Друга валюта в рядку означала б, що сума залежить від дати читання, а документ — від курсу, який змінився після виписки. П19 читається саме так: «кілька валют із фіксованим курсом НА САЙТІ» | кодова сесія 3, 06.09.2026 | `core/currency.ts` `displayRates()`; `currency.rates.check.ts` |
| О7 | **Колонки `fixed_rate` в `organization_currencies` НЕ заведено — всупереч букві задачі Блоку 5a, і ось чому.** Задача просила 0112 з `fixed_rate NUMERIC(18,8)`, який «перемагає `finance_exchange_rates` для показу». Але ARCHITECTURE §2.2.1 уже містить протилежне рішення з причиною («колонка курсу поруч зі списком валют — друге джерело істини, яке розійдеться тихо»), і його стереже `currency.check`. Те саме досягається без другої колонки: `rate_source` уже розрізняє `manual` і `cnb`, курс живе в `finance_exchange_rates` з історією, а «фіксований» означає, що прохід ČNB такі валюти **пропускає** (раніше він писав чотири зашиті валюти кожному готелю, хоч би що той оголосив, — тобто «джерело курсу» не значило нічого). Показ читає одні двері `displayRates()`. Якщо контролер наполягає на колонці — це свідоме скасування §2.2.1, і тоді разом із нею мусить піти гейт | кодова сесія 3, 06.09.2026 | ARCHITECTURE §2.2.1; `core/fx/cnb.ts`; задача Блоку 5a §2.3 |
| О3 | **Зручності живуть у `properties`, а не окремим модулем.** Без обʼєкта і типу номера зручність нема на що вішати — власного життєвого циклу в неї немає; а новий модуль народжується зі стелею меж НУЛЬ (`check-boundaries`), тож кожен читач — гостьова сторінка, віджет, майбутній канал — став би пробоєм у той самий день. Двері — фасад `@properties` | кодова сесія 3, 06.09.2026 | `properties/data/amenities.repo.ts`, `properties/domain/amenity-catalog.ts` |
| О4 | **У зручності є ОБЛАСТЬ (`scope`: обʼєкт / тип номера / обидва), і вона перевіряється в писачі, а не лише на екрані.** Ліфт буває в будинку, фен — у номері; без області матриця призначення однакова обабіч і готель повісить ліфт на двомісний. Джерело форми (Hoteliera) розрізняє це позначками `L`/`R`/`L+R`. Перевірка стоїть біля запису, бо ідентифікатори приходять із тіла запиту: «екран не покаже» — це зручність, а не заборона | кодова сесія 3, 06.09.2026 | 0111; `amenities.repo.check.ts` сцена «область тримає» |
| О5 | **Стартовий каталог (12 розділів, 61 позиція) — у КОДІ, а призначення — у базі.** Це словник продукту, яким розмовляють сайт, гостьова сторінка і канал; коди стабільні, назви приходять мовою готелю і перейменовуються. Інваріант 20 не порушено: у коді немає нічого, що належить конкретному клієнту. Каталог досівається при першому читанні (`ensureAmenityCatalog`) — і для готелю, заведеного до 0111, і для нового: заведення готелю його НЕ сіє, бо ядро не має знати про модуль навіть через фасад (перша редакція кликала `@properties` з `core/provisioning.ts` і зламала `provision-org.mjs`, який біжить голим node без резолвера аліасів — побачив це CI). Міграція теж не годиться: вона мусила б вгадувати мову кожного просто в SQL. **Мов у словнику СІМ — рівно ті, що приймає `provision-org.mjs` (`uk en de cs pl nl fr`), 07.09.2026.** Перша редакція мала чотири (uk, cs, de і `en` запасною) як свідоме обмеження; власник його зняв, і причина в тому, що ціна зволікання тут нерівномірна: **засів одноразовий**, тож польський готель, заведений до перекладу, лишився б з англійськими рядками доти, доки хтось не перейменує їх руками у своїй базі. Запасною `en` лишається для мови ПОЗА сімкою (італійський готель бачить «Elevator», а не «Ліфт»): англійська для нього — незнайома мова, українська — мова, якої в його світі немає взагалі. Повноту (73 рядки × 7 мов) тримає `amenities.repo.check`; наступна мова додається рядком у `amenity-catalog.ts`, без міграції | кодова сесія 3, 06.09.2026 | `amenity-catalog.ts`; `provisioning.ts`; ARCHITECTURE §3.1 |
| О2 | **Мережа береться ОДНИМ рівнем — назва й пароль разом або жодного, і це правило діє на ВСІХ трьох рівнях** (обʼєкт → тип → номер). Резолвер не змішує рівні всередині одного факту: назва мережі типу з паролем обʼєкта — це підключення, якого не буде, і дзвінок на рецепцію о другій ночі. Той самий довід, що в інваріанті 17: половина відповіді гірша за її відсутність. 06.09 правило стояло лише на рівні номера, а рівень типу лишався двома незалежними `||` — виправлено разом із гейтом (рецензія раунду 4, п. 2.2) | кодова сесія 3, 06.09.2026, доповнено 07.09 | `guests/data/guest-portal.repo.ts`; гейт `guest-page-levels.check.ts` |
| О12 | **«Називає обʼєкт» = ОБМЕЖУЄ обʼєктом, а не згадує `property_id` у фільтрі.** Перше означення гейта зараховувало названою й вісь ОРЕНДАРЯ — `JOIN properties p ON p.id = u.property_id WHERE p.organization_id = ?`, тобто «усі обʼєкти цього рахунку», рівно ту форму, з якої почався INC-029. Різниця виміряна: 137 пар у 60 файлах, базлайн 370 → 507, і серед них справжня діра — чекліст покоївки показував брудні номери всіх обʼєктів рахунку. Ціна звуження — одноразовий перерахунок базлайна на трьох гілках; здобуток — сто тридцять сім місць, які рахувались захищеними і не були | контролер, 09.09.2026, за звіркою сесії 3 | `scripts/lib/property-scope-scan.mjs`; `docs/tasks/2026-09-09-INC-029-tenant-join-list.md` |
| О13 | **Канонічне імʼя осі обʼєкта в запиті — `property_id`; `?property=` лишається застарілим синонімом, який читається, але не пишеться.** Обидва вже живуть у коді: `property_id` шлють fetch-и чотирнадцяти екранів, `property` пише провайдер в адресу вкладки, і посилання з ним уже пересилають колегам — тому синонім не викидається, а читається. **Порожнє значення параметра означає «усі обʼєкти» СКАЗАНО, відсутній параметр — «нічого не сказано»**: два різні стани, і їх злиття робило б «покажи всі» невідрізнимим від «памʼятай мій вибір» | контролер, 09.09.2026 | `core/property-scope.ts` `requestedPropertyParam()`; NAMING §8 |
| О14 | **Область обʼєкта в даних — ТИП `PropertyScope`, а не `property_id?: string`; «усі обʼєкти» пишеться словом.** `undefined` сьогодні тихо означає «всі», і це значення, яке забувають, а не обирають — той самий клас, що інваріант 8 (публічний маршрут не має мовчазного дефолту) і 13 (не знайшли — відмовляємо). Читач scoped-таблиці приймає `PropertyScope` і **не має значення за замовчуванням**; `ALL_PROPERTIES` лишається законним для зведеного звіту, синку каналу й консолідованої шахматки, але видно його грепом і поруч стоїть причина. Двері з запиту — `requirePropertyScope(raw)`, симетрично до `requirePropertyId` для писачів | кодова сесія 3, 09.09.2026 (INC-029) | `src/core/property-scope.ts`; ARCHITECTURE §4.3.1 | _(номер змінено з О9 на О14 при злитті 09.09.2026: О9 уже займало рішення від 08.09; правило — пізніший рядок дістає новий номер)_
| О15 | **Базлайн храповика `check-property-scope` лежить ОКРЕМИМ файлом даних, а не мапою всередині гейта.** `audit-by-id-scope` і `check-boundaries` тримають стелі в тілі гейта, і для одного автора це правильно. Блок INC-029 роблять три сесії паралельно, кожна опускає стелю СВОЇХ файлів, а логіка гейта належить одній із них — спільна мапа всередині чужого файла означала б або редагування чужого файла (заборонено `docs/tasks/README.md`), або три копії гейта. Дані окремо від коду: сесія опускає рядок у JSON, гейт лишається за господарем, git зливає рядки сам | кодова сесія 3, 09.09.2026 | `scripts/property-scope-baseline.json`; `docs/tasks/README.md` | _(номер змінено з О10 на О15 при злитті 09.09.2026, з тієї ж причини)_
| О11 | **Спільна фікстура осі обʼєкта живе в ЯДРІ й одна на три сесії — 5 і 7 номерів, не 2 і 2.** Друга половина інваріанта 26 вимагає, щоб очікуване число було арифметично несумісне з альтернативним прочитанням: 2 і 2 сумісні з чим завгодно, а на 5 і 7 читач, що забув вісь, віддає 12 — число, яким не є жоден обʼєкт. Три власні фікстури розійшлися б числами, і «очікували 5, отримали 12» в одній сесії означало б не те саме, що в другій. Сам засів **відмовляє**, якщо будь-яка пара значень осі зрівнялась: той, хто «підрівняє» 5 і 7, побачить це в ту ж секунду, а не через місяць у вигляді втраченого твердження | кодова сесія 3, 09.09.2026 | `src/core/fixtures/two-properties.ts`; реєстр осей `check-fixture-axes.mjs` |
| О8 | **Пароль мережі й код замка НОМЕРА виходять зі СПИСКІВ НОМЕРІВ лише під `manage_properties` — з усіх, а не з названого.** Списків два: `/api/units` і номери всередині `/api/properties/[id]`. Маршрут лишається під `withActor` — його читають екрани зміни (чекліст покоївки, календар, картка броні), і 403 на весь список зламав би чотири робочі екрани заради двох полів, яких вони не показують. Тому список той самий, але без секретів; повний бачить лише той, хто ці поля й редагує. Роль `housekeeper` має рівно одне право (`nav:dashboard`), тож до правки код замка кожного номера лежав у телефоні кожного, хто ввійшов — і `lock_code` був там ще до Блоку 5a | кодова сесія 3, 07.09.2026 (рецензія раунду 4, п. 2.1) | `properties/data/units.repo.ts` `listUnits(…, { secrets })`; `check-isolation` — вісь ролі **Перша редакція закрила рівно `/api/units`** — той, про який питало твердження, — і покоївка далі отримувала обидва поля з картки обʼєкта, бо там номери бралися `SELECT u.*` (рецензія раунду 6, п. 3.1). Тому колонки номера тепер виходять із модуля ОДНИМ закритим переліком (`unitColumnsSql`): зірочка віддає те, чого в ній ще немає, тобто наступну колонку міграції — тим самим днем і мовчки. Гейт `check-isolation` називає обидва маршрути, і додатково стверджує, що з переліку не зникло те, чим картка живе: помилка в бік «занадто вузько» має бути видима. |
| О9 | **Рід житла (`property_type`) — колонка обʼєкта без дефолту, перелік — вендорський.** Вендор пише про це поле «affects billing», тобто воно вирішує, скільки готель заплатить: «hotel» за замовчуванням виставив би кемпінгу чужий тариф. Тому значення називає готель (інваріант 20), а не названо — `NULL`, і каталог у канал не їде з названою відмовою замість вигаданого роду. Перелік 22 значень узятий дослівно з документації вендора і лежить одним списком у ЯДРІ (`core/lodging-kinds.ts`; спершу він стояв у `channels/ui/property-types.ts`, і переїхав рішенням К10 — базовий писач обʼєкта не має відмовляти за списком вендора каналів готелю, який каналів не купував); CHECK у базі немає свідомо — чужий перелік може зрости, а міграція, що відстала, відмовляла б на валідному типі. На екрані значення показуються вендорськими кодами без перекладу, як ISO-код валюти | кодова сесія 3, 08.09.2026 | 0113; `properties.repo.ts`; `catalog-sync.check` |
| О10 | **Часовий пояс лишається на ОРГАНІЗАЦІЇ, а при заведенні виводиться з країни і показується на підтвердження.** На `properties` він не переїхав: `organizations.timezone` уже читає весь «день готелю» (`todayFor`) і вже питає екран Налаштування → Загальні, тож друга колонка була б другим джерелом того самого факту; оператор кількох обʼєктів у різних поясах — рядок у LATER поруч із `pricing_advanced`. А мовчазний дефолт `'Europe/Prague'` при заведенні прибрано: пояс вирішує, де проходить межа доби, і канал торгує саме датами заїзду, тож для українського готелю празький пояс — це зсунуті броні без жодної помилки в логах. Тепер пояс або називають, або виводять із країни (лише країни з ОДНИМ поясом), і `provision-org.mjs` друкує висновок словами «← виведено з країни» з проханням звірити; ні пояса, ні країни — названа відмова, як із валютою | кодова сесія 3, 08.09.2026 | `core/hotel-day.ts` `timezoneForCountry`; `provisioning-timezone.check` |
| О300 | **Згода GDPR живе на ОСОБІ, і версія тексту обовʼязкова.** `guest_registrations.consent_*` лишається і значить інше — згоду на ЦЬОМУ перебуванні, частину Meldeschein; нова пара `guest_consents` + `consent_texts` — про людину, і переживає всі її брони, включно з нульом броней. Це не покращення на потім, а умова ІМПОРТУ: у Winhotel `ADR_DATENSCHUTZ` — 61 305 рядків на адресах, і згоді людини без броні в нашій системі не було куди лягти. Полагодити після імпорту не можна — даних уже не буде. Версія без тексту не доводить нічого («погодилась на щось, а на що — невідомо»), тому писач звіряє версію з довідником НАЗВАНОЮ відмовою; ключем це не звʼязано свідомо — текст існує в кількох мовах, а згода дається на редакцію, не на переклад. **Відкликання — не видалення рядка**: наглядачеві показують пару дат, а рядка, якого немає, не досить в обидва боки | кодова сесія 3, 10.09.2026 (TASK 9) | 0300; `guests/data/guest-consents.repo.ts`; `guest-consents.check.ts` |
| О301 | **Колонки `guests.marketing_opt_in` НЕ заведено — всупереч букві CORE-GAPS п. 11, і ось чому.** Там вона названа «як похідне», і саме тому її немає: колонка, яку хтось мусить не забути оновити, — це друге джерело тієї самої правди, а два джерела розходяться і відповідає те, яке спитали останнім. «Чи можна цій людині слати листи» рахується з журналу (`marketingAllowed`) щоразу. Ціна — джойн на екрані розсилки; ціна протилежного — лист тому, хто відмовився. Той самий довід, що О7 про `fixed_rate` | кодова сесія 3, 10.09.2026 (TASK 9) | `guest-consents.repo.ts`; сцена, твердження 5 |
| О302 | **При злитті гостей ВІДКЛИКАННЯ ПЕРЕМАГАЄ ЗГОДУ, завжди.** Дві людини, у однієї «маркетинг: так», у другої «ні»: обʼєднати в «так» означало б зробити злиття способом обійти відмову. Обрано суворіше — інваріант 8 («жодного тихого дефолту») тут коштує грошей і довіри. Рядки згод при цьому не зникають: доказ лишається, змінюється лише чинність | кодова сесія 3, 10.09.2026 (TASK 9) | `guest-merge.repo.ts`; `guest-merge.check.ts` твердження 7 і його ПАРА 8 |
| О303 | **Ланцюгів `merged_into` не буває за побудовою: злиття ПЕРЕНАЦІЛЮЄ старі посилання, а зливати вже злитого — відмова.** Альтернатива — розкручувати A→B→C у кожному читачі, тобто покладатися на те, що всі читачі мають запобіжник на циклі. Тут обрано так, щоб читача взагалі не було про що питати: `merged_into` завжди веде на живого ОДНИМ кроком. Злитий рядок при цьому не видаляється — посилання на нього лежать у виданих документах і в чужих системах, і «такого гостя немає» гірше за «це та сама людина» | кодова сесія 3, 10.09.2026 (TASK 9) | `guest-merge.repo.ts`; сцена, твердження 5 і 6 |
| О304 | **`fin_folios` при злитті гостя НЕ переноситься — свідомо, і це записано в коді, а не забуто.** Дві причини, друга важливіша: це тека модуля фінансів (сесія 1), і рядок від цього не ламається — `merged_into` лишає його розвʼязним до живої людини. Повноту рішення стереже `check-guest-merge-tables`: кожна таблиця з `guest_id` мусить бути або в списку перенесення, або названа «не переносимо» РАЗОМ ІЗ ПРИЧИНОЮ. Сказати «ні» дозволено, промовчати — ні | кодова сесія 3, 10.09.2026 (TASK 9) | `guest-merge.repo.ts` `NOT_MOVED`; `scripts/check-guest-merge-tables.mjs` |
| О305 | **Ключ походження — один кваліфікований рядок `external_ref`, а не пара колонок і не таблиця мапи.** CORE-GAPS п. 12 припускав, що місце вже є (`import_entity_mappings`); виміряно — такої таблиці в схемі немає, а з 24 сутностей імпорту місце мали 2, причому `reservations.external_uid` зайнятий iCal-синком і каналами і навмисно не є ключем. Значення `winhotel:adressen:1234` тримає систему й таблицю всередині: пара «система» + «ключ» розходиться (ключ без системи нічого не означає), а `guests.source` уже має інший зміст. Ідемпотентність повторного імпорту тримає **частковий UNIQUE-індекс із орендарем**, а не уважність імпортера | кодова сесія 3, 10.09.2026 (TASK 10) | 0301; `import-origin.check.ts` |
| О306 | **Пошта САМА ПО СОБІ не є ознакою дубліката — усупереч `IMPORT-PLAN.md` §2.7.** План каже «однаковий `E_MAIL` (не порожній) → один гість»; готель у Ґрайці приймає сімʼї, і подружжя з дітьми живуть під однією поштою, тож за цим правилом вони стали б ОДНІЄЮ людиною — незворотно, і виявилось би це на видачі чужої фактури. Пошта працює лише в парі з іменем. Ознаки за силою: документ → пошта+імʼя → телефон+імʼя → імʼя+дата народження → саме лише імʼя (останнє ніколи не «майже напевно»). Відстані редагування немає навмисно: на німецьких прізвищах вона дає сотні хибних пар, і без вимірювання її заводити заборонено | кодова сесія 3, 10.09.2026 (TASK 10) | `guest-duplicates.repo.ts`; сцена «сімʼя під однією поштою — не кандидати» |
| О307 | **Згода, чиєї редакції тексту в базі немає, іде в КАРАНТИН, а не під нинішній документ.** Winhotel зберігає лише поточний текст трьох пунктів, а `ADRESSEN.DS_VERSION` у людей різний; підставити нинішній текст означало б підпис під документом, якого людина не бачила. Три роди карантину — `unknown_kind` (пункт не названо людиною), `no_version` (адреса не знає редакції), `text_not_in_database` (редакція інша). Мапи «пункт → наш рід» і «мова → локаль» приходять ЗЗОВНІ: у базі немає нічого, з чого їх вивести без здогаду, а невідоме значення не дістає замовчування (інваріант 8). **Виміряно принагідно:** редакція лежить на `ADRESSEN`, а не в довіднику пунктів — колонки версії `DATENSCHUTZSTAM` не має взагалі | кодова сесія 3, 10.09.2026 (TASK 10) | `guests/domain/consent-import.ts`; `consent-import.check.ts` |
| О308 | **«Це різні люди» — ПАРА, а не напрямок, і порядок тримає `CHECK`, а не домовленість у коді.** Злиття має бік (кого лишили, кого злили), відмова не має: «Іван і Іван — різні люди» однакове в обидва боки. Якби рядки писались як прийшли, у таблиці зʼявились би (A,B) і (B,A) як дві різні відмови, і шукач, що питає одну форму, показав би пару, яку вже відхилили. `CHECK (guest_low_id < guest_high_id)` робить другу форму неможливою — доведено зломом: скасування впорядкування валить сцену відмовою БАЗИ, а не твердженням | кодова сесія 3, 10.09.2026 (TASK 11) | 0302; `guest-duplicates.check.ts` |
| О309 | **Хто злив і коли — КОЛОНКАМИ на `guests`, а не журналом подій.** Це факт самого рядка, буває рівно один раз, і живе поруч із `merged_into`. Окремий журнал розділив би одну правду на два місця, які розходяться при першій же чистці: `merged_into` лишається назавжди, а журнал ротують — і тоді «ким став цей рядок» знає одна таблиця, а «хто це зробив» — інша, вже порожня. Сусідній `booking_activity_log` не годиться й технічно: `reservation_id NOT NULL`, а злиття гостя броні не має | кодова сесія 3, 10.09.2026 (TASK 11) | 0302; `guest-merge.repo.ts` |
| О310 | **Ретенція НЕ чіпає `guest_consents` — і це рішення, а не пропуск.** Таблиця за побудовою не містить персональних даних: організація, посилання на рядок, рід, версія, джерело і дві дати; ні імені, ні пошти, ні IP — на відміну від `guest_registrations`, яку ретенція саме тому й ВИДАЛЯЄ (там `consent_ip`). Після знеособлення рядок каже «запис такий-то погодився тоді-то на редакцію таку-то» і не називає нікого — це рівно той доказ, який показують наглядачеві. **Згоду при цьому не відкликають:** `revoked_at` стверджував би, що людина її відкликала, а вона цього не робила — підробка юридичного запису заради охайності. Вибирати між підробкою і втратою доказу не довелося: пошту стерто, тож розсилка й так неможлива. Ціна рішення — рядок живе довше за термін зберігання профілю, і це свідомо | кодова сесія 3, 10.09.2026 (TASK 11) | `retention-tables.ts`; `consent-retention.check.ts` |
| О311 | **Повнота ретенції стережеться реєстром, а не пильністю.** Знеособлення перелічує таблиці поіменно, тож нова таблиця з даними гостя лишається поза ним МОВЧКИ — саме так туди не потрапили `guest_consents` і `consent_texts`, новіші за ретенцію. Це клас «функція зникла»: нічого не падає, персональні дані просто переживають термін. Тепер множина таблиць із `guest_id` мусить дорівнювати об'єднанню трьох кошиків реєстру, і сказати «лишаємо» дозволено лише РАЗОМ ІЗ ПРИЧИНОЮ | кодова сесія 3, 10.09.2026 (TASK 11) | `scripts/check-retention-tables.mjs` |

---

## Застосунки

Блок «Застосунки» (`docs/tasks/2026-09-09-block-apps.md`), сесія 4. Префікс `З`.
З1–З9 — власник, 09.09.2026; далі — рішення сесії.

| № | Рішення | Ким, коли | Обґрунтування |
|---|---|---|---|
| З1 | **Застосунок — окрема одиниця, буває безкоштовна і платна; пізніше — відкритий код**, щоб сторонні писали свої. `winhotel_import` — безкоштовний | власник, 09.09.2026 | `core/apps.ts` — записи читаються як маніфест: `pricing: free \| paid \| included` |
| З2 | **Каталог показує і те, чого в готелю ще немає** — стан «скоро» і кнопка «хочу», яка рахує попит | власник, 09.09.2026 | `app_wishes` (0400), `POST /api/settings/apps/:id/wish`, лічильник — лише постачальнику |
| З3 | **«Застосунки» — вкладка в Налаштуваннях**, не пункт головного меню | власник, 09.09.2026 | `/app/settings/apps`; картка в хабі налаштувань і рядок у `core/navigation.ts` без `feature` |
| З4 | **Channex — не застосунок.** Менеджер каналів — частина ядра/модуля `channels`: через нього піде листування, через нього підключатиметься більшість готелів. Порядок нових застосунків: `winhotel_import` → `dirs21` → `fiskaly` | власник, 09.09.2026 | `channels` — `kind: 'module'`; `channel_manager` лишається в `integration-credentials`; стан із `cm_connections` показується в здоровʼї як «менеджер каналів» |
| З5 | **Гілка зливається після `beta → main`**; спершу живе на беті | власник, 09.09.2026 | блок чіпає спільні файли сесій 1–3 |
| З6 | **Назва: uk «Застосунки», de «Anwendungen», cs «Aplikace», en «Apps»** | власник, 09.09.2026 | словники `de.json`, `cs.json` |
| З7 | **Помилка звʼязку показується готелю з ТЕКСТОМ останньої помилки** | власник, 09.09.2026 | `app_connections.last_error`, картка і здоровʼя |
| З8 | **Сторінка постачальника «стан застосунків усіх готелів» — потрібна** | власник, 09.09.2026 | `/app/platform/apps`, `GET /api/platform/apps` |
| З9 | **Пошта (`smtp`) — у каталозі здоровʼя** | власник, 09.09.2026 | `core/mail/email.ts` звітує стан |
| З10 | **Шлюзи в `apps.ts` — записи з `gateway: true` БЕЗ label/where/live; ці три поля підставляються з `PAYMENT_PROVIDERS` у місці читання (`appCatalog()`), і гейт доводить, що це той самий обʼєкт за посиланням.** `payments → integration-credentials → apps`, тож імпорт `payments.ts` з реєстру — цикл; переписати три поля означало б друге джерело `live`, яке `payments.check` пильнує. Той самий прийом, що юніон каналів: чесність тримає перевірка, а не імпорт | сесія 4, 09.09.2026 | `core/apps.ts` шапка, `apps.check.ts` тв. 5 |
| З11 | **Закон стану звʼязку: креденшели — організації, підключення — обʼєкту; `scope` у маніфесті, писач рядок усупереч закону НЕ пише.** smtp — `property_id` NULL завжди; fiskaly — заповнений завжди (обʼєкт знаходиться за `tss_id` у `fin_fiscal_settings`, бо конфіг клієнта обʼєкта не знає, а `folio-payments.repo` — чужа тека) | сесія 4, 09.09.2026 | `core/app-connections.ts`, `apps.check.ts` тв. 7 |
| З12 | **`reportOk`/`reportError` ніколи не кидають** — стан звʼязку не має ламати операцію, яку описує; відмова бази йде в `console.error`. Доведено FK на неіснуючу організацію на обох рушіях | сесія 4, 09.09.2026 | `apps.check.ts` тв. 6 |
| З13 | **Статуси картки: `soon` (коду немає), `disabled` (вимикач off), `unknown` (ще не зверталися), `connected`, `degraded`, `error`; у базі — лише останні чотири.** Кілька обʼєктів — найгірший стан і є стан картки: одна зламана TSE це зламана каса на тому обʼєкті | сесія 4, 09.09.2026 | `core/apps.ts AppStatus`, `api/settings/apps/_handlers.ts statusOf` |
| З14 | **Твердження про політику (сусід не бачить «хочу» голим запитом) живе лише в `check:pg`.** На SQLite політик немає, і воно було червоним саме там — тобто не стверджувало нічого; `rls-check.sql` доводить обидві нові таблиці на Postgres | сесія 4, 09.09.2026 | `apps.check.ts` тв. 8, `db/postgres/rls-check.sql` |
| З15 | **Екран «Модулі» ховає ключі застосунків за реєстром `APPS` (клієнт-безпечний), а не за окремим списком; гейт тримає, що це рівно ключі з `kind: 'app'`.** `features.handlers.ts` не мій, а `@core/features` тягне базу в бандл клієнта | сесія 4, 09.09.2026 | `settings/features/page.tsx`, `apps.check.ts` (між тв. 4 і 5) |
| З16 | **Посилання «онлайн-оплата → /app/settings/payments» переїхало разом із рядком онлайн-оплати на «Застосунки» (рядок здоровʼя), бо на «Модулях» ключа `online_payments` більше немає** | сесія 4, 09.09.2026 | `settings/apps/page.tsx`, `PaymentGatewayNotice compact` |
| З17 | **PIN і PUK адміністратора TSE — на рядку обʼєкта (`fin_fiscal_settings.tse_admin_pin/puk`, міграція 0401), під тим самим `seal()`, що й `channel_credentials`; нових колонок у `channel_credentials` не заведено.** Там три колонки, дві з них у fiskaly зайняті ключами API; а PIN/PUK — атрибут TSS, тобто ОБʼЄКТА (закон З11), і в організації з двома готелями їх два. `seal()`/`unseal()` експортовано з `integration-credentials.ts` (не змінено). Без `APP_SECRET_KEY` підключення відмовляє 503 — відкритим текстом PIN не пишеться ніколи | сесія 4, 09.09.2026 | `0141-*.sql`, `api/settings/apps/_handlers.ts connectTseForProperty`, `apps.check.ts` сцена 3.8 |
| З18 | **Підключення TSE — за документацією quickstart, з кроком `UNINITIALIZED` перед зміною PIN; живого проходу не було.** Клієнт кидає з текстом вендора на кожному кроці й іде через `reported()`, тож розбіжність документації з живою відповіддю (інваріант 28 — три такі вже були) покажеться на картці текстом, а не мовчанням. Перший живий виклик ключем TEST — чекпоінт для того, хто матиме акаунт fiskaly | сесія 4, 09.09.2026 | `fiskaly-sign-de.ts fiskalyConnect`, ARCHITECTURE §2.3 |
| З19 | **Сирітська TSS запамʼятовується на рядку обʼєкта (`tse_pending_tss_id` + PUK, 0402) і дограється повторним натиском; `tss_id` до завершення — NULL.** Альтернатива «лише назвати id у тексті» лишала б готелю ручну роботу в кабінеті вендора і другу TSS за перший же повторний натиск. Дограння йде від стану, який назвав `GET /tss/{id}` (переходи односторонні, PATCH у поточний стан відхиляється): `CREATED` — з `UNINITIALIZED`, `UNINITIALIZED` — з `/admin`, `INITIALIZED` — `/admin`, `/admin/auth`, `/client`; інший стан — названа відмова з id і станом, без кроків (В1, рецензія 3) | сесія 4, 10.09.2026 | `fiskalyConnect(…, { resume, onCreated })`, `connectTseForProperty`, `apps.check.ts` А1 |
| З20 | **Замок від подвійного натиску — колонка `tse_connecting_at` на тому ж рядку, узята одним `UPDATE … WHERE замка немає`; рядок обʼєкта створюється ДО походу до вендора.** Не окрема таблиця і не `tss_id = 'pending:…'`: підписувач читає `tss_id` і будь-який непорожній текст там спробував би підписати. Мітки — з JS в ISO з обох боків (INC-011). Покинутий замок старший за 10 хв — переходить | сесія 4, 10.09.2026 | `connectTseForProperty`, `apps.check.ts` А2 |
| З21 | **Фасад `@invoicing` не завантажується голим node** (`domain/invoice-pdf.ts` читає `__dirname` на завантаженні), тому гейт підставляє `globalThis.__dirname` перед імпортом, щоб зайти в модуль дверима, а не через `data/`. Правильна правка — `fileURLToPath(import.meta.url)` у самому файлі (тека сесії 1, notes) | сесія 4, 10.09.2026 | `apps.check.ts` шапка |
| З22 | **Доставка даних Winhotel: нічний `gbak`-знімок агентом на сервері готелю + завантаження на наш сервер + контейнер-міст; живу базу з мережі не читаємо, CDSoft у ланцюжку немає.** Це варіант (а) з `docs/research/winhotel/IMPORT-PLAN.md` §4, посилений агентом, щоб знімок був щоночі без участі готелю; форма переходу — дзеркало на ~місяць | власник, 10.09.2026 | `docs/tasks/2026-09-10-block-winhotel-import.md` §1; `apps/winhotel-agent/`, `deploy/bridge/`, `src/apps/winhotel-import/` |
| З23 | **Токен агента — `<організація>.<секрет>`: організація в токені відкрито, секрет 32 випадкові байти; у базі лише `sha256(секрет)` під `seal()` у `channel_credentials` (канал `winhotel_import`, `access_token`), значення показується раз; новий токен скасовує старий.** Альтернатива — відкрити `channel_credentials` для читання за токеном через `PUBLIC_TOKEN_READ` — це рядок із секретами fiskaly/smtp, і другий спосіб його читати не потрібен; ідентифікатор організації не секрет (він у кожній адресі кабінету), а хеш звіряється вже під `runWithOrganization` названої організації | сесія 5, 10.09.2026 | `src/apps/winhotel-import/data/agent-token.ts`, `winhotel-import.check.ts` сцена 1 |
| З24 | **Один знімок на добу на організацію — правило писача, доба від часу прийому (UTC); той самий sha256 будь-коли → 200 з тим самим id; невдалий (`failed`) знімок повторну спробу того ж дня не блокує.** Не констрейнт: доба — не колонка, а `UNIQUE (організація, sha256)` тримає дубль файла | сесія 5, 10.09.2026 | `snapshots.repo.ts snapshotsReceivedToday`, гейт сцена 5 |
| З25 | **Міст читає базу через `isql-fb` у вбудованому режимі (роздільники ASCII 31/30 у SQL сутностей), а не через `node-firebird`.** Драйвер говорить із сервером по TCP: у контейнері довелося б тримати fbserver, порт і пароль SYSDBA — три речі, що ламаються окремо; вбудований `gbak -c` + `isql` відкривають файл самі, без мережі й пароля — так пройшли справжні 407 МБ 10.09 (`extract.sh`). Ціна — розбір текстового виводу, сплачена одним роздільником; SQL кожної сутності лишається запускним рукою | сесія 5, 10.09.2026 | `deploy/bridge/lib/extract.mjs` шапка, `apps/winhotel-import/sql/` |
| З26 | **Перетворення — один раз у мосту: CP1252 → UTF-8, суми з доменів `NUMERIC(12,3)`/`(12,2)`/`DECIMAL(12,4)` як десятковий рядок БЕЗ ділення (виправлено 10.09 після живого проходу: `table-columns.tsv` показує тип зберігання `BIGINT`, а isql віддає вже `141.000` — стаб зі здогаду ділив і давав 0.141), дати < 1900 → null, порожній текст → null; `2050-12-31` «без кінця» лишається як є — це рішення застосунку.** Банківські колонки, картки й пароль порталу з `ADRESSEN` не вибираються взагалі; у `aggregates.json` — лише лічильники й суми | сесія 5, 10.09.2026 | `deploy/bridge/lib/convert.mjs`, `apps/winhotel-import/sql/addresses.sql` |
| З27 | **Міст працює від uid 1001 (як `nextjs`), без мережі, `cap_drop: [ALL]`, `no-new-privileges`, але без `read_only` на кореневу ФС: вбудований Firebird пише таблицю блокувань і `firebird.log`.** Робоча тека — том `bridge-work`, не tmpfs: база на ~1 ГБ у памʼяті зʼїла б стелю контейнера | сесія 5, 10.09.2026 | `deploy/bridge/Dockerfile`, `deploy/docker-compose.yml` |
| З28 | **Файли з витягом (`*.jsonl`) — персональні дані готелю: живуть лише на томі й у базі орендаря; у git — лише `aggregates.json` (числа) і стаб із вигаданими даними** | сесія 5, 10.09.2026 | `apps/winhotel-import/bridge-local.sh`, `fixture/stub-db.sql` |
| З29 | **Імпорт Winhotel пише в ядро лише дверима фасадів (`@guests findOrCreateGuest`, `@companies/kernel`, `@invoicing/kernel addCharges`/`recordReservationPayment`, `@bookings/overlap insertingStay`, `@channels/outbox`); спільні таблиці (`reservations`, `reservation_guests`, `guests`) — свій SQL; те, для чого дверей немає (фактури з чужою нумерацією, зміна рядка чи оплати, сальдо, GDPR, каса), — `winhotel_staging` з причиною, а не власний писач у чужу таблицю.** Компанія створюється через `createCompanyForTests` — це єдині двері `@companies/kernel` без HTTP; перейменування дверей — справа модуля | сесія 5, 10.09.2026 | `src/apps/winhotel-import/import/importer.ts` шапка |
| З30 | **Транзакції на сутність немає навмисно: фасади беруть `getSql()` самі, і на Postgres усередині `sql.tx` це інше зʼєднання (інваріант 11) — обгортка лише прикидалась би. Замість неї — ref (`winhotel_refs`) одразу після кожного рядка, тож падіння посередині лишає базу, з якої наступний прогін продовжує без дублів.** Відхилення від §2.5 п. 9 задачі, назване | сесія 5, 10.09.2026 | `importer.ts` шапка, гейт Б3 |
| З31 | ~~**Готівка й картка на обʼєкті `DE` без `fiscal_de` лягають у staging `fiscal_guard`, а не в фоліо.**~~ **Скасовано 10.09.2026 контролером (З34):** історична оплата вже підписана касою Winhotel; вона йде у фоліо з `source = 'import'` і походженням, варта лишається для нашої каси | сесія 5, 10.09.2026; скасовано того ж дня | З34 |
| З32 | **Дедуплікація гостей — правило `@guests` (email → телефон → імʼя+дата), не `SUCHNAME+PLZ+GEBDAT` з IMPORT-PLAN §2.2; повторний імпорт знаходить свого гостя за `winhotel_refs`, не за полями.** Друге правило дедуплікації в застосунку — друге джерело правди про те, хто той самий гість | сесія 5, 10.09.2026 | `importer.ts` крок 2–3, `explained` у звірці |
| З33 | **Бронь, яку ми імпортували, а в новому знімку її немає, — `cancelled`, ніколи `DELETE`; псевдо-номери (`ZINR ≥ 9000`, `STOCK = 99`) не імпортуються, бронь на них — без `unit_id`; минулі брони — з дати `since` (дефолт 01.01.2025, поле на картці), майбутні — всі.** Довідники імпорт не створює: незнайдений код — відмова з переліком до першого запису | власник (дата «з», псевдо), сесія 5, 10.09.2026 | `importer.ts` кроки 1 і 4, гейти Б1, Б5 |
| КІ1 | **Оплати на кіоску в першій версії немає.** Крок оплати замінюється кроком реєстрації; екран каже «оплата вранці на рецепції / при виїзді». Технічна форма — політика ОБʼЄКТА `properties.checkin_payment_policy ∈ prepaid \| allow_pay_later` (0410), а не прапорець кіоска: якби слово носив пристрій, той самий готель відповідав би «можна» терміналу і «не можна» власній рецепції на ту саму бронь. Дефолт `prepaid` дослівно повторює те, що робив PATCH-хендлер числом, тож наявні готелі поведінки не змінюють. Unzer — другим заходом, окремим застосунком роду `payment` | власник, 10.09.2026 | `domain/checkin-policy.ts`, міграція 0410, `kiosk.check.ts` сцена 3 |
| КІ2 | **Ключ — код скриньки з `units.lock_code`**; скриньки без Wi-Fi, коди вписує адмін руками ввечері, інтеграцій замків у першій версії немає. Двері — `@properties/kernel.lockCodeForStay`: навмисний виняток із «секрети номера лише під `manage_properties`», звужений до одного номера, призначеного одній броні, свого корпусу, і лише коли гість уже заселений | власник, 10.09.2026 | `units.repo.ts lockCodeForStay`, гейт сцена 7 |
| КІ3 | **Підпис пальцем для іноземців** (`meldescheinNeeded`); для громадян ФРН Meldeschein не потрібен, отже й підпису немає. Лежить на РЕЄСТРАЦІЇ (`guest_registrations.signature_png` + `signed_at`, 0410), а не на гостеві: той самий чоловік наступного року підписує заново. Пишеться лише фасадом — форму (`data:image/png;base64,…`) і розмір (≤ 200 КБ) перевіряє одне місце, а не кожен із трьох писачів. Знеособлення окремого правила не потребує: ретенція видаляє рядок реєстрації цілком | власник, 10.09.2026 | `guests/data/signature.repo.ts`, гейт сцена 10 |
| КІ4 | **Принтера немає — фактура листом** | власник, 10.09.2026 | частина Б, сценарій 6 |
| КІ5 | **ПК термінала — Windows; камера дисплея не потрібна**: документ гість знімає власним телефоном через QR і наявний OCR | власник, 10.09.2026 | частина Б, сценарій 3 |
| КІ6 | **Денна дельта Winhotel кожні 15 хв — так** (задача 8 сесії 5, не цього блоку) | власник, 10.09.2026 | `docs/tasks/2026-09-10-block-winhotel-import.md` |
| КІ7 | **Мови екрана — DE + EN**, і лише вони. Список їде в сесії термінала, а не зашитий в екран | власник, 10.09.2026 | `session.handlers.ts`, гейт сцена 2 |
| КІ8 | **Walk-in — через ВЛАСНИЙ онлайн-модуль готелю (CDSoft «Onlinebuchung»), не через нашу наявність і не через DIRS21** (перевірено 10.09: сайт готелю вбудовує саме CDSoft, IIS/ASP.NET на сервері готелю, пише бронь просто у Winhotel і читає живу наявність; DIRS21 — лише Booking.com/CRC). Адреса — `properties.kiosk_walkin_url` (0410), порожньо = walk-in вимкнено; сторінка віддає `X-Frame-Options: DENY`, тож у рамку не йде — відкриває її оболонка кіоска окремою сторінкою. Жодного пулу, жодного подвійного продажу. Після переходу на ALiSiO адреса міняється на наш віджет — налаштування, не код (інваріант 20) | власник, 10.09.2026 (редакція задачі c66292d3) | міграція 0410, `session.handlers.ts`; потік — частина Б §3.2 крок 7 |
| КІ9 | **Рецепція завжди досяжна кнопкою** — телефон і WhatsApp із `property_guest_config` | власник, 10.09.2026 | частина Б, сценарій 1 |
| КІ10 | **Префікс `К` у цьому реєстрі вже зайнятий каналами (К1–К8), тому рішення кіоска — `КІ`.** Задача блоку називає їх «К1–К9»; узяти цей префікс дослівно означало б два різні рішення під одним іменем — рівно той дефект, після якого зʼявився `check-decisions-registry` (Р13.3). Відхилення від букви задачі назване в звіті блоку | сесія 6, 10.09.2026 | `scripts/check-decisions-registry.mjs`, звіт блоку «Кіоск» |
| КІ11 | **Мова екрана кіоска — власний словник (`src/apps/kiosk/ui/translations.ts`), не `t()`.** Задача каже «DE + EN через `t()`»; узяти це дослівно не можна трьома способами одразу: `t()` — мова ОПЕРАТОРА (інваріант 19: текст, який читає не оператор, звідти не береться), англійського словника в ньому НЕМАЄ і завести його означало б 3593 рядки адмінки заради двадцяти на терміналі (`check-translations` вимагає 100 %), і взірець для гостьової поверхні вже є — віджет бронювання зі своїм словником і своїм перемикачем. Тип `Record<KioskLang, KioskStrings>` не дає забути рядок в одній із мов | сесія 6, 10.09.2026 | `apps/kiosk/ui/translations.ts`, звіт блоку §Б |
| КІ12 | **QR (`guest_page_token`) — достатній чинник пошуку сам по собі; «завжди два чинники» стосується решти.** Той самий токен без жодного другого чинника відчиняє весь гостьовий портал; вимагати біля нього ще й прізвища означало б, що на терміналі перепустка слабша, ніж у браузері гостя, — тобто зробити QR-кнопку марною (гість сканує і однаково друкує). Прізвище саме по собі при цьому не шукає нічого | сесія 6, 10.09.2026 | `apps/kiosk/domain/search.ts enoughFactors`, гейт сцена 13 |
| КІ13 | **CHECK на `reservations.source` не додаємо** (рішення контролера, рецензія Б 10.09). Його не існує на жодному рушії, і 0412 справедливо скасовано. Додати його — це рішення про ДАНІ: у живій базі готелю після імпорту з Winhotel уже лежать значення, яких закритий список не передбачить, і міграція з CHECK-ом упала б на першому ж такому рядку посеред деплою. Береться окремо, разом із переліком реальних значень із Winhotel, а не в задачі кіоска. Передісторія: **CHECK на `reservations.source` не існує — і міграція 0412 не потрібна.** Рецензія А дозволила «розширити CHECK одним словом»; перевірено обома двигунами: на SQLite міграція «remove CHECK constraint from reservations.source» знімає його з КОЖНОЇ бази (прогін на порожній теці: у щойно народженої CHECK-у немає), на Postgres його не додає жодна міграція і немає в `schema.sql`. Слово `kiosk_walkin` писалось увесь час. Чи має ця колонка взагалі мати обмеження — окреме рішення про ЖИВІ дані (додати CHECK на колонку, якої ніхто не стеріг, можна лише переконавшись, що жоден наявний рядок його не порушує), і воно не побічний ефект блоку «Кіоск» | сесія 6, 10.09.2026 | `src/lib/db.ts` (CREATE reservations, коментар), звіт блоку §Б першим абзацом |
| З34 | **Оплата, перенесена з попередньої системи (`fin_folio_payments.source = 'import'`), іде у фоліо повз фіскальну варту і не підписується — але лише з походженням `origin = winhotel:<LNR>`; варта лишається для всього без `source = 'import'`, імпортна без походження відмовляється.** Історична готівка вже підписана TSE Winhotel (fiskaltrust) і в нашій касі не відбувалась; вимагати для неї наш TSE — ніколи не мати сальдо гостя (295 із 436 платежів живого знімка). Скасовує З31 (staging `fiscal_guard`) | контролер 2, 10.09.2026 (рецензія Б п. 4) | 0405, `folio-payments.repo.ts`, `folio-payments.check.ts` (дві сцени), `importer.ts` крок 6 |
| З35 | **Денна дельта Winhotel — знімок роду `delta`: агент кожні 15 хв шле вивід `isql` за вікном −1…+3 дні (не базу), маршрут вимагає `X-Winhotel-Window` (без нього 400), правило «один на добу» — лише для повних; імпорт дельти оновлює тільки броні з витягу, нічого не скасовує за відсутністю, фактур і сальдо не несе; повний знімок, узятий раніше за дельту, її не відкочує (`winhotel_refs.source_taken_at`).** Пакет — секції ASCII 29 із сирим виводом isql, міст перетворює тими самими правилами без `gbak`; шаблони `sql-delta/` мають ті самі `-- columns:`, що SQL мосту (гейт Б9) | власник (К6 «кожні 15 хв — ок»), контролер 2, сесія 5, 10.09.2026 | 0405, `apps/winhotel-agent/` (`-Mode Delta`, `sql-delta/`, `install.ps1`), `deploy/bridge/lib/extract.mjs processDelta`, `importer.ts`, гейт Б8–Б9 |
| З36 | **Незатирання: імпорт (повний і дельта) пише `status` лише вперед (`tentative → confirmed → checked_in → checked_out`; `cancelled` — лише з TA_STATUS або відсутності в повному; назад із `cancelled` — лише коли Winhotel сам зняв сторно), не чіпає `registration_status`, `reservation_guests`/`guest_registrations` не від імпорту і `fin_folio_payments` без `source = 'import'`.** Кіоск заселив о 10:00 — нічний знімок із `CI_STATUS 0` цього не скасовує | контролер 2 (KIOSK §5), сесія 5, 10.09.2026 | `mapping.ts statusForward`, `importer.ts` крок 4, гейт Б7 |
| З37 | **Живі дані Winhotel проти мапи (рецензія Б): компанія — `ADR_WAHL = 1`, не `DEBI_NR > 0` (номер має кожен, хто отримував рахунок); `LEISTSTA.WG` — LNR групи, код — `WARENGRUPPE.WGNR` через джойн, і жодного числа групи без нього; послуги груп 100/600/700/750/800 не звіряються, решта — нормалізовано і без відмови (перелік у `explained`); джерело броні — назва каналу за словом у текстових колонках `GASTKREF` (`TEXT1..5`; `EXT_SOURCE` на живому — число, номер броні каналу → `hostex_reservation_code`; рецензія Б2), не `MARKSEG`; `DEBI_NR` компанії — в `companies.debtor_no` дверима `adoptDebtorNo` (0140 прийшов із гілки робіт злиттям `block-apps`; лічильник рахунку зсувається за прийнятим номером), зайнятий іншою фірмою → staging `debtor_no_pending`.** Три з чотирьох — інваріант 28 у чистому вигляді: коди вгадані з документації, а не побачені в живому витягу | контролер 2, сесія 5, 10.09.2026 | `mapping.ts`, `importer.ts`, стаб із `wg ≠ wgnr` (інваріант 26), гейт Б2 |
| КІ14 | **Політики кіоска — колонки ОБʼЄКТА (0413), вигляд — `config_json` ПРИСТРОЮ.** Правило («пускати до оплати», «підпис», «найраніше заселення», «чи обирає термінал кімнату сам») належить готелю і живе далі, коли термінал заберуть; вигляд (робоча смуга, лого, фон) належить ДИСПЛЕЮ — у холі сусіднього корпусу може стояти інший, і 35–85 % на ньому означає іншу висоту в сантиметрах. `kiosk_signature` — три слова, а не прапорець: `TRUE/FALSE` не вміє сказати «за законом», і третій стан довелося б вигадувати наступною міграцією | сесія 6, 10.09.2026 | міграція 0413, `KioskCard.tsx`, гейт сцена 23 |
| КІ15 | **Картка застосунку живе під `/api/settings/apps/kiosk/`, а не під публічним префіксом кіоска.** Перша редакція клала її поруч із маршрутами термінала, у сегмент `admin/`, і мусила захищати його заперечним поглядом уперед у `check-route-guards`. Працювало, але означало другу домівку для однієї картки і виняток, який треба памʼятати. Частина В прибрала і сегмент, і виняток: під публічним префіксом лишилось ЛИШЕ те, що відповідає терміналу | сесія 6, 10.09.2026 | `src/proxy.ts`, `scripts/check-route-guards.mjs`, `admin.handlers.ts` шапка |
| КІ16 | **Доба «Kiosk heute» — доба ГОТЕЛЮ (`todayIn(timezone)`), не UTC.** Заселення о 00:40 місцевого часу взимку в Берліні припадає на 23:40 UTC ПОПЕРЕДНЬОЇ доби. «Нуль заселень» у ранок після зміни, коли їх було троє, — не дрібниця обліку: саме за цим листом рецепція вирішує, кому виставляти фактуру в чужій системі. Підсумок рахує одне місце на екран і на лист — інакше вони розійшлися б при першій правці | сесія 6, 10.09.2026 | `data/today.repo.ts`, гейт сцена 20 |
| КІ17 | **«Номер броні» — це ТРИ наявні поля, а не четверта колонка; коли зʼявиться четвертий рід коду, це буде ТАБЛИЦЯ, не колонка.** Для гостя номер один — те число, що в листі; у базі залежить, звідки бронь: наш `id`, `external_uid` (код каналу, тобто Booking.com), `external_ref` із префіксом `winhotel-ob:` (номер онлайн-модуля готелю). Усі три вже шукаються, тож переносити код Booking.com «до нас як код бронювання» окремим полем нічого не додає — він УЖЕ там. Справжня вада була інша й знайшлась читанням писача каналів: груповий заїзд (одне бронювання на кілька кімнат) кладе в `external_uid` не код, а `<код>#<ключ кімнати>` — інакше рядки не були б унікальні, — і рівність не збігалася жодного разу. Виправлено префіксом до `#` з екрануванням `%`/`_` (інакше пошук за номером став би пошуком за зразком, тобто підбором чужих броней). **Четверта колонка (`booking_com_ref` і далі) не заводиться:** один код Booking.com належить N нашим броням групи, а колонка такого зв'язку не тримає — це рядок `reservation_codes (organization_id, reservation_id, kind, code)` з індексом по `(organization_id, code)`, де новий канал додає РЯДОК, а не міграцію з новою гілкою `OR` у пошуку. Заводиться, коли зʼявиться рід коду, якого наявні три не вміщають, — не наперед | власник + сесія 6, 11.09.2026 | `apps/kiosk/data/stay.repo.ts` (`findStays`, гілка `confirmation`), гейт сцена 26 |
| О312 | **«Персональні дані» — визначення в КОДІ (`personal-data.ts`), а не в голові, і властивість реєстру ретенції розширено з «таблиці з `guest_id`» на «таблиці з персональними даними».** Виміряно: перших пʼять, других двадцять — тобто пʼятнадцять таблиць тримали пошту, телефон, документ чи адресу живої людини і не були названі ніде. Дивимось на КОЛОНКИ, а не на назви таблиць: саме назва підвела минулого разу, а `waitlist` і `site_incoming_leads` слова «гість» не містять. Визначення каже лише «тут може лежати щось про людину, тож мовчати не можна»; чи стирати — вирішує реєстр, і кожен рядок із причиною | кодова сесія 3, 10.09.2026 (TASK 12) | `guests/domain/personal-data.ts`; `check-retention-tables` |
| О313 | **`RETENTION_GAPS` — окремий кошик для «мусило б стиратись, але не стирається», і гейт від нього НЕ червоніє.** Три таблиці (`booking_drafts`, `site_incoming_leads`, `waitlist`) тримають імʼя, пошту й телефон людини, яка ще не стала гостем: виїзду немає, тож термін ретенції не настає ніколи. Полагодити це з теки гостей не можна — вони належать віджету й бронюванню. Альтернативи гірші обидві: зробити гейт червоним означало б або залатати чужі теки, або вимкнути гейт; змовчати означало б, що дірку видно раз на рік у звіті. Тому список друкується ЩОРАЗУ, і мовчання про нього неможливе | кодова сесія 3, 10.09.2026 (TASK 12) | `retention-tables.ts` `RETENTION_GAPS` |
| О314 | **Гейт перевіряє власний прилад: правило, яке не збігається з жодною колонкою схеми, — мертве.** Знайдено зломом власного гейта: звузивши визначення персонального, я дістав ЗЕЛЕНЕ — гейт просто перестав когось бачити. Без цієї перевірки визначення можна вихолостити, і жоден прогін цього не скаже. Це §3.2.1 у власному коді: гейт, який не вміє червоніти, гірший за відсутній | кодова сесія 3, 10.09.2026 (TASK 12) | `scripts/check-retention-tables.mjs` |
| КІ18 | **Склад броні накопичує СЕРВЕР, а не екран: маршрут `register` кіоска ДОПИСУЄ гостя, а не замінює список.** `@guests.saveRegistrations` замінює склад цілком, і це правильно для ПОРТАЛУ — там одна форма на всю сімʼю. Біля термінала стоїть по одній людині, і памʼять про попередніх, що жила в React-стані, стиралась скиданням за бездіяльністю (60 с): сімʼя, яка вписала батька й відійшла по валізу, стирала його, вписуючи матір. Обидві відповіді `ok`, бронь не зареєстрована ніколи. Семантику фасаду НЕ чіпано (два різні писачі розійшлися б) — злиття живе в кіоску (`domain/party.ts`): особа це пара «імʼя + прізвище» без огляду на регістр, повтор ВИПРАВЛЯЄ рядок, порожнє поле не стирає збереженого паспорта | сесія 6, 11.09.2026 | `apps/kiosk/domain/party.ts`, `api/stay.handlers.ts` (`registerStay`), гейт сцена 29 |
| КІ19 | **Відмова заселення доходить до гостя РЕЧЕННЯМ, а код фасаду на екран не їде ніколи.** Коди (`no_unit`, `unit_dirty`, `payment_required`, `too_early`, `balance_blocking`…) для людини з валізою не означають нічого — це той самий рід, що `e.message` клієнту (інваріант 6). Мапа `KIOSK_REFUSALS` дає речення на кожен код обома мовами, і воно каже, що РОБИТИ; невідомий код — загальне «зверніться на рецепцію» як свідомий запас. Знайдено живим проходом: готель, який роздає номери руками, віддає `no_unit` 409-ю, а крок картки повідомлень не показував ВЗАГАЛІ — головна кнопка термінала не реагувала, і поруч не було кому пояснити | сесія 6, 11.09.2026 | `apps/kiosk/ui/translations.ts` (`refusalText`), гейти сцени 30 і 27б |
| КІ20 | **На ГОСТЬОВІЙ СТОРІНЦІ (телефон) мова береться з пристрою, а перемикач стоїть лише на перших двох екранах.** Гість, який сканував QR на склі, тримає в руці телефон, що вже знає його мову; питати вдруге — зайвий екран перед людиною, яка ще не зайшла в готель. Перемикач лишається на старті й наступному за ним: далі гість уже в потоці з набраними даними, і мову там міняють не «бо захотілось», а бо помилились на початку. **На термінал у холі це НЕ переноситься** (КІ7 чинний): пристрій там готельний, його `Accept-Language` каже про рецепцію, а не про гостя, і вибір гостя гине зі скиданням; на телефоні пристрій належить гостю, тож і мова з нього правдива, і вибір можна памʼятати в `localStorage`. Дві межі: читати `Accept-Language` на СЕРВЕРІ при першому рендері, не `navigator.language` після гідратації (інакше спалах чужої мови, а на телефоні перший екран і є весь екран); і інваріант 19 без змін — мова ДОКУМЕНТА за юрисдикцією, тож англійський інтерфейс і німецький Meldeschein у німецькому готелі це не суперечність | власник, 11.09.2026 | ТЗ модуля ще немає; правило записане наперед, ARCHITECTURE §5 |
| О315 | **Дата у ФІКСТУРІ не пишеться літералом: спільна фікстура віддає свої дати, і вони відносні до дня запуску.** 11.09.2026 `npm run check` став червоним без жодної зміни коду — календар дійшов до `2026-09-10`, бронь фікстури стала простроченим заїздом, і сцена тривог дістала 2 замість 1. Літерал не старіє, старіє календар; а найгірше те, що таке червоне САМОУСУВАЄТЬСЯ (за два тижні рядок випадає з вікна тривог) — тобто бомба лише перезаряджається. Тепер `two-properties` віддає `month`/`from`/`to`, `quietMonth`/`quietFrom`/`quietTo` і `freeDay`, а сцена бере діапазон У НЕЇ замість того, щоб писати вересень руками. Тримає `two-properties.check.ts`: властивість «для будь-якого дня засіву дати попереду нього», перевірена на 401 день, тож літерал майбутнього року падає так само, як минулого | сесія 6, 11.09.2026 | `core/fixtures/two-properties.ts`, гейт `two-properties.check.ts` |
| О316 | **Непересічність вікон — обчислена, а не обіцяна коментарем.** Три сцени казали «вікно запиту (листопад) навмисно не перетинається з бронями фікстури (вересень)». Це було правдою рівно доти, доки обидва місяці були літералами: щойно фікстура поїхала на відносні дати, ЇЇ місяцем став листопад, і «навмисно не перетинається» перетворилось на червоні твердження про зайняті дати. Тому фікстура тепер сама називає ТИХИЙ місяць і сама ж відмовляється сіятись, якщо хоч одна її бронь у нього заходить (`assertQuietMonthIsQuiet`, перекриттям відрізків, а не порівнянням назв місяців — бронь через межу місяця пройшла б повз назви) | сесія 6, 11.09.2026 | `assertQuietMonthIsQuiet`; `availability-scope`, `day-sheets.scope` |

---

## Відкрите: що справді потребує власника

**Закрито 05.09.2026** (власник, 37 відповідей — `claude/STATE-AND-QUESTIONS-2026-09-05.md`
у проєкті Cowork). Три попередні питання:

| Було | Куди пішло |
|---|---|
| Скільки коштує дитина, вікові вилки | це налаштування готелю, не рішення платформи; форма — Ц30 (повна модель надбавок), ціна — вводить готель |
| Ядро проти модулів за тарифом | П15: ключі й дефолти; тарифів підписки немає, лише прапорці |
| Акаунт Channex: продакшн-ключ і підписка | К3: акаунта ще немає, вмикається після сертифікації |

Нових відкритих питань на 05.09.2026 немає. Наступні ЧЕКПОІНТИ — у `docs/MASTER-PLAN.md` §4
(злиття `beta → main`; знімки оболонки; міграція матриці в сезони; мапінг OTA; ПРРО).
