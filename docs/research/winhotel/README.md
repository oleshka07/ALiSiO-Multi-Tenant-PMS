# Winhotel.MX (Firebird) — дослідження схеми для `winhotel-import`

Задача: `docs/tasks/2026-09-09-winhotel-schema.md`. Звіт:
`docs/tasks/2026-09-09-winhotel-schema.report.md`. Це дослідження, не код: нічого
звідси не імпортується продуктом.

| Файл | Що це | Стан 09.09.2026 |
|---|---|---|
| `notes.md` | блокер (§1), встановлені факти про Firebird, конвенції вендора, TSE | **читати першим** |
| `extract.sh` | витягання DDL, лічильників, колонок, довідників з `.fbk`/`.FDB`; сам видаляє базу | тестований на допоміжній базі |
| `DDL-WHMXDOKUMENT.sql` | повний DDL допоміжної бази документів (`isql-fb -x`, як є) | є |
| `TABLES.md` | таблиця → рядків → здогадка | лише допоміжна база |
| `MAPPING.md` | 11 сутностей → таблиці Winhotel; «що шукати» по кожній | усі 11 — «не знайдено», з інструкцією пошуку |
| `REFERENCE-SAMPLES.md` | зразки довідників без персональних даних | порожньо, правила заповнення |
| `COUNTS-2025-09.md` | контрольні числа для звірки імпорту | порожньо, список запитів |

Головна база (`WINHOTEL.fbk`, `DDL.sql`) з'явиться тут після того, як контролер обере
один із трьох шляхів у `notes.md` §1.

Перед комітом сюди: `git grep -iE "iban|@|strasse|str\." docs/research/winhotel/` має
бути порожнім; рядки гостей, компаній, броней, фактур, оплат не потрапляють у git ні
в якому вигляді.
